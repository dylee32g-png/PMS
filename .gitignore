# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
*.bak-*
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Firebase 배포 캐시 (자동 생성)
.firebase/

# Claude Code 로컬 설정 (PC마다 다름, 공유 불필요)
.claude/settings.local.json

# 회사 실데이터·임시 파일 (저장소에 올리지 않음, 2026-07-21)
# - 엑셀 원본/진척자료, Firebase 데이터 백업, 커밋 메시지 임시파일
*.xlsx
PMS백업_*.json
커밋메시지_*.txt

# 작업용 임시 폴더 (Claude 패치 스크립트 등 — 삭제해도 무방, 2026-07-27)
_to_delete/

# NAS 클라우드 자동반영기 비밀값 (2026-07-30) — 절대 커밋 금지
# config.json 에는 Entra 클라이언트 비밀키, firebase-key.json 은 Firestore 마스터 키가 들어있음
# 형식은 nas-reader/config.example.json 참고
nas-reader/config.json
nas-reader/firebase-key.json
nas-reader/.pip/
