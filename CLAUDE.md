# Memory

## Me
염경록 팀장. 팀별 **공정현황(주간·월간 진행) 보고 자료**를 분석하고 추가·보완·수정하는 업무.
- 이 `pms-app` 프로젝트(VS Code 파일)를 인수인계 받아 맡고 있음.
- **인수인계일: 2026-06-09** (이날부터 담당).
- 코딩 비전문가 → 설명은 쉽고 단계적으로, 초등학생 눈높이로.

## Project: pms-app
React + Firebase로 만든 웹앱. 팀들이 **공정현황(주간/월간 진행상황)**을 입력·보고하는 시스템.
이름: TechTeam PMS (현재 v6.9.x).

### 뼈대 (핵심은 전부 `src` 안)
| 위치 | 역할 |
|------|------|
| `src/App.js` | 대장 파일. 어느 화면 보여줄지 결정 (제일 큼, ~7100줄) |
| `src/firebase.js` | 클라우드 저장소(Firebase) 연결 |
| `src/constants.js` | 고정 설정값 |
| `src/utils.js` | 자주 쓰는 도우미 기능 |
| `src/components/` | 화면 하나하나 (아래 표) |
| `dataconnect/` | 데이터 저장 구조(설계도) |
| `node_modules`, `build`, `public` | 자동 생성/부품 — 직접 안 건드림 |
| `src/V6.7.0`, `v6.8.7`, `v6.8.9` | 옛날 백업본 |

### 화면(components)
| 파일 | 화면 |
|------|------|
| LoginScreen | 로그인 |
| ProjectListScreen | 프로젝트 목록 |
| WeeklyInputScreen | 주간 입력 |
| WeeklyReportScreen | 주간 보고서 |
| WeeklySummaryScreen | 주간 요약 |
| EstimateScreen | 견적 |
| UserManagementScreen | 사용자 관리 |
| ProgressModal | 진행 입력 팝업 |

### 동작 흐름
로그인 → 팀 선택 → 메뉴 고르기 → (공정현황 입력·수정 등) → Firebase에 저장.

## Terms
| 용어 | 뜻 |
|------|----|
| 공정현황 | 팀별 주간·월간 진행상황 보고 자료 (= 월간보고/pms 모드) |
| currentMode | App.js에서 현재 어느 화면인지 정하는 표시 |
| currentTeam | 현재 선택된 팀 |
| FortuneSheet | 화면 안의 엑셀 같은 표 부품 |
| Firebase / Firestore | 데이터가 저장되는 구글 클라우드 |

### Firebase 권한 (중요)
- 프로젝트 ID: `techteampms`. **팀장님은 소유자(관리자)가 아님** — 본인 계정을 추가받아 **편집 권한만** 부여받은 상태. 관리자는 별도로 있음.
- 따라서 콘솔 설정 변경(인증 방식, 사용자 삭제 등)은 **관리자에게 먼저 알리고/요청하고** 진행하는 것이 원칙.

## Preferences
- 한국어로, 간결하고 직접적으로.
- 코드/구조 설명은 비전문가 눈높이로 단계별 + 필요하면 그림.
- `npm start`(개발 미리보기), `npm run build`(완성본 만들기)는 허용됨.

## ★★ 불변 기준 — List 메인표 기본 화면 (2026-07-09 팀장님 강조 · 절대 준수)
- **어떤 작업을 하든, 어디서든 항상 이 모습이 List의 기본 화면이어야 한다.**
- **한 줄 펼침**: 각 셀 내용은 줄바꿈 없이 **한 줄로 전부 펼쳐** 보인다(내용에 맞춰 열이 넓어지고 가로 스크롤로 확인). 좁은 열에서 2·3·4줄로 접히는 것 금지.
- 구현 = `src/index.css` 의 `.list-oneline { white-space:nowrap !important; max-width:none !important }` + 메인표 `<table>` 에 `list-oneline` 클래스. **이 규칙 제거·약화 금지.**
- **금지**: fit-to-width(화면 폭 맞춤)로 열을 축소해 셀을 여러 줄로 접는 방식 — 2026-07-09 시도했다 팀장님 반려→원복. 다시 도입 금지.
- 상태칸(칩+보조줄 2줄)은 의도된 flex 세로배치라 유지(접힘 아님). [[list-default-view]]

## 작업 루틴 규칙 (매일 지킬 것)
1. **시작** — 팀장님이 "오늘 시작하자" 하면 → CLAUDE.md '진행 상황'을 읽고 *지난 작업·다음 할 일*을 먼저 브리핑.
2. **코드 추가·수정** — 추측 금지. 해당 파일·줄을 실제로 확인한 뒤 진행하고, 바꾼 뒤 빌드로 점검. **빌드 점검은 팀장님 VS Code(`npm start` 자동 리컴파일 또는 `npm run build`)에서** — Claude 샌드박스의 파일 미러는 낡을 수 있어 그쪽 빌드는 신뢰 금지(2026-06-11 확인).
3. **추천 포함** — 모든 답변에 내 추천을 섞되, *왜 더 효율적인지 근거*를 함께 제시. 귀찮음 기반 추천 금지.
4. **화면 설명** — 화면 관련 질문은 진짜 자세히 설명(비전문가 눈높이).
5. **진행 방식** — 작업·학습은 한 단계씩(완료 후 다음). 단순 단답 질문은 바로 답.
5-1. **★★작업 계속 원칙 (2026-08-11 팀장님 강력 지시 · 절대 준수)** — 팀장님이 **"오늘 작업 종료"라고 말하기 전까지는 무조건 다음 작업을 이어서 계속 진행**한다. "오늘은 여기까지 할까요?" "A안(마무리) vs B안(계속)" 같은 **종료 제안·추측성 질문 금지**. 단계 완료 보고 후 곧바로 다음 단계 착수. (이 지시를 반복하게 만들지 말 것.)
6. **마무리** — 팀장님이 "오늘 작업 종료" 하면 → ① `일일리포트/2026-MM-DD_주제.md` 상세 리포트 작성(아래 양식) ② 요약 1~2줄을 '진행 상황'에 복사 ③ 깃허브 동기화 1회.
7. **깃허브** — 현재 원격 저장소 없음(최초 설정 필요). 실제 push는 인증이 있는 VS Code 쪽에서 실행, Claude는 커밋 메시지 정리·점검 지원.
8. **수치 검증 (2026-06-11 확정)** — 시스템의 모든 수치는 아직 **미검증 초안** (누구도 공식 확인한 적 없음). 따라서 로드맵 각 단계 완료 시 **수치 테스트 시뮬레이션 필수 통과 후** 다음 단계 진행: ① 자동 시나리오 테스트(로직 검증, 예: 6/11 시운전 8케이스) ② 실기기 키인 테스트(팀장님 직접) ③ 손검산 대조(화면 숫자 vs 수동 계산, 예: 6/11 공정률 5행 검산).

### 일일 리포트 양식
```
# 2026-MM-DD 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app
## 오늘 한 일
## 변경한 파일
## 결정·근거
## 다음 할 일 / 막힌 점
```

## 진행 상황 (새 챗에서 이어가기용)
- 2026-06-09: Cowork 초기 설정 완료 — 플러그인(operations·productivity·product-management) 설치, `D:\pms-app` 폴더 연결, 이 CLAUDE.md 생성.
- pms-app 전체 구조·동작 흐름 파악 완료(위 표 참고).
- `npm start` 미리보기 실행 확인함 (포트 3000 충돌 시 기존 서버 종료 후 재실행).
- 깃허브 연결 완료: 비공개 저장소 https://github.com/TipPick07/pms-app (최초 커밋 78784f1, main). 실제 push는 인증된 VS Code 쪽에서 수행. **※2026-06-25 원격 이전됨 → 정식 원격 = https://github.com/dylee32g-png/PMS (전무님=최초개발자·인수인계자 계정). 옛 TipPick07/pms-app 주소는 더 이상 사용 안 함. origin set-url + force push(d3e5c72→d056c1a)로 통일, 앞으로는 일반 `git push`(-f 불필요).**
- 일일리포트 작성: `일일리포트/2026-06-09_Cowork초기설정-깃허브연동.md` (첫날 상세 기록).
- 2026-06-10: **보고체계 방향 논의 시작.** 배경: 회사는 기술1·2·3팀+소프트웨어팀 4개 팀, 팀마다 엑셀 양식 상이. 루틴 = 각 팀 담당자가 월간 보고 제출 → 윗분 한 분이 취합 → 사장님 보고. 쟁점 = ① 엑셀 작성 후 웹 업로드 vs ② 웹 단독 입력. Claude 권고 = **웹 단독** + 입력 보조 기능(전월 자동 이월, 엑셀 붙여넣기, 드롭다운) + 단계적 확대(기술2팀 검증 → 타팀 확대 → 자동 취합). 근거: 이중관리 제거, 임의 엑셀 자동인식은 유지보수 부담 커서 "유지관리 간편" 요구와 충돌. **아직 결정 안 됨 — 팀장님 답변 대기 중인 질문 4개**: ① 4개 팀 전체 사용 합의 가능성 ② 타팀 접속용 서버/호스팅 환경 유무 ③ 입력 주체(팀별 담당자 1명 vs 각자) ④ 투자심의·삭제·보류 변경 이력 기록 필요 여부.
- 현장 특성 메모: 프로젝트는 투자심의로 시작까지 오래 걸릴 수 있고, 도중 삭제·보류 잦음 → 상태 변경 추적 중요.
- 2026-06-10(계속): 질문 4개 답변 완료(4팀 전부 사용/서버 미정·웹 유지/팀원 각자 입력/이력 필요). 양식 4종 분석 완료. **기준문서/자료기준_v1.0.md 확정** — 상태 2단계(계약현황/작업현황), 자동표시(신규·금월완료·잔여·증감), '이전'→발주처 변경 이력으로 대체, 이력기록 4종(+담당자), 이월 A안(자동 이어보기).
- 1단계(이월) 구현 중 연쇄 버그 발견 → **방식 전환 확정: 건바이건 수정 중단, 전수검사+계산 로직 일원화+필요시 모듈 분리(병렬 작업)**. 미해결 버그: 시운전 금월 키인해도 누적 미합산.
- **동기화 대원칙 확정(기준문서 8장)**: 프로젝트 List = 어미 자료, 월간보고 = 같은 데이터의 보고용 창. 양쪽 어디서 키인하든 상호 동기화. 연결 키는 실행번호 대신 내부 고유 ID 추천(팀장님 내부 확인 예정). 진행실적 팝업 항목은 팀 설정에서 on/off 가능하게(메모).
- 2026-06-10(마감): **보안 잠금 완료** — SKIP_LOGIN 해제, 공용 계정(ID/PW) 생성, firebase deploy로 neconsys.web.app 로그인 필수화. 일일리포트: `일일리포트/2026-06-10_기준확정-이월기능-보안잠금.md`.
- 2026-06-11: **시운전 포인트 계산 일원화 완료** — 전수검사로 원인 확정(누적 칸만 장부B(progressRecords)를 봄), 통일 계산기(getPointsTriple, 달마다 A:monthlyData→레거시→B) 신설, 표·내보내기·그래프·셀편집 전부 일원화, 자체/통합 병기(monthlyData.intPoints 신설), 수정팝업 월별 리스트→monthlyData 직결, 이월 씨앗에서 실적 제외, sub행 주간키 누락 해결. 검증: 자동 8케이스 + 실기기(금월 키인→누적 합산·저장 유지) + 공정률 검산 5행 일치. 문서: `기준문서/전수검사_시운전포인트_v1.0.md`
- ▶ **작업 로드맵 (2026-06-11 확정 — 이 순서대로. 단계마다 화면·팝업 아주 자세히 설명, 연계 작업 전부 마무리 후 다음으로)**
  - **★ UI/UX 개선 (2026-06-23 1순위)**: 메인화면 색 정리→여백(밀도)→테마, '현재 vs 개선안' 시안 비교 후 단계적. **컬럼 구조 제외**(곧 기술2팀 표준으로 일괄통일·소프트웨어팀 제외).
  - **0. 보안 마무리**: Firebase 콘솔 익명 로그인 중지 + 익명 사용자 삭제 (콘솔 ~10분)
  - **A. 어미줄기 (프로젝트 List ↔ 월간보고 동기화 축)**: A-1 프로젝트 List 화면 상세설명 → A-2 List↔월간보고 데이터 흐름 코드 실태 확인 → A-3 연결 키 확정(내부 고유 ID 권고) → A-4 동기화 간극 구현+검증
  - **B. 월간보고 팝업 2종**: B-1 진행실적 팝업 설명+검증(주간키인→월간적용 S2·S3) → B-2 프로젝트 수정 팝업 설명+검증(S4) → B-3 시운전 정산 엑셀 검증(S6)
  - **C. 기준문서 잔여 구현**: 상태 2단계(계약/작업현황) → 자동표시(신규·금월완료·잔여·증감) → 이력기록 4종(+담당자)
  - **D. 주간보고 라인**: 주간 입력→뷰어→요약 화면 설명 + 월간과의 연계
  - **E. 운영·확장**: 데이터 정리(POINT<누적 빨간 행들), 4팀 확대 입력보조(엑셀 붙여넣기·드롭다운), 불필요 파일 정리(`src/import tkinter as tk.ini`)
- 테스트 프로젝트(이월확인 1·2·3): 우클릭→삭제 방법 안내함, 팀장님 삭제 실행 예정.
- 2026-06-11(계속): A-1(프로젝트 List 화면 분석)·A-2(List↔월간보고 흐름 실태) 완료 — 문서: `기준문서/A2_List-월간보고_데이터흐름_실태_v1.0.md`. 핵심: 두 장부 완전 분리(동기화 0건), 함정 5개(주간장부 갈라짐·고아기록·List팝업 월간적용 없음·메모리 미갱신·중복 5종 수동관리). 실행번호 운영 실태는 회사 내부 미확인 — A-3 보류 중.
- **전제 메모: 시스템 수치 전체가 미검증 초안** (실행번호 포함 누구도 공식 확인 안 함) → 루틴 규칙 8(단계별 수치 시뮬레이션 필수) 적용.
- 2026-06-11(계속): **A-3 확정 — 연결 키 = 내부 고유 ID** (팀장님 승인). 실측 근거: 최신 엑셀 2종 분석 결과 실행번호 62% 빈값·1/3 중복·List 원본엔 컬럼 자체 없음, 이름 매칭 6%. 변경·삭제 정책 포함(ID 불변, 삭제는 soft delete '삭제 도장'). 문서: `기준문서/A3_연결키_결정안_v1.0.md`. **경고: 보존 병합(A-4c) 전 원본 엑셀로 웹 List 덮어쓰기 금지**(웹 등록 실행번호 소실 위험).
- 2026-06-11(계속): **A-4 설계 승인 + A-4a 구현·검증 완료.** 설계: `기준문서/A4_동기화_설계도_v1.0.md` (pid 허브, 4단계 ⓐⓑⓒⓓ). 구현: ① 데이터 백업 버튼(설정 메뉴, JSON — 1호 백업 확보) ② generatePid(utils.js)+신규 4경로 자동발급 ③ 일괄 발급 모달(월간 362건+List 1,836건 발급, 재실행 0건=불변성 검증) ④ pid 뱃지(수정 팝업·List 상세) ⑤ 프로젝트 연결 모달(실행번호+pid 통일, List 우클릭 '프로젝트 연결', 연결 뱃지) ⑥ 삭제 도장(soft delete: 상태=삭제+_deletedAt/_deletedBy, 데이터 보존) ⑦ 우클릭 메뉴 하단 잘림 수정. 검증: 발급 수 일치+멱등성(캡처)+연결 후 양쪽 ID 동일(실기기)+삭제 도장 동작(실기기). **다음 = A-4b: 주간장부 docKey를 pid로 통일 + 갈라진/고아 기록 병합(드라이런→실행).** 그 후 A-4c 보존 병합(6/11 엑셀 실전 시험), A-4d 어긋남 표시.
- 메모: 테스트행은 현재 '새 2개'(POINT 100×2, 오늘 재생성·pid 보유) — 삭제 도장 테스트에 사용됨. 어제 3형제는 팀장님이 엑셀 제공 전 진짜 삭제(코드 수정 전).
- 2026-06-11(마감): **A-4b 구현 + 미해결 버그 1건.** 구현: 주간장부 docKey pid 우선(ProgressModal 폴백 포함), 주간패널 적용 pid화+메모리 즉시 갱신, 주간장부 통일 병합 도구(설정 메뉴, 스캔→실행, 충돌 보류, 중복 실행번호 '주인 불명 보류' 차단). 팀장님 병합 실행 완료(건수 미기록). **◆ 미해결(내일 1순위): 진행실적 팝업 키인→적용 저장(✓ 확인됨)→새로고침 시 원복 버그.** 저장/읽기 장부 이름 불일치 의심. 다음 행동 = F12 콘솔에서 팝업 열 때 `[ProgressModal] loadData docKey=` 줄 캡처(키인 전/저장 후 각 1회)로 원인 확정. 26-1(중복 실행번호) 장부 이중 복제 여부도 점검(2호 백업 있음). 일일리포트: `일일리포트/2026-06-11_계산일원화-pid체계-A4동기화.md`
- 2026-06-12: **List↔월간 동기화 보완 + 진행실적 팝업 UI 1차 + 합계 규칙 확정.** ① 어제 원복 버그 → 재현 안 됨(해소) ② 연결 시 진행실적 옛pid→새pid 이전(saveExecNo, 고아 방지) ③ List 팝업 양방향(App.js `applyProgressByPid`+'적용하기' 노출, 26-2 실기기 확인) ④ 팝업 2개월 기본+'전체 기간 보기' 토글(합계는 보기 무관 — 계산용 ALL_WEEKS 분리)·키보드 이동(Enter/↑/↓)·포커스 자동선택·휠 오입력 방지·합계 핀 고정 ⑤ **합계·진척률 규칙 (가) 확정**: 공정항목=누적%(빈 주 직전값 이월)·합계=기준월 최신값·시운전=합, 진척률 동일기준(MAX/SUM 불일치 해소, PLC20·ETOS15 실기기 확인). 변경: ProgressModal.jsx·ProjectListScreen.jsx·App.js(+백업 .bak-2026-06-12). 일일리포트: `일일리포트/2026-06-12_List월간동기화보완-진행실적팝업UI-합계규칙.md`. **미커밋 — VS Code에서 push 필요.** 다음: 항목 on/off(#7)→이전값(#8)→자동연결 A-4c(#5).
- 2026-06-17: **#7 항목 on/off 완료 — 프로젝트별·9개 전환.** ① 진행실적 팝업이 항목 설정 반영(A안) ② **팀설정→프로젝트 저장 전환**(handleSubmit `delete progressItems` 제거·DEFAULT 9개·handleOpenModal 기본값 주입) ③ 수정팝업 체크박스 5→9개·`formData` 단위 ④ 읽는곳 `getAppliedKeys(p)`·팝업 props `pmsProgressRow` 기준·graphData p전달 ⑤ **SIMPLE(도면입수·IOMap·화면작성·기준정보) 진척률 제외**(메인 공정률 5개와 통일, 표시·입력은 유지) ⑥ 엑셀 공정률 자동반영(별도작업 불필요)·시운전열·주간요약 스킵. 검증 3종 통과(자동 시뮬 / 실기기 26-1 HMI끄기→팝업 줄삭제·진척률 21.2%·메인 N/A·26-3/26-6 독립 / 손검산 21.2%). 변경: App.js·ProgressModal.jsx(+백업 `.bak-2026-06-17` a/b/c). 개념정리: 메인vs팝업 계산차=반올림 시점(통일 보류) / 시운전 4·5=포인트(만점=`totalCommissioningPoints`, 담당자 입력). 일일리포트: `일일리포트/2026-06-17_항목onoff-프로젝트별9개전환.md`. **미커밋 — VS Code push 필요.** 다음: #8 이전값 / A-4c 자동연결 / 엑셀 selfEnabled 유령코드 정리.
- 2026-06-18: (완료) **#8 이전값**(공정항목 빈칸에 직전값 placeholder 표시, 시운전 제외·읽기전용, 자동 6/6·실기기 26-1·손검산 21.2% 통과) · **B-2 수정팝업** 상세설명(그림, 적용항목·TOTAL POINTS·월별실적 연계) · **B-3 정산엑셀 '만점' 정합성 수정**(`point`→`totalCommissioningPoints||point`로 읽기쪽 통일 — 정산엑셀 commissioning 분기·ProgressModal `totalPt`, 수정팝업이 tCP만 갱신해 point가 옛값/빈값 되던 문제, 자동 6/6) · **D(주간보고 라인) 보류**(사용 미정→내부회의 후 결정, 건들지 말 것 — 개요·흐름 설명만 완료, [[weekly-report-line-on-hold]]). (진행중) **A-4c 1단계 = 드라이런(병합 미리보기) 구현 완료** — `computeMergePreview`(연도+번호→Project명 매칭·_pid/실행번호/이력 보존·연도 스코프·읽기전용) + List 설정메뉴 '병합 미리보기' 버튼(업로드 pending 상태에서만 노출), 자동 12/12 통과. **엑셀 파일이 없어 실기기 테스트는 나중에**(엑셀 확보 후: 엑셀 업로드→설정→'병합 미리보기'→'갱신 N·신규 M·엑셀에 없음 K' 확인) → 결과 보고 **2단계(실제 보존 병합 저장)** 진행(저장 직전 JSON 백업). 또 **TOTAL POINTS 저장 버그 수정**(handleSubmit에서 point=tCP 동기화 — 표 POINT 열이 옛 point 읽어 변경 안 보이던 문제, 자동5/5+실기기 50반영), **sub→'하위' 라벨 전면 변경**(표시만·저장값 'sub'·'s' 유지 — 월간표 칩·뱃지·실행번호/프로젝트명 마커, 드롭다운 2곳, 엑셀 마커, List 칩), **프로젝트명 인라인수정 표시버그**(셀이 원본 p.project 그려 안 보임 → dp.project, 메인·하위 공통), **하위 행 실행번호 읽기전용**('s' 마커 편집 시 하위→메인 풀림 방지). 백업 `.bak-2026-06-18`·`App.js.bak-2026-06-18b`. **미커밋 — VS Code push 필요(오늘 분량 큼: App.js·ProgressModal.jsx·ProjectListScreen.jsx·CLAUDE.md).** 일일리포트: `일일리포트/2026-06-18_이전값-정산만점-A4c드라이런-sub하위.md`.
- 2026-06-19: **neconsys.web.app 배포(6/18분, 터미널 `npm run build`→`firebase deploy --only hosting`)** + **C 착수: 상태 2단계 단계적**. 순서=①상태2단계→②자동표시→③이력기록. **1차**=App.js가 상태 단일소스 확정(`constants.js`·`TeamSelector.jsx`는 고아·미사용→삭제는 E에서). **2차**=`mapLegacyStatus`(utils.js) 신설+`handleSubmit`에서 두 칸(계약현황·작업현황) 자동 파생+수정팝업 '↳자동2단계' 미리보기, 검증3종 통과(자동12/12·실기기 진행/예상/신규·손검산). 매핑: 예상·검토중→계약현황 / 미작업·진행·금월완료·보고완료→작업현황(계약'수주') / 신규·sub·취소·삭제→보류(4차). 기존 progressStatus 유지(호환). 백업 `.bak-2026-06-19`. **미커밋 — VS Code push 필요.** 다음=2차 마무리(인라인 편집 경로 `handleInlineSave`·List 인라인에도 매핑)→3차 화면전환→4차(신규·금월완료 자동표시②, sub 구조마커). 일일리포트: `일일리포트/2026-06-19_사이트배포-상태2단계1-2차.md`. [[deploy-procedure]]
- 2026-06-22: **C 2·3차 마무리 + 4차 자동표시(신규·금월완료) 구현·검증 완료.** 2차=인라인 편집(`handleInlineSave`)에도 상태2단계 매핑(자동 시뮬13/13). 3차=진행현황 칸에 계약·작업현황 보조줄(A안, 메인=보조줄·하위(SUB)=생략, 어두운표라 amber400/blue400, 실기기). 4차설계=`기준문서/C4_자동표시_판정기준_v1.0.md`(신규=최초등록월·금월완료=작업현황 완료전환, 현장확인 반영). 4-1 신규=`_firstMonth`(새 프로젝트만 저장, 과거는 monthlyData 첫 달 추정)+`getRegMonth/isNewThisMonth`+프로젝트명 옆 파란배지(자동10/10·실기기). 4-2 금월완료=`isCompletedThisMonth`(이번달 완료전환·직전완료면 제외=이월복사 제외)+초록배지(자동9/9·실기기). 안전전환 1단계(자동마커 추가+수동값 병행). 백업 `App.js.bak-2026-06-22`(시작)·`-b`(4차전)·`-c`(4-2전). **미커밋 — VS Code push 필요(App.js·C4문서).** 일일리포트: `일일리포트/2026-06-22_C2-3차마무리-4차자동표시.md`. **다음=★6/23 UI/UX 1순위(색→밀도→테마, 컬럼 제외)→이후 4-3(드롭다운서 수동 신규·금월완료 제거, 자동표시 안정 확인 후)·sub마커 점검·C 이력기록.**
- 2026-06-23: **UI/UX 개선(★1순위) — A안 클린 화이트 + 견본판 + 공정률 막대.** 다크→라이트 전환·색 브랜드블루(#1e7ac8) 통일, **디자인 견본판(`src/index.css` `:root` 토큰) 신설**(색·글씨 중앙관리, 한곳 수정→전체반영: --brand·--line·--head-bg·상태색·글씨크기). 메인 팀선택(기술1·3팀 하위메뉴·4팀 아이콘색 통일·제목확대). 월간보고 표(세로선 제거·둥근모서리·헤더확대·숫자 우측정렬·Pretendard). 모달 3종(실적그래프·상세수정·진행실적등록) 견본판 연결. 진행실적 숫자잘림 수정. **공정률(%) 칸 막대 추가**(100%초과=막대 꽉참+숫자 그대로). 컬럼·셀값·엑셀 인/아웃 불변(모양만 변경). 백업 `.bak-2026-06-23`(·-b·-c). **커밋·push 완료(`5d49c70`, main→origin/main).** 임시파일 3개(App_tmp.js·fix357.sed·t.sed, 0바이트) 삭제 완료. 일일리포트: `일일리포트/2026-06-23_UIUX개선-견본판-공정률막대.md`. **다음=프로젝트 List 관리 화면을 이 형식/틀로 통일** / 막대 후속(배경·금월칸) / 수치검증(데이터삭제→새엑셀).
- 2026-06-24: **프로젝트 List ↔ 월간보고 UI/UX 전면 통일 + 실적그래프 2종 + 메인 메뉴/버튼.** List: 헤더·본문 글자(회색+브랜드블루·11px·Pretendard, mono제거)·진행현황 칩(라운드+2단계 보조줄·`mapLegacyStatus` import)·상태필터(담당자색)·저장버튼(초록)·헤더(아이콘 브랜드블루)·우클릭메뉴·상단팝업5종(프로젝트추가·알림·초기화·설정드롭다운·저장확인) 라이트화, 관리칸 너비 34→120px(아이콘 4개 짤림). 월간: 진행현황 칩 라운드·그룹경계 세로선(`th-group-sep`/본문 #9aa8b8, 헤더 2줄→그룹행 제거 1줄). **실적그래프**: 개별(막대 둥근 그라데이션·Y축 목표8000→실제 누적기준·범례 막대네모/누적점선/공정률실선 일치·작업기간 제거·실적0 "안내"), 팀(선→**stacked 누적막대**·신규 파랑#2563eb→보라#7c3aed·맨위 segment만 path로 둥글게). 메인 팀메뉴 순서(**프로젝트 List 관리 위→월간 업무 보고 아래**, card.map=1·2·3팀)·월간 상단 List관리 설정 바로 앞으로(List '월간보고'와 위치 통일). **★ Edit 도구 파일끝 절단 반복(2회)→Python 부분치환 전환**(assert 매칭검증, 백업 App.js`.bak-2026-06-24-b~m`·ProjectListScreen`-a~i`). 컬럼·셀값·엑셀 불변(모양만). 검증: babel parse OK 전건. **미커밋 — VS Code push 필요(분량 큼: App.js·ProjectListScreen.jsx).** 일일리포트: `일일리포트/2026-06-24_List월간UIUX통일-그래프stacked-메인메뉴순서.md`. **다음=화면 최종확인·push·백업파일(-b~m) 정리·상태2단계 4차(취소·삭제·신규·하위·Hold·이전 매핑=업무규칙 결정 필요)·자동표시 잔여(증감)·이력기록4종.** 팀그래프 12개월 평평=집계 데이터 매월 동일(수치검증 별도).
- 2026-06-25: **방향 전환 — List를 원본 엑셀 기준으로 전면 재정비(팀장님 확정).** 월간보고 작업 중단, List=메인뼈대/원본(실시간 관리·정리). **원칙 ①엑셀 절대우선 ②엑셀 항목 임의삭제·변경 절대금지(반드시 팀장님 확인 후 함께) ③없는 항목은 Claude 제안→확인→적용.** 삭제법=설정메뉴 `엑셀 업로드`(pending, 기존 안 지움)→`Firebase 확정 저장`(기존 자동 전량삭제+엑셀 교체, handleSaveToFirebase 530~550). **저장 전 백업 필수**(pid·실행번호·이력 엑셀에 없음; `엑셀 생성`으로 내보내기). 오늘 작업: List 헤더 월선택기(월간보고 `기준월` 동일 스타일)+그달만/이전전체 토글(모양만)+연도드롭다운 삭제 일원화+필터 `고른 개수 합산`(month1~5 백업, **미커밋**). 확인사실: 표 `날짜`=엑셀 원본 그대로(계산X·제목칸이 의미 결정), 팝업↔표는 같은 행 공유로 순차수정 OK·팝업이 복사본이라 동시수정 시 덮어쓰기 빈틈. 인수인계: `일일리포트/2026-06-25_인수인계_List원본엑셀재정비.md`. 주의: Edit 도구가 대형 ProjectListScreen.jsx 끝 절단 재발 → Python 부분치환으로 수정.
- 2026-06-25(계속): **원본 엑셀(2026년 시트 152건·32항목) 전수 분석 → ①②③④ 항목 전부 역할 확정 + 기준문서 작성.** ★설계도 `기준문서/2026_기술2팀_List_항목정의서_v1.0.md`. 핵심: 공사계약/완료=날짜칸(달력), 날짜↔내용 자동연동(내용 수정시 날짜=오늘), 관리자 신규추가(선택), 발주처(U)→발주처담당자 개명, O체크3개, 참조=폴더명+UNC경로 복사버튼, 시운전6개+SIMPLE4개+자체/통합시운전+총진척률+④안전8개=표 숨김/표현 대상, ④8개 전부빈칸 유지+숨김, Hold/HOLD통일, 현재 '공사리포트제출'→엑셀 '공사계획서제출'. 컨셉=표↔팝업 동기화·팝업서 표항목 숨김/표시. **다음=코드분리**(ProjectListScreen.jsx 2600줄→8조각: projectColumns→useProjectData→DetailModal→FilterBar→Header→Settings→Table→디자인, 안전순서·각 백업+VS Code 빌드확인) 후 기준문서 기능구현. 인수인계: `일일리포트/2026-06-25_인수인계2_항목확정-코드분리착수.md`. 미커밋 누적(month1~5+필터+문서).
- 2026-06-25(계속2): **코드분리 1·2·3조각 완료**(`projectColumns.js`·`projectListData.js`·`DetailModal.jsx` 신규), 4조각+ 중단(필터·헤더·설정·테이블 얽힘→props 폭발, 분리 일단락). ProjectListScreen 약 2,247줄. **기능구현 착수**: ⑧Hold통일(`normalizeStatus`)·⑤O체크토글(`isCheckCol`, 상세팝업)·①공사계약/완료 날짜칸(`isDateCol`)·헤더공백픽스(공백제거 비교). 화면확인 OK·**미커밋(VS Code push 필요)**. ⚠️bash 마운트 지연으로 babel/grep 신뢰불가→파일도구 Read로 검증·최종 VS Code 빌드. 인수인계: `일일리포트/2026-06-25_인수인계3_코드분리완료-기능구현착수.md`. **다음=②날짜↔내용 연동**(이후 ③발주처담당자 개명·관리자컬럼, ⑥참조=팀별 UNC경로 대기, ⑦숨김 후순위, ⑨동기화).
- 2026-06-25(계속3): **엑셀 업로드 전 기능 9종 전부 완료.** ②날짜↔내용 연동(표·상세팝업, 커밋 5ee6ddf)·**깃허브 전무님 계정 이전**(origin→dylee32g-png/PMS, force push d3e5c72→d056c1a, gitignore 정리 4c39048, CLAUDE.md 원격갱신)·③④발주처담당자 분리+관리자(parseExcelHeaders 중복'발주처'→'발주처 담당자' 자동구분, 덮어쓰기 방지)·헤더 줄바꿈 정규화(공백통일)+메인표 매칭 공백무시·⑦관리자/안전 기본숨김(isDefaultHiddenCol+isMainTableCol 후보+useEffect, 설정 '열 표시/숨기기'서 켜기)·⑥참조 폴더명(엑셀값=폴더명이라 이미 충족, 경로복사는 팀별 UNC prefix 대기)·⑨상세팝업 저장 동시수정 보완(latest+popupChanges merge). 변경=projectListData·projectColumns·ProjectListScreen. 자동 시뮬 전부 통과, **실기기는 내일 엑셀 업로드 시 한꺼번에**. ③④⑥⑦⑨ 미커밋. 일일리포트 `2026-06-25_②날짜연동-깃허브이전-③⑥⑦⑨.md`. **다음=엑셀 업로드 테스트**(설정 '엑셀 업로드'→'Firebase 확정저장'=기존 전량삭제+교체, 저장 전 백업 필수: '엑셀 생성'으로 pid·실행번호·이력 내보내기). [[deploy-procedure]]
- 2026-06-26: **List 엑셀 재정비 실전 — audit·수정 다수 (★전부 미커밋, 마지막 커밋 3046aa0).** ① 엑셀 업로드+드라이런(갱신1566·신규317·없음275 → 클라우드≈엑셀이라 '그냥 교체' 안 함) ② 기존데이터=throwaway 확정, pid 규칙은 후속작업 ③ **audit 수정**: 공사계약·완료 6자리날짜(YYMMDD 251125→2025-11-25) 파싱(`toDateInputVal`), **헤더기준시트=열최다(2026)로**(맨앞 SM대응 10열이 2026 가리던 버그 해결) ④ **공사진행 12칸** 엑셀 수정본 제작(`outputs/01 2026년_파주 공사 List_기술3팀_260626_수정.xlsx`: 월간 진행현황 9항목 정렬+시운전(일반)삭제, 데이터152행 보존) ⑤ **표 숨김/표현(팀장 지정)** 보임13(번호·발주처·Project·진행현황·담당자·날짜·내용·PLC·ETOS·HMI·자체시운전·통합시운전·포인트)/숨김22 — `isDefaultHiddenCol` 정확목록+useEffect 헤더변경시 재적용(1회제한 버그 수정) ⑥ No.칸제거(번호중복)·실행번호 표숨김·시운전%너비210→60·중앙정렬·너비손잡이14px ⑦ **★★핵심 교훈: `src/index.css`의 `!important`가 JSX 인라인 style을 전부 이김** → 표 색/세로선/배경은 **반드시 CSS에서**(클래스로). 세로격자선 복원(border-right none→1px var(--line)) + 공사진행 강조 클래스(`prog-head/prog-cell/prog-l/prog-r`). 변경파일=projectColumns.js·ProjectListScreen.jsx·index.css. **마지막 CSS수정 실기기확인 대기 / 전부 미커밋(VS Code push 필요)**. 현재 엑셀 **pending(확정저장 안함, 새로고침 시 소실)**. 인수인계 상세: `일일리포트/2026-06-26_인수인계_List재정비-날짜-숨김표현-CSS시인성.md`. **다음=① 마지막 화면확인 ② 커밋/push ③ pid 표현규칙 설계.**
- 2026-06-26(오후): **표 너비 자동맞춤·엑셀 3층헤더·전체표시·팝업 표시/숨김 토글.** ① 표 글자짤림 근본해결(`table-fixed→table-auto`+`truncate`제거=내용맞춤 자동; 공사진행 세로선1px·배경옅게·관리자 열 표시) ② **엑셀 3층 헤더**(공사진행>진행현황/Point>PLC·ETOS·HMI/총·누적) `parseExcelHeaders` 지원 + **최신연도(2026)만** 읽기(extractYear 4자리필터로 SM대응·과거연도 제외; 시뮬+실기기 검증, 공사진행 8칸 정확) ③ `isMainTableCol` **전체표시**+기본숨김 useEffect 제거 ④ `DetailModal` 항목별 **표시/숨김 토글**(파랑=표시/회색=숨김, 숨김 라벨 흐리게, hiddenCols 조작). 백업 `.bak-2026-06-27`. **전부 미커밋(마지막 3046aa0).** 다음=팝업 시인성·너비·항목순서 조정, 커밋/push, 미사용 import(isDefaultHiddenCol·isProgHiddenCol) 정리, 주석 날짜 06-27→06-26. 일일리포트: `일일리포트/2026-06-26_표너비자동-엑셀3층-전체표시-팝업토글.md`.
- 2026-06-29: **메인표 포인트 칸 `실적/만점` + 진행실적 자체/통합 택1 + 시운전 누적 기준.** ① 포인트 칸 = `실적/만점`(예 50/200): 만점=상세팝업 '포인트'(고정·읽기), 실적=신규 `포인트실적`. 셀 클릭→실적 직접 입력(commitCellEdit 포인트칸은 `포인트실적`에만 저장=만점 보호, 빈값 `0/200`). ② 진행실적 팝업 푸터·적용확인창에 **[자체][통합] 선택**(합 제거→택1) → `포인트소스`+`포인트실적` 저장. ③ **totalPt 폴백 `row['포인트']`**(진행실적이 monthlyData만 봐 만점 0→시운전% 0·메인표 옛 수동값 남던 버그 해결, 자체 50÷200=25%). ④ 시운전 메인표%·포인트 = '그달치(getMonthSum)'→**전체 누적(accSelfPts/accIntPts)** 기준(합계200=100%); 주차표·주별키인 유지(보고만 월1회). ⑤ 공정7개는 이미 합계(itemFinalPct=refWKey)=메인표 일치 확인(20~70% 전부). 자동시뮬+손검산 통과, **실기기·테스트값(자체150·통합50 섞임)정리·push 대기.** 변경: ProjectListScreen.jsx·ProgressModal.jsx(`.bak-2026-06-29-pt`)·기준문서_진행실적-메인표_동기화_규칙. 일일리포트: `일일리포트/2026-06-29_포인트실적만점-자체통합선택-시운전누적기준.md`. **미커밋.**
- 2026-07-06: **'만점'→'총점' 통일 · 총점초과 빨강경고 4중(메인표포인트칸·팝업합계·하단배너·적용패널) · 진행실적 초기화(백지) 우클릭메뉴 · 실적그래프 List전환(pid) + 인포그래픽 재디자인(달성률도넛·총점대비진행바·지표카드·Y축총점포함·공정률 점/선) · ★progressRecordsMap 로드조건 `pms`만→`pms\|\|projectList`(List화면 시운전 F5소실 근본원인) · 공정률추이=진행실적(progressRecords)기반 getRecordMonthlyProgress(5개평균, 유닛4/4·손검산62%) · 연도선택기 단순화(월선택기+그달만/이전전체=모양만이라 제거) · 주간보고적용·프로젝트연결 숨김(`{false&&}`) · 디버그모드·진행실적초기화=관리자게이팅 메모([[pms-admin-gating]]).** ★**List 진행현황·담당자 마스터관리**: 하드코딩(DEFAULT_STATUS_OPTIONS·ASSIGNEE_LIST)→teamSettings 폴백지역변수(1단계). 구조정정=teamSettings 팀별 `{status(객체배열)·manager}`, List상태는 월간보고 status와 별개→**List전용 `teamSettings[팀].listStatus`**. 설정'진행현황 관리' 모달=추가/이름수정/삭제/순서(2단계). **★일괄반영(이름변경→데이터 상태값도 변경) confirm건수 불안정(눌러도 152→88) 버그→롤백(`.bak-2026-07-06v`)=현재 목록이름표만 저장·데이터 안건드림. 데이터 안전 확인(칩 기존동일 152, 확인3번 눌러도 실제 미반영).** 변경: App.js·ProjectListScreen.jsx·ProgressModal.jsx(백업 `.bak-2026-07-06*` a~v). **다음=① 일괄반영 안전재설계 vs 이름변경막기(팀장님 결정)→2단계 마무리 ② 색선택·삭제경고 ③ 3단계 담당자(listManager).** 일일리포트: `일일리포트/2026-07-06_총점경고-그래프재디자인-List재정비-마스터관리.md`. **미커밋(분량 매우 큼).** [[deploy-procedure]]
- 2026-07-07: **진행현황 관리 2단계 마무리 + 담당자 관리 3단계 + 드롭다운 배치·이름형식·컴팩트 개선.** ① 진행현황 관리 안전안(기존이름 잠금·추가/삭제·배정 0개일 때만 삭제, **어제 ① 일괄반영 폐기** — 값이 152개 문서 분산 + 필터 카운트로 152→88 불안정이 원인). ② **담당자 관리 3단계 신설**(설정메뉴, `teamSettings[팀].listManager`, 진행현황과 동일 방식). ③ **인라인 드롭다운 4종 상하 자동배치**(`dropAnchor` 신설: 아래 공간 부족하면 위로+클릭지점 기준+넘치면 스크롤). ④ **담당자 이름 형식 통일**(`extractName` 순수 한글 이름 비교=김준혁TL↔김준혁 팀장, 필터 카운트 정상화, 시뮬 9/9·실기기 확인). ⑤ 삭제잠금을 화면 탭과 동일 기준(`assigneeCountMap` 재사용, '탭0인데 삭제안됨' 해소). ⑥ 컴팩트: localStorage 기억+라벨=현재모드, **검은색 버그 수정**(기본값 null→컴팩트 복구 + 버튼 배경 3모드 인라인 색). 변경: ProjectListScreen.jsx·projectColumns.js(백업 `.bak-2026-07-07` a~h). Python 부분치환+babel 전건 통과. **미커밋 — VS Code push 필요.** 일일리포트: `일일리포트/2026-07-07_진행현황담당자관리-드롭다운배치-이름형식-컴팩트.md`. [[deploy-procedure]] [[pms-edit-tool-truncation-trap]]
- 2026-07-08: **List ② 색선택·삭제경고 + ⑥ 참조 UNC경로복사 완성 → 항목정의서 9기능 전부 완료.** ① 진행현황 관리에 **프리셋9색 선택**(색동그라미→팔레트, `projectColumns.STATUS_COLOR_PRESETS`, 저장=`teamSettings[팀].listStatusColors`, `STATUS_COLORS` 병합 재사용, 안건드린색 원본유지). ② 진행현황·담당자 관리 **✕ 즉시 '예/아니오' 확인**(`statusDelIdx`/`managerDelIdx`). ③ **참조 UNC경로복사**(상세팝업 '경로복사' 버튼→폴더명 앞 서버주소 붙여 클립보드→탐색기): `UNC_PREFIX_BY_TEAM`+`buildUncPath`+`isRefCol`, 경로=`\\neconsys_pj\001 Project\001 파주\107 기술3팀 2026년 Project\`+폴더명. **기술2팀만 확정**(Z:속성=`NECONSYS_PJ(\\neconsys_pj)`로 서버명 검증, UNC라 개인드라이브 무관 공통), 나머지팀(해외·구미)은 지역달라 추후. 헤더 '참 조'(공백) 버그→`isRefCol`·`isWideField` 공백제거. 첫에러=폴더명오타(엑셀 `Snl`(엘) vs 실제 `SnI`(아이))=값 맞추면 폴더열림 확인. ★증감(공정률 금월−전월 ▲▼)은 App.js 구현했다 **월간보고 영역이라 원복**(List완성 우선방침, 작업물 outputs 보관). 변경: `projectColumns.js`·`ProjectListScreen.jsx`·`DetailModal.jsx`(백업 `.bak-2026-07-08*`). 자동시뮬(색14/14·증감12/12·UNC13/13)+babel전건+실기기 통과. **미커밋 — VS Code push 필요.** 다음=엑셀 확정저장 실전(백업필수)·번호자동+1확인·나머지팀 UNC경로. 일일리포트: `일일리포트/2026-07-08_색선택-삭제경고-참조UNC.md`. [[deploy-procedure]]
- 2026-07-09: **List 이상무 점검 + 숨김 지속성 + 죽은코드 정리 + 운영문서 2종 + pid 충돌 점검·발급기 교체.** ① List 4파일 파싱OK(미커밋만). ② **숨김(열 표시/숨기기)** 세션한정→**localStorage 팀별 지속**(헬퍼 `hiddenColsKey/loadHiddenCols/saveHiddenCols`, 초기로드+팀전환 재로드+3변경지점 저장; 시뮬5/5). ③ 죽은코드 `DEFAULT_HIDDEN_COLS·isDefaultHiddenCol·isProgHiddenCol` 10줄 제거(사용처0). ④ 운영가이드 신규 `운영가이드/치트시트_항목이름규칙_v1.0.docx`·`운영가이드/표준절차서_항목추가변경_v1.0.docx`(엑셀헤더=열 자동인식·이름규칙 근거, `parseExcelHeaders`). ⑤ **pid**: 방향=옵션1(무의미 열쇠 유지+충돌 보강, A-3 근거). 스트레스=현행 대량발급 100%중복(2198×30 전부·최대26). 실데이터 audit(`PMS백업_기술2팀_20260709_1132.json`: 월간367 고유·빈칸0 / List1883 중 **_pid 중복 2건**=`P-mquj359p-vki`·`P-mquj356x-kto`, 정상연결0). **`generatePid`→crypto 랜덤32(폴백)** 교체=스트레스 0건, 예시 `P-32hex`. 백업 `ProjectListScreen/projectColumns .bak-2026-07-09`·`utils.js.bak-2026-07-09-pid`. **미커밋.** ⚠**최종 파일 업로드 전 build+deploy로 새 발급기 반영 필수**→신규ID `P-`+32자리 확인→테스트삭제→업로드(순서 틀리면 재중복). 실기기 대기: 숨김 F5유지·팀독립 / 신규ID. 일일리포트: `일일리포트/2026-07-09_숨김지속성-죽은코드-운영문서-pid교체.md`. [[deploy-procedure]] [[pms-edit-tool-truncation-trap]]
- 2026-07-09(계속): **사이트 배포 복구 + List 기본화면 확정.** ⓐ neconsys 배포 실패(Page Not Found)=`build`에 index.html 없음=빌드 미완. 원인=내 `generatePid`가 `globalThis` 사용→CRA ESLint `no-undef`로 빌드 실패. 수정=`globalThis`→`window`(백업 `utils.js.bak-2026-07-09-pid-fix`), 재빌드 성공(경고만)→`firebase deploy --only hosting` 배포 완료. (무해 경고: App.js 110 '보고완료' 중복키 등, 나중 정리) ⓑ List 최초화면 '전부 보이게' 요청→fit-to-width 구현했으나 셀이 2·3·4줄로 접혀 팀장님 반려→**원복**. ⓒ **한 줄 펼침 확정**(`.list-oneline` in index.css, 백업 `ProjectListScreen.jsx.bak-2026-07-09-oneline`·`index.css.bak-2026-07-09`) = ★위 '불변 기준' 섹션 참고. **미커밋(localhost 확인 완료, neconsys 반영엔 build+deploy 필요).** [[list-default-view]]
- 2026-07-10: **List 최종펀치 + 상세팝업 정렬 + ★작업 백로그 시스템 신설.** ① **역방향 동기화**(메인표 공정률 셀→진행실적 주차장부, **현재 주차** 기록, 공정률 7개만·시운전/포인트 제외) ② **알림 정중앙**+z10000(진행실적 팝업 z9500 위로) ③ 진행실적 팝업: 이전·금주·다음=전체기간보기일때만·**0입력→빈칸**(공정률 이월유지·시운전 동일)·새로고침 숨김 ④ **상세팝업(DetailModal)**: 칸 너비 2열통일+라벨고정폭, 행높이 통일(rows2→1·minHeight34·**토글 라벨안→행 오른쪽 이동**) ⑤ **★작업 백로그**: `src/auditLog.js`(append-only 팀별 `auditLog_${team}` 감사로그)+`src/components/BacklogScreen.jsx`(전용화면·**랜딩=전체팀병합/팀안=단일**·사람·동작·기간·검색 필터·이메일→이름) 신설, App.js 라우팅(currentMode='backlog')+팀메뉴하단/​List상단 버튼+**뒤로가기 복귀(backlogReturn)**+랜딩분기 backlog예외(먹통수정), ProjectListScreen **6경로 연결**(수정=셀편집·상세팝업·진행실적적용/추가/삭제=deleteRow+상태'삭제'/보류=상태'보류'), App.js **월간삭제(executeDeleteProject)** 연결. who=회사이메일. **★개인로그인 이미 구축됨**(사용자관리 registeredUsers 명단+구글/이메일링크 로그인+명단 게이트, 계정등록 완료)→아까 "전무님 필요"는 정정, 공용→개인 로그인 사용 전환만 하면 실명. 검증: 역방향 시뮬35/35·0빈칸12/12+node--check·새파일 파싱OK·실기기 백로그(수정 57→65·삭제 진행중→삭제) 확인, 대형파일은 마운트지연으로 Read/Grep+VS Code빌드 검증. 백업 `ProjectListScreen.jsx.bak-2026-07-10`. 일일리포트 `일일리포트/2026-07-10_역방향동기화-상세팝업-백로그시스템.md`. **미커밋(분량 매우 큼).** 다음=월간 직접수정 기록(선택·App.js 1611/3093/3164)·개인로그인 전환 안내·감사로그 보안규칙(전무님). [[pms-edit-tool-truncation-trap]] [[pms-admin-gating]]
- 2026-07-13: **프로젝트 추가 팝업 = 상세 팝업 통합(DetailModal `mode='add'`, 2열·폭 통일·팀 마스터 목록·자동완성)** · **등록일 `_regDate` 신설**(추가 시 자동 기입·읽기전용·내부필드, 두 팝업 '등록 정보' 섹션 표시, 기존 152건 빈칸 / ⚠엑셀 확정저장 시 pid처럼 소실) · **List 헤더 기준월 드롭다운**(전체/1~12월/기타, 기준=공사 계약, 기준연도+년월일 완전한 날짜만 그 달 / 기타=나머지 전부 → 월합+기타=전체) · **개인 화면 설정(localStorage·이 PC 한정)**=표 배율 70~150% + 열 너비 기억(팀별) + 설정메뉴 '내 화면 설정 초기화' · **실적 그래프 개편**: 기간=공사계약~완료(빈칸 규칙·자동확장·근거 배지, '2022년'류 연도만 값은 계약일 미인정 → 회의 후 재검토) / 시인성(폭 채움·누적 면적+실선·중복점 제거·공정률 알약라벨·총점선 인그래프·범례 칩·툴팁) / **축 통일: 왼쪽 최대=총점=오른쪽 100%** / 줌 먹통 복구(100%=폭채움 기준 × 줌). 검증: babel 전건 + 시뮬(등록일8/8·기준월12/12+10/10·개인설정7/7·기간10/10) + 실기기. 변경: DetailModal·ProjectListScreen·App.js (백업 `.bak-2026-07-13*`). **미커밋(분량 큼) — VS Code push 필요.** 다음=회의 후 연도만 날짜 처리 결정 / 팀 그래프 포인트 기준 검토 / 메인표 등록일 열 노출 여부. 일일리포트: `일일리포트/2026-07-13_추가팝업통합-등록일-기준월-개인화면설정-그래프개편.md`. ⚠**App.js는 그래프 모달이 2개(팀/개별) — 문자열 앵커가 팀 그래프에 먼저 걸림, 반드시 '실적 그래프 모달' 기준점 이후에서 검색**(오늘 컴파일 에러 1회). [[pms-edit-tool-truncation-trap]] [[deploy-procedure]]

- 2026-07-14: **동시편집 안전장치 + 관리자 권한 게이팅 + 실적그래프 전면 개편.** ① **진단**: 서로 다른 프로젝트 동시수정은 원래 안전(프로젝트 1건=Firestore 문서 1개, onSnapshot 실시간). 위험=같은 칸 동시수정·엑셀 확정저장(전량삭제)·전체저장. ② **관리자 게이팅**(`isAdmin`=registeredUser.role): 엑셀 업로드(+병합미리보기·로컬저장·확정저장)·전체저장·진행현황/담당자 관리·디버그·전체삭제·진행실적 초기화·**프로젝트 완전삭제(신설)** → 일반직원 설정메뉴=엑셀생성/내화면설정/열표시 3개만. 버튼숨김+함수가드 이중. (기존 TODO 2건 해소) ③ **동시수정 감지**: `stampSave`(_updatedAt/_updatedBy 도장) + 상세팝업 `findConflicts`(팝업 열때 원본↔서버 대조, **겹치는 칸만** 경고=오탐0) + 인라인 `findCellConflict`(셀편집=`editOrigRef` 편집시작 시점값 기준, 드롭다운=저장직전 서버확인). 확인창 시간 UTC→KST. 역방향동기화를 저장확정 후로 이동(취소 시 장부만 바뀌던 순서버그). **실기기 확인**(빨간창 정상). 드롭다운은 원리상 경고 거의 안 뜸=정상(실시간반영으로 '내가 본 값=최신값'). ④ **로그인 유지**: 이메일링크는 **탭 B(링크로 열린 탭)에서 로그인 완료**되는데 거기서 persistence 미지정 → 브라우저 닫으면 풀림. 링크탭에 `browserLocalPersistence` 명시 + 선택값 기억(`pms_stay_logged_in`). 구글은 팝업이라 무관(확인). 에러메시지에 코드 표시. ⑤ **완전삭제**: `deleteRow()`는 있었으나 **호출 버튼이 0개**여서 테스트행조차 못 지웠음 → 우클릭 메뉴 신설(관리자·확인창·백로그기록). ⑥ **백로그 프로젝트별 보기**: 프로젝트 필터 드롭다운 + 프로젝트명 클릭→필터 + [시간순]↔[프로젝트별] 카드 전환. 묶기 키=**pid→projectId→이름**(이름 바뀐 프로젝트도 한 묶음, 카드엔 최신이름). ※백로그에 4개만 뜨는 건 정상(7/10 신설 이후 **손댄 프로젝트만** 쌓임). ⑦ **진행실적 팝업**: 저장안하고 닫기 경고 / 전체기간보기→**현재월로 스크롤**(1월로 가던 문제) / [실적그래프] 버튼 / **진척률 행 강조**(11→15px, 합계 19px, 높이42, **`%` 제거**=44px칸에 `68.9%`(46px) 물리적으로 안 들어가 붙었음, 소수부 작게, 연회청 배경). ⑧ **실적그래프 개편**: 달성률 도넛 밑 **계산식 노출**(176÷200) / **총점 대비 진행=2단막대**(위=총점 목표·아래=진행 실제, 잔여는 막대 밖) / 지표카드 설명부제 / **완료일 없으면 그해 연말까지**(`2026.06~2026.12 (예정)`) / 헤더 제목 말줄임+버튼 nowrap(세로깨짐) / 차트 플롯박스 테두리 / **하단표 좌우정렬**(라벨을 테두리 밖으로 → 차트와 동일 구간, 검산3/3) / 차트↔표 스크롤 동기화 / [진행실적 등록] 버튼(왕복). ⑨ **공사계약/완료 짝 검증**(추가 팝업만): 계약O완료X·계약X완료O=저장불가, 둘다없음=OK. ⑩ **공정률 검산**: 6월10%·7월81%·8월85% 코드 재현 완전일치(=PLC·ETOS·HMI·자체%·통합% 5개 평균, SIMPLE 4개 제외). 검증: 전건 파싱 + 시뮬(충돌14/14·인라인11/11·백로그11/11·기간8/8·날짜6/6) + 실기기. 변경: App.js·ProjectListScreen·ProgressModal·BacklogScreen·LoginScreen (백업 20개 `.bak-2026-07-14*`). **미커밋 — VS Code push + build/deploy 필요(분량 매우 큼).** ⚠**파일끝 절단 3회 재발**(대형 JSX에 Write/Edit) → 백업복구+Python부분치환으로 재적용, 최종 10개 파일 파싱 통과. 다음=① 커밋·push·배포 ② 백업파일 정리 ③ **[[contract-date-pair-rule]] 보류**: 날짜 짝 규칙을 상세/수정 팝업에도 적용(기존 152건 완료일 빈 행 다수 → 먼저 건수 집계 후 결정) · 계약>완료 순서검사 도입 여부. 일일리포트: `일일리포트/2026-07-14_동시편집안전장치-관리자권한-그래프개편.md`. [[deploy-procedure]] [[pms-edit-tool-truncation-trap]] [[pms-admin-gating]]

- 2026-07-15: **모바일 진행실적 입력 화면 신설 (담당자 전용).** 방향 확정=별도 앱 X, **한 앱에 모바일 화면 1장**(코드 1벌·배포 1번·장부 동일 → 이중관리 없음). 팀장님 결정 2건=담당자 식별 **개인 로그인 연동** / 입력 범위 **PC 팝업과 동일**. ① `MobileInputScreen.jsx` 신규: 로그인 displayName↔List 담당자 **extractName 매칭**(7/7 규칙 재사용, 새 필드 없음), 내 담당 메인행만 카드(삭제 제외·하위 s/-행은 팝업 '하위'로), **ProgressModal 통째 재사용**=저장·계산 100% 동일, '적용'=applyProgressToMainRow 동일 규칙(도장+_changeHistory+백로그 note '모바일 진행실적 적용')+applyProgressByPid 월간 동기화, **상태 필터 칩**(실제 상태값 자동 집계·건수순·localStorage 기기별 기억·기억값 소실 시 자동 전체·HOLD→Hold·빈값='(상태없음)'). ② App.js 6곳: **휴대폰 UA 자동 진입**(1회, 'PC 화면' 버튼으로 나가면 재진입 안 함), 랜딩 조건 예외+`mobileInput` 분기, 팀선택 우상단 **[모바일 입력]** 버튼(PC 점검용), import+Smartphone. ③ ProgressModal 2줄: 팝업 폭=min(기존, 화면-8) — PC 불변(시뮬 확인)·폰은 내부 가로스크롤. 검증: babel 전건+시뮬 23/23+9/9+실기기 PC(카드 60건·칩 동작). ⚠**신규 파일도 샌드박스 미러 잘림**(213줄 스냅샷으로 babel 헛실패 → 실제 파일 Read로 확정, /tmp 사본 파싱 통과) — 미러 파싱 실패 시 실제 파일부터 확인. 백업 `App.js/ProgressModal.jsx .bak-2026-07-15`·`MobileInputScreen.jsx.bak-2026-07-15b`. **미커밋(7/14분+오늘분 누적) — VS Code push 후 build+deploy 해야 폰 테스트 가능.** 다음=①커밋·push·배포 ②폰 실기기(팝업 사용감) ③담당자 계정 등록+'이름'=List 담당자 표기 일치(사용자관리) ④PWA 홈화면 추가(후순위). 일일리포트: `일일리포트/2026-07-15_모바일진행실적입력화면.md`. [[deploy-procedure]] [[pms-admin-gating]]

- 2026-07-16: **하위(공종) 행 — 방향 확정 + 1단계 구현·검증·배포 완료.** 방향=**웹(List) 관리**(엑셀 원본 불변·협의 불필요), 판별 규칙=**실행번호 `s`/`-`**(월간보고·팝업·그래프·모바일 공통 — **추후 엑셀 원본에 하위 줄(실행번호 s) 넣어도 코드 수정 없이 호환**, 팀장님 요청 반영). 팀장님 확정 3건=하위는 프로젝트 시작 시 등록 / **메인 POINT=하위 합계 자동(2단계 구현 예정)** / 기존 월간 하위 이관 보류(List=뼈대). ① **우클릭 '하위(공종) 추가'**: `_id=부모_id_sub##`(문자순 정렬로 부모 바로 아래 고정, 시뮬11/11), 실행번호 s·진행현황 'sub' 칩·pid·_regDate·_subParent 자동, 공장동·발주처·담당자 상속, 추가 팝업(DetailModal) 재사용, pending/local은 insertRowInOrder ② 표 `└ 하위` 보라 마커 ③ **★부모 따라가기**(저장했는데 안 보임 — 원인=기본 상태칩 `진행중·추진중`(79행)이 'sub' 걸러냄): 상태칩·기준월·담당자·열필터·검색·정렬 전부 **메인만 판정**·하위는 보이는 부모 아래 고정·건수 집계 4곳 메인만·검색은 하위 매칭 시 부모 묶음(시뮬7/7) ④ 완전삭제: 메인=하위 동반(개수 안내)·하위 우클릭=그 행만 ⑤ **★그래프 이중합산 수정(App.js)**: sub_i_* 키 있는 장부는 옛 메인 직접 키인 무시(getRecordMonthlySums·getRecordMonthlyProgress) — 251/200 총점초과 오표시 해소·팝업·메인표와 통일(시뮬7/7·실기기 165pt 확인), 옛 데이터는 삭제 아닌 집계 제외 ⑥ ⚠주의: 하위 실적=순번(sub_i) 저장 → **새 하위는 맨 뒤에만·순서변경 금지**·실행번호 s 지우면 하위 해제 ⑦ **커밋·push·build·deploy 완료**(utils/EstimateScreen는 CRLF 노이즈 → checkout으로 버림). **◆미완: neconsys.web.app 이메일 링크 로그인 `auth/unauthorized-continue-uri`** = Firebase **승인 도메인에 neconsys.web.app 없음**(기본=localhost·techteampms.*) → 콘솔 Authentication→설정→승인된 도메인 추가 필요(**인증 설정이라 관리자 알림 후**, 폰 테스트 선행조건). 변경: ProjectListScreen.jsx(`.bak-2026-07-16`·b·c)·App.js(`.bak-2026-07-16`). 다음=①콘솔 도메인 추가 ②2단계 POINT 자동합산 ③3단계 확정저장 하위 보존+부모 재연결 ④팝업 진척률(9개 평균)↔그래프 공정률 카드(5개 평균) 통일 여부 결정. 일일리포트: `일일리포트/2026-07-16_하위공종행-부모따라가기-이중합산.md`. [[deploy-procedure]]

- 2026-07-20: **하위 2단계(POINT=하위합 자동, 실기기 통과)·3단계(확정저장=보존병합, 실기기 대기) + 번호 3자리 + 모바일 대개편 + 적용 원클릭.** ① 2단계: 총점=하위 '포인트' 합 파생(합0=부모값 유지·팀장님 확정), POINT칸 `실적/Σ합`·그래프·팝업(subRows pt)·모바일·엑셀생성·상세팝업 6곳 일원화, 부모 손입력 잠금 2종(셀 키인 차단+팝업 🔒). 헬퍼 subPtByParent/getSubPt/effTotalPt(ProjectListScreen). 시뮬 31/31+실기기 통과. ② 그래프 자동스크롤(A안): 열 때 마지막 실적 달로(App.js useEffect). ③ 3단계: computeMergePlan(projectListData) — 매칭(연도+번호→이름)=기존 _id 유지·엑셀값만 갱신·_pid/실행번호/_regDate/이력/포인트실적 보존·값동일 쓰기생략, 신규 추가, 엑셀에없음+하위 유지(삭제0), 저장전 JSON 자동백업+확인창. 시뮬 20/20+실데이터 152건 전원매칭·pid100%·쓰기0. **실기기(업로드→드라이런→확정저장) 대기.** ④ 번호 3자리: 업로드 1→001(padProjectNo)+★병합매칭 정규화('6'↔'006' — 미적용 시 전원 신규 함정)+정렬 숫자비교+편집 3경로 패딩+설정 '번호 3자리 정리' 관리자 버튼(기존 일괄, 실측 1,192건 대상). 시뮬 21/21. ⑤ 모바일: 번역팝업 차단(index.html lang=en→ko+notranslate) / 칩 전체→진행중→추진중→건수순 / 한 주 간편입력 패널(mobileMode — 오늘 주만 큰 칸·◀▶주이동·직전값 힌트·하위별 시운전, '전체 기간 보기'=기존 표, PC 불변) / 이전/다음 프로젝트 이동+목록(카드목록 순서=팀→연도↑→행순 '그대로' 확정, dirty 가드, key 리마운트) / 팝업 높이 vh→innerHeight(폰 툴바에 [적용하기] 바 가려짐 해결)+주선택 sticky. ⑥ 적용하기 원클릭(PC·모바일): 확인패널·[저장] 단계 제거 — 1클릭=장부저장+메인표+월간, 수치는 완료 메시지로. ⑦ ★4자리 잘림: 원인 실측(Playwright)=크롬 number 칸 스피너 예약 ~16px→44px 칸에 3자리만(978 OK·1000→'100' 오인). index.css 스피너 전역 숨김+4자리↑ 글씨 자동축소(cellFontFit). 데이터는 정상. ⑧ ★배포 캐시: Firebase 기본 1시간 캐시가 '배포해도 폰 그대로' 원인(번들 마커 실측으로 빌드 정상 확인) — firebase.json에 no-cache(문서)/immutable(static) 규칙. 첫 반영은 시크릿 탭으로 확인. ⑨ utils/EstimateScreen CRLF 노이즈 재발→HEAD 재원복+**.gitattributes `* -text` 신설(줄끝 변환 전면 금지 — 재발 근절)**. 오전분 커밋 2aeca43 푸시 완료, 오후분 커밋 필요(커밋메시지_0720b.txt). 다음=③실기기·폰 시크릿탭 확인·번호정리 버튼 실행·승인 도메인(관리자). ⚠엑셀 원본에 하위 줄 금지(매칭 규칙 없음). 일일리포트: `일일리포트/2026-07-20_하위2-3단계-번호패딩-모바일개편.md` [[deploy-procedure]]

- 2026-07-21: **전체삭제→최종엑셀 확정(163행) + 심기 확장 + 프로젝트별 적용/미적용 + 열고정 + 주간보고 pid.** ① **진행실적 심기**(설정): 공정률 7개('90%' 표기 인식)→현재주차 장부 + **누적(진행pt)→통합시운전 장부+% 자동(누적÷포인트, 다를 때만)**, 그래프 즉시반영, 재실행 안전. ② **포인트=총점·누적=진행분 확정** — 포인트 칸 '실적/총점'→엑셀 값만(클릭=값 편집, 하위 Σ·잠금 유지), 그래프 pt=자체+통합 합산. ③ **적용/미적용(_naItems·_naOn)**: 상세팝업 공정/시운전 9칸 오른쪽 토글(별도 섹션 X=이중관리 방지), 미적용=메인표 회색 ×+편집잠금+팝업 행숨김·진척률 제외+그래프 제외(naToProgressItems). **비엑셀 5항목(도면입수·I/O Map·화면작성·기준정보·자체시운전) 전 프로젝트 기본 미적용**(_naOn 예외, 재업로드 자동), 모바일 연결(projectListData.naProgressItemsOf). ④ 총점 '포인트'→'총' 폴백 3곳. ⑤ **열 고정**: 기본=Project까지+PC별 팀별 기억, 더블클릭 토글+**헤더 우클릭 '이 열까지 고정/해제'**, 오프셋 실측(첫 데이터행 offsetLeft·첫칸 정규화·±2px·의존성 기반 — ★매렌더 실측=무한루프 금지), 경계 그림자 .frz-edge. 열 스냅은 도입→팀장님 불편으로 제거(자유 스크롤). ⑥ 주간보고 연결 키 실행번호→pid('실행번호 미등록' 폐지). ⑦ **NAS 진척엑셀 2종 분석**: L1(#1 L1 진행현황)=P9 85.4%/P10 76.6% 블록 공정률·ETOS 열 없음 / L2(진척률요약 Main) 30행 총계=화면개발 94.5%·시운전 93.8%·통합시운전 21.9% — **매핑 4건 결정 대기(내일)**: PLC 출처·ETOS 출처·HMI 값·통합 21.9% 확인 → 첨부 업로드 시 자동 공정률 반영 구현 예정 (L1은 010·011 두 행 포함=연결 규칙 설계 필요). ⚠사고 2건 복구: 미러 낡음→심기 소실→백업 재통합(이후 디바이스 직접 패치 전환) / 실측 무한루프→의존성+±2px 2중 장치. 검증: 시뮬 다수(14/14·11/11·12/12 등)+babel 전건+실기기. 변경: ProjectListScreen·DetailModal·ProgressModal·projectListData·MobileInputScreen·App.js·index.css(백업 .bak-2026-07-21*). **미커밋 — VS Code push + build/deploy 필요(오후분 localhost만, 마지막 배포 13:50)**. 일일리포트: `일일리포트/2026-07-21_심기확장-적용미적용-열고정-주간보고pid.md` [[deploy-procedure]] [[pms-edit-tool-truncation-trap]]

- 2026-07-22: **NAS 진척자료 자동 연결 완성(011 실전) + 하위 공종표 자동생성 + 관리자 열/필터 + NAS 잠금 체계.** ① 방식=첨부(복사본) 폐기→NAS 원본 직접: 규칙·경로=행 `_extSync`(클라우드), 폴더 핸들=PC별 IndexedDB(PmsExtSyncDB), List 열때 팀별 1회 자동확인→변경감지 미리보기→[모두 반영]=메인표(도장·이력·백로그)+주간장부(현재주차)+그래프. 최신파일=이름조각 포함(공백·대소문자 무시)→파일명 끝 6자리(YYMMDD)→mtime, 하위폴더 3층(Backup·~$ 제외). ② 파일1('01 진행현황'/'#1 L1 진행현황'): PLC=(L5+M5+L14+M14)/4 · ETOS=L24:M25 평균(PLC와 겹침 유지=전체vs부분, 팀장님 확정). 규칙 셀 범위(F24:M25) 지원. ③ 파일2('진척자료'/'진척률요약(Main)') 하위 공종표(EXT_SUBTABLE_PRESET, 프리셋 1클릭): 공종 8행 하위 자동생성/갱신 — 이름열 C로 행 찾기(행밀림 안전), 6항목%(H·M·R·Z·AH·AP)+포인트(AG)+누적(AN)+_naOn 자동, 부모=총계 4항목%+**HMI=기준정보생성 총계**+누적(→통합%=누적÷Σ총점 자동)+포인트=Σ하위, 팝업 하위별 통합(sub_i_int) 자동(자체는 그래프 자체+통합 합산 규칙 충돌로 보류). ④ 잠금: NAS 칸 전 경로(셀·상세팝업·진행실적팝업·모바일, isExtLockedCell)+[적용하기]·심기 보호, 하위 행 진행실적=부모 팝업 통일(progressRowFor). ⑤ 원본열기=ms-excel 프로토콜 오피스 원천차단 확인→폐기, 공식=파일별 [경로 복사](따옴표)+Win+R, '이 PC용 주소(Z:)' localStorage 우선(\\neconsys_pj 이름해석 실패 PC 대응). ⑥ 관리자: 업로드시 담당자 앞 열 자동 보장(parseExcelHeaders idx:-1), 담당자식 드롭다운(셀·팝업·하위 상속), 상단 칩=실제 값만 건수순(표기변형 이름핵심 묶음) — 기존 92건은 실데이터(김준혁 팀장45·최영환 담당18·조장현 팀장16, 7/9 백업 검증). ⑦ 그래프 지표 색 통일(포인트=파랑·공정률=초록)·반영 완료창 요약형. ⑧ 수치 확정: 달성률 22%=3766÷17230 / 그래프 공정률=(PLC+ETOS+HMI+통합)/4(미적용 자체 제외) / 팝업 진척률 83.1%=적용 8항목 평균 — 팝업↔그래프 통일 여부 미결(7/16 잔여). 검증=시뮬(20/20·6/6·12/12·8/8)+babel 전건+백지 재업로드 실기기 2회 통과. 변경: ProjectListScreen·projectListData·ProgressModal·DetailModal·MobileInputScreen·projectColumns·App.js(백업 .bak-2026-07-22). 운영 루틴=월요일 아침 담당 PC List 열기→감지 시 반영. **미커밋(7/21분+오늘분 누적, 분량 매우 큼) — VS Code push + build/deploy 필요.** 다음=①커밋·push·배포 ②파일1 셀주소 고정 주의(행 추가/삭제 시 규칙 수정) ③월간보고 팝업 잠금 ④진척률↔공정률 통일 결정 ⑤하위 자체 팝업 반영 논의. 일일리포트: `일일리포트/2026-07-22_NAS자동연결-하위공종표-관리자필터.md` [[deploy-procedure]] [[pms-edit-tool-truncation-trap]]

- 2026-07-27: **메인 PC 자동 반영(30분) + 구글 로그인 숨김 + 주차 날짜표시 + ★NAS WebDAV 착수(진행중).** ① **메인 PC 자동 반영(A안)**: 설정 [이 PC를 메인 PC로 지정](localStorage `pms_ext_mainpc`)→30분마다 NAS 재검사, **값 갱신은 무확인 자동 / 하위 행 신규 생성만 확인창**(팀장님 확정). **1분 심장박동+벽시계 비교**(크롬 백그라운드 스로틀·절전 복귀 대응, 단순 setInterval은 밀림), 결과=우하단 토스트(무인 PC에 모달 쌓이면 다음 검사 막힘), 허가증 만료·오류=빨간 경고 토스트. ② **관리자 게이팅**: 메뉴는 관리자 전용, ★**자동 실행엔 isAdmin 가드 금지**(플래그가 localStorage=PC단위·계정무관 → 공용 PC를 일반 계정으로 두어도 계속 돌아야 함. 관리자 계정 상주는 엑셀업로드·전체삭제 노출로 위험), 일반 계정용 **하단 상태바 `● 메인 PC 자동 반영 중 (HH:MM 확인)`**. ③ 로그인 화면 **구글 버튼 숨김** — 삭제 아닌 스위치 `SHOW_GOOGLE_LOGIN=false`(→true로 복구), 푸터 문구 정리. ④ 주차 혼동 해소: 앱 주차는 **날짜로 자른 구간**(1~7·8~14·15~21·22~28·29~)이라 달력 주와 다름=정상 → 규칙·데이터 유지하고 **W4 아래 `22~28` 표기+툴팁**만 추가(16곳 공용 규칙이라 변경 시 기존 장부 주차 키 어긋남). ⑤ **★웹 편집 브리핑 — 실측**: 진척자료 = **수식 73,005개** 계산파일. 값 안 고치고 재저장만 해도 **계산값 168,311→152,753(15,558 소실)**, PMS가 읽는 총계 8칸(H·M·R·Z·AH·AP·AG·AN) **전부 빈칸** → **브라우저 라이브러리 재저장 방식 배제 확정**. 4안 비교 후 **길A(NAS WebDAV+진짜 엑셀로 열기·저장)** 채택. **7/22 'ms-excel 원천차단' 정정 = UNC 경로라 막힌 것, http(s)는 열림**. ⑥ WebDAV 진행: DS412+/DSM 6.2.4, IP 192.168.0.110, WebDAV Server 설치(볼륨1)·HTTP5005/HTTPS5006·로그 켬, WebClient 자동, **인증서=necon-pj.synology.me(Synology DDNS 공인, 26-10-01 만료) 이미 WebDAV에 할당** → hosts에 `192.168.0.110 necon-pj.synology.me` 추가(안랩 복원팝업=아니오)해 **크롬 HTTPS 경고 없이 통과**. **관문 4개 중 3개 통과, 탐색기 `\\necon-pj.synology.me@SSL@5006\001 파주` = '액세스할 수 없습니다'에서 멈춤**(유력 원인=공유폴더명 한글+공백). 검증: 시뮬 19/19·12/12·19/19 + babel 전건 + no-undef 0. 변경: ProjectListScreen·LoginScreen·ProgressModal·.gitignore(백업 .bak-2026-07-27*). **미커밋·미배포(커밋메시지_0727.txt 준비됨) — VS Code push + build/deploy 필요.** **다음(→7/28 전부 완료) = `인수인계_WebDAV_진행중.md` 읽고 이어서**(net use 오류번호 확인 → 영문 공유폴더 검증 → 엑셀 열기·저장 실전 → PMS [엑셀로 열기] 버튼 → DNS Server로 전직원 확산). 일일리포트: `일일리포트/2026-07-27_메인PC자동반영-구글로그인숨김-WebDAV착수.md` [[deploy-procedure]]
- 2026-07-28(월): **WebDAV 길A 완성 + STEP5(전직원 확산 기반) 완료 + 오후 개선 4건.** ① 관문4 해결 — 오류67 진짜 원인=**공유폴더명 오인**(한글 무관! 진짜 루트=**NECONSYS_PJ**(영문), `\\necon-pj.synology.me@SSL@5006\NECONSYS_PJ` → X: 연결 성공. 어제 '001 파주=공유폴더' 기록은 오인) ② Office Basic 인증 차단 해제 = `reg add "HKCU\Software\Microsoft\Office\16.0\Common\Identity" /v basichostallowlist /t REG_EXPAND_SZ /d "necon-pj.synology.me" /f` ③ 엑셀+WebDAV 저장 기계검증: **수식 318/318 계산값 보존·소실 0**(어제 라이브러리 재저장 15,558 소실과 대비), R5=85.4/R14=76.6 7/21 실측 일치. 실험 복사본은 NAS `#999 Backup`으로 격리 ④ **[엑셀로 열기] 버튼** — NAS 모달 파일별, `ms-excel:ofe|u|https://necon-pj.synology.me:5006/NECONSYS_PJ/…`, 주소변환 davUrlFor(UNC·IP·Z:·공유명 중복 전부 정규화, 시뮬 8/8), [경로 복사] 병행 유지. 실기기 파일1·2 원본 열림(읽기전용 없음) ⑤ **풀 리허설**: JSON백업→전체삭제→오늘 엑셀 확정(178행=170+하위8)→011 주소·규칙3종·폴더지정 재설정→수치 전부 일치(PLC 89.6/ETOS 75/HMI 94.5/통합 21.9%/Σ17,230/누적 3,766·손검산 ✓). 전체삭제가 지우는 것=행+_extSync+헤더, 남는 것=장부·백로그·팀설정·PC 허가증(행ID 바뀌면 무용) ⑥ 공용 PC 가동: 폴더지정+메인 켬(`● 자동 반영 중`)+1회준비. **준비 .bat**=`운영가이드/PMS_엑셀열기_1회준비.bat`(hosts+Office예외+ping 확인, CP949, 재실행 안전, 로그인 안내=pms) ⑦ **STEP5 완료**: NAS **DNS Server**(볼륨1, 영역 necon-pj.synology.me+본체 A=192.168.0.110, 해상도 서비스+전달자 168.126.63.1/8.8.8.8, nslookup 2종 통과) + **공유기=Synology Router(192.168.0.1:8001)** DHCP DNS→기본 110/보조 KT(**원복 기록: 기본 192.168.0.1·보조 빈칸**), 폰 Wi-Fi 'Not Found' 실증. ⚠정정: 어제 'DHCP=NAS 운영' 기록 오류 — **NAS DHCP는 꺼져 있음, 절대 켜지 말 것(IP 충돌)**. ⚠발견: **사무실 PC 대부분 고정 IP** → 기존 PC는 .bat이 표준 절차, DHCP 갈래는 노트북·폰·신규 기기용 ⑧ **pms 전용계정** 생성(users만·NECONSYS_PJ 읽기/쓰기만·나머지 접근금지 명시·WebDAV Server만 허용·암호변경 금지) — 실사용 검증은 직원 테스트로 남음. ⚠오피스가 세션 캐시로 자격증명 삭제 후에도 자동로그인 지속=**재부팅이 리셋** ⑨ 전무 보고서 `운영가이드/보고_NAS주소인식_구축완료_260728.docx`(DHCP 주체=공유기·고정IP 현실 반영 정정판. 구 승인요청 docx는 `_to_delete`) ⑩ 오후 개선 4건: (a) 관리칸 NAS 버튼→**파란 칩**(규칙 등록 시 모든 사용자·모든 PC에서, nofolder 포함, 상태색으로 칩 전체 채움) (b) **담당자 다중 입력** — `splitAssigneeCell`(쉼표+옛 '이름 직책 이름 직책' 공백형식 사람별 분리, 단위테스트 10/10)·`toExcelAssignee`(C→책임·TL→팀장·DD→담당)·**저장 표기=쉼표 없이 공백(기존 엑셀 그대로)**·List 드롭다운+상세팝업 칩 토글(클릭=추가/해제)·칩 필터/카운트 다중 각각 집계 (c) **하위(공종) 행 PLC·ETOS·HMI 키인 잠금**(NAS 잠금과 동일 안내) ⑪ 검증: babel 전건+시뮬 3종(주소 8/8·분리 10/10·왕복)+리허설 수치. 변경: ProjectListScreen·DetailModal·projectColumns(+.bak-2026-07-28*). **오전분(버튼)은 배포됨, 오후분 미배포 — `커밋메시지_0728.txt` 갱신본으로 커밋·push+build/deploy 필요** [[deploy-procedure]]. ★**다음 = 오피스365 Business Standard 30일 평가판 테스트**(가입 직후 관리센터 '되풀이 청구' 끄기! → ⑴OneDrive에 진척 엑셀 사본 업로드 ⑵폰·외부에서 열기 ⑶2명 동시편집 ⑷메인 PC OneDrive 동기화폴더로 PMS 자동반영 되는지 ⑸버전 복원. '평가판 저장소 제한'=용량 축소 안내일 뿐 엑셀 테스트 무관 · 요금: Basic 9,500/Standard 31,800원 1인/월·연간약정) + 잔여: 오늘분 커밋·배포, pms 실검증, 직원 안내문(.bat 공지), 인증서 26-10-01(9월 확인), 볼륨2 93%. 일일리포트: `일일리포트/2026-07-28_WebDAV길A완성-STEP5-담당자다중.md`
- 2026-07-31: **오피스365 1·2·3순위 완성 + 프로젝트별 폴더 구조 도입.** ① **규칙 등록 화면 부활** — 화면 전용 스위치 `RULE_UI_ENABLED` 신설(NAS_SYNC_ENABLED는 계속 false), ★`extRulesOf`는 NAS 꺼지면 빈 배열이라 그걸로 저장하면 **기존 규칙 전멸** → `extRulesRawOf`(원본 읽기)로 교체, NAS 전용 UI는 지우지 않고 `NAS_SYNC_ENABLED &&`로 감쌈(복구 가능). ② **하위 8행 값 갱신**(짝 8/8·엑셀 24칸 일치·재이용 포인트 강제변경→자동교정, `hdr_of`로 열 이름 공백 무시). ③ **[지금 확인] 즉시 실행** — 리더를 20초 틱으로 바꿔 `pmsReaderRequest` 감지, 최대 15분→**20초**. ④ **오피스365 [엑셀 열기]** — 문서의 "원리상 불가"는 오판, `webUrl`로 구현. ⑤ **폴더 구조(팀장님 결정)** — 전용=프로젝트 폴더 / 공통=맨 위, 행마다 `_extSync.folder`, 리더 하위폴더 재귀(Backup 제외), 011행 무중단 전환(0칸 갱신). ⑥ 수정: 부모 행 열이름 매칭 버그(`통합 시운전` 공백)·Graph 504 재시도(3·8·15초)·파일목록 상한 12→60. ★전 구간 왕복 실증: 웹 엑셀 70→85 수정 → [지금 확인] → **20초 내 PMS 반영**. ⚠엑셀 파일 이름은 고객사 제출물이라 변경 금지. 변경: projectListData.js·ProjectListScreen.jsx(백업 .bak-2026-07-31~i)·nas-reader/main.py(NAS 반영 완료)·인수인계문서 전면갱신. **미커밋 — VS Code push 필요.** 다음=①배포 ②010행(P9) 규칙 ③subCreate 미구현 ④문구 손질. 일일리포트: `일일리포트/2026-07-31_규칙등록화면-하위8행-지금확인-폴더구조.md`

- 2026-08-03: **subCreate 리더 이식·실전검증 + P9(010행) 규칙 등록 + 전체 재테스트 한 바퀴 성공 + 통합시운전 자동·그래프 실시간화.** ① subCreate를 리더에 이식(cbeb604)·오프라인 26항목+P9 실전 등록 전부 일치(하위 6행 자동, PLC 98.6·HMI 31.6) ② 전체삭제→재업로드(183행)→011 규칙 재등록→반영→심기 재현 성공(운영 중 꼬여도 복구 가능 입증) ③ 리더에 부모 통합시운전 자동 계산 추가(b42b963)+주간 장부 onSnapshot 실시간 구독(App.js)→빌드·배포 완료 ④ 오피스365 테스트 ③동시편집(같은 칸은 나중 값이 경고 없이 덮음)·⑤버전복원 통과, 비용=편집자 Basic(₩9,500) 충분 결론, OneDrive 폴더째 공유로 외부 계정 권한 해결. 상세=`일일리포트/2026-08-03_subCreate-P9규칙분석-동시편집검증-비용판단.md`

- 2026-08-04: **★방향 전환(최종) — 오피스365 중단, 기존 NAS 방식 복귀 확정 (팀장님 회사 보고 결과).** 오전: P9 재등록 확인 완료(하위 6행·통합 28.7% 자동·심기 없이 그래프 참 = 실시간 구독 최종 검증 통과)·011행 PLC 규칙 **현행 유지** 결정(L5,M5,L14,M14 = L1 전체 평균 의도)·직원안내문 v1.0 작성. 오후: NAS 복귀 확정 → Basic 전환·웹엑셀 안내문·폰 테스트 전부 취소/폐기. **다음(내일 새 챗) = `일일리포트/2026-08-04_인수인계_NAS복귀-전체리셋.md`의 STEP 0부터 그대로**: pms-reader 컨테이너 중지 → `projectListData.js` 318행 NAS_SYNC_ENABLED=true(RULE_UI_ENABLED는 true 유지) → 배포 → [전체 데이터 삭제](백업 생략 — 전부 테스트 데이터, 팀장님 확인) → 엑셀 업로드 → NAS 폴더·규칙 재등록(011: 공종표+PLC L5,M5,L14,M14+ETOS L24,L25,M24,M25 / 010: 공종표+PLC L5,M5·ETOS 없음) → 손검산 검증·심기. 기능은 전부 유지(코드 삭제 없음).
- 2026-08-05: **NAS 복귀 실행 완료(STEP 0~6) + ★공용 폴더 기능 + ★그래프 유령 칸 수리.** ① 리더 중지→스위치 true→배포→전체삭제→재업로드(183행)→011·010 규칙 재등록→손검산(PLC 98.6 = 8/3 예측 일치)→심기→그래프 정상(29%) ② P9·P10 공용 파일(011 폴더 소재)을 행별 [공용 폴더(선택)] 보조 허가증으로 해결 — 자기 폴더 우선·없는 파일만 공용(오배정 방지), `공용` 배지 클라우드화 + 공용 폴더 주소 칸(공용 파일을 010 모달에서 바로 열기 — 주소는 빌려 읽는 행에만, 011은 비움) ③ 010 그래프 누적 3,377>총점 1,221 원인 = 장부 유령 sub_6(P10 공종표 잔재 3,027) — 반영 시 하위 개수 밖 sub키 자동 삭제(자기교정)로 수리·재발 방지 ④ 엑셀열기 .bat 최종본(동작 명령은 7/28 검증본과 동일)·규칙 섹션 문구 간소화·공용 메모는 배지와 중복이라 제거. 시뮬 7/7·7/7·8/8 + 실기기 검증. 상세=`일일리포트/2026-08-05_NAS복귀실행-공용폴더-유령칸수리.md`
- 2026-08-06: **UI 정리 3건 + 틀고정 그림자 제거(배포) + ★외부 접속 VPN 구축·국내 실측 성공 + ★Office 2010 대응.** ① 8/5 배포 반영 확인 → UI 정리(주소 설명 오류·하단 5줄→3줄·규칙 칩 축약)+`index.css` frz-edge 그림자 주석 처리, 배포 1회 ② 외부 접속 = **VPN(B안) 확정**(대상: 해외 파트 중국·베트남 2~3명 — 봇 노출 없음·탐색기 파일 등록까지 가능) ③ 보안 4겹: 새 관리자 `necon-kr26`→admin 비활성(직원 CIFS는 전부 NECONSYS 계정 확인)→자동차단 5회/5분+사내 허용→VPN 권한 necon-kr26 1개만 ④ VPN Server(OpenVPN UDP 1194, ★LAN 액세스 허용)+SRM 포워딩 **외부 21194→1194** 1개 ⑤ 폰 핫스팟 실측: Connected→DSM(192.168.0.110)→PMS 엑셀 열기·저장→탐색기 등록 전 구간 OK(3점 일치 222.101.203.90 — 진단법 리포트 6장) ⑥ **Office 2010 대발견**: ms-excel 신호=2013+ 전용 & 경로 218 제한이 **바이트(한글=2자)** 기준(212B 성공 vs 244B 실패로 입증) → 통역사 .bat v3(P: 드라이브 접기 244→171B)로 공용 파일까지 성공 = **전 버전 지원**. 산출물: 안내문/절차서 v1.1 · `PMS_엑셀열기_1회준비_Office2010.bat`+해제 .bat(운영가이드). 부수 발견: VNC 외부 45900 열림(추후 VPN 경유 전환 검토). **다음(내일) = ① 절차서 v1.2(2010 장+해외 파일 올리기 절) ② 운영 잔여(메인 PC 지정+폴더 3개+30분 관찰 — 8/5 이월) ③ 해외 직원 계정·출장 전 리허설 ④ 깃 커밋 확인** — 관찰: admin 끈 뒤 백업 정상 여부·볼륨2 93%·인증서 10/1. 상세=`일일리포트/2026-08-06_UI정리-VPN구축-Office2010대응.md`
- 2026-08-10: **★[엑셀로 열기] 실패 원인 규명 = 오피스365 계정 (4대 PC 대조).** 증상: 파란 칩 [엑셀로 열기] → `액세스 거부(SharePoint 사용 권한 요청)` → `파일을 사용할 수 없습니다` → **다운로드 폴더에 복사본**(여기 작업하면 NAS 원본 미반영 — 위험). **원인 확정**: 365(평가판) 계정을 넣은 PC만 실패 — 노트북·직원1(365 있음)=실패 / 직원2·공용PC(없음)=정상. 메커니즘 = 365 넣은 Office는 **조직 계정 인증(Modern Auth)만 시도하고 아이디/비번(Basic)으로 폴백 안 함** → 로그인 창이 아예 안 뜸. 엑셀 UI 로그아웃만으론 안 풀림. 날짜도 일치(7/28 첫 성공 → **7/29 365 계정 투입** → 이후 막힘. "재부팅 때문"은 우연). **해결(노트북 실기기 OK)** = 일반 cmd에서 `EnableADAL=0` + **`SignedOutADUser=1`**(이것까지 넣어야 열림), `HKCU\...\Office\16.0\Common\Identity`. ⚠HKCU는 실행 계정 것 → **관리자 권한으로 실행하면 엉뚱한 계정에 등록**됨. **아니라고 판명(다시 파지 말 것)**: 경로 218바이트(Z:와 X:는 같은 길이인데 Z:만 열림)·Office 버전·basichostallowlist·BasicAuthLevel·WebClient·cmdkey 자격증명·X: 매핑(공용PC엔 없는데도 됨)·폴더 지정(코드상 무관 `ProjectListScreen.jsx:1184~1193`). 부수: WebDAV(X:·웹버튼) 실패 / SMB(Z:·P:·UNC) 성공 · 크롬 폴더지정은 드라이브 루트 못 고름(UNC로) · 다운로드 복사본 열려 있으면 같은 이름이라 원본 안 열림. **변경**: `운영가이드/PMS_엑셀열기_1회준비.bat` 3단계 개편(①버전 자동확인+2010 안내 ②Office 예외 **15.0 추가**(기존 16.0만이라 2013 PC 헛돎) ③**365 인증 끄기 신설** ④hosts ⑤연결확인, 백업 `.bak-2026-08-10`) — 365 안 쓴 PC엔 무해라 전 직원 같은 파일 배포 가능. **작업환경**: 웹 Claude Code 첫 사용 → 제약 실측(컨테이너 localhost 안 보임·firebase CLI/인증 없어 **배포 불가**·`git push` **403**, pull만 가능) → **VS Code 복귀 결정**(깃허브·Firebase 인증이 노트북에 있음). **다음** = ①**직원1 PC 재테스트**(엑셀 365 로그아웃→완전종료→BasicAuthLevel=2+cmdkey 2개→재부팅→확인. 오늘은 레지스트리 2줄만 해서 실패 상태. **5·6번만으로 되는지 1~4번이 필요한지 여기서 갈림**) ②노트북 **재부팅 검증**(내일 아침) ③커밋(.bat 1개, 7/21~ 미커밋 누적과 함께) ④풀리면 직원안내문 v1.2에 '365 쓰던 PC' 절 추가. 일일리포트: `일일리포트/2026-08-10_엑셀로열기-오피스365원인규명.md` [[deploy-procedure]]
- 2026-08-10(오후·VS Code): **★일반 사용자 [엑셀 반영(추가·수정)] 신설(보존 병합 업로드) + NAS칩 보라 + VPN 실전 2건·문서 2종 + 팀 개편 방향.** ① **[엑셀 반영]**: 직원 적응기 대응 — 엑셀 올리면 웹과 비교해 **신규+갱신만** 반영(삭제 0). 파서 공용화 `parseListWorkbook`(관리자 업로드와 해석 일원화: 최신연도·3층헤더·번호패딩) → `handleUserExcelPick`(열 짝짓기=웹∩엑셀 공백무시·실행번호 제외 / 엑셀 하위형 줄(s·`- `·`└`) 무시 / computeMergePlan(웹 하위 제외=완전 보존) / **NAS 잠금 칸+하위부모 포인트 보호**(엑셀 옛값이 자동값 못 덮음) / 행별 diff) → 미리보기 모달(요약 칩·old→new 상세·**매칭 0 빨간 경고**·삭제없음 안내) → `applyUserMerge`(JSON 자동백업→changed+신규만 쓰기·**메타(표구조) 불변**→백로그 1건). 설정메뉴 전 사용자(firebase 상태만). 검증=**시뮬 24/24(실소스 computeMergePlan 원문 eval·멱등 포함)**+babel 전건. 실기기=버튼 확인(워처 미감지→touch로 해결), **실전 테스트=직원 최종 엑셀 대기**. ② NAS 칩 '변경 감지' 파랑→**보라 #7c3aed**(칩 4368행+모달 점 3154행 — 파랑 2종 혼동 해소, 등록됨=파랑 유지). ③ **VPN 외부 실전 2건 해결**(직원 테더링): "Missing external certificate"=ID/PW 방식이라 인증서 원래 없음→`.ovpn`에 `setenv CLIENT_CERT 0`+재등록 / "Trying to connect" 멈춤=사무실 hosts 고정(ping이 192.168.0.110)→`nslookup … 8.8.8.8`로 공인IP 확인→`remote <공인IP> 21194`(★포트 21194)→연결 성공. **hosts는 유지**(연결 후 터널로 사내주소 통해 엑셀 정상). 유동 IP 바뀌면 재발→같은 방법. ④ **문서**: `구축절차서_외부접속_VPN_v1.3.docx`(8-7·8-8 신설+2-3 예방줄+참고표) + `설치및접속가이드_외부접속_VPN_v1.0.docx` **신규**(외부·해외 배포용 STEP1~5+문제해결4) — python-docx 설치 후 생성·텍스트 검증 통과(서식 육안 확인 대기). ⑤ **팀 개편(팀장님)**: 기술1팀=서식 차이 커서 **별도 엑셀 유지**(기술2팀 통일 철회), 기술3팀=기술2팀안 **회신 대기**. 메인 카드+팀 헤더 아이콘·설명 교체(1팀 Globe 해외(중국·베트남)·파주외 / 2팀 Factory 파주 LGD 전담 / 3팀 MapPin 구미). **팀 프로파일 카드 계획 브리핑**(`src/teamProfiles/` tech2=기준·tech3=상속·tech1=신규, 화면은 카드만 읽음, 4단계 이행 — 데이터는 이미 팀별 분리라 기능 하드코딩만 문제) — **승인+기술1팀 엑셀 실물 대기**. **커밋 완료(오전 365분+오후분).** **★내일(8/11) 시작 = 기술1팀 엑셀 원본 파일 수령(팀장님 제공 예정)부터** — 순서: ①전수 분석(시트·헤더·항목 역할, 6/25 기술2팀 방식) ②`기준문서/항목정의서_기술1팀_v1.0.md` 작성 ③팀 프로파일 카드(tech1) 내용 도출→1단계 골격 착수. 그 외 잔여=①[엑셀 반영] 실전(직원 최종 엑셀) ②배포 확인(보라칩·엑셀반영·아이콘 — 8/10 저녁 배포 명령 안내함) ③직원1 PC 365 재테스트·노트북 재부팅(오전 잔여) ④기술3팀 회신 ⑤VPN 문서 Word 확인→배포. 일일리포트: `일일리포트/2026-08-10_엑셀반영기능-VPN실전-팀개편.md` [[deploy-procedure]]

- 2026-08-11: **기술1팀 프로파일 완성 + ★전면 디자인 개편(홈·List·백로그) + KPI 대시보드 2단 + 진입속도 3단 + 공지사항 — 배포 완료.** ① 규칙 5-1(작업 계속 원칙) 신설. ② 기술1팀: 확정(공정률 계산식 없음·담당자 칩='수행'·상태 칩='계약'·UNC 미정), 담당자 5명(심광호DD·염경록TL·김윤재C·양연수C·김수민C — 김윤재·김수민은 기술2팀 지원중 양쪽 명단), 파서 카드(한시트·헤더3행째·필수열 순번+공사명 AND·패딩없음=집계4행 자동제외 12행), **엑셀 확정 저장 완료**, 항목정의서 v1.0 실측 보정(43열). `src/teamProfiles/` 가동(LIST_TEAMS·generic 폴백·UNC 카드화). ③ 디자인: getdesign.md+monday/리니어/갤러리3곳 벤치마크→시안 A~F(outputs 2종)→**D안 구현**·F안(타임라인)·보고모드 후속 후보·E안 보류(한줄펼침 충돌). 홈 랜딩 개편(파스텔 카드·[열기]행·**주간보고 3종 숨김 {false&&}**·Software팀 공사중)·List 헤더(**홈+팀탭**·미저장시 이동차단·**전체저장 숨김**)·표(12.5px·시운전/공정률 막대 pctCell·웜잉크·tabular-nums)·백로그 통일(스타일만). ④ **KPI 2단**: List 상단 카드 4장(kpiData — 모수=칩과 동일 monthFilteredRows·공정률=PLC·ETOS·HMI·통합 평균·포인트=Σ누적÷Σ총점 effTotalPt·공사완료 날짜 기준 명시) **4장 항상 표시, 없으면 회색 '—'+사유 캡션**(0% 배제=오해 방지) / 홈 팀 카드 미니 대시보드(`teamStats.js` 신규 — 같은 규칙·올해 _year·도넛 68px 2개+근거 캡션). 시뮬 11/11·9/9·6/6+실기기(130=197−14−53). ⑤ **진입속도**: firebase.js 영구캐시(initializeFirestore persistentLocalCache)+가림막 제거(fbLoaded 표내 스피너)+**세션 메모리 캐시**(_memRowsCache=재진입 0초)+빈화면 게이트. 수천 행 되면 연도별 분리(지금 불필요). ⑥ **틀고정 잘림 근본수리**: 원인=getW 누적 폴백(번호22px)이 내용맞춤 폭보다 좁아 스크롤 시 겹침 — **크롬 헤드리스 재현으로 입증**(실측=0/폴백=22px 조각). 수리=실측 전용(colSpan 행 스킵)+폴백 폐기. ⚠워처 미감지 재발→touch+새 코드 표식 문구로 확인. ⑦ **공지사항**: `src/notices.js`(원본, 배포 시 자동 반영)+홈 공지줄+팝업+빨간 점 — **마무리 루틴에 공지 추가 포함**. ⑧ 모바일 헤더 flex-wrap·홈 FHD 무스크롤·대시보드 구상메모 v0.1(월말 보고용 — 안건 4개 팀장님과 결정 대기). ⑨ **배포 완료**. 공용PC 팀순환 이상무 — 기술1팀은 NAS 규칙 0이라 순환 제외 권고(기술1팀 List 설정 토글, 관리자 계정). 다음=직원 반응 수렴·F안/보고모드/대시보드 결정·기술1팀 UNC·기술3팀 회신·8/10 잔여(직원1 365·VPN 문서)·백업 정리. 일일리포트: `일일리포트/2026-08-11_기술1팀프로파일-디자인개편-KPI대시보드-공지.md`

- 2026-08-13: **★기술1팀 월간보고 웹 재현 완성(엑셀 업로드 기반) + [월간 마감] + 홈 카드 월간 합산 — 미배포.** ① 엑셀 전수 분석: 전월/금월=**시트 복사**(수식 0건·D2 기준월이 전부), 행 오른쪽 **월별 매트릭스**(AA~AM 수량/AN~AZ 공정률, 월0~12)+HLOOKUP. 문서 `기준문서/월간보고엑셀_기술1팀_구조분석_v1.0.md`. ② 컨셉(팀장님): **List=뼈대·보고는 웹·월간보고=임원 보고 최종 자료**(A안 내보내기 폐기). 결정 4개: 항목 전부 유지+자재%=납품O→100 / 스냅샷=[월간 마감] b안 / 26년만 / 번역표 승인('보고완료'=수동 승격). ③ **1단계 [월간 마감]**(List 설정, tech1 카드 `월간마감:true`, `projectListSnapshots_팀/<YYYY-MM>`) 실기기 통과(2026-08 12건). ④ **2단계 `Tech1MonthlyScreen.jsx`**(currentMode 'pms'+기술1팀 분기): [엑셀 업로드]='금월' 시트 전체→`monthlyReport_팀/2026`(전 사용자 공유)·[엑셀 생성]·**기준월 1~12월 선택(D2 재현)**·틀고정(공사명까지 고정폭 sticky)+헤더 고정+세로 스크롤 표 안·상태 칩·검색·정렬·KPI 4장·[월별 추이] 그래프(막대 클릭=월 이동)·**행 클릭=보고 팝업**(공정률 전월→금월 ▲▼·시운전 전월→금월·남은 작업 잔여+100% 미만 항목 알약+월별 실적 표)·아래 끝=List와 일치(flex). ⑤ **★매칭 키=견적번호(견적코드)** — 수행번호 26-XX가 상단(실행 승격)·하단(견적 이력)에 **다른 프로젝트로 중복**(99행 26-01=Infra vs 160행=용인) 실측, 승격 2줄이면 상단만 연동. 현재월=List 실시간/과거월=마감본이 엑셀 덮음, List 전용 행 항상 추가. ⑥ 규칙: 누적=매트릭스 전체합(V)·잔여=Point−(누적||0)(BB)·%=소수→×100(1.5 초과는 그대로)·'출장일정' 행부터 파싱 중단. **검증: 실엑셀 166행 전수 검산 26/26**(V·W·X·Y·Z·BB 일치, 수식 지워진 45·46행은 웹 보완). ⑦ **홈 기술1팀 카드=월간보고 기준 그 달 합산**(teamStats `computeMonthlyStats`: N월 진행 X/166·실적 pt·그 달 공정률 평균·달성률, 기술2·3팀 불변). ★기준월 12월 오표시 수리=**D2 최우선**(엑셀 8~12월 잔존 값 행 20여 개가 원인 — 진위 심광호 확인 권고, 시뮬 5/5). 변경: Tech1MonthlyScreen(신규)·ProjectListScreen·projectListData·teamStats·tech1·App.js·notices(08-13)·구조분석 문서(백업 `.bak-2026-08-13`). **다음=①배포(8/12분 포함)+실기기(업로드→월별 조회→팝업→홈) ②홈 'N월 진행' 기준(기록 있는 행 vs 상태 진행중) 팀장님 답 ③후속: 마감→매트릭스 자동 축적·홈 카드 월간보고 직행·세부 행("1) 공조") 접기.** 일일리포트: `일일리포트/2026-08-13_기술1팀월간보고-웹재현-월간마감-홈합산.md`
- 2026-08-14: **기술1팀 월간보고 일단락(+행 팝업 실적 그래프) · ★기술2팀 월간보고 전수 분석→협의 대기 · ★기술3팀 List 가동(기술2팀 양식 확정).** ① 기술1팀: 보고 팝업에 실적 그래프(막대=월별 수량·이월 회색·기준월 강조, 선=전체 공정률) 추가 — **추후 심광호 협의로 일단락**(8~12월 잔존 값 진위 포함). 배포 명령 안내(8/13분). ② **기술2팀 월간보고**(`월간보고_기술2팀_2026년8월_260803_파주.xlsx`, 최영환) 수식+서식+숨김 10장 전수 분석 → `기준문서/월간보고엑셀_기술2팀_구조분석_v1.0.md` **협의 대기 도장**. 구조=시트 1장(전월 시트 없음)·D2+매트릭스+HLOOKUP(1팀과 동일 엔진)·메인 275+**하위 143행**(부모 Point=SUM(자식)·%=SUM/분모 하드코딩 82개·**수량 매트릭스는 부모≠자식합**). ★핵심 문제=**매칭 키 부재**(웹 견적코드 0/152 빈칸·엑셀 견적번호 형식 4종 혼재·실행번호 양쪽 절반)+웹 26년 ~130건 vs 엑셀 26년 31건. 못 채우는 항목·이상한 점 12개·결정 6개(매칭 키/하위 이관/자재 x·1 의미/발주처 대응/미등재 100건/번역표) 문서화 — **최영환 협의 후 재개, 문서 7장=질문지**. ③ **기술3팀**: `01 2026년_구미 공사 List_기술3팀_260814.xlsx`(공사 List 원본, 29열·44건) 분석 → `기준문서/항목정의서_기술3팀_v1.0.md`. ★**7/30 백업 기술2팀 웹 헤더 29개와 완전 일치**(회신=양식 채택이 파일에 반영, 변환 불필요). **tech3 카드 확정**: `파서옵션.필수열=['번호','Project']`(번호만 적힌 예약행 45~49 제외 — 없으면 49건 유령)+`담당자목록=['김승환 팀장','황준연 담당']`(기술2팀 상속 명단 칩 버그 — 기술1팀 8/11 동일 사례). 검증=**실파서 코드 추출 실행 16/16**(헤더 일치·44건·001 패딩·발주처 중복 분리)+실기기 업로드 44건 완료. 메모: '작업 완료' 1건(표기 변형 — 김승환 확인 권고)·HOLD 3(웹 정규화)·HMI 정수% 잘림(3.3→3%)·구미 UNC 미정. 변경: Tech1MonthlyScreen(그래프)·tech3.js·notices(08-14)·기준문서 2종 신규. **다음=①기술2팀 월간보고 협의(최영환) ②기술1팀 협의(심광호) ③배포 상태 확인(8/13분+오늘분) ④이월(VPN 폰 LTE·직원1 365·UNC 2건·백업 정리).** 일일리포트: `일일리포트/2026-08-14_기술1팀그래프-기술2팀월간분석-기술3팀List가동.md`
- 2026-08-19: **★기술1팀 List 대개편 — 전연도 적재(2014~2026, 434건·열번역·비고보존·탭간중복만 제거) + 수식 체계(자체=금월÷총물량%·누적·전체=누적·[월간마감] 롤오버=금월성 칸 비움) + 팝업·그래프 연동(ETOS T/S 매핑·pt push·잠금·그 달 기준 진척률) + 지난연도 보고카드(완료÷전체 달성율) + 단어 드롭다운(카드 `드롭다운열`) + 통합시운전 기본미적용(2026만).** 공통: 월간보고 3팀 잠금(`MONTHLY_REPORT_OPEN` 스위치)·그룹 경계선(헤더 2줄)·날짜 YY/MM/DD 표시·헤더 (2) 꼬리표 숨김·NAS 파일목록 최신1개(f631e2d 커밋). 버그수리=주차장부 동시쓰기 경쟁(순차 await — ETOS 소실 원인). 테스트=우클릭 '7월 실적 심기'(admin+localhost, 검수 후 제거). ⚠CRLF 오염 복구(python 저장 — PLS만 원본 CRLF). **◆미해결(내일 1순위): 그래프 공정률 8월 25% — 7월 20%·8월 25% 조합은 getRecordMonthlyProgress로 설명 안 됨 → 점 데이터 소스 재추적(①캐시 ②mdEntry 경로 ③적용하기 값 ④그래프 모달 2개 트랩). 그 후=마감 실기기·전연도 실전 업로드·배포.** 일일리포트: `일일리포트/2026-08-19_기술1팀수식-전연도적재-드롭다운-월간보고잠금.md`
- 2026-08-20: **기술1팀 수식 최종 확정(팝업=입력 원장·금월=기준월 합 자동·진척률 검산 ✓·테스트 도구 제거) + 보고카드 실분포 수리(진행중=빼기 폐지, 434건 전수 검산) + 자동 칸 표시(.cell-auto — 인라인은 index.css !important에 밀림 재확인) + NAS 미포함 항목 ×(010 ETOS) + 홈 카드 3팀 List 기준 통일(기술1팀 공정률='전체' 평균) + 실적 그래프 전면 재설계(미니멀) + ★기술2팀 과거 연도 적재 기능(2013~2025 · 1,712건 전수 분석·확정 4건·시뮬 13/13 — 실전 실행은 내일) + 기술2→3팀 자동 동반 적용 원칙(메모리 저장).** 미배포(8/18~20 누적). 다음=①과거 적재 실전 ②월간 마감 롤오버 실기기 ③배포. 일일리포트: `일일리포트/2026-08-20_기술1팀수식마무리-그래프재설계-기술2팀과거적재.md`
- 2026-08-21: **기술1팀 연도별 1:1 재정비(meta.byYear 연도별 헤더 벌 + [연도별 1:1 적재·검증] 도구 + 별칭 매핑 aliasCol — 2026·2025 100% 일치, 나머지 팀장님 진행) · 지난 연도 날짜 26 오표시 수리(행 _year) · 관리자 열 제거 · 백업/복원 검수 통과 · 관리 칸 NAS만 · 기술1팀 규칙 다수(드롭다운 4개+담당/수행 다중·카드 전체/준비/진행중/완료·상태색·빈칸 회색·기본 맞춤 100%·수행번호 YY-NNN+[+]·헤더 테두리) · 홈 카드 6항목.** 미배포(8/18~21). **★차주 월(8/24) = 기술2팀 작업 시작(변수 많음)** — 과거 적재 실전부터. 일일리포트: `일일리포트/2026-08-21_기술1팀-연도별1대1재정비-백업복원-수행번호.md`
- 2026-08-24: **★기술2팀 260822 재적재 완주(14연도 1:1·칩/카드 규칙) + NAS 재배선(누적→Point·통합시운전→진행율% — extWebCol 별칭 10곳·isPointCol 수리·011/010 재등록 실전) + 관리칸 원클릭 엑셀([P9][P10][진행] — lastFiles 클라우드 보존) + 기술3팀 260824 새양식 전환·과거 2021~2025 월간보고 기반 List 생성(202건·27/27 검증, outputs) + 기술1팀 칩=작업칸·수행번호 [+] 당해만 + 공통(전체카드=3종 합·빈칸칩·캐시 무제한+속도계측·3팀 순환 간격 자동·월간마감 3팀 통일).** 커밋/푸시 완료·**미배포(8/18~24 누적)**. 다음=①배포+공용PC 재설정(3팀 토글·NAS 허가증) ②기술3팀 실전 적재(2026+과거 5개년) ③속도 계측 확인 ④협의(최영환·심광호·김승환). 일일리포트: `일일리포트/2026-08-24_기술2팀재적재-재배선-기술3팀전연도-NAS원클릭.md`
- 2026-08-24(마감후): **◆기술2팀 2026년 번호 008 중복 = 유령 행 1건 진단.** 엑셀 260822 원본 188행 전수=중복 0(정상). 위쪽 008=옛 양식 행(공장·공사분류 빈칸·맨 위 정렬=옛 ID)·부활 경로 추정=[전체 삭제] 후 옛 화면 켜 둔 다른 PC/탭이 저장(진행실적 적용 등)→클라우드 "없으면 새로 만들기"로 옛 복사본 부활. **내일 1순위 조치=①위쪽 008 우클릭 [완전 삭제] ②직원 전원 F5 공지 ③2026 행수=엑셀 188건(+하위) 대조·공장 빈칸 행 추가 점검**(삭제 후 재등장=옛 화면 PC 잔존). 상세=일일리포트 6번.
- 2026-08-25: **008 삭제·8/18~24 배포 완료 + "N월완료" 카드 삭제(3팀) + ★파수꾼 새벽 5:30 자동 재시작**(전용 창 영구 미갱신 구멍=유령 행 범인 — 신규 restart 2파일+설치/해제/상태보기 갱신, ⚠공용 PC 설치.bat 재실행 필요) **+ 010 진행율% 소실 수리**(팝업 저장 paRecalc 분모 0→effTotalPt 폴백) **+ 그래프 공정률 카드 계산 근거 표기**(PLC+ETOS+HMI+통합 동적) **+ ★기술2·3팀 통합시운전 기본 ON**(통합열 '진행율 %' 별칭 3곳)·팝업 [자체/통합] 기본=통합(무조건) **+ ★Point 양방향 동기화**: 메인표 키인→장부 증분 배치(이번 주=새 누적−다른 주차 합·감소 차단·시뮬 7/7) / 팝업 [적용하기]→Point 열+진행율% 재계산(185·92.5% 검산). 팝업 진척률 vs 그래프 공정률 차이=현행 유지 확정. **오후분 미배포.** **내일 1순위=Point 동기화 실기기 테스트 3단계**(적용하기 185 / 200 키인 +15 / 100 키인 차단)→배포→파수꾼 재설치→기술3팀 실전 적재. ⚠관찰: NAS 하위 장부 주차 넘어갈 때 팝업 합계 부풀기 의심(월요일 확인). **+마감후: 프로젝트 추가 번호 자동+1(그 연도 최대+1·복사여도 새 번호)+중복 저장 차단(⛔경고, '번호'/'순번' 3팀·연도별 독립·하위 제외, 시뮬 7/7)**. 일일리포트: `일일리포트/2026-08-25_유령행삭제-파수꾼재시작-Point양방향동기화.md`
- 2026-08-26: **Point 동기화 실기기 3단계 통과 + ★메인표 키인 느림 원인 규명·수리(완성본+Playwright 자동 재현으로 검증).** 원인: ①**Firebase 무료 할당량 소진 확정**(REST 429→16:00:37 정상, 저장 3.8초·멈춤의 뿌리 → 사용량 확인·Blaze 검토) ②저장 후 열 폭 흔들림→틀고정 재실측→188행 전체 재렌더 433ms+강제배치 478ms ③편집 입력창이 열 폭 변경→표 전체 재배치 ④행 전체+무한 이력 업로드·장부 서버 재읽기 ⑤hover 애니메이션. 수리: 편집 칸=보통 칸 상자+입력창 폭 고정 / 틀고정 오프셋=CSS 변수(`--frz-i`, 재실측이 재렌더 안 일으킴, 값 변경은 0.4초 뒤) / 기본맞춤 자연폭·창폭 캐시 / 행 저장 merge+이력 300건 상한 / 장부=progressRecordsMap 구독본 / App 장부 증분 / transition 제거 / 영구 캐시 유지(스위치). 계측 코드 전부 제거. 결과: 클릭 20~50ms·Enter 3~12ms·저장 후 전체 재렌더 0. ★캡처 요청 폐기→**자동화 재현 체계**(`scratchpad/pw/run_speed.js`, 공용 계정 일반 권한). **미배포(8/25 오후분+8/26) — 팀장님 배포 명령 안내함.** 다음=①Firebase 사용량 확인·요금제 ②배포 후 공용PC 파수꾼 재설치 ③첫 로그인 18초 조사 ④기술3팀 실전 적재. 일일리포트: `일일리포트/2026-08-26_키인속도-원인규명-Firebase할당량.md`
- 2026-08-27: **★메인표 = 모아서 저장(초안) 전환**(키인·드롭다운→노란 칸, [저장 N칸]/Ctrl+S·[취소]·F5 유지·이동 가드·동시수정 확인 — 키인 중 Firebase 통신 0 실측) **+ ★창 렌더링**(화면 근처 ±15행만 그리기, 최초 1회 전체 렌더로 열 폭 실측·고정 winPinW — 열기 656→54~120ms·엔터 1,273→84~245ms, 배율 80%·틀고정·하이라이트 검증) **+ ★일괄 저장 장부 소실 수리**(연속 쓰기가 낡은 구독본 base로 서로 지움 → ledgerFreshRef 15초 최신본 캐시 — PLC·ETOS·HMI 팝업 미동기의 원인, 실기 재현 확정) **+ 드래그 범위 선택→Del 일괄 지우기**(DOM 하이라이트=재렌더 0·초안 적재·잠금/드롭다운/실행번호 제외) **+ 지우기 동기화**(빈칸 저장=장부 '이번 주' 삭제·이력 보존) **+ 0=지우기 통일**(7/10 팝업 규칙을 메인표 공정률·Point·포인트총점에 확장) **+ 그래프 % 라벨 겹침 v2**(막대 라벨 실좌표 회피). 검증=Playwright 자동 재현 다수(6단계·왕복·다칸 배치). 부수: [저장 N칸] 깜빡임 box-shadow→opacity(Layerize 1.9s 제거)·값만 변경 시 frzTick 렌더 생략·노란 칸=실변경 칸만. **커밋 완료·미배포 — build+deploy 필요.** 다음=배포→공용PC 파수꾼 재설치·Firebase 사용량/Blaze·기술3팀 실전 적재·협의 3건. 일일리포트: `일일리포트/2026-08-27_초안저장-창렌더링-장부소실수리-드래그삭제.md`
- 2026-08-27(마감후): **담당자 직책 표기 한글 통일** — 상단 담당자 칩·담당자 관리 화면이 명단 코드(DD·TL·C)를 그대로 보여주던 것을 셀·드롭다운과 같은 변환(담당·팀장·책임)으로, 팀 카드 명단(tech1·tech2)·ASSIGNEE_LIST도 한글화(데이터·필터 불변). 커밋 완료·미배포. **+ 기술1팀 이름만 저장→표시 때 직책 자동 부착**(asgTitleOf/decorateAsg — 담당·수행·접수자 칸, 칩·드롭다운 라벨; 저장값은 이름만 그대로). **+ 기술1팀 순번 3자리 통일**(정리 도구·편집 패딩·자동+1이 '번호'만 보던 것 → 번호/순번 공용, 카드 번호패딩 true — [번호 3자리 정리]는 관리자 실행 필요). **+ (8/28 아침) 기술1팀 트리거 칸 클릭만→자동 칸 0 노란칸 버그 수리**(값 실변경 때만 수식·진행율 재계산 + 수식 결과 0=빈칸, 자동 검증 통과 — 기존 0 노란칸은 [취소]로). **+ 수행번호 [+] 같은 번호 반복·중복 저장 무방비 수리**(activeRowsRef 최신 목록·손 키인 ⛔·[저장] 최종 중복 검사, 자동 검증 ✓ — 이미 저장된 26-003 중복 행은 ✕ 회수 후 [+] 재부여 필요). **+ 헤더 정렬 3단계**(오름→내림→해제 — '수행번호 넣으면 행이 아래로' = 내림차순 정렬 켜진 상태였음). **+ 수행번호 손 키인 금지**(메인표 클릭=토스트만·상세/추가팝업 🔒 — [+]/✕만, 당해 연도 메인 행). **+ 하단 상태줄 '정렬: 열 ↓ [해제]' 표시**(정렬 켜진 줄 몰라 행 이동 혼란 재발 — 1클릭 해제). **+ 행 더블클릭→상세 팝업 제거**(팀장님: 혼란 — 우클릭 메뉴로만). **+ 프로젝트 추가(행 복사) 시 수행번호 복사 제외**(중복 방지). ※'행 선택됨' 문구=행 클릭 선택→추가 팝업 초기값 복사 기능(동작 확인) — 단 셀 클릭은 전파 차단이라 편집창 재클릭으로만 선택되는 숨은 경로(우클릭 메뉴 이관 권고).
- 2026-08-18: **홈 화면 압축(FHD 무스크롤 복원) + List 틀고정 오프셋 수리 2건(표시행 변화 재실측 `frzTick`·sticky offsetLeft 오염 → 비고정 셀 기준 역산) + Claude Code 업데이트 재실행으로 챗 소실→파일 흔적으로 복원.** 공지 08-18 추가. **미배포**(8/13·8/14분 포함). 다음=실기기 확인(홈 스크롤·기술3팀 칩 후 가로스크롤)→배포→이월(기술2팀 월간 협의·기술1팀 협의·VPN·365·UNC). 일일리포트: `일일리포트/2026-08-18_홈압축-틀고정재실측-업데이트튕김.md`
- 2026-08-12: **최초 진입 '없습니다' 번쩍임 잔존 수리 + VPN 집 접속 진단(진행 중).** ① 원인=헤더(메타)·행이 별도 통신인데 어제 게이트는 행만 확인 — 행이 먼저 오면 "도착 완료+헤더 없음"으로 업로드 안내 오판. 수리=`fbMetaLoaded` 신설, **행+헤더 둘 다 도착해야** 빈 팀 판단(오류 시 무한 스피너 방지 포함). 시뮬 5/5·백업 `.bak-2026-08-12`. **미배포 — 다음 배포 때 반영**(공지 08-12건 notices.js에 준비됨). ② VPN 집에서 "Trying to connect" 무한 재시도: 가이드 `운영가이드/문제해결_VPN_집에서접속안됨_20260812.md` 작성. 실측 소거 = 자동 차단 목록 비어 있음(위치: DSM 보안→**계정 탭**→차단 목록) + **회사 공인 IP 222.101.203.90 = 8/6과 동일(불변)** → 차단설·IP변경설 탈락. **새 1순위 = 집에서 쓴 .ovpn이 8/10 수정본 아닐 가능성**(`remote 222.101.203.90 21194`+`setenv CLIENT_CERT 0` 두 줄 확인). 결정적 실험=사무실 폰 LTE로 같은 파일 연결(되면 집 회선/안 되면 파일·서버) — **결과 대기, 다음 챗 여기서 이어가기**. 집 IP 49.161.36.18(참고용). 일일리포트: `일일리포트/2026-08-12_진입번쩍임수리-VPN집접속진단.md`

### 실제 앱 메뉴 ↔ 코드 매핑 (6/9 실행화면 기준)
| 앱 메뉴 | 설명(앱 표기) | 코드 |
|---------|---------------|------|
| 팀 선택 | 기술1·2·3팀, Software팀 | TeamSelector.jsx |
| 월간 업무 보고 | 시운전 실적·공정률 월간 현황 (=공정현황) | App.js `currentMode='pms'` |
| 프로젝트 List 관리 | 팀 전체 프로젝트 목록 엑셀 기반 관리 | ProjectListScreen.jsx |
| 주간 보고 뷰어 | 업로드→저장→목록에서 바로 열기 (XLSX) | WeeklyReportScreen.jsx / WeeklyPanelViewer.jsx |
| 주간 보고 입력 | 엑셀 템플릿 분석→주차별 날짜 입력→공정률 자동계산 | WeeklyInputScreen.jsx |
| 주간보고 요약 | 주차 실적·이슈·계획→담당·임원보고 자동화 | WeeklySummaryScreen.jsx |
| 견적 | 견적 산출 | EstimateScreen.jsx |
