# 2026-06-23 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일
UI/UX 개선 (로드맵 ★6/23 1순위) — **A안 클린 화이트** 적용:
1. **다크→라이트 전환 + 색 통일** — 화면 전반을 브랜드블루(#1e7ac8)로, 산재하던 cyan·indigo 색 정리
2. **디자인 견본판 신설** (`src/index.css`의 `:root` CSS 변수) — 색·글씨 크기를 한 곳에 모음. 여기 한 줄만 고치면 표·모달 전체에 반영. 토큰: `--brand`(브랜드색), `--line`(표 선색), `--head-bg`(헤더배경), 상태색 4종, 글씨크기 3단계(제목/부제목/본문)
3. **메인 팀선택 화면** — 기술1·3팀에 하위메뉴(Software팀 칸) 추가, 4개 팀 아이콘색 통일, 카드 정돈, 제목 확대(2xl→3xl)·버전표기 제거
4. **월간보고 표** — 세로선 전부 제거(가로선만 연하게), 모서리 둥글게, 헤더 글씨 키움+중앙정렬, 숫자 우측정렬 통일, 폰트 Pretendard
5. **모달 3종**(실적그래프·상세수정·진행실적 등록) 견본판 색에 연결
6. **진행실적 등록 팝업 숫자 잘림 수정** (TOTAL 열 폭 확대)
7. **공정률(%) 맨끝 칸 막대 추가** — 막대+숫자 병기, 100% 초과값(2567% 등)은 막대 꽉참+숫자 그대로 표시

## 변경한 파일
- `src/App.js` — 표(renderTable)·메인 팀선택·모달 3종·공정률 막대
- `src/index.css` — 견본판(:root 토큰)·표 스타일(세로선·색)
- `src/components/ProgressModal.jsx` — 모달 색·숫자 잘림
- 백업: `App.js.bak-2026-06-23`(·-b·-c), `index.css.bak-2026-06-23`(·-b·-c), `components/ProgressModal.jsx.bak-2026-06-23`

## 결정·근거
- **견본판 중앙화**: 스타일이 7천 줄 거대 파일에 흩어져 관리 불가 → CSS 변수로 한 곳에 모음. 한 번 고치면 전체 반영 (팀장님 핵심 요구). 시연으로 `--line`을 빨강으로 바꿔 표 선 전체가 빨개지는 것 확인
- **컬럼 구조·셀 값·엑셀 인/아웃 절대 불변**: 모양(className/CSS)만 변경 — 엑셀 업로드/다운로드 호환 보호 (팀장님 강조)
- **수치 검증은 디자인 후로**: 공정률 2567% 등은 키인/엑셀 데이터 오류로 추정되나, 디자인 마무리 후 데이터 전체 삭제→새 테스트 엑셀 업로드로 검증 (팀장님 합의). 시스템 수치 전체가 미검증 초안(루틴8)
- **Edit 도구 파일끝 반복 절단 대응**: sed 우선 사용 + 매 변경 후 NUL·babel parse·끝줄 3종 검증, 절단 시 백업에서 끝부분을 이어붙여 복구

## 다음 할 일 / 막힌 점
- **내일: 프로젝트 List 관리 화면을 오늘 만든 형식/틀(견본판·라이트)로 통일**
- 공정률 막대 후속(팀장님 확인 후): ① 막대 칸 연파랑 배경 제거 여부 ② 금월 공정률 칸에도 막대 적용 여부
- 수치 검증: 데이터 전체 삭제 → 새 테스트 엑셀 업로드 → 검증
- 막힌 점: 임시파일 3개(`App_tmp.js`·`fix357.sed`·`t.sed`, 모두 0바이트)가 샌드박스에서 삭제 권한 없음 → VS Code에서 삭제하거나 커밋에서 제외 필요
