# 2026-07-15 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. 모바일 버전 방향 확정 (질의 → 결정)
- 요구: 담당자가 휴대폰으로 접속해 **진행실적 등록만** 할 수 있게. 걱정 = 모바일/PC 두 버전 관리 부담.
- 결정: **별도 앱을 만들지 않고, 지금 웹앱 안에 모바일 전용 화면 1장 추가.**
  - 코드 1벌 · 배포 1번 · 장부(Firebase) 동일 → 이중관리 문제 자체가 없음.
  - 별도 앱(스토어 배포)·List 전체 반응형은 비추천으로 정리 (전자=코드 2벌, 후자=표가 커서 폰에 무리 + 목적 과잉).
- 팀장님 선택 (질문 2건): ① 담당자 식별 = **개인 로그인 연동** ② 입력 범위 = **PC 팝업과 동일** (공정률+시운전 전부).

### 2. 코드 실태 확인 (추측 금지 규칙)
- `registeredUsers/{email}` (displayName·role·active) 로그인 시 자동 조회 — App.js 688행.
- `extractName`(projectColumns.js 84행): 김준혁TL ↔ 김준혁 팀장 동일인 처리 — **계정↔담당자 연결에 재사용, 새 저장 필드 불필요.**
- ProgressModal = 자체 로드/저장(progressRecords_팀, docKey=pid 우선) 독립 부품 → 통째 재사용 가능 확인.
- List 행 = `projectListRows_팀`(_id 정렬), 적용 규칙 = applyProgressToMainRow(709행): patch + 도장(stampSave) + _changeHistory + 백로그.
- 라우팅 = currentMode 분기(5687행 부근), 랜딩 조건(5462행)에 예외 필요 확인.

### 3. 구현
**(a) `src/components/MobileInputScreen.jsx` 신규 (~257줄)**
- 4팀 List 로드(meta+rows, _id 정렬) → 담당자 칸(공백 무시, '업체' 제외) 값과 로그인 이름을 extractName으로 매칭.
- 카드 = 메인 행만(하위 s/- 행은 팝업 '하위'로 따라감), '삭제' 도장 행 제외. 카드에 프로젝트명·실행번호·상태칩·총진척률.
- 카드 탭 → **ProgressModal 통째 재사용** = 저장·계산·적용 경로 PC와 100% 동일.
- '적용하기' = applyProgressToMainRow와 동일 규칙 구현(도장 _updatedAt/_updatedBy + _changeHistory + 백로그 EDIT, note '모바일 진행실적 적용') + applyProgressByPid로 월간보고 동기화 + 적용 후 해당 팀 재로드.
- **상태 필터 칩**: 내 카드의 실제 상태값 자동 집계(하드코딩 없음) → [전체 N] + 상태별 칩(건수 많은 순). 선택은 localStorage(`pms_mobile_status_filter`, 기기별) 기억, 기억된 상태가 데이터에 없으면 자동 전체. HOLD→Hold 통일(normalizeStatus), 빈 상태 = '(상태없음)' 칩.
- 상단바: 새로고침 / PC 화면으로 / 로그아웃. 담당 0건이면 이름 확인 안내(사용자관리 유도).

**(b) `src/App.js` 6곳 (Python 부분치환)**
- import MobileInputScreen + lucide `Smartphone`.
- **휴대폰 UA 자동 진입** effect: 로그인 확인 직후 1회, `/Android|iPhone|iPad|iPod/` → `setCurrentMode('mobileInput')`. 'PC 화면' 버튼으로 나가면 재진입 안 함(didMobileRouteRef).
- 랜딩(팀선택) 조건에 `mobileInput` 예외 + 라우팅 분기 신설(backlog 분기 앞).
- 팀선택 우상단 **[모바일 입력]** 버튼(전 사용자) = PC에서 화면 점검용 수동 입구.

**(c) `src/components/ProgressModal.jsx` 2줄**
- `progressPanelW = Math.min(visW+48, max(320, 화면폭-8))` + 초기 x좌표 여백 20→4.
- PC(넓은 화면)는 계산상 변화 없음(시뮬 확인), 폰에서만 팝업 폭이 화면에 맞고 주차 영역은 기존 내부 가로 스크롤 사용.

### 4. 검증
- babel 파싱: App.js·ProgressModal.jsx·MobileInputScreen.jsx 전건 통과.
- 자동 시뮬 **23/23**(이름 매칭·하위행·삭제 제외·patch/changes 규칙·UA 감지·팝업 폭 PC 불변/폰 축소) + **9/9**(칩 집계·건수순·Hold 통일·상태없음·기억 필터 소실 시 전체·필터 합계=전체).
- 실기기(PC, 팀장님): [모바일 입력] 진입 → 담당 카드 60건 표시 → 칩 [전체 60][완료 28][추진중 16][2023년이관 7][2018이전 5][진행 2][이전 2] 동작 확인.
- 폰 실기기는 배포 후 예정.

### 5. 이슈·교훈
- **샌드박스 미러 잘림 재확인**: 새로 만든 MobileInputScreen.jsx가 미러에서 213줄로 잘린 스냅샷으로 읽혀 babel이 헛 실패. 실제 파일(파일도구 Read)은 257줄 온전 → 전문을 /tmp 사본으로 만들어 파싱 통과. **미러 쪽 파싱 실패 시 실제 파일 Read로 먼저 확정할 것** (기존 [[pms-edit-tool-truncation-trap]]과 짝이 되는 함정).
- 채팅(Cowork) 미리보기로 JSX 열면 "지원되지 않는 라이브러리" 오류 = 정상 (Firebase 등 앱 전용 import 때문, 앱 안에서만 동작).

## 변경한 파일
- `src/components/MobileInputScreen.jsx` **(신규)**
- `src/App.js` (백업 `App.js.bak-2026-07-15`)
- `src/components/ProgressModal.jsx` (백업 `ProgressModal.jsx.bak-2026-07-15`)
- 중간 백업: `MobileInputScreen.jsx.bak-2026-07-15b` (칩 추가 전)

## 결정·근거
- **한 앱 + 모바일 화면 1장** (별도 앱 X): 계산기·장부·배포가 하나라 이중관리 없음 — 6/11 계산 일원화 원칙과 동일 논리.
- **개인 로그인 연동**: 이미 구축된 registeredUsers 활용, 실명 백로그와 자연 연결. 공용 계정으론 '해당 담당자만' 불가.
- **extractName 매칭** (새 필드 대신): 7/7 필터·카운트에서 검증된 동일 기준 재사용 → 유지보수 최소.
- **ProgressModal 재사용** (모바일 전용 입력폼 신설 X): 로직 중복 0, PC와 수치 불일치 원천 차단.
- **상태 칩 자동 생성** (목록 하드코딩 X): 팀별 명칭 차이·신규 상태 자동 대응.
- 진입 = UA 감지 자동 (화면폭 감지 X): PC 창 좁힘에 안 끌려가고 예측 가능.

## 다음 할 일 / 막힌 점
1. **커밋·push (VS Code)** — 7/14분 + 오늘분 누적, 분량 큼. 이후 `npm run build` → `firebase deploy --only hosting` 해야 폰 테스트 가능.
2. **폰 실기기 테스트** (배포 후): 자동 진입, 카드 목록, **진행실적 팝업 사용감**(폭·키인·푸터 버튼 줄바꿈) — 불편하면 다듬기.
3. **담당자 준비**: 사용자관리에 개인 계정 등록 + '이름'을 List 담당자 표기와 일치(직책 붙어도 됨).
4. 후순위: PWA(홈 화면에 추가) 안내, 4팀 전체 로드 최적화(필요 시), 미러 잘림 대비 검증 절차 문서화.
