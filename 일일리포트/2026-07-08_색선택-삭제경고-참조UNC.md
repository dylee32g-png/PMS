# 2026-07-08 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일
1. **진행현황 색 선택 (List, ②)** — 진행현황 관리 모달에서 상태별로 프리셋 9색(파랑·청록·초록·앰버·주황·빨강·보라·분홍·회색) 중 골라 지정. 색 동그라미 클릭 → 팔레트 펼침 → 선택. 저장하면 `teamSettings[팀].listStatusColors`에 기록되어 칩·필터·드롭다운에 자동 반영(기존 STATUS_COLORS 병합 코드 재사용). 안 건드린 색은 원본 유지. 실기기 확인 완료.
2. **삭제 경고 (List, ②)** — 진행현황·담당자 관리 모달에서 ✕ 누르면 바로 지우지 않고 그 자리에서 "예/아니오" 확인. 실기기 확인 완료.
3. **증감 (월간보고) — 구현 후 원복** — 자동표시 '증감'(공정률 금월−전월 ▲▼)을 App.js에 구현했으나, "List 완성이 먼저"라는 방침과 충돌 → 원복(App.js 최종 무변경). 작업물은 outputs에 보관(월간보고 차례에 재사용).
4. **항목정의서 완성도 점검** — `2026_기술2팀_List_항목정의서` '구현 예정 기능 9개' 대조. 8개 완료 확인, 참조 UNC 경로 복사만 미구현이었음 → 오늘 구현으로 9개 전부 완성.
5. **참조 UNC 경로 복사 (List, ⑥)** — 상세팝업 참조 칸에 '경로 복사' 버튼. 폴더명 앞에 팀 서버주소(UNC)를 붙여 클립보드 복사 → 탐색기 붙여넣기로 폴더 열기. 기술2팀만 경로 확정(`\\neconsys_pj\001 Project\001 파주\107 기술3팀 2026년 Project\`), 나머지 팀은 지역 달라 추후.
   - 초기 버그: 헤더가 '참 조'(중간 공백)라 `isRefCol`이 못 알아봄 → 공백 제거 비교로 수정(`isRefCol`·`isWideField`).
   - 실기기: Z: 드라이브 속성이 `NECONSYS_PJ (\\neconsys_pj)` → 코드의 서버주소 정확 확인. UNC라 개인 드라이브 문자(Z: 등) 무관 공통. 첫 에러 원인은 폴더명 오타(엑셀 `Snl`(엘) vs 실제 `SnI`(아이))였고, 값 맞추면 폴더 열림 확인.

## 변경한 파일
- `src/components/projectColumns.js` — `STATUS_COLOR_PRESETS`(9색), `UNC_PREFIX_BY_TEAM`·`buildUncPath`·`isRefCol`(공백 제거)
- `src/components/ProjectListScreen.jsx` — 진행현황 관리 색선택 UI + 삭제확인, 담당자 관리 삭제확인, `saveStatusMgr`가 `listStatusColors` 저장, DetailModal에 `currentTeam` 전달
- `src/components/DetailModal.jsx` — 참조 경로복사 버튼·`copyUncPath`·`currentTeam` prop, `isWideField` 공백 제거
- `src/App.js` — 증감 구현 후 원복(최종 무변경)
- 백업: `ProjectListScreen.jsx.bak-2026-07-08`, `projectColumns.js.bak-2026-07-08-unc`, `DetailModal.jsx.bak-2026-07-08`, `App.js.bak-2026-07-08`

## 결정·근거
- **색 선택 = 프리셋 9색**: 배경·글씨·테두리·선택색이 한 세트라 배색이 항상 조화롭고 클릭 한 번으로 끝. 자유 색상선택기는 글씨 대비 문제·코드 복잡 → 배제.
- **삭제 경고 = ✕ 즉시 "예/아니오"**: 실수 방지에 가장 직관적.
- **증감 원복**: 월간보고 영역이라 List 완성 우선 방침과 충돌. 팀장님 지적 반영.
- **참조 = UNC 방식**: 드라이브 문자(Z: 등)는 PC마다 달라 하드코딩 불가. UNC(`\\neconsys_pj`)는 매핑 무관 공통. Z: 속성으로 서버명 확정.
- **검증**: 자동 시뮬(색 14/14, 증감 12/12, UNC 13/13) + babel parse 전건 통과 + 실기기.

## 다음 할 일 / 막힌 점
- **오늘 변경 미커밋 → VS Code에서 push 필요** (projectColumns·ProjectListScreen·DetailModal).
- 참조 UNC 잔여: ① 나머지 팀(해외·구미) 경로 확보 후 `UNC_PREFIX_BY_TEAM`에 한 줄씩 추가 ② 폴더명 데이터 정확도(엑셀값 vs 실제, `l`/`I` 같은 오차)는 참조 칸 편집으로 개별 수정.
- List 남은 것: 엑셀 확정저장 실전(저장 전 백업 필수), 번호 자동+1 확인.
- 월간보고 차례 오면 증감(outputs 보관 작업물) 재적용.
