# 2026-09-03 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. 헤더 2단 재발 수리 (3번째 재발 — 회귀 테스트 표준화) `7c7b4c9`
- 증상: 상태 칩 다중 선택 시 주황 '필터 N' 배지 + [저장 N칸][취소] 버튼이 겹치면 헤더가 2단으로 감김.
- 실측: FHD 평시 여유 ~113px, '필터 N' 배지 −36px, 저장·취소 −64px → 초과.
- 수리: 미니 요약 칩 172→**152px** (내용 잘림 없음 실측).
- **회귀 테스트 표준화**: `scratchpad/pw/hdr_filter_test.js` — 3팀 × (평시/필터/초안+필터) 12상태 + 칩 내용 잘림 검사. **앞으로 헤더에 뭐든 추가하면 이 테스트 통과 후 완료 보고** (코드 주석에도 명시).

### 2. ★엑셀식 키보드 전면 구현 (팀장님: "표를 엑셀이라 생각하고 가능한 기능 전부") `ad46327`
- 셀 커서(파란 칠): 화살표 이동 · Enter=아래 · Tab=오른쪽 · 타이핑=즉시 입력 시작(영문·숫자) · F2=편집 · Esc=취소 · Shift+화살표=범위→Del · 범위 Ctrl+C=TSV(엑셀 붙여넣기) · Home/End·Ctrl+화살표·Ctrl+Home/End·PageUp/Down.
- 편집 열기=실제 클릭과 동일 경로(td.click) → 잠금 안내·드롭다운·열 폭 측정 등 기존 규칙 불변. 창 렌더 밖 점프 시 스크롤+칠 복원. 팝업·드롭다운 열림 시 자동 비켜섬.
- 한계: 한글 IME는 첫 글자 타이핑으로 편집 시작 불가 → F2/클릭 (공지에 명시).
- 검증: Playwright `kb_test.js` **16/16** (이동·타이핑·저장 후 이동·범위·TSV·195행 점프·취소 원복·페이지 에러 0). 테스트가 "편집창 닫힐 때 커서 칠 소실" 버그를 잡아 함께 수리.

### 3. ★편집창 화살표 = 엑셀 입력모드 (더블클릭 전환 없이 해결) `78786f4`
- 팀장님 지적: 원클릭=편집이라 클릭하는 순간 화살표가 글자 이동이 됨 → "더블클릭으로 가야 하나?"
- 해결 = 엑셀의 입력/편집 2모드 재현: 클릭으로 열린 입력창에서 **↑/↓=항상 저장+위/아래 칸 이동**(한 줄 입력창이라 캐럿 용도 없음), **←/→=타이핑 전엔 옆 칸 이동·타이핑 후엔 글자 사이 이동**(`editDirtyRef` 깃발). 값 안 고치고 지나가면 초안(노란 칸) 안 생김. 날짜 칸 동일.
- 검증: `kb_arrow_test.js` **11/11** + 기존 키보드 16종 회귀 **16/16**.

### 4. 배포 (팀장님 실행)
- 9/2 날짜 칸 개편 + 헤더 수리 + 키보드 2건 + 마감 문서 = 누적 5커밋 일괄 build+deploy 완료.

### 5. ★프로젝트 List 사용 설명서 v1.0 (전 팀 공통) `7c34816`
- **`운영가이드/사용설명서_프로젝트List_v1.0.docx`** (16장·표 9개) + 웹 버전(Claude 아티팩트 링크, 목차 점프).
- 구성: 접속/로그인 → 화면 구성 → 키인 기본(초안 저장·날짜 형식표) → 엑셀식 키보드 17종 → 범위/행 복붙·서식 → **자동 칸 6종**(NAS/수식/하위합/진행율%/수행번호 — "값을 바꾸려면" 표) → 추가/하위/삭제 → 진행실적 등록(주차 개념·적용하기) → 실적 그래프 → 상세 팝업 → **NAS 등록 절차 상세**(칩 색 4종·관리자 7단계·일반 PC·엑셀로 열기 .bat·메인 PC·빨간 경고 대처) → 엑셀 반영/업로드 → **외부 VPN 설치·접속**(STEP1~5+문제해결 4건, 8/10 검증 가이드 원문) → 팀별 차이 표 → 백로그 → FAQ 12건.
- 정확성: 버튼 이름·절차 전부 코드 실문구 대조(우클릭 메뉴·NAS 창 ①②③·[폴더 지정/변경]·[지금 확인] 등).

## 변경한 파일
- `src/components/ProjectListScreen.jsx` — 헤더 칩 152px · 키보드 블록(selRef 커서·handleGridKey·commitCellEdit 래퍼) · 편집창 화살표(editDirtyRef)
- `src/notices.js` — 09-03 공지 "표를 엑셀처럼" (화살표 항목 포함)
- `운영가이드/사용설명서_프로젝트List_v1.0.docx` — 신규
- 검증 스크립트(scratchpad): hdr_filter_test.js · kb_test.js · kb_arrow_test.js · make_manual_0903.py

## 결정·근거
- **원클릭 편집 유지 + 엑셀 입력모드** (더블클릭 전환 X): 직원 습관 보존하면서 화살표 문제 해결 — 엑셀 자체가 입력모드에선 화살표로 칸을 이동함.
- ←/→는 "타이핑 후엔 캐럿"으로 절충: 긴 한글 텍스트 중간 수정 사용성 보호.
- 매뉴얼은 docx(배포·인쇄) + 웹(링크 공유) 2종 — 같은 내용.

## 다음 할 일 / 막힌 점
- 수행번호 일괄 기입: 팀장님이 `outputs/수행번호매칭_기술2팀_260902.xlsx` 검토 → `apply_exec_0902.js --run`(◉+실행 40건) → 검산. △4건 결정 + #058/#129 동명 중복 정리.
- 공용 PC NAS 폴더 재지정(010·011) 결과 확인 — 빨간 경고 소멸 여부.
- 이월: 협의 3건(최영환·심광호·김승환) · Firebase Blaze 검토 · NAS 9월 주차 이중 합산 관찰 · 매뉴얼 피드백 반영(v1.1).
