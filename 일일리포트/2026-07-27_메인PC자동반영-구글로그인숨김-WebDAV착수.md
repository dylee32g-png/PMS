# 2026-07-27 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1) NAS 자동 반영 구조 설명 → 메인 PC 자동 반영 구현 (A안)
- 질문 시작점: "하위 있는 프로젝트에 NAS 연결돼 있는데, 다른 PC는 연결 안 해도 왜 업데이트되나"
  → 답: 다른 PC는 **엑셀을 읽는 게 아니라** 허가증 PC가 클라우드에 넣어둔 **결과값**을 받는 것.
  규칙·경로(`_extSync`)와 반영값은 Firebase 공유 / 폴더 허가증(핸들)만 PC별 IndexedDB.
- 이어진 요구: "메인 PC 정해서 자동 업데이트" → **A안 확정** (공용 PC가 항상 켜져 있음)
- 구현 (`ProjectListScreen.jsx`)
  - 설정 메뉴 **[이 PC를 메인 PC로 지정]** (localStorage `pms_ext_mainpc`)
  - **30분 주기 자동 검사·반영**. 값 갱신은 무확인 자동 / 하위 행 **신규 생성만 확인창** (팀장님 확정)
  - **1분 심장박동 + 벽시계 비교** 방식 — 크롬 백그라운드 탭 타이머 지연·PC 절전에도 밀린 검사를 따라잡음
    (단순 30분 `setInterval`은 스로틀에 밀림)
  - 자동 반영 결과 = 모달 아닌 **우하단 토스트** (무인 PC에 확인창 쌓이면 다음 검사가 막힘)
  - 폴더 허가증 만료·읽기 실패 시 **빨간 경고 토스트** (무인 PC라 조용한 실패 금지)
- 관리자 게이팅 (팀장님 지적 반영)
  - 설정 메뉴 항목을 **관리자 전용**으로 이동
  - ★**자동 실행에는 `isAdmin` 가드를 걸지 않음** — 플래그가 localStorage(PC 단위·계정 무관)라
    관리자가 켜둔 뒤 **일반 계정으로 바꿔 로그인해도 계속 돌아야 함**.
    (공용 PC를 관리자 계정으로 켜두면 엑셀 업로드·전체삭제 버튼 노출 → 위험)
  - 메뉴가 안 보이는 일반 계정을 위해 **하단 상태바에 `● 메인 PC 자동 반영 중 (HH:MM 확인)`** 표시

### 2) 로그인 화면 구글 로그인 숨김
- 직원 전원 회사 메일 링크 로그인 → 선택지가 늘면 혼동만 됨
- **삭제 아닌 스위치**: `LoginScreen.jsx`의 `SHOW_GOOGLE_LOGIN = false` → `true`로 되살림
- 푸터 문구도 '회사 메일로 받은 로그인 링크로 접속합니다'로 정리

### 3) 진행실적 팝업 주차 머리글에 날짜 범위 표시
- 질문: "7/27인데 왜 7월 4주에 들어가나"
  → 앱 주차는 **날짜로 자른 구간**(1~7·8~14·15~21·22~28·29~). 달력 주(월~일)와 다름. **정상 동작**
- 규칙·데이터는 그대로 두고 **W4 아래 `22~28` 작게 표기 + 툴팁** 추가 (`ProgressModal.jsx`)

### 4) ★ 웹 편집·저장 방식 브리핑 → WebDAV(길 A) 착수
- 팀장님 문제 제기: "엑셀이 주인이면 웹은 뷰어다. 웹에서 열고 고치고 저장하게 바꾸고 싶다"
- **실제 진척자료 파일로 왕복 저장 실험** — 값 하나 안 고치고 다시 저장만 했을 때
  | 항목 | 원본 | 재저장 후 |
  |---|---|---|
  | 수식 | 73,005 | 73,005 (보존) |
  | **계산된 값** | **168,311** | **152,753 (15,558개 소실)** |
  | PMS가 읽는 총계 8칸(H·M·R·Z·AH·AP·AG·AN) | 100%·94.5%·17230·3766 … | **전부 빈칸** |
  → **브라우저 라이브러리로 재저장하면 PMS가 못 읽는 파일이 됨.** 방법 2(NAS 직접 쓰기) 배제 근거
- 브리핑 4안 비교 → **길 A(NAS WebDAV + 진짜 엑셀로 열기·저장) 채택**
- 7/22 "ms-excel 프로토콜 원천차단" 결론 정정 → **네트워크 경로(`\\...`)라 막힌 것. http(s) 주소는 열림**

## 변경한 파일
- `src/components/ProjectListScreen.jsx` — 메인 PC 자동 반영 + 관리자 게이팅 + 하단 표시
  (백업 `.bak-2026-07-27`, `.bak-2026-07-27b`)
- `src/components/LoginScreen.jsx` — 구글 로그인 숨김 스위치 (백업 `.bak-2026-07-27`)
- `src/components/ProgressModal.jsx` — 주차 머리글 날짜 범위 (백업 `.bak-2026-07-27`)
- `.gitignore` — `_to_delete/` 제외 추가
- `커밋메시지_0727.txt` 작성 완료

## 결정·근거
- **A안(메인 PC) 채택** — 공용 PC가 24시간 켜져 있음. C안(별도 프로그램+작업 스케줄러)은
  Firebase 서비스 계정 키(전무님 승인) + 이중관리가 필요해 "유지관리 간편" 원칙과 충돌
- **자동 반영 범위 = 값 갱신만** — 데이터가 늘어나는 하위 행 신규 생성은 사람 눈으로 한 번 확인
- **자동 실행에 관리자 가드 금지** — 걸면 공용 PC를 일반 계정으로 두는 순간 자동 반영이 조용히 멈춤
- **주차 규칙 유지** — 16곳에서 쓰이는 핵심 규칙. 바꾸면 기존 주간 장부 주차 키가 전부 어긋남
- **웹 편집은 WebDAV로** — 웹이 엑셀을 '고치면' 깨지고, 진짜 엑셀이 저장하면 안 깨짐(실험으로 확인)

## 검증
- 메인 PC 자동 반영 시뮬 **19/19** (스케줄·백그라운드 스로틀·절전 복귀·반영 분리·실행 가드·권한 경고·기억)
- 관리자 게이팅 시뮬 **12/12** (메뉴 숨김 / 실행 유지 / 하단 표시 / 운영 시나리오)
- 주차 경계 시뮬 **19/19** (1·7·8·14·15·21·22·27·28·29·31일 + 2월·윤년)
- babel 파싱 3개 파일 전건 OK · eslint `no-undef` 0건
- ⚠ **실기기 확인 미완** — localhost에서 메뉴 표시·토글 동작 확인 필요

## 다음 할 일 / 막힌 점
1. **미배포·미커밋** — `npm run build` → `dir build\index.html` 확인 → `firebase deploy --only hosting`
   커밋: `git add -A` → `git commit -F 커밋메시지_0727.txt` → `git push`
   ※ 7/21·7/22분은 `dd537d3`으로 **이미 커밋됨** (CLAUDE.md의 '미커밋' 메모는 낡은 정보였음)
2. **WebDAV 이어서** → `인수인계_WebDAV_진행중.md` 참조 (관문 4개 중 3개 통과, 마지막에서 멈춤)
3. 메인 PC 지정은 공용 PC에 **관리자 계정으로 잠깐 로그인 → 켜기 → 일반 계정으로 전환** 순서
4. 볼륨 2 디스크 93% (경고 중) — 별건이지만 언젠가 정리 필요
