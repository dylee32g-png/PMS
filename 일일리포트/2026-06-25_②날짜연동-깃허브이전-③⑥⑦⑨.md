# 2026-06-25 일일 작업 리포트 (세션: ②·깃허브이전·③④⑥⑦⑨)
- 작업자: 염경록 팀장 / 프로젝트: pms-app
- ※ 같은 날 인수인계1~3 이후 이어진 본 작업 세션. 엑셀 업로드 전 기능 9종 전부 마무리.

## 오늘 한 일

### 1. ② 공사진행 내용↔날짜 자동 연동
- 내용 수정 시 같은 행 '날짜'를 오늘로 자동 갱신.
- `projectColumns.js`: `isProgressContentCol`('내용')·`isProgressDateCol`('날짜') 신설.
- `ProjectListScreen.jsx`: 두 저장 경로에 적용 — 표 인라인(`commitCellEdit`)·상세팝업(`saveDetailRow`). 내용 실제 변경 시에만 날짜 갱신, 변경이력에 내용·날짜 동시 기록. 공사계약/완료는 '날짜' 글자 없어 자동 제외(보호).
- 검증: 자동 23/23 + 실기기(번호10 P9 AP4 Infra — 날짜 2026-06-25 전환·변경이력 확인).
- **커밋 5ee6ddf (push 완료).**

### 2. 깃허브 원격 전무님 계정으로 이전
- 배경: 전무님(dylee32g-png)=최초 개발자→인수인계. 팀장님 로컬이 최신본. 앞으로 전무님 계정에서 관리.
- 협업자 초대 수락(회사메일 jeep@neconsys.co.kr → TipPick07 계정 매칭).
- `origin set-url`: TipPick07/pms-app → **dylee32g-png/PMS**. force push(전무님 옛 1커밋 d3e5c72 → 우리 최신 d056c1a)로 통일.
- `.gitignore` 정리: `.firebase` 캐시·`.claude/settings.local.json` 추적 제외(`git rm --cached`+커밋 **4c39048**).
- CLAUDE.md 원격 주소 갱신. 이후 일반 `git push`로 동기화(-f 불필요).

### 3. ③④ 발주처 담당자 분리 + 관리자 도입
- 엑셀 분석: '발주처' 헤더가 **2개**(2열=회사 152건, 21열=담당자 103건, 이름 동일) → 그대로 올리면 `obj[name]` 충돌로 한쪽 데이터 소실.
- `parseExcelHeaders`(projectListData.js): 중복 헤더 자동 구분 — 2번째 '발주처'→**'발주처 담당자'**. 덮어쓰기 방지.
- 관리자(7열, 79건) 자동 인식.
- 화면: 관리자·발주처담당자는 상세팝업 전용. `isClientCol`/`isAssigneeCol` 보정(발주처담당자가 회사·내부담당자 드롭다운에 잘못 걸리던 것 제외).
- 검증: 엑셀 파싱 7/7, 화면판별 15/15.

### 4. 엑셀 헤더 줄바꿈 정리
- 엑셀 헤더에 줄바꿈 다수("공사⏎계약" 등) → 그대로면 '공사계약' 매칭 실패로 ①②③ 헛돔.
- `parseExcelHeaders`: 헤더명 정규화(`\s+`→공백, "공사 계약").
- `isMainTableCol`: MAIN_COL 매칭을 공백무시 비교로 보강(참조·진행현황 등 누락 방지).
- 검증: 엑셀 전체 11/11.

### 5. ⑦ 표 항목 기본 숨김/표현
- 결정: 시운전 6개=기본 표시. 안전 8개+관리자=기본 숨김(필요시 설정 '열 표시/숨기기'에서 켜기).
- 발견: 안전 칸은 원래 `isMainTableCol`=false라 이미 메인표 제외(처음 넣은 useEffect가 헛돌았음). 관리자 '켜기'도 불가(체크박스 목록 미노출).
- 재설계: `isDefaultHiddenCol`(안전·관리자) 신설. `isMainTableCol`에서 관리자·안전을 **후보로 포함**(체크박스 노출) + useEffect로 기본 `hiddenCols` 추가(기본 숨김). 발주처담당자만 영구 상세팝업 전용.
- 검증: 11/11(체크박스 켤수있음·기본숨김·시운전 표시).

### 6. ⑥ 참조 폴더명 표시
- 엑셀 참조 값 = 폴더명("01 LGD GP" 등 151건). 하이퍼링크는 폴더명만(전체경로 없음).
- 폴더명 표시는 이미 충족(엑셀 업로드 시 자동). 경로복사 버튼은 팀별 UNC prefix 받으면 `[prefix+폴더명]`으로 (보류).

### 7. ⑨ 표↔상세팝업 동시수정 보완
- 빈틈: 상세팝업=행 복사본. 팝업 열린 채 표 인라인 수정 시, 팝업 저장이 복사본 전체로 덮어써 표 편집 소실.
- `saveDetailRow`: '현재 최신 원본(latest=activeRows)' + '팝업에서 실제 바뀐 칸(popupChanges)'만 merge. 변경이력도 latest 기준.
- 검증: 7/7(동시수정 보존·같은칸 팝업우선·무변경 표값유지).

## 변경한 파일
- `src/components/projectColumns.js` — isProgressContentCol·isProgressDateCol(②), isClientCol·isAssigneeCol 보정(③), isDefaultHiddenCol(⑦)
- `src/components/projectListData.js` — parseExcelHeaders: 헤더 정규화 + 중복헤더 자동구분(③④줄바꿈)
- `src/components/ProjectListScreen.jsx` — commitCellEdit·saveDetailRow(②⑨), isMainTableCol 재설계+매칭보강(③④⑦), useEffect 기본숨김(⑦), import
- `CLAUDE.md` — 원격 주소 갱신
- `.gitignore` — .firebase·.claude/settings.local.json
- 백업: `*.bak-2026-06-25-feat2`(세션 시작본)

## 결정·근거
- 발주처 담당자 개명은 기준문서(항목정의서)에 이미 확정 — 이번 엑셀로 "왜 필수인지"(이름 동일→충돌) 근거 확보.
- 화면 표시(③⑦)는 엑셀 업로드 후 효과 → 실기기 검증은 내일 업로드 시. 지금은 자동 시뮬로 검증(미검증 초안 방지, 루틴 규칙 8).
- ⑦ 재설계: 팀장님 "관리자 필요시 켜기" 요구에 맞춰 영구제외→후보+기본숨김으로 전환.
- bash 마운트 지연 재확인: babel 일부 FAIL은 착시(파일도구 Read로 D드라이브 정상 확인). 대형 파일은 Python 부분치환.

## 다음 할 일 / 막힌 점
- ★ **내일: 엑셀 업로드 테스트** — 설정 '엑셀 업로드'(pending)→'Firebase 확정 저장'(기존 전량삭제+엑셀 교체). **저장 전 백업 필수**(엑셀 생성으로 pid·실행번호·이력 내보내기 — 엑셀엔 없음). 업로드 후 ③④⑥⑦⑨ 실기기 확인·문제 잡기.
- ⑥ 경로복사 버튼: 팀별 UNC prefix(`\\neconsys_pj\...\107 기술N팀...`) 받으면 구현.
- **미커밋: ③④⑥⑦⑨+줄바꿈** (projectListData·projectColumns·ProjectListScreen) → VS Code에서 커밋·push.
- 커밋 메시지(제안):
  ```
  feat(list): 엑셀 기준 재정비 ③④⑥⑦⑨ + 헤더 줄바꿈 정리

  - ③④ 발주처 담당자 분리·관리자 도입(중복헤더 자동구분)
  - 헤더 줄바꿈→공백 정규화 + 메인표 매칭 공백무시 보강
  - ⑦ 관리자·안전 기본숨김(설정서 켜기), 발주처담당자 상세팝업 전용
  - ⑨ 상세팝업 저장 동시수정 보완
  ```
