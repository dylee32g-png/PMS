# 2026-09-14 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일
1. **설정(⚙) 메뉴 중복 점검** (팀장님 캡쳐 2장 — "중복되는 게 있는지 확인하고 순서를 깔끔하게")
   - 결과: **완전 중복(같은 기능 두 번)은 0건**. 대신 **이름이 비슷해 헷갈리는 3쌍**이 실제 혼선 원인이었음.
   - ① 엑셀 업로드 ↔ 엑셀 반영: 둘 다 보존 병합이지만 업로드만 **표 구조(헤더)까지** 엑셀 기준으로 바꾸고 표에 미리 띄워 확정(관리자). 반영은 겹치는 열의 값만(모든 사용자·삭제 없음).
   - ② 번호 3자리 정리 ↔ 수행번호 3자리 정리: 대상 열이 다름(번호 1→001 / 수행번호 26-01→26-001).
   - ③ 전체 백업(JSON) ↔ 지금 백업→폴더: 같은 백업, 목적지만 다름(내려받기 / NAS 폴더).
   - 조치 = 기능은 그대로 두고 **부제로 차이를 드러냄** + 서로 붙여 배치.
2. **메뉴 재배치 — 하는 일별 6묶음**(회색 소제목 `MenuSec`, 기능 삭제 0 · 버튼 27개 → 27개 · onClick 전부 동일)
   - ① 엑셀: 엑셀 생성 · 엑셀 반영 · 엑셀 업로드(+업로드 중에만 나오는 병합 미리보기·확정 저장·로컬 임시 저장·로컬 삭제·업로드 취소를 **들여쓰기로 바로 아래 묶음**) · 연도별 1:1 적재 · 과거 연도 적재(카드 켜진 팀만)
   - ② 마감·백업: 월간 마감 · 전체 백업(JSON) · 자동 백업 폴더 지정(켜지면 지금 백업·끄기·상태줄) · 백업 복원(빨강)
   - ③ 팀 설정·NAS: 진행현황 관리 · 담당자 관리 · 이 PC를 메인 PC로 지정
   - ④ 정리 도구·일괄 실행: 번호 3자리 · 수행번호 3자리 · 진행실적 심기
   - ⑤ 내 화면·이 PC만: 열 표시/숨기기 · 내 화면 설정 초기화
   - ⑥ 관리자: 데이터 삭제(맨 아래 고정)
   - 폭 224→288px, 화면보다 길면 메뉴 안에서 스크롤(`max-height: calc(100vh - 120px)`).
   - 일반 직원에게 보이는 것 = ①(생성·반영) · ②(월간 마감) · ⑤ 뿐 — 종전과 동일(게이팅 조건 무변경).
   - 설명서·공지에 이미 적힌 이름 2개(자동 백업 폴더 지정 / 이 PC를 메인 PC로 지정)는 **일부러 유지**(문서 불일치 방지).
3. **디버그 모드 메뉴 숨김**(팀장님 지시) — `{false && ...}` 규약으로 감쌈. 코드·상태(`showDebug`)는 그대로라 true로 바꾸면 즉시 복구.
4. **★[연도별 1:1 적재·검증] 올해 보호 장치 신설**
   - 위험: 이 기능은 **그 해를 통째로 교체**(기존 행 전량 삭제 → 시트 그대로 삽입). 지난 연도는 엑셀이 원본이라 안전하지만, **2026년 시트에 실행하면 웹에서만 만든 값**(수행번호 58건 · NAS 자동 반영 규칙 2건 · 하위(공종) 행 14건 · 변경 이력 · pid · 포인트 실적)이 **전부 소실**. 종전 확인창은 "기존 N건은 지우고 넣습니다"뿐이라 올해 경고가 없었음.
   - 수리: `handleYearLoad`에서 **올해 + 그 해 웹 행이 1건 이상**이면 빨간 ⛔ 창을 먼저 띄우고, 지워질 값을 실제 건수로 보여준 뒤 **`2026 교체`를 직접 타이핑**해야만 진행. 취소·오타는 "올해 보호"로 중단. 지난 연도·백지 상태(0건)는 종전과 동일.
5. 공지 09-14(메뉴 6묶음 · 헷갈리던 이름 설명 · 올해 경고) · 배포 명령 안내.

## 변경한 파일
- `src/components/ProjectListScreen.jsx` — 설정 메뉴 블록 전면 재배치(`MenuSec` 라벨 컴포넌트 신설, 72행) · 디버그 메뉴 `{false &&}` · `handleYearLoad` 올해 보호 블록
- `src/notices.js` — 2026-09-14 공지
- 백업 `.bak-2026-09-14` (PLS, gitignore 대상)

## 결정·근거
- **중복 제거 대신 부제 추가**: 3쌍 모두 실제로 쓰임이 달라 지우면 기능이 빠짐(엑셀 업로드=구조 변경, 1:1 적재=연도 복구). 혼선의 뿌리는 "이름만 보고는 차이를 모름"이라 부제·인접 배치로 해결하는 것이 안전.
- **묶음 소제목**: 메뉴가 20개를 넘겨 구분선만으로는 순서가 안 읽힘. 하는 일로 묶으면 직원은 위 3묶음만 보면 되고, 위험한 삭제는 항상 맨 아래 한 곳.
- **올해 보호를 타이핑 확인으로**: 부제만 바꾸면 사람 주의에만 기댐. 9/4 소실 사고(백업·흔적 없이 전 연도 삭제)와 같은 유형이라 코드가 막는 쪽이 맞음. 확인창 한 번 더가 아니라 **문구 입력**인 이유 = 습관적 Enter를 막기 위함.

## 검증
- babel 파싱 OK · `npx eslint` **0 errors**(경고 27건은 기존) · 버튼 수 27→27 · 옛 블록의 핸들러·ref 전량이 새 블록에 존재(자동 대조 `missing: []`)
- 올해 보호 규칙 시뮬 **6/6**(지난 연도 통과 / 올해 취소 차단 / 오타 차단 / 정확 입력 통과 / 웹 0건이면 통과 / 연도 문자열·숫자 혼용) — `scratchpad/test_yearguard.js`
- notices.js ES 모듈 로드 확인(맨 위 2026-09-14, 총 20건)

## 다음 할 일 / 막힌 점
1. **배포**: `npm run build` → `firebase deploy --only hosting` → 시크릿 탭 확인. 이번 배포 = 9/10 미배포분(정렬 엑셀 순 · 헤더 엑셀 메뉴 · 열 폭 흔들림 수리 2건) + 오늘분 전부.
2. 배포 후 실기 확인: ⚙ 메뉴 6묶음 · 관리자 묶음에 데이터 삭제만 · 2026 시트 [적재] 눌러 빨간 창 확인 후 **취소**.
3. 사용설명서 v1.2: 칸 색 2가지 · 스위치 · x/Del 구분 · 0 · 헤더 메뉴 · 키보드 가로 따라가기 + **오늘 메뉴 재배치**.
4. 이월: 공용 PC 방문(악성코드 점검→파수꾼 v2 설치→NAS 010·011 폴더 재지정→자동 백업 폴더 지정) · 보안 후속(IT 보고·V3·비밀번호) · 발신 이름/도메인 관리자 요청 · 사용자 관리 소속 팀(김윤재·김수민) · 수행번호 △4건·#058/#129 · 협의 3건 · Firebase Blaze.
