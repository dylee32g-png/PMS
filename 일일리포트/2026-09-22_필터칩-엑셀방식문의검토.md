# 2026-09-22 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일
1. **문의 검토 — "진행현황 칩이 엑셀 자동필터처럼 동작해야 하지 않나"** (전체를 누르면 0건 · 전체 상태에서 진행중을 누르면 진행중을 뺀 것 · 다시 누르면 복구)
   - 엑셀 동작 확인: 문의자 말이 맞음. ▼ 메뉴의 (모두 선택) 체크박스가 그 구조. 단 엑셀은 [확인] 전까지 화면이 안 바뀌고, "전부 해제"는 몇 개만 체크하려는 중간 단계일 뿐.
   - 현재 PMS 구조 확인(코드 실측): **두 방식이 이미 공존**.
     - 상단 칩 줄 = **고른 것만 보기(포함)**: 칩 클릭 = 집합에 넣기/빼기 토글(`activeStatusChips`), 전체 = 집합 비우기(필터 없음). 기본값 진행중·추진중.
     - 열 제목 ▼ 메뉴(9/10 신설) = **엑셀 자동필터 그대로**: (모두 선택)·(빈칸)·값 목록 체크박스.
   - "전체 클릭이 안 된다" = 이미 전체(필터 없음) 상태라 누를 것이 없음. 고장 아님.
2. **판단 = 칩 줄은 현행 유지 (추천·팀장님 확인 대기 없음, 코드 무변경)**
   - 빼기 방식이면 상태 6개 중 1개만 보는 데 5번 클릭(지금 1번). 실제 쓰임(진행중만·내 담당만)은 전부 '골라 보기'.
   - 칩은 즉시 반영이라 전체를 누르는 순간 0건 화면이 실제로 뜸(엑셀은 확인 전까지 안 바뀜).
   - 삭제(58)·Hold처럼 평소 안 보는 상태를 매번 빼야 함. 기본값 진행중·추진중도 포함 방식 전제.
   - 9/21 칩 숫자 연동("이 칩 누르면 몇 건")의 뜻이 "몇 건 빠지나"로 바뀌어 설명서 2-1도 재작성 필요.
   - 빼기 방식은 ▼ 메뉴에 이미 있어 중복.
   - 안내문: "전체에서 특정 상태만 빼려면 **진행 현황 열 제목 ▼ → 체크 해제**".

## 변경한 파일
- 코드 변경 없음. 이 리포트 · `CLAUDE.md` 진행 상황.

## 결정·근거
- 칩(고르기)과 ▼ 메뉴(엑셀식 빼기)는 역할이 다른 표준 방식 두 가지 → 칩을 엑셀식으로 바꾸지 않음. 근거는 위 2번.

## 다음 할 일 / 막힌 점
- (선택) 사용설명서 FAQ에 "전체 칩은 왜 눌러도 안 바뀌나 / 빼기 필터는 ▼ 메뉴" 문답 1개 추가 — 팀장님 지시 시.
- **배포 미실시** — 9/18 마감분 + 9/21(`11bbc47`·`01ee7cc`) 전부 라이브 미반영: `npm run build` → `firebase deploy --only hosting` → 시크릿 창(관리 칸 `L1` `AX`·칩 숫자 연동) → **공용 PC F5**(옛 코드가 NAS 규칙을 되돌릴 수 있음). 배포 전엔 라이브 창 열어 두지 말 것.
- 이월: Software팀 과거 연도 적재 실행 · 김준형 확인 3건 · 공용 PC 방문(파수꾼 v2·NAS 재지정·자동 백업 폴더) · `openvpn/`·`AGENTS.md` 미추적 결정 · 소속 팀 지정(김윤재·김수민) · 수행번호 △4건·#058/#129 · 협의 3건 · Firebase Blaze · NAS 인증서 2026-10-01 · 볼륨2 93% · `_changeHistory` 같은 '사본 전체 저장' 패턴 미점검.
