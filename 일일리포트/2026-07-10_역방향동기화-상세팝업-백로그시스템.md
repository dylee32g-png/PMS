# 2026-07-10 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. 프로젝트 List 최종 펀치 (오전)
- **메인표 → 진행실적 팝업 역방향 동기화**: 기존엔 팝업→메인표만('적용하기') 되던 걸, 반대로 **메인표 공정률 셀을 고치면 진행실적 주차장부(progressRecords)에도 반영**되게 함. 대상 = 공정률 7개(도면입수·I/O Map·화면작성·기준정보·PLC·ETOS·HMI)뿐. 시운전·포인트는 단위(포인트)+주차 합산이라 제외(주차 기록 손상 방지). 기록 위치 = **오늘이 속한 현재 주차**(팀장님 지정: 1주=1~7·2주=8~14·3주=15~21·4주=22~28·5주=29~). 첫 버전이 '기준월 마지막 주'에 넣던 걸 반려받아 현재주차로 수정, 그 잔재는 재입력 시 자동 정리.
- **알림 팝업 위치**: 화면 맨 아래 중앙 → **정중앙**. 진행실적 팝업(z-9500)에 가려 안 보이던 것 → 알림 z-index를 10000으로 올려 팝업 위로.
- **진행실적 팝업 정리**: ① 이전·금주·다음 버튼은 표 좌우 스크롤용이라 '전체 기간 보기'일 때만 노출(기본 2개월 화면에선 숨김) ② 셀에 **0 입력 시 빈칸 처리**(공정률=직전값 이월 유지, 시운전=0과 동일이라 무해) ③ **새로고침 버튼 숨김**(팝업이 열 때 자동 로드라 중복 + 미저장 입력을 경고 없이 되돌리는 위험).

### 2. 상세 보기/수정 팝업(DetailModal) 정렬
- **칸 너비 일관화**: 섹션마다 2·3열이 섞여 폭이 튀던 걸 **전부 2열 통일 + 라벨 고정폭(→입력칸 시작·폭 일치)**.
- **행 높이 통일**: Project·내용 여러줄(rows=2)→1줄, 필드행 **minHeight 34px + 입력 세로중앙**, 메인표 표시 토글을 라벨 박스 안(왼쪽)→**행 오른쪽 끝으로 이동**(긴 라벨도 한 줄에 들어가 높이가 일정해짐).

### 3. ★ 작업 백로그 시스템 (신규 — 오후 핵심)
"누가·언제·무엇을 수정/추가/삭제/보류 했는지"를 기록·조회하는 시스템.
- **`src/auditLog.js` 신설**: 팀별 append-only 감사로그(`auditLog_${team}`) 기록(logAudit)·구독(subscribeAuditLog). 프로젝트별 변경이력(_changeHistory)과 별개.
- **`src/components/BacklogScreen.jsx` 신설**: 전용 전체화면. 사람·동작·기간·검색 필터, 이메일→이름 변환. **랜딩(팀 미선택)=전체 팀 병합(팀 배지+팀 필터), 팀 안=그 팀만**.
- **App.js**: 라우팅(currentMode='backlog'), 팀 메뉴 하단 「작업 백로그」 버튼, 뒤로가기 '열었던 화면'으로 복귀(backlogReturn 상태), 랜딩 분기 조건에 backlog 예외 추가(버튼 먹통 수정), auditLog import, **월간보고 삭제(executeDeleteProject) 기록**.
- **ProjectListScreen**: recordAudit 헬퍼(who=user.email, firebase 모드만) + **6경로 연결** — 셀편집·상세팝업·진행실적적용(수정), 프로젝트추가(추가), deleteRow·상태를'삭제'로변경(삭제), 상태를'보류'로변경(보류). **List 상단 「백로그」 바로가기 버튼**.
- **개인 로그인 확인(중요)**: 사용자 관리(registeredUsers 명단) + 로그인 화면의 구글/이메일링크 로그인 + 로그인 후 명단 대조 게이트가 **이미 구축돼 있음**. 팀장님이 팀원 개인 이메일 계정 등록 완료. → 아까 "전무님 콘솔 작업 필요"는 **정정**(불필요). 공용→개인 로그인 사용 전환만 하면 '누가'가 실명으로 찍힘.

## 변경한 파일
- (수정) `src/components/ProjectListScreen.jsx` — 역방향 동기화, 알림 정중앙, recordAudit 헬퍼+6경로, List 백로그 버튼
- (수정) `src/components/ProgressModal.jsx` — 네비버튼 조건부 노출, 0→빈칸, 새로고침 숨김
- (수정) `src/components/DetailModal.jsx` — 칸 너비·행 높이 일관화, 토글 오른쪽 이동
- (수정) `src/App.js` — 백로그 라우팅/메뉴/뒤로가기/랜딩예외, auditLog import, 월간 삭제 기록
- (신규) `src/auditLog.js` — 감사로그 토대
- (신규) `src/components/BacklogScreen.jsx` — 백로그 전용 화면
- 백업: `src/components/ProjectListScreen.jsx.bak-2026-07-10`

## 결정·근거
- 역방향 동기화 주차 = **현재 주차(오늘 기준)** — 팀장님 지정. 메인표 수정은 '지금' 벌어진 일이므로.
- 0→빈칸 — 공정률은 누적%라 중간 0(되돌림)은 거의 안 쓰고 이월 힌트가 살아나 화면이 깔끔, 시운전은 0=빈칸이라 무해.
- 상세팝업 토글 오른쪽 이동 — 라벨을 좁게 유지하면서도 긴 라벨을 한 줄에 넣어 **행 높이 일정**.
- 백로그 = **별도 append-only 감사로그** — 다툼 방지엔 아무도 못 고치는 기록이 핵심. who=회사 이메일(백로그 화면에서 명단의 이름으로 변환).
- 개인 로그인 = **신규 구축 불필요**(이미 있음). 전무님은 (선택) 구글/이메일링크 provider 활성화·감사로그 보안규칙만.

## 검증
- 역방향 동기화: 자동 시뮬 35/35 + 독립 실행(오늘 7/10 → 7월 W2 기록 확인).
- 0→빈칸: 시뮬 12/12 + node --check.
- 새 파일(auditLog·BacklogScreen): babel 파싱 OK.
- 대형 파일(App.js·ProjectListScreen): 마운트 지연으로 babel 파싱 신뢰 불가 → **파일도구 Read/Grep으로 연결지점·파일 끝 온전성 검증**. 최종은 VS Code 빌드.
- 실기기: 백로그 화면에서 수정(도면입수 57→65, HMI 87→90)·삭제(진행중→삭제) 정상 기록·조회 확인.

## 다음 할 일 / 막힌 점
- **오늘 변경분 전부 미커밋** → VS Code에서 커밋·push (분량 큼).
- (선택) 월간보고 '직접 수정' 기록 — App.js 저장 경로 여러 곳(1611·3093·3164) 연결. ※진행실적 '적용하기'로 월간 반영되는 건 List 쪽에서 이미 기록되어 중복 아님.
- **개인 로그인 사용 전환** — 팀원에게 "회사 이메일(구글/이메일링크)로 로그인" 안내 → 백로그 '누가'가 실명으로(지금은 공용계정이라 'neconsys').
- (선택·전무님) `auditLog_*` 보안규칙(추가만 허용·수정/삭제 금지)으로 감사로그 잠금.
- 마운트 파서 지연 지속 — 대형 파일 편집 후 Read/Grep + VS Code 빌드로 검증. [[pms-edit-tool-truncation-trap]]
