# Cowork 인수인계·초기설정 대화 요약 (2026-06-09)

> 새 Cowork 프로젝트에 첨부해 컨텍스트로 참고하기 위한 문서.
> 더 상세한 작업 메모는 `D:\pms-app\CLAUDE.md` 참조.

## 1. 담당자 & 역할
- 염경록 팀장. 팀별 **공정현황(주간·월간 진행) 보고 자료**를 분석하고 추가·보완·수정.
- `pms-app` 프로젝트(VS Code)를 인수인계 받아 담당. **인수인계일: 2026-06-09**.
- 코딩 비전문가 → 설명은 초등학생 눈높이로 쉽고 단계적으로.

## 2. 프로젝트 개요: pms-app
- React + Firebase 웹앱. 이름 **TechTeam PMS (v6.9.x)**. 앱 표기는 "통합 프로젝트 관리 플랫폼 v1.0".
- 팀들이 공정현황(주간/월간 진행상황)을 입력·보고하는 시스템.

## 3. 폴더 뼈대 (핵심은 전부 `src` 안)
| 위치 | 역할 |
|------|------|
| `src/App.js` | 대장 파일(~7100줄). 어느 화면 보일지 결정(`currentMode`) |
| `src/firebase.js` | 클라우드 저장소(Firebase) 연결 |
| `src/constants.js` / `src/utils.js` | 고정 설정값 / 도우미 기능 |
| `src/components/` | 화면 하나하나 |
| `dataconnect/` | 데이터 저장 구조(설계도) |
| `node_modules`,`build`,`public` | 자동 생성/부품(직접 안 건드림) |
| `src/V6.7.0`,`v6.8.7`,`v6.8.9` | 옛 백업본 |

## 4. 동작 흐름
로그인 → 팀 선택 → 메뉴 고르기 → (공정현황 입력·수정 등) → Firebase에 저장.

## 5. 앱 메뉴 ↔ 코드 매핑 (6/9 실행화면 기준)
| 앱 메뉴 | 설명 | 코드 |
|---------|------|------|
| 팀 선택 | 기술1·2·3팀, Software팀 | TeamSelector.jsx |
| 월간 업무 보고 | 시운전 실적·공정률 월간 현황(=공정현황) | App.js `currentMode='pms'` |
| 프로젝트 List 관리 | 팀 프로젝트 목록 엑셀 기반 관리 | ProjectListScreen.jsx |
| 주간 보고 뷰어 | 업로드→저장→목록에서 바로 열기(XLSX) | WeeklyReportScreen.jsx / WeeklyPanelViewer.jsx |
| 주간 보고 입력 | 엑셀 템플릿 분석→주차별 날짜→공정률 자동계산 | WeeklyInputScreen.jsx |
| 주간보고 요약 | 주차 실적·이슈·계획→담당·임원보고 자동화 | WeeklySummaryScreen.jsx |
| 견적 | 견적 산출 | EstimateScreen.jsx |

## 6. 2026-06-09 한 작업
- Cowork 초기 설정: 플러그인(operations·productivity·product-management) 설치, `D:\pms-app` 폴더 연결, CLAUDE.md 생성.
- pms-app 전체 구조·동작 흐름 파악.
- 작업 루틴 7규칙 확정, 일일리포트 폴더/양식 생성.
- 미리보기(`npm start`) 포트 3000 충돌(run-pick 서버) 해결 → `localhost:3000` 정상.
- 깃허브 비공개 저장소 연결 + 최초 커밋(78784f1) push 완료.

## 7. 확정한 작업 루틴 7규칙
1. 시작 — "오늘 시작하자" 하면 CLAUDE.md '진행 상황' 읽고 지난 작업·다음 할 일 브리핑.
2. 코드 추가·수정 — 추측 금지, 파일·줄 실제 확인 후 진행, 변경 뒤 빌드 점검.
3. 모든 답변에 추천 포함 + 왜 더 효율적인지 근거 제시(귀찮음 추천 금지).
4. 화면 질문은 진짜 자세히 설명(비전문가 눈높이).
5. 작업·학습은 한 단계씩(완료 후 다음), 단순 단답은 바로 답.
6. 마무리 — "오늘 작업 종료" 하면 ① 일일리포트 작성 ② 요약을 진행상황에 복사 ③ 깃허브 동기화 1회.
7. 깃허브 — 실제 push는 인증된 VS Code 쪽, Claude는 커밋 정리·점검 지원.

## 8. 깃허브 정보
- 비공개 저장소: https://github.com/TipPick07/pms-app (main, 최초 커밋 78784f1).
- push는 VS Code(GitHub 로그인된 TipPick07 계정)에서 수행.

## 9. 다음 할 일 (6/10~)
- **화면(메뉴)별 상세 설명 모드** — 각 메뉴가 어떤 기능이고 무엇을 위해 만든 메뉴인지 자세히 설명. (앞으로 주 작업)
- (선택) 불필요 파일 `src/import tkinter as tk.ini` 정리.

## 10. 주의사항
- 미리보기 켜기 전 포트 3000을 다른 앱(run-pick 등)이 쓰고 있는지 확인.
- 큰 변경 전 백업. 옛 버전 폴더(V6.7.0 등)는 보존.
