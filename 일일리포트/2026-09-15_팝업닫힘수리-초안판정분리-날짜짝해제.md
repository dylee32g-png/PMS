# 2026-09-15 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일
1. **★팝업 바깥 클릭으로 닫히는 버그 — 전 화면 전수 점검·수리 (21곳)**
   - 증상(팀장님): 상세/수정 팝업에서 값을 고치다가 팝업 옆 빈 곳(어두운 배경)을 클릭하면 저장 없이 팝업이 사라짐.
   - 원인: 팝업 뒤 전체 화면 덮개(`<div className="fixed inset-0 …" onClick={닫기}>`)에 "클릭=닫기"가 걸려 있었음. 안쪽 상자는 `stopPropagation`이라 팝업 안은 안전했지만, **덮개가 보이는 모든 여백**(팝업 좌우·위아래)이 닫기 버튼이나 마찬가지였음.
   - 수리: 덮개의 클릭 닫기 핸들러를 제거(주석으로 사유 표기). 닫는 길 = 각 팝업의 **X · 닫기 · 저장 버튼**만.
   - 고친 팝업: 상세/수정(=프로젝트 추가) · NAS 규칙 등록 · 엑셀 반영 미리보기 · 연도별 1:1 적재 · 진행현황 관리 · 담당자 관리 / 공지 · 견적 비밀번호 · 실적 그래프(팀·개별) · 엑셀 미리보기 2종 / 기술1팀 월간 보고 팝업 / 사용자 추가·수정·삭제 확인 / 견적 이력 2종 · WebDAV.
   - **전수 검사 방법**: 화면 파일 20개(App.js + components/*.jsx)에서 전체 화면 덮개 태그를 뽑아 태그 단위로 클릭 핸들러 유무 판정(`scratchpad/scan_overlays.js`). 결과 = **팝업 덮개 51개 중 배경 클릭으로 닫히는 것 0개**. 나머지 30개는 원래부터 없던 것(로딩·확인창·알림창).
   - 유지(의도된 동작): 드롭다운·우클릭 메뉴·⚙ 설정·헤더 ▼ 같은 **메뉴** 클릭받이 12개는 바깥 클릭에 닫힘. 문서 전체 클릭 감시 2곳도 메뉴 전용(헤더 필터 목록 / 월간보고 우클릭)이라 팝업과 무관.
2. **★메인표 초안(노란 칸) = 저장 전에는 행이 안 움직이게 (판정/표시 분리)**
   - 증상(팀장님): '추진중' 칩으로 좁힌 상태에서 한 행을 '진행중'으로 바꾸면 **저장 전인데 즉시 사라지고** 위쪽 건수(52→51)도 바뀜. [저장 1칸]만 켜진 상태.
   - 원인: 초안을 덧입힌 `activeRows`가 화면 표시뿐 아니라 **필터·칩 건수·기준월·검색·정렬 판정에도** 그대로 쓰임(2026-08-27 초안 저장 도입 때부터).
   - 수리: 판정 줄기(연도 → 기준월 → 칩·헤더 필터·검색 → 정렬)를 **서버 저장값**(`activeRowsBase`)으로 계산하고, 초안은 **맨 끝 표시 단계에서만** 덧입힘(`sortedRowsBase` → `sortedRows`). 하위(공종) 부모 따라가기 묶음도 같은 기준으로 통일.
   - 결과: 저장 전 = 행이 제자리에 남고 값만 노랗게 바뀜, 건수·칩 숫자·정렬 순서 불변 / [저장] = 그때 이동·사라짐 / [취소] = 원래대로. 아직 안 보이던 행을 그 칩 값으로 바꿔도 저장 전에는 나타나지 않음.
   - 상세/수정 팝업 [저장]은 그 자체가 저장이라 종전처럼 즉시 반영.
3. **공사 계약 / 공사 완료 짝 규칙 해제** (팀장님: "그냥 공사 일자만 키인할 수 있게")
   - 2026-07-14 규칙(추가 팝업에서 한쪽만 넣으면 저장 불가)을 스위치 `CONTRACT_DATE_PAIR_RULE = false`로 끔. 계약일만·완료일만 각각 저장 가능. 검사 함수·팀 카드 `열.날짜짝`은 보존(그래프 기간·기준월 판정에 계속 쓰임)이라 되살릴 때 한 줄.
4. 공지 09-15(팝업 닫힘·저장해야 이동·날짜 따로) · 라이브 배포 명령 안내(2회) · 로컬 개발 서버 기동/종료(오늘 코드 컴파일 확인, 경고만·에러 0).

## 변경한 파일
- `src/components/ProjectListScreen.jsx` — 덮개 5곳 · 초안 판정/표시 분리 · 날짜 짝 스위치
- `src/App.js`(덮개 6곳) · `src/components/DetailModal.jsx`(1) · `EstimateScreen.jsx`(3) · `Tech1MonthlyScreen.jsx`(1) · `UserManagementScreen.jsx`(2)
- `src/notices.js` — 2026-09-15 공지
- 백업 `.bak-2026-09-15`(6개 파일, gitignore 대상)

## 결정·근거
- **덮개 클릭 닫기 전면 제거**(일부만 두지 않음): 팝업마다 다르게 동작하면 "이 팝업은 사라지나?"를 매번 겪게 됨. 닫는 길을 버튼 하나로 통일하는 편이 입력 중 사고를 막고 배우기도 쉬움. 메뉴는 성격이 달라(가볍게 열고 닫음) 종전 유지.
- **판정=저장값 / 표시=초안**: 초안 저장(8/27)의 취지가 "저장 눌러야 반영"인데 필터만 즉시 반영되어 규칙이 반쪽이었음. 갈라 놓으면 화면에서 바뀐 값을 눈으로 확인하면서도 목록은 흔들리지 않음. 판정 줄기 한 곳만 바꿔 칩·기준월·검색·정렬·건수가 한꺼번에 해결됨(개별 수정 대비 누락 위험 낮음).
- **날짜 짝은 삭제 대신 스위치**: 규칙 자체는 데이터 품질에 쓸모가 있어 [[contract-date-pair-rule]] 논의가 남아 있음. 되살릴 때 한 줄이면 되게 보존.

## 검증
- babel 파싱 6개 파일 OK · `npx eslint` **0 errors**(경고 27건 = 수정 전과 동일, 백업본과 메시지 대조로 신규 경고 0 확인)
- 덮개 전수 스캔: 팝업 51개 중 배경 클릭 닫힘 **0개** (메뉴 12개는 의도대로 유지)
- 초안 판정 규칙 시뮬 **5/5**: 초안 유지 / 건수 불변 / 미표시 행 미노출 / 저장 후 이동 / 취소 후 복귀
- 로컬 개발 서버 컴파일 성공(경고만)

## 다음 할 일 / 막힌 점
1. **배포**: `npm run build` → `firebase deploy --only hosting` → 시크릿 탭 확인. 이번 배포 = 9/10 잔여(정렬 엑셀 순·헤더 메뉴·열 폭) + 9/14(설정 메뉴 6묶음·디버그 숨김·올해 보호) + 오늘분 전부. **배포 전까지 라이브(neconsys.web.app)는 옛 동작** — 오늘 오전 "아직 수정 안 됐다"는 지적도 라이브 확인 때문이었음.
2. 배포 후 실기 확인 3가지: 상세/수정 팝업 옆 어두운 곳 클릭(안 닫힘) · 추진중 칩에서 진행중으로 바꿔도 안 사라짐, [저장] 후 사라짐 · 추가 팝업에서 공사 계약만 넣고 저장.
3. 사용설명서 v1.2: 칸 색 2가지·스위치·x/Del·0·헤더 메뉴·키보드 가로 따라가기 + 설정 메뉴 재배치 + **오늘 3건**.
4. 이월: 공용 PC 방문(악성코드 점검→파수꾼 v2→NAS 010·011 폴더 재지정→자동 백업 폴더) · 보안 후속(IT 보고·V3·비밀번호) · 발신 이름/도메인 관리자 요청 · 사용자 관리 소속 팀(김윤재·김수민) · 수행번호 △4건·#058/#129 · 협의 3건 · Firebase Blaze.
