# 2026-08-28 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app
- ※ 오전 세부 기록(10~17번)은 어제 리포트 `2026-08-27_초안저장-창렌더링-장부소실수리-드래그삭제.md` 뒷부분에 이어 적혀 있음. 이 파일은 요약+인수인계.

## 오늘 한 일 (커밋 8건, 전부 push 완료 · ★미배포)
1. **기술1팀 트리거 칸 클릭만 → 자동 칸 0 노란칸 버그** — 값이 실제로 바뀐 때만 수식·진행율 재계산 + 수식 결과 0=빈칸. (ff3e3b5)
2. **수행번호 [+] 같은 번호 반복 + 중복 저장 무방비** — 원인=행 렌더 캐시(MemoRow)가 초안 반영 전 옛 목록을 붙잡음 → `activeRowsRef` 최신 목록 참조 · 손 키인 중복 ⛔ · [저장] 직전 최종 중복 검사. (8be0fd0)
3. **헤더 정렬 3단계**(오름→내림→해제) + **하단 상태줄 `정렬: 열 ↓ [해제]`** — "번호 넣으면 행이 움직인다" = 수행번호 내림차순 정렬 켜진 상태였음. (bfc9ae6·1b7b80d)
4. **수행번호 손 키인 금지** — 메인표 칸 클릭=안내 토스트만, 상세/추가 팝업 🔒 잠금(`DetailModal execLockedCols`). [+]/✕만. 당해 연도 메인 행 한정. (be87b19)
5. **행 더블클릭 → 상세 팝업 제거** — 우클릭 메뉴로만. (e0d9343)
6. **프로젝트 추가(선택 행 복사) 시 수행번호 복사 제외** — 중복 방지. '행 선택됨 … 복사' 기능 동작 확인(정상). (18fd1b5)
7. **추가 팝업 버튼 [저장]→[추가]** (66f2c14)
8. 공지 08-28 추가 (이 커밋)

## 변경한 파일
- `src/components/ProjectListScreen.jsx` — 1~6
- `src/components/DetailModal.jsx` — 4·7
- `src/notices.js` — 08-28 공지
- `CLAUDE.md`, 일일리포트 2건

## 결정·근거
- 수행번호는 자동 부여 전용(팀장님): 손 키인 경로를 전부 막아야 중복·형식 오류가 근본 차단됨.
- 정렬은 기능 유지하되 '켜진 것이 보이게'(상태줄) + 끌 수 있게(3단계·[해제]).
- 더블클릭 팝업 제거: 셀 편집과 충돌해 혼란. 상세는 우클릭 한 경로로 통일.

## 사고·교훈
- 패치 스크립트에 `
`·`\d` 같은 백슬래시를 heredoc으로 넘기면 도구가 `\`를 하나로 접어 **JS 문자열에 실제 줄바꿈이 들어가 컴파일 에러**(1회, 즉시 git 원복). → 백슬래시가 든 패치는 **Write 도구로 .py 파일을 쓰고 실행**하거나, 줄 단위 치환(문자열에 백슬래시 없음)으로.
- JSX 태그 속성 자리엔 `{/* */}` 주석 불가(컴파일 에러 1회) → `/* */` 사용.

## ★ 내일 인수인계 (이 순서대로)
1. **배포**: `npm run build` → `firebase deploy --only hosting` → 시크릿 탭으로 확인(08-28 공지 빨간 점·수행번호 [+]·하단 정렬 표시). 8/25 오후~8/28 전부 미배포 상태.
2. 배포 후 **공용 PC 파수꾼 `설치.bat` 관리자 재실행**(8/25 이월).
3. **팀장님 결정 대기 2건**
   - (a) '행 선택됨 → 프로젝트 추가 복사' 숨은 경로(편집창 재클릭 때만 선택됨)를 **우클릭 [이 행 복사해서 프로젝트 추가]** 로 옮길지.
   - (b) **엑셀식 선택·복사·붙여넣기**: 설계 확정안 = 번호 칸 클릭=행 전체 선택 · Shift+클릭=범위 · Ctrl+클릭=행 추가/해제 · Ctrl+C/V(copy/paste 이벤트, 권한 팝업 없음) · 붙여넣기=초안(노란 칸) 1회 렌더 · 잠금/수행번호/번호/드롭다운 제외 규칙. 속도 유지 가능(DOM 색칠+초안 적재 = Del 지우기와 동일 경로). 분량 150~200줄·반나절. **팀장님 "만들지 말고 확인만" 상태.**
4. 이월: Firebase 사용량·Blaze 검토(전무님) / 첫 로그인 ~18초 조사 / 기술3팀 실전 적재 / 협의 3건(최영환·심광호·김승환) / NAS 하위 장부 주차 전환 관찰(월요일).
5. 참고 데이터 상태: 기술2팀 2026 수행번호는 26-001~003이 001~003행에 정상 저장, 중복 없음. 테스트 행 193(복사본)은 팀장님이 삭제 완료.
