# 2026-08-19 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일
1. **NAS 모달 파일 목록 정리** — 같은 계열(날짜 6자리만 다른 파일)은 최신 1개만 표시 (커밋 f631e2d·push 완료)
2. **월간보고 잠금** — 기술1·2·3팀 월간보고 진입 차단("추후 업데이트 예정"). App.js `MONTHLY_REPORT_OPEN=false` 스위치 1개 — true로 바꾸면 전부 복원(홈 카드·List 헤더 버튼·우클릭)
3. **List 그룹 경계선** — 카테고리(진행현황·진행% 등) 양끝에 세로선. 팀장님 피드백 2회 반영: 연한 1px(#c3cad7)·헤더 2줄만(본문 제외). `.grp-sep`(index.css)+`grpEndCols`
4. **날짜 표시 통일** — 모든 날짜 칸 `26/08/19`(YY/MM/DD) 표시 전용 변환(fmtDate). 저장·달력 편집은 표준 유지, 해석 불가 값('?')은 원본
5. **단어(카테고리) 드롭다운** — 셀 클릭=사전목록+실사용 단어 택1+직접입력+빈칸. 기술1팀 11열(계약·작업·고객사 등), 기술2팀 공장동(3팀 상속). 팀 카드 `드롭다운열`
6. **기술1팀 전연도 적재** — 엑셀 시트 19장(2014~2026) 전수 분석 → 파서 `전연도` 모드: 총 434건(연도별 12~89), 2026은 '2026 (2)'만, 옛 시트 열번역(프로젝트명→공사명·견적→계약·진행/상태/현황→작업 등 — 값 분포 실측 근거), 2026 양식에 없는 열은 비고에 `[열: 값]` 보존(82건), 같은 연도 '다른 탭' 중복만 제거(같은 탭 동명 L1/L2 행 6건은 진짜 다른 공사라 보존 — 시뮬이 검출)
7. **보고 카드** — 지난 연도(2014~2025) 선택 시 임원 보고형 4장: 총건수·계약현황별·달성율(완료÷전체)·전체 포인트. 당해(2026)는 기존 카드
8. **통합 시운전 기본 미적용** — 기술1팀 2026 행만 토글 off(팀 카드 `기본미적용`), 상세팝업 켬 예외
9. **★수식 (기술1팀 2026, 팀 카드 `수식`)** — 수동: PLC·ETOS T/S·HMI·총물량·금월 / 자동(키인금지+안내창): 자체 시운전=금월÷총물량% · 누적=지난달까지(_accBase)+금월 · 금월(공정률)=(PLC+ETOS+HMI+자체)÷4 · 전체=지난달까지(_pctBase)+금월(공정률) [누적] / [월간 마감]=전월←금월·베이스 확정·금월성 칸(금월·PLC·ETOS·HMI) 비움(팀장님 확정)
10. **수식↔팝업↔그래프 연동** — ETOS T/S→etos 매핑, 자체 자동값→주차장부(pt, 그 달 한 칸만), 팝업 자동 칸 잠금(🔒 자동), 자체 합계 % 표기, 진척률 자체 성분=그 달 실적÷총물량(sumAsPct — 기술2팀 불변), 총점 폴백 '총물량' 2곳(팝업 totalPt·effTotalPt)
11. **버그 수리** — 주차장부 동시쓰기 경쟁(두 기록이 같은 문서를 읽고 통째 쓰기→나중 것이 먼저 것 덮음, ETOS 소실 원인) → 순차 await. 시뮬로 재현·수리 입증
12. **테스트 장치** — 우클릭 [테스트] 7월 실적 심기(20pt·20%, admin+localhost만). ⚠검수 끝나면 제거
13. **CRLF 사고 복구** — python 저장이 LF 파일 5개를 CRLF로 오염(diff 10,770줄) → LF 복원. ProjectListScreen만 원본이 CRLF라 유지. 실변경 447줄만 커밋

## 변경한 파일
App.js · ProjectListScreen.jsx · ProgressModal.jsx · index.css · teamProfiles/tech1.js · teamProfiles/tech2.js

## 결정·근거 (팀장님 확정)
- 달성율=완료÷전체 / 2017(P) 파주 53건 포함 / 옛 전용 열=비고 보존 / 월 전환=[월간 마감] 버튼 / 공정률 전체=누적 / 마감 때 PLC·ETOS·HMI 비움 / 헤더 (2) 꼬리표=화면·엑셀생성에서 숨김

## 다음 할 일 / 막힌 점
- **◆미해결(내일 1순위): 실적 그래프 공정률 8월 25%** — getRecordMonthlyProgress를 월 기준으로 수리했으나 화면 미반영. ★단서: 7월 20%·8월 25% 조합은 이 함수(구버전이면 7월 5%)로 설명 안 됨 → 점·카드의 실제 데이터 소스 재추적 필요. 후보: ①리컴파일/캐시(Ctrl+F5·npm start 재시작 먼저) ②graphData의 mdEntry(monthlyData) 우선 경로 ③[적용하기]가 쓴 값 ④그래프 모달 2개 트랩(7/13). 카드=lastProgress(App.js graphData), 점=timeline.progressPct
- 월간 마감 롤오버 실기기 테스트(심기 행) → 검수 통과 후 테스트 심기 제거
- 전연도 실전: 전체삭제→재업로드(434건 확인)→기준연도별 보고 카드 검산
- 배포(8/18분 포함 미배포) · 이월(기술2팀 월간 협의·기술1팀 심광호 협의·VPN·365·UNC)
