# 2026-06-25 인수인계 3 — 코드분리 완료 + 기능구현 착수 (세션 전환용)

- 작업자: 염경록 팀장 / pms-app
- 목적: 대화가 길어져 **새 챗에서 이어가기** 위한 인수인계

---

## 오늘 한 일 (이번 세션)

### A. 코드 분리 — ProjectListScreen.jsx(약 2,600줄)를 조각내기
| 조각 | 결과물(신규 파일) | 내용 | 상태 |
|------|------------------|------|------|
| 1조각 컬럼규칙 | `src/components/projectColumns.js` | 컬럼 판별·색상·담당자 등 16개 규칙 | 빌드 OK + 커밋 |
| 2조각 데이터도구 | `src/components/projectListData.js` | Firebase경로·IndexedDB·엑셀파싱·병합계산 | 빌드 OK + 커밋 |
| 3조각 상세팝업 | `src/components/DetailModal.jsx` | 상세보기/수정 팝업 화면(props 6개) | 화면확인 OK, **미커밋** |

- ProjectListScreen.jsx 현재 약 **2,247줄**(분리 전 약 2,600).
- **4조각(FilterBar) 이후는 중단.** 남은 부분(필터·헤더·설정·테이블)은 상태·핸들러가 강하게 얽혀 props가 폭발 → 분리 이득 < 비용. **코드분리는 여기서 일단락.**

### B. 기능 구현 (기준문서 `기준문서/2026_기술2팀_List_항목정의서_v1.0.md` 기반)
- **⑧ Hold/HOLD 표기 통일**: `normalizeStatus`(projectColumns) 추가 → DetailModal 상태칩·드롭다운에 적용. 표시만, 데이터 불변.
- **⑤ O체크 토글**: `isCheckCol`(projectColumns) 추가 → DetailModal 편집그리드에서 `☑ O (제출) ↔ ☐ 미제출` 클릭 토글. 대상: 영업견적·공사계획서·리포트·완료처리.
- **① 공사계약·공사완료 날짜칸**: `isDateCol`에 추가 → 표·상세팝업·추가팝업에서 달력칸 자동.
- **헤더 공백 픽스**: 엑셀 헤더 띄어쓰기('영업 견적','완료 처리','공사 계약')로 매칭 실패 → `isCheckCol`·`isDateCol`을 **공백 제거 후 비교**로 수정. 화면확인 OK(셋 다 토글, 날짜칸 정상).

---

## 현재 파일 상태
- 신규: `projectColumns.js`, `projectListData.js`, `DetailModal.jsx`
- 수정: `ProjectListScreen.jsx`(분리로 축소 + import 연결), `projectColumns.js`(normalizeStatus·isCheckCol·isDateCol)
- 백업(components 폴더): `.bak-2026-06-25-split1/2/3`, `-h8`, `-feat5`, `-feat1`

## 커밋·빌드 상태 (중요)
- **1·2조각**: 빌드 OK + 커밋 완료.
- **3조각·⑧·⑤·①·공백픽스**: 팀장님 화면 확인 완료. 단 **미커밋 → VS Code에서 커밋·push 필요.**
- 커밋 메시지(제안):
  ```
  feat(list): 상세팝업 분리(3조각) + Hold통일(⑧) + O체크 토글(⑤) + 날짜칸(①) + 헤더공백 대응

  - DetailModal.jsx 분리(상세팝업, props 6개), ProjectListScreen 약 360줄 감소
  - projectColumns: normalizeStatus(HOLD→Hold)·isCheckCol·isDateCol(공사계약/완료) 추가, 공백무시 비교
  - 상세팝업: 상태칩/드롭다운 표기통일 + 영업견적·공사계획서·완료처리 O토글
  ```

## ⚠️ 주의사항 (새 챗 필독)
1. **bash 마운트 지연**: 이번 세션 내내 bash(babel/grep/diff)가 D드라이브 최신 변경을 못 따라잡았음(낡은 파일을 봄). → 코드 검증은 **파일 도구 Read**(D드라이브가 진실), 최종은 **팀장님 VS Code 빌드**. bash babel "에러"는 대부분 마운트 착시였음.
2. **헤더 공백**: 엑셀 헤더에 띄어쓰기 있음 → 컬럼 판별은 반드시 공백 제거 후 비교(projectColumns의 isCheckCol·isDateCol 패턴 참고).
3. **엑셀 최종 업로드는 맨 나중**: 보존병합(A-4c) 완성 + 백업(엑셀 생성) 후. 그 전에 원본 엑셀로 웹 List 덮어쓰기 금지(pid·실행번호·이력 소실). 업로드 시점 되면 **Claude가 먼저 "이제 업로드 테스트할 때" 라고 알리기로 약속함.** 미리보기(드라이런)는 기존 데이터 안 건드리니 일찍 가능.

## 다음 할 일 (우선순위)
1. **② 날짜↔내용 연동** ← 다음 시작점: 내용 칸 수정 시 날짜 칸을 오늘로 자동 갱신. (분석부터)
2. ③ 발주처(담당)→'발주처 담당자' 개명 + 관리자 컬럼 신규 추가.
3. **⑥ 참조 폴더명+경로복사** — 팀별 UNC 경로 입력 대기 중(기술1·2·3팀 각각 다름. 팀장님이 경로 주면 바로 구현).
4. ⑦ 표 항목 숨김 — 후순위(SIMPLE·안전은 이미 표 숨김, 시운전은 설정메뉴서 수동 숨김 가능, 자동 기본숨김만 복잡).
5. ⑨ 표↔상세/수정 팝업 동기화.

## 새 챗 시작 방법
1. `D:\pms-app\CLAUDE.md`의 '진행 상황'을 읽고 브리핑.
2. 이 인수인계 문서를 읽는다.
3. **② 날짜↔내용 연동**부터 (또는 팀장님 지시).
4. 코드 작업 시: 파일 도구 Read로 검증, 큰 변경 전 백업, 최종 빌드는 팀장님 VS Code.
