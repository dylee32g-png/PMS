# 2026-06-19 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. neconsys.web.app 사이트 배포 (6/18 작업분)
- 로컬은 최신인데 배포 사이트가 옛 버전이던 문제 해결 = **배포(deploy)가 안 됐던 것**.
- 절차 학습·실행: VS Code **터미널**에서 `npm run build` → (`The build folder is ready to be deployed.` 확인) → `firebase deploy --only hosting` → `Deploy complete!`.
- 교훈: **챗 패널에 명령을 넣으면 "Deciphering…"에서 멈춰 build가 시작도 안 됨** → 반드시 터미널에서 실행. build 시간은 변경량 무관(전체 재컴파일, 2~5분). 배포는 매일이 아니라 반영하고 싶을 때만. push(백업) ≠ deploy(사이트 반영).
- 6/18 작업분 GitHub push도 완료(아침에).

### 2. C(기준문서 잔여 구현) 착수
- C 순서 결정: **① 상태 2단계 → ② 자동표시 → ③ 이력기록**.
- 현황 파악(②자동표시 미리 점검): 잔여(Point−누적)·증감(금월−전월)은 **이미 구현됨**, 신규·금월완료는 없음.

### 3. 상태 2단계 — S-1 현황 파악
- 상태가 **한 칸**(`progressStatus`)에 12~13개(계약/작업/자동표시/구조/공통)가 섞여 있음.
- 상태 목록 정의가 코드 두 곳(`constants.js` 6개 / `App.js` 10개)에 존재 → **확인 결과 App.js가 단일 소스**. `constants.js`·`TeamSelector.jsx`는 **어디서도 import 안 되는 고아(미사용) 코드**(검색으로 확정).
- 상태는 이미 월별(`monthlyData[].progressStatus`)로 기록됨. 삭제는 soft delete(`_deletedAt/_deletedBy`)로 안전 처리됨.

### 4. 상태 2단계 — 단계적 계획 확정 + 1차
- 구현 방식: **단계적**(위험 분산). 1차 토대 → 2차 두 칸 신설 → 3차 화면 전환 → 4차 정리.
- **1차(코드 일원화)**: 확인 결과 App.js가 이미 단일 소스라 **합칠 것 없음**. 고아 파일 삭제는 위험0 확인 후 E(정리)에서 별도.

### 5. 상태 2단계 — 2차(두 칸 신설) 구현 + 검증
- `utils.js`에 **`mapLegacyStatus`** 추가: 기존 한 칸 → {계약현황, 작업현황, pending}. 자료기준 §1-C 기준.
- 매핑 규칙: 예상·검토중→계약현황 / 미작업·진행·금월완료·보고완료→작업현황(계약현황은 '수주' 자동) / 신규·하위(sub)·취소·삭제→보류(4차).
- `App.js handleSubmit`: 저장 시 두 칸 자동 파생(기존 progressStatus는 유지=호환). 수정 팝업에 '↳ 자동 2단계' 실시간 미리보기.
- **검증 3종 통과**: 자동 12/12 · 실기기(진행→수주/진행, 예상→예상/–, 신규→보류) · 손검산(매핑표 일치).

## 변경한 파일
- `src/utils.js` — `mapLegacyStatus` 함수 추가
- `src/App.js` — utils import에 `mapLegacyStatus` 추가 / `handleSubmit` newEntry에 `contractStatus`·`workStatus` 자동 파생 / 수정 팝업(약 6769행)에 '↳ 자동 2단계' 미리보기
- 백업: `src/App.js.bak-2026-06-19`, `src/utils.js.bak-2026-06-19`

## 결정·근거
- **C 순서 = 상태 2단계 먼저**: 자동표시(금월완료)·이력(상태 변경)이 모두 상태 위에 올라감 → 상태를 먼저 확정해야 재작업이 없음.
- **단계적 구현**: 데이터 신뢰 최우선, 한 번에 큰 구조 변경 시 위험 집중 → 조각으로 분산.
- **기존 칸 유지(병행)**: progressStatus는 안 지우고 두 칸만 추가 → 문제 시 즉시 복구.
- **작업 단계 값의 계약현황을 '수주'로 자동**: 작업 중이면 이미 수주된 것이라는 합리적 기본값.
- **1차 일원화**: 추측("App.js만 쓴다") 대신 직접 확인 → constants.js·TeamSelector가 고아임을 확정. 추측 금지 원칙의 실효 사례.

## 다음 할 일 / 막힌 점
- **2차 마무리**: 인라인 편집 경로에도 `mapLegacyStatus` 적용 — 월간보고 표 인라인(`handleInlineSave`), List 인라인 드롭다운(ProjectListScreen). 이래야 월간보고·List가 어긋나지 않음.
- **3차**: 화면 전환(표·팝업·필터를 두 칸 기준으로), 기존 한 칸 의존 제거.
- **4차**: 신규·금월완료를 자동표시(②)로, 하위(sub)를 구조 마커로 분리.
- **미커밋**: 오늘 변경(`utils.js`·`App.js`) VS Code에서 push 필요.
- **사이트 배포**: 오늘 변경분은 상태 2단계가 더 진행된 뒤 배포(현재 2차 미완).
- 막힌 점: 없음.
