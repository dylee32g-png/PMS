# 2026-06-10 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일
1. **보고체계 방향 확정** — 웹 단독 입력(엑셀 업로드 방식 폐기), 4개 팀 전부 사용, 팀원 각자 입력, 상태 변경 이력 기록 필요. 임의 엑셀 자동인식은 유지보수 부담으로 배제.
2. **양식 4종 분석** — 기술1·2팀 프로젝트현황 + 월간보고. 발견: 월간보고는 두 팀 뼈대가 거의 동일(통일 용이), 프로젝트현황은 상이. 기술1팀의 상태 2단계(계약/작업) 구조 채택.
3. **기준문서 확정** — `기준문서/자료기준_v1.0.md` 작성·확정:
   - 상태 2단계: 계약현황(예상→검토→물량→투심→견적→수주) / 작업현황(대기→진행→완료→보고완료)
   - 자동 표시: 신규(전월 보고에 없음), 금월완료, 잔여(Point−누적), 증감
   - '이전' = 상태 아님 → 발주처 변경 이력+배지로 대체 / sub = 모-자 공종 구조
   - 이력 기록 4종: 상태·발주처·종료일·담당자 변경 (사유 한 줄 팝업)
   - 이월 A안: 새 달 기록 없으면 최근 달 자동 이어보기 (금월 실적만 0부터)
   - 8장 동기화 방침: 프로젝트 List=어미 자료, 월간보고=같은 데이터의 창, 양방향 동기화, 연결 키는 내부 고유 ID 추천
   - 9장 작업 방식: 전수검사+로직 일원화, 뭉친 코드는 과감히 모듈 분리(병렬 작업)
4. **1단계 이월 기능 구현** (App.js):
   - 읽기: 기준월 이전 기록 겹쳐 읽기(빠진 칸은 최근 값으로 채움)
   - 쓰기: 새 달 기록 생성 시 이전 달 이어받기(인라인 입력 + 진행실적 팝업 두 경로)
   - 누적: 기준월까지만 합산으로 수정 / 누적 칸 직접 입력 차단(자동 계산 칸)
   - 상태 읽기·팀 실적 그래프: 상태 기록된 최근 달 기준으로 통일
5. **시뮬레이션 테스트** — 테스트 프로젝트로 6월→7월 전환 검증: 상태·공정률 이월 OK, 전월 값 자동 OK. **단, 시운전 금월 키인 시 누적 미합산 버그 발견(미해결)**.
6. **방식 전환 결정** — 건바이건 수정 중단. 원인: 같은 데이터를 읽고 쓰는 갈래가 5곳+ 각자 구현, 레거시(겉표지)와 월별 데이터 혼재. → 전수검사+계산 로직 일원화로 전환.
7. **보안 잠금 + 배포** — neconsys.web.app이 누구나 접속 가능하던 문제 해결: `SKIP_LOGIN=false`, 사용자 관리에서 공용 계정(ID/PW) 1개 생성, `npm run build`+`firebase deploy --only hosting` 배포 완료. Firebase CLI 설치·gmail 계정 권한 정리 포함.

## 변경한 파일
- `src/App.js` — 이월 읽기/쓰기 로직(getMonthEntry, getEffectiveStatus, getCalculatedRowData 누적, handleInlineSave seed, handleApplyProgressToMonthly seed, 그래프), accPoints 입력 차단, SKIP_LOGIN=false
- `기준문서/자료기준_v1.0.md` — 신규 작성 (1~9장)
- `CLAUDE.md` — 진행 상황 갱신
- `일일리포트/2026-06-10_기준확정-이월기능-보안잠금.md` — 본 리포트

## 결정·근거
| 결정 | 근거 |
|------|------|
| 웹 단독 입력 | 이중관리 제거. 임의 엑셀 자동인식은 "유지관리 간편" 요구와 충돌 |
| 이월 A안(자동 이어보기) | 매달 버튼 조작 불필요, DB 복사본 없음, 깜빡할 일 없음 |
| 내부 고유 ID를 연결 키로 추천 | 실행번호는 누락·변경 가능 → 동기화 끊김. ID는 불변 |
| 전수검사+로직 일원화 전환 | 데이터 신뢰가 시스템 존재 이유. 두더지잡기로는 신뢰 확보 불가 |
| 모듈 분리 기준 | 7,100줄 단일 파일이 연쇄 버그의 토양. 쪼개서 병렬 작업 가능하게 |

## 다음 할 일 / 막힌 점
- **다음 할 일**: ① 전수검사 — 월별 데이터 읽기/쓰기 갈래 전부 목록화 → 계산 로직 한 곳으로 모으기 → 시나리오 체크리스트 검증 ② 누적 미합산 버그(전수검사 1순위) ③ Firebase 콘솔에서 익명 로그인 사용 중지 + 익명 사용자 삭제(옛 게스트 세션 차단) ④ 시크릿 창으로 배포 사이트 로그인 잠금 최종 확인 ⑤ 화면별 상세 설명 모드(보류)
- **막힌 점**: Cowork 샌드박스 파일 동기화 지연 → 빌드 검증은 팀장님 PC에서 수행하는 체계로 진행
- **메모**: 공용 계정 비밀번호가 DB에 평문 저장(추후 개선), Firestore 보안 규칙(금고 잠금)은 6단계에서, 진행실적 팝업 항목 팀별 on/off 설정화, 테스트 프로젝트(이월확인 1·2·3) 삭제 여부 확인 필요
