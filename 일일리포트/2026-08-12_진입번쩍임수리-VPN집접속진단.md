# 2026-08-12 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. 최초 진입 '등록된 List가 없습니다' 번쩍임 잔존 수리
- 증상: 최초 접속(F5 직후) 팀 List 진입 시 업로드 안내 화면이 1~2초 비쳤다가 표가 뜸.
- 원인: 어제 게이트는 **행 데이터** 도착만 확인 — List는 헤더(메타)와 행을 **별도 통신**으로 받는데, 행이 먼저 도착하면 "도착 완료 + 헤더 없음" 상태가 되어 빈 팀(업로드 안내)으로 오판. 어제 수리는 반쪽이었음.
- 수리: `fbMetaLoaded`(헤더 첫 도착 여부) 신설 — **행+헤더 둘 다 도착해야** 업로드 안내 판단, 그 전엔 스피너. 팀 전환 리셋·세션 캐시 초기값·오류 시 무한 스피너 방지까지 연결.
- 검증: babel 파싱 + 도착 순서 시뮬 **5/5** (행 먼저/메타 먼저/진짜 빈 팀 포함, 어제 버그 전이 지점 재현). 워처 미감지 대비 touch.
- **미배포** — 다음 배포 때 반영 (localhost 확인 완료 상태로 커밋).

### 2. VPN 집(외부) 접속 불가 진단 — 진행 중
- 증상: 집에서 OpenVPN "Trying to connect" 무한 재시도 (8/10 사무실 테더링은 성공).
- 진단 가이드 작성: `운영가이드/문제해결_VPN_집에서접속안됨_20260812.md` (퇴근 전 준비 6항 + 집 STEP 1~6 + 로그 판독표 + 재발 방지 도메인 방식).
- 실측 결과:
  - DSM 자동 차단 목록 = **비어 있음** → 차단 아님 (위치: 보안 → 계정 탭 → 차단 목록)
  - 회사 공인 IP = **222.101.203.90 (8/6 실측과 동일 — 안 바뀜)** → IP 변경설 탈락
- **새 1순위 = 집에서 쓴 `.ovpn`이 8/10 수정본이 아닐 가능성** (`remote 222.101.203.90 21194` + `setenv CLIENT_CERT 0` 두 줄 확인 필요).
- 결정적 실험 안내: **사무실에서 폰 LTE로 같은 파일 연결 시도** — 되면 집 회선 문제 / 안 되면 파일·서버 문제(사무실서 해결 가능). **결과 대기.**

## 변경한 파일
- `src/components/ProjectListScreen.jsx` — fbMetaLoaded 게이트 (백업 `.bak-2026-08-12`)
- `src/notices.js` — 08-12 공지 1건 추가 (다음 배포 시 노출)
- 신규: `운영가이드/문제해결_VPN_집에서접속안됨_20260812.md`
- `CLAUDE.md`, 이 리포트

## 결정·근거
- 빈 팀 판정은 "행+헤더 둘 다 도착" 후에만: 통신 도착 순서는 보장이 없으므로 어느 쪽이 먼저 와도 안내 화면 오판이 없어야 함.
- VPN은 실측으로 후보를 소거(차단 X → IP 변경 X)한 뒤 남은 가설을 실험으로 가르는 순서 — 막연한 재시도는 자동 차단(5회/5분)만 쌓임.

## 다음 할 일 / 막힌 점
1. **VPN**: 폰 LTE 실험 결과 → 갈래별 조치 (파일 수정 or 집 회선 대응). `.ovpn` 도메인 방식(재발 방지) 전환 검토.
2. 진입 번쩍임 수리 **배포** (다음 배포 때 — 직원들은 그때까지 어제 버전).
3. 8/11 이월: 직원 반응 수렴 · 공용 PC 기술1팀 순환 제외(관리자 계정) · F안/보고모드/월말 대시보드 결정 · 기술1팀 UNC · 기술3팀 회신 · 직원1 PC 365 · 백업 파일 정리.
