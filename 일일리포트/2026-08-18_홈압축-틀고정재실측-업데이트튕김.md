# 2026-08-18 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일
1. **홈 화면 압축** (09:40) — 기술3팀 카드 가동으로 4카드 전부 커져 FHD에서 스크롤 재발 → 제목부·카드 여백·도넛(68→56px)·숫자(30→25px)·[열기] 행(py-3→2)·공지줄·하단 여백을 한 단계씩 축소해 무스크롤 복원.
2. **List 틀고정 오프셋 수리 2건** (11:01~11:08, 기술3팀 '진행중 칩' 깨짐·발주처↔Project 틈 벌어짐)
   - ① 칩·검색·정렬로 표시 행이 바뀌면 내용맞춤 열 너비가 변하는데 실측 의존성엔 activeRows만 있어 옛 오프셋 잔존 → `frzTick` 신호(sortedRows 변경 시 +1)로 재실측 연결. frzTick은 sortedRows에 영향 없어 무한루프 없음.
   - ② 측정 방식 교체: sticky 셀의 offsetLeft는 가로 스크롤 중엔 '붙은 위치'를 돌려줘 재측정이 무효 → 첫 비고정 셀(자연 위치)에서 고정 셀들의 offsetWidth를 거꾸로 빼 자연 위치 복원(첫 칸=0 정규화 유지). 비고정 셀 없을 때만 기존 방식 폴백.
3. **Claude Code 업데이트 재실행으로 챗 기록 소실** → 파일 흔적(mtime·diff·백업)으로 작업 복원. 주석 날짜 4곳·백업 파일명 08-14→08-18 정정, 공지(notices.js) 08-18건 추가, babel 파싱 통과.

## 변경한 파일
- src/App.js (백업 App.js.bak-2026-08-18)
- src/components/ProjectListScreen.jsx (백업 ProjectListScreen.jsx.bak-2026-08-18)
- src/notices.js

## 결정·근거
- 틀고정 실측은 '레이아웃이 바뀔 때만'(의존성 명시) 원칙 유지 — 매 렌더 실측 금지(7/21 무한루프 교훈). 신호값 방식으로 표시행 변화만 추가.

## 다음 할 일 / 막힌 점
- 실기기 확인: 홈 FHD 무스크롤 / 기술3팀 List 칩 클릭 후 가로 스크롤 시 고정 열 정합.
- 배포(8/13·8/14분 + 오늘분 미배포).
- 이월: 기술2팀 월간보고 협의(최영환)·기술1팀 협의(심광호)·VPN 폰 LTE·직원1 365·UNC 2건·백업 정리.
