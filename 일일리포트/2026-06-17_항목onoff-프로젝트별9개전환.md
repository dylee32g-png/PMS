# 2026-06-17 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### #7 진행실적 팝업 '항목 on/off' — 프로젝트별·9개 전환
1. **A안 1차(팀 설정 기반)**: 진행실적 팝업(ProgressModal)이 기존 팀 설정 `progressItems`(5개: PLC·ETOS·HMI·자체·통합)를 따르게 함. 꺼진 항목 숨김 + 진척률 계산 제외. 키 매핑(commissioning↔internalTest, intCommissioning↔integratedTest) 추가.
2. **방향 전환(팀장님 요구)**: "팀 전체 일괄은 의미 없다 → 프로젝트별", "모든 항목(9개) on/off".
   - **1단계(저장)**: `DEFAULT_PROGRESS_ITEMS` 5→9개(도면입수·I/O Map·화면작성·기준정보 추가). `handleSubmit`의 `delete progressItems` 제거 → 프로젝트에 저장. `handleOpenModal`에서 기본값(전부 켜짐) 주입.
   - **2단계(UI)**: 수정 팝업 '적용 항목' 체크박스 5→9개, 팀 설정 변경 → `formData`(그 프로젝트) 변경. 안내문구 수정.
   - **3단계(읽기)**: `getAppliedKeys()` → `getAppliedKeys(p)` 인자화(프로젝트 우선). 호출처(`getCalculatedRowData`, `graphData`) p 전달. 진행실적 팝업 props를 `pmsProgressRow.progressItems` 기준으로. 타이틀 요약도 formData 기준.
   - 신규 프로젝트 기본값 = 9개 전부 켜짐(팀장님 결정).
3. **진척률에서 SIMPLE 제외**: 도면입수·I/O Map·화면작성·기준정보 4개를 진행실적 팝업 '진척률(%)' 계산에서 제외(표시·입력은 유지). 메인 표 '공정률(%)'(원래 5개 = PLC·ETOS·HMI·자체·통합)과 항목 기준 통일.
4. **4단계(엑셀/주간요약)**: 엑셀 공정률(%)은 이미 프로젝트별 자동 반영(`getAppliedKeys(p)` 덕분). 엑셀엔 PLC/HMI 개별 컬럼 없음(시운전·공정률만). 엑셀 시운전 열·주간보고 요약은 실익 작아 스킵(팀장님 '여기서 마무리' 결정).

### 개념 설명 (비전문가 눈높이, 그림 포함)
- **메인 공정률 vs 팝업 진척률이 다른 이유**: 항목·개수 같아도 ① 데이터 출처 다름(메인=월별표 저장 스냅샷, 팝업=주간 실시간 누적) ② 시운전 단위 다름(메인=정수% 저장값, 팝업=포인트→% 소수 환산). 26-1 실제 = 메인 17%(정수 36·45 평균) vs 팝업 17.6%(소수 36.36·45.45 평균) — 반올림 시점 차이, 버그 아님. 팀장님 '이해만, 통일 보류' 결정.
- **시운전 4·5는 %가 아니라 포인트(점수)**: 만점(총포인트) = `point` = `totalCommissioningPoints`(담당자가 수정 팝업 'TOTAL POINTS' 칸 또는 엑셀 '총포인트' 컬럼에서 직접 입력). 26-1은 11점 → 자체 4/11=36%, 통합 5/11=45%.

## 변경한 파일
- `src/App.js`: DEFAULT_PROGRESS_ITEMS 9개 / handleOpenModal(progressItems 주입, 기존·신규) / handleSubmit(delete progressItems 제거) / getAppliedKeys(p) 인자화 / getCalculatedRowData·graphData 호출에 p 전달 / 수정팝업 체크박스 9개+formData / 타이틀 요약 formData 기준 / ProgressModal props pmsProgressRow 기준 / 체크박스 안내문구.
- `src/components/ProgressModal.jsx`: progressItems props / ITEM_SETTING_KEY 9개 / isItemOn·SIMPLE_ON·SECONDARY_ON·WEEKLY_ON / 진척률(pctByWeek·overallPct)에서 SIMPLE 제외 / renderSubRow 시운전 줄 조건부(rowSpan 보정) / hasData.
- 백업: `App.js.bak-2026-06-17`(a/b/c), `ProgressModal.jsx.bak-2026-06-17`(a/b/c).

## 결정·근거
- **적용 단위 = 프로젝트별**(팀 일괄 폐기): 프로젝트마다 하는 항목이 달라 팀 일괄은 무의미.
- **항목 = 9개 전부 / 신규 기본 = 전부 켜짐**: 기존 데이터(전부 true) 호환, 실수로 빠뜨릴 위험 없음.
- **SIMPLE 진척률 제외**: 메인 공정률(5개)과 항목 통일. 계산 '방식'(메인=평균, 팝업=누적/포인트)은 그대로 둠(이해만, 통일 보류).
- **엑셀 마무리**: 공정률 자동 반영, 나머지 스킵(작업량 대비 실익 작음).

## 검증 (규칙 8: 자동 / 실기기 / 손검산)
- **자동 시뮬**: 진척률 로직 — 회귀(전부 켜짐=변경 전 동일) + 항목별 토글 + SIMPLE 무관 + 0 나눗셈 방지. 3회 모두 전부 통과(7/7, 10/10, 7/7).
- **실기기(26-1, 기술2팀)**: HMI 끄고 저장 → 팝업에서 HMI 줄 사라짐, 진척률 21.2%, 메인표 HMI=N/A·공정률 21%. 옆 프로젝트 26-3(HMI 7%)·26-6(HMI 32%) 그대로 = **프로젝트별 독립 확인**. 화면 정상 렌더 = 빌드 정상.
- **손검산**: 21.2% = (1+2+36.36+45.45)÷4, 예측과 정확히 일치.

## 다음 할 일 / 막힌 점
- **VS Code에서 커밋·push 필요**(오늘 변경 미커밋).
- 26-1 HMI 테스트 원복 확인(다시 켜고 저장).
- 정리 후보: `src/App.js` 엑셀 `selfEnabled`(~2538줄)가 아직 `teamSettings.progressItems` 참조 — 이제 항목은 프로젝트별이라 '유령 코드'(동작 정상, 항상 true). 나중에 프로젝트별 or 고정으로 정리.
- 로드맵 잔여: #8 '이전값' 표시 / A-4c 자동연결(보존 병합) / C(기준문서 잔여: 상태 2단계·자동표시·이력기록) / D(주간보고 라인) / E(운영·확장).
