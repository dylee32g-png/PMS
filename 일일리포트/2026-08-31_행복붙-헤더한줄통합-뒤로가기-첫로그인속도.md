# 2026-08-31 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. 엑셀식 행 복사/붙여넣기 (팀장님 지시 — 8/28 "확인만" 했던 설계를 구현)
- **행 선택**: 번호 칸 클릭 = 행 전체 선택(파란 하이라이트), Shift+클릭 = 여러 행 확장. 번호 값을 고칠 땐 **더블클릭**(클릭이 선택으로 바뀌었기 때문).
- **Ctrl+C**: 선택 행을 내부 보관(rowClipRef) + 엑셀용 TSV를 클립보드에 복사(엑셀에 바로 붙여넣기 가능). 글자를 드래그해 놓은 상태면 보통 복사로 통과.
- **Ctrl+V**: 새 프로젝트로 추가 — **번호 = 그 연도 마지막 번호+1**(패딩 규칙 포함), **수행번호 = 빈칸**(자동 부여 체계 보호), 새 `_id`/`_pid`/`_regDate`, 도장(stampSave)+백로그(추가) 기록. 붙여넣기 중복 실행 방지(pasteBusyRef).
- 8/28에 만든 우클릭 [이 행 복사해서 추가](팝업 경유)도 병행 유지 — 값을 고치며 추가할 땐 우클릭, 그대로 복제할 땐 Ctrl+C/V.
- 검증: Playwright 자동 재현(rowcopy_cv_test — 선택→복사→붙여넣기→번호 확인 왕복). ⚠검증 중 실데이터에 **기술2팀 193번(AP4 TCNV36 복사본) 1건 생성** — 팀장님 확인/삭제 필요.

### 2. List 헤더 한 줄 통합 — KPI 미니 요약 (팀장님 안 승인)
- 흐름: 상단 요약 카드 여백이 허전 → 추천안 A(카드에 스택바/도넛) 시안 → **팀장님 안**(팀 탭 삭제 + 요약을 제목 라인에 흡수) 시안 B → 승인 → 구현.
- **팀 탭 스트립 제거** → 제목 "**팀명 ▾**" 클릭 = 팀 전환 드롭다운(switchTeam, 노란 칸 이동 차단 guardNav 동일 적용). 홈 버튼은 컴팩트 왼쪽으로.
- **KPI 카드 줄 삭제** → 헤더에 미니 요약 4칸(동일 폭 190px): ①전체=상태 구성 스택바+범례 ②추진중 ③진행중 ④완료 = 건수+**전체 대비 비중 도넛**. 완료 칸엔 **▲N 이번 달** 배지(공사 완료 날짜가 이번 달인 행 수 — kpiData.doneThisMonth 신설).
- 도넛 기준 통일: ③진행중도 비중으로(팀장님: "평균 공정률은 모수가 애매" — 평균 공정률은 툴팁으로만).
- 헤더 188/202행 카운터 제거(하단 상태줄과 중복 — 팀장님 지시). [+ 프로젝트 추가]→[+ 추가], 백로그·설정 아이콘만. FHD(1920) 한 줄 확인, 표가 위로 올라와 세로 ~100px(행 3~4줄) 확보.
- 지난 연도 보고 카드(완료÷전체 달성율)는 기존 그대로 유지.
- **▲이번 달 검산(팀장님 지시)**: 기술2팀 3건 = 140·168·170번 실측 일치. ⚠**170번 = 완료일 26/08/04인데 상태 '진행중'** — 데이터 정리 대상 발견(팀장님 확인).

### 3. 브라우저 뒤로가기(←) = 앱 이전 화면 (팀장님 지시)
- App.js: 화면 전환(currentMode·currentTeam)마다 history.pushState 기록, popstate로 복원(histNavRef로 무한 기록 방지, 최초 진입 replaceState 도장).
- 노란 칸(초안)은 localStorage 보관이라 뒤로가기에도 안전.
- "안맞는데" 신고 → :3000·:5000 재검증 전부 통과 — 원인은 **배포판(neconsys.web.app)이 미배포 상태**였던 것. 팀장님 확인 완료.

### 4. 첫 로그인 19.7초 → 홈 1.5초 (조사·수리 3건)
- 계측(Playwright login_speed): 캐시 없는 첫 PC 기준 로그인→홈 표시 19.7s.
- 1차: 로그인 직후 **월간보고 전체(projects 367건) 구독**을 pms/List/모바일 진입 시로 지연 + 홈 게이트=설정 도착 시 해제 → 16.6s(미미 — 주범 아님).
- **진짜 병목**: 홈 팀 카드 통계(homeStats)가 3팀 전체 행(~2,600건)을 다운로드하며 **registeredUsers 명단 getDoc(홈 진입 게이트)을 줄 세움** → 명단 확인 후에 통계 시작으로 순서 변경 → 홈 표시 **2.0s**.
- **홈 카드 = 올해 행만 서버 조회**: teamStats를 `where('_year','in',[문자연도, 숫자연도])`로 전환. 전환 전 3팀 전 행 2,600건 전수 실측 — **`_year` 없는 행 0건**(기술1팀 434/기술2팀 1,914/기술3팀 252) → 카드 숫자 불변 보장 확인 후 적용.
- **결과**: 홈 표시 1.5s / 홈 카드 숫자 17.4→**2.3s** / List 첫 진입 10.0→**4.3s** / **홈 방문당 Firestore 읽기 2,600→264건(90%↓)** — 8/26 할당량 소진 문제 직접 완화.

## 변경한 파일
- `src/components/ProjectListScreen.jsx` — 행 복/붙(copySelectedRows/pasteCopiedRows/keydown Ctrl+C·V/번호 칸 클릭·더블클릭), 헤더 한 줄 통합(팀 드롭다운·미니 KPI 4칸·miniDonut·MINI_STATUS_COLORS·doneThisMonth), 카운터 제거
- `src/App.js` — 브라우저 히스토리(pushState/popstate), 로그인 effect에서 projects 구독 분리(모드 게이트 신설), homeStats 명단 후 실행
- `src/components/teamStats.js` — 올해(_year) 행만 getDocs(query+where)
- `src/notices.js` — 08-31 공지
- 커밋 8건: 97fdbcb → 1b76c74 → aeba1be → 4b4ba8f → 57d76ae → 53f6d4a → 799a1af → 111a1a1 (전부 push 완료)

## 결정·근거
- **진행중 도넛 = 비중으로 통일**: 평균 공정률은 값 있는 행만의 평균이라 모수가 애매(팀장님) — 툴팁으로 강등, 4칸 기준 일원화.
- **팀 탭 삭제 대신 제목 ▾ 드롭다운**: 홈까지 나갔다 오는 클릭 절약 + 미저장 노란 칸 이동 차단 규칙 동일 적용.
- **번호 칸 = 클릭 선택 / 더블클릭 편집**: 엑셀과 같은 손맛 + 기존 번호 수정 경로 보존.
- **연도 조회 전환 전 전수 실측**: `_year` 없는 행이 있으면 카드에서 빠지므로, 0건 실측 확인 후 적용(수치 검증 원칙 — 루틴 규칙 8).

## 다음 할 일 / 막힌 점
- **★배포(팀장님)**: `npm run build` → `firebase deploy --only hosting` — **8/25 오후분~8/31 전부 미배포**. 배포 후 시크릿 탭으로 확인 + **공용 PC 파수꾼 설치.bat 관리자 재실행**(8/25 이월).
- 팀장님 확인 2건: ① 기술2팀 **193번**(AP4 TCNV36 복사본 — Ctrl+V 검증 잔재) 확인/삭제 ② **170번**(완료일 26/08/04인데 상태 '진행중') 데이터 정리.
- 이월: Firebase 사용량·Blaze 검토(오늘 읽기 90% 절감으로 완화 — 전무님 보고감), 기술3팀 실전 적재, 협의 3건(최영환·심광호·김승환), NAS 하위 장부 주차 전환 관찰(월요일), List 첫 진입 잔여 4.3s(수천 행 되면 연도별 분리 후보 — 기록만).
