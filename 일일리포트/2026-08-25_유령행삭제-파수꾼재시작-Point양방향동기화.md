# 2026-08-25 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. 기술2팀 008 유령 행 — 삭제 완료 + 배포
- 어제 진단대로 위쪽 008(옛 양식 행) 팀장님이 [완전 삭제] 실행 → 재등장 없음
- **라이브 배포 실행**(팀장님, 8/18~24 누적분) — 크롬 실기기에서 008 정리·NAS 최신 반영 확인

### 2. 상단 요약 "N월 완료" 카드 삭제 (3팀 공통)
- 팀장님: "의미 없음" — 공용 카드 1개라 한 곳 삭제로 3팀 전부 반영, 남은 카드가 flex로 폭 자동 분배
- 사용처 없어진 계산 코드(공사완료 날짜 세기·신규 등록 건수·doneCol)도 함께 정리

### 3. ★메인PC 파수꾼 갱신 — 매일 새벽 자동 재시작 (운영가이드/메인PC파수꾼)
- **발견한 구멍: 전용 창은 한 번 뜨면 영원히 새로고침 안 함** → 배포 후에도 옛 코드로 돌고, 재적재 후 옛 데이터 화면이 저장하면 지운 행 부활(= 008 유령의 유력 범인)
- 수리: `pms_restart.ps1`+`pms_restart_hidden.vbs` 신규(전용 프로필 크롬만 종료→3초 후 새 창) + 설치.bat에 **매일 05:30 재시작 스케줄** 등록 + 안내문 3팀 순환 기준 갱신 + 해제/상태보기 반영
- ⚠공용 PC에서 **설치.bat 관리자 재실행 필요**(재실행 안전 — 로그인·허가증 유지)
- 부수 교훈: .bat=cp949·CRLF — python 쓰기 전 인코딩 검증(0바이트 절단 사고 1회→git 복구)
- NAS 칩 색 확인: 파랑=허가증 없는 크롬(정상·검사만 안 함), 초록=검사 완료 최신 — 원클릭 엑셀 버튼은 주소 기반이라 파랑에서도 열림

### 4. 010 진행율% 사라짐 버그 수리 (paRecalc 분모)
- 증상: 010만 진행율% 빈칸(011은 26.3%) — 원인 = **상세팝업 저장이 진행율%를 무조건 재계산**하는데 분모('포인트' 저장값)가 건설 행에선 빈칸(총점=하위 Σ 표시일 뿐) → 0으로 나눠 빈칸으로 지움. ETOS 토글하려고 연 팝업의 [저장]이 방아쇠
- 수리: 분모 빈칸이면 `effTotalPt`(하위 Σ→자체) 폴백 — NAS 자동식(누적÷Σ총점)과 통일. 시뮬 5/5(010→28.7·011→26.3)
- 검산 확정: 010=350÷1,221=28.7% / 011=4,534÷17,230=26.3% / 팝업 진척률 52%=적용 7항목 평균(통합 28.7 포함) / 그래프 공정률 72%=핵심 4항목 평균 — **팝업 vs 그래프 기준 차이는 현행 유지**(팀장님 확정: 그래프에 근거 표기로 해소)

### 5. 실적 그래프 공정률 카드 — 계산 근거 표기
- 카드 밑 설명 = 실제 들어간 항목 동적 표기("PLC+ETOS+HMI+통합시운전 평균", 미적용 자동 제외·잘리면 툴팁) — App.js 개별 그래프 스탯 4종. 시뮬 4/4

### 6. ★통합시운전 기본 ON (기술2·3팀)
- 원인: 기본 미적용 규칙="헤더에 그 이름 열 없으면 끔" — 새 양식은 통합열이 '진행율 %'라 오판
- 수리: 팀 카드 통합열이 표에 있으면 통합시운전='열 있음' 취급 — 3곳(List defaultNaItems·projectListData naProgressItemsOf(+통합열 파라미터)·모바일 호출부). 시뮬 4/4(직접 끈 프로젝트 존중·기술1팀 불변)
- 진행실적 팝업 [자체/통합] 기본 = **통합**(tech2 카드 `시운전.기본소스:'통합'`, tech3 상속) — 행 저장값 무관 무조건(팀장님 재확정, 저장값 우선 1차안은 001의 옛 'self'가 이겨서 폐기)

### 7. ★Point(실적) 양방향 동기화 — 팀장님 확정 "증분 배치" 로직
- 개념: 팝업 장부="주마다 그 주에 딴 점수"(합산) ↔ 메인표 Point="총합"(누적) → 그대로 넣으면 이중 계산
- **메인표→팝업**: Point 키인 → 이번 주차 = 새 누적−(다른 주차 합) 자동 기록(`syncAccPointToLedger` 신설, doCommitCell 2경로) — 합계=키인값·과거 이력 보존·같은 주 재키인 멱등·**감소는 메인표 저장까지 전체 차단+경고**("팝업에서 해당 주차 수정") · 빈칸 무시·NAS/하위 행 제외(sub_i 장부 가드 이중 안전)·그래프 즉시 반영. 시뮬 7/7
- **팝업→메인표**: [적용하기] 실적이 웹 전용 '포인트실적'에만 쓰여 Point 열이 안 바뀌던 어긋남(팝업 185 vs 메인 140) → 팀 누적열('Point')에도 쓰고 진행율% 재계산(applyProgressToMainRow). 시뮬 PASS(185·92.5%)
- ⚠상세팝업 안에서 Point를 고치는 경로는 아직 미연결(셀 키인만) — 테스트 후 확장 결정

### 8. (마감 후 추가) 프로젝트 추가 — 번호 자동 +1 + 중복 차단 (3팀 공통)
- [프로젝트 추가] 창 열 때 번호 = **그 연도 메인 행 최대 번호+1 자동 기입** (`nextProjNo` — 행 복사 추가여도 번호는 새로 받아 중복 방지)
- 저장 시 **같은 연도 메인 행과 번호 중복이면 강한 경고 + 저장 금지** ("⛔ 프로젝트 번호 중복 — 저장할 수 없습니다!" + 겹치는 프로젝트명 표시)
- 번호 열 = '번호'(기술2·3팀, 3자리 패딩 비교 '1'='001') 또는 '순번'(기술1팀, 패딩 없음) / 연도별 독립(2025의 050과 2026의 050은 중복 아님) / 하위(s) 추가·빈 번호는 제외
- 검증: 파싱 OK + 시뮬 7/7 (자동번호 189·051 / 중복 188·001 차단 / 패딩 비교 / 연도 독립 / 빈칸 통과)

## 변경한 파일
- `src/components/ProjectListScreen.jsx` (N월완료 카드·paRecalc 분모·defaultNaItems 별칭·syncAccPointToLedger·적용하기 재배선 — python 패치 a~e)
- `src/App.js` (그래프 공정률 카드 근거 표기) · `src/components/ProgressModal.jsx` (자체/통합 기본) · `src/components/projectListData.js`·`MobileInputScreen.jsx` (통합열 별칭) · `src/teamProfiles/tech2.js` (기본소스)
- `운영가이드/메인PC파수꾼/` 설치·해제·상태보기.bat 갱신 + pms_restart.ps1·pms_restart_hidden.vbs 신규
- `src/notices.js` (08-25)

## 결정·근거 (팀장님 확정)
- 팝업 진척률(8항목) vs 그래프 공정률(4항목) = **현행 유지**, 그래프에 계산 항목 표기로 혼선 해소
- Point 동기화 = 증분 배치 + 감소 차단 ("중요한 판단이라 가장 이상적인 로직으로")
- 통합시운전 기본 ON·팝업 기본 [통합] = 기술2·3팀 무조건

## 다음 할 일 / 막힌 점
1. **★Point 양방향 동기화 실기기 테스트**(팀장님, 내일 1순위): ① 001 팝업 [적용하기]→Point 185·진행율 92.5% ② Point 200 키인→팝업 이번 주 +15 ③ Point 100 키인→경고 차단 ④ 팝업 [통합] 기본 확인 → 방식 확정되면 상세팝업 Point 경로 확장 결정
2. 테스트 통과 후 **배포**(오늘 오후분 전부 미배포) + 공용 PC **파수꾼 설치.bat 재실행**(새벽 재시작 등록)
3. 기술3팀 실전 적재(전체 삭제→260824+과거 5개년) / 속도 계측 확인 / 협의 3건(최영환·심광호·김승환)
4. 관찰: NAS 하위 장부가 '누적 스냅샷을 현재 주차에' 쓰는 방식 — 주차가 바뀌면 팝업 합계가 부풀 수 있는 구조적 의심(8/5 유령칸과 별개) → 다음 주 월요일 자동 반영 후 010·011 팝업 합계 = Point 값인지 확인
