# 2026-07-20 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일
1. **하위(공종) 2단계 — 메인 POINT = 하위 합계 자동 (완료·실기기 통과)**
   - 하위(실행번호 s/-)가 있는 메인 행의 총점 = 하위 '포인트' 합으로 **파생 계산** (저장 안 함 — 어긋남 원천 차단. 하위 총점이 전부 빈칸이면 기존 부모 총점 유지 = 전환기 안전, 팀장님 확정 2건).
   - 반영 6곳 일원화: 메인표 POINT 칸 `실적/Σ합`(툴팁·초과 빨강경고 합 기준) · 실적그래프 총점 · 진행실적 팝업 totalPt(subRows에 pt 전달) · 모바일 · 엑셀 생성 · 상세팝업.
   - 손입력 잠금(팀장님 확정): 부모 POINT 칸 키인 차단+안내창, 부모 상세팝업 '포인트' 🔒 합계 표시. 하위 없는 프로젝트는 100% 불변.
   - 검증: 자동 시뮬 31/31 + 실기기(Σ300 손검산 일치 · 하위 총점 변경 즉시 반영 · 잠금 2종 · 팝업 총점 300) 전부 통과.
2. **실적그래프 자동 스크롤 (A안 확정)** — 열릴 때 마지막 실적 달(없으면 오늘 달)이 보이게 이동+하단 표 동기. 옛 계약(2025-01) 프로젝트가 "그래프 안 뜸"으로 보이던 오해 해소. 기간 규칙·계산 불변.
3. **하위 3단계 — 엑셀 확정저장 = 보존 병합 (구현 완료 · 실기기 테스트 대기)**
   - 옛 '전량삭제→교체' 폐기 (pid 전멸로 실적 장부·하위·이력이 끊기던 구조).
   - `computeMergePlan`(projectListData.js, 순수 함수): 매칭(연도+번호→연도+이름) 행 = 기존 _id 유지·엑셀 컬럼만 갱신(엑셀 절대우선)·웹 전용 값(_pid·실행번호·_regDate·이력·포인트실적) 보존·값 동일 행 쓰기 생략 / 신규 = 추가 / 엑셀에 없음·하위 = 유지(삭제 0건) / 저장 직전 JSON 자동 백업 + 확인창 개수 안내.
   - 검증: 시뮬 20/20 + 실데이터(7/9 백업 1,883행) 재업로드 = 152건 전원 매칭·pid 100% 보존·쓰기 0건.
4. **번호 3자리 통일** — 정렬이 1,10,100,11… 로 섞이던 문제.
   - 업로드 시 1→001 자동(padProjectNo, 원본 엑셀은 그대로) + **병합 매칭 정규화('6'↔'006' 동일 취급 — 안 하면 확정저장 때 전원 신규가 되는 함정 방어)** + 정렬 숫자 비교(기존 데이터도 즉시 순서 정상) + 웹 편집 3경로(셀·상세팝업·추가) 패딩.
   - 설정 메뉴 **'번호 3자리 정리 (1→001)' 관리자 버튼** 신설 — 엑셀 업로드 없이 기존 클라우드 일괄 변환(번호 칸만 merge, 실측 대상 1,192건/2026년 98건). 검증: 시뮬 21/21.
5. **모바일 개편 4+2종**
   - 번역 팝업 차단: 원인 = index.html이 CRA 기본 `lang="en"`+영어 문구 → `lang="ko"`+한글화+`notranslate` 메타.
   - 상태 칩 순서: 전체 → 진행중 → 추진중 고정, 나머지 건수순.
   - **한 주 간편 입력 패널**(mobileMode): 오늘 주 하나만 큰 입력칸(48px)·◀▶ 주 이동·오늘 복귀·공정률 직전값 힌트·하위별 시운전·진척률 요약. '전체 기간 보기' = 기존 표(좌우 스크롤+이전/금주/다음). PC 불변.
   - **이전/다음 프로젝트 이동 + 목록 버튼**: 키인 → 다음 카드로 바로. 순서 = 카드 목록(칩 필터 반영, 팀→연도↑→엑셀 행순 — 팀장님 '그대로' 확정). 미저장 이동 시 경고. key 리마운트로 장부 새로 읽음. 시뮬 6/6.
   - 팝업 높이: vh가 폰 하단 툴바 뒤까지 포함해 [적용하기] 바가 화면 밖으로 밀리던 문제 → innerHeight 기준 + 주 선택 줄 상단 고정(sticky). 폰 뷰포트(390×660) 실측 확인.
6. **적용하기 원클릭 (PC·모바일 공통)** — [적용하기] → 확인 패널 → [저장] 2단계를 1클릭으로: 주차 장부 저장+메인표 반영+월간 동기화 일괄 실행, 수치 요약은 완료 메시지로.
7. **4자리 포인트 잘림 수정** — 1000pt가 '100'처럼 보여 값 훼손으로 오인되던 문제. Playwright 실측으로 원인 확정: **크롬 number 입력칸이 스피너(▲▼) 공간 ~16px을 항상 예약** → 44px 칸에서 3자리까지만 표시(978 OK·1000 잘림). 수정 = index.css 스피너 전역 숨김 + 4자리↑ 글씨 자동 축소. **데이터는 원래 정상**(합계가 증거).
8. **배포 캐시 문제 해결** — "배포했는데 폰에 안 바뀜"의 원인 = Firebase 기본 1시간 캐시(index.html 포함). 번들 마커 실측으로 빌드 정상 확인 후 firebase.json에 캐시 규칙 추가(index.html 등 no-cache / static 해시파일 immutable). 이번 배포부터 폰 새로고침 즉시 반영.
9. **저장소 정리** — 오전 커밋(2aeca43) 푸시 완료. utils.js·EstimateScreen.jsx 줄끝(CRLF) 노이즈가 오전 정리 후 재발 → HEAD 내용으로 재원복 + **`.gitattributes`(`* -text`) 신설로 줄끝 자동 변환 전면 금지(재발 근절)**.

## 변경한 파일
- src/components/ProjectListScreen.jsx — 2·3단계, 번호 패딩·정리 버튼
- src/components/projectListData.js — computeMergePlan·padProjectNo·매칭 정규화
- src/components/ProgressModal.jsx — 2단계 totalPt·모바일 패널·이동바·원클릭·잘림·높이
- src/components/DetailModal.jsx — 부모 '포인트' 잠금 표시
- src/components/MobileInputScreen.jsx — 칩 순서·mobileMode·이동 목록
- src/App.js — 실적그래프 자동 스크롤
- src/index.css — 숫자칸 스피너 숨김
- public/index.html — lang=ko·notranslate·한글화
- firebase.json — 캐시 규칙
- .gitattributes — 신설(줄끝 변환 금지)
- CLAUDE.md · 일일리포트(본 문서)

## 결정·근거 (팀장님 확정)
- 2단계 잠금 = 총점+실적 둘 다 / 하위 총점 전부 빈칸 시 부모값 유지 — 이중 입력 차단·전환기 안전.
- 3단계 = 보존 병합 / 엑셀에 없는 행 유지 — 하위 실적 장부가 부모 pid에 묶여 있어 pid 보존 없인 반쪽. 삭제는 우클릭 개별로.
- 그래프 = A안(자동 스크롤) — 기간 규칙 불변, 변경 최소.
- 모바일 이동 순서 = 카드 목록 그대로(팀→연도↑→행순) 유지.
- 적용하기 = 원클릭 — 단계 축소 우선.

## 다음 할 일 / 막힌 점
1. **3단계 실기기 테스트**: 엑셀 업로드 → 병합 미리보기(드라이런) 개수 확인 → 확정저장(백업 JSON 자동) → 하위·Σ·그래프 정상 확인.
2. **폰 최종 확인**: `firebase deploy` 후 이번 한 번만 시크릿 탭 — 주 선택 줄 상단 고정 + [적용하기] 바 표시 확인(다음부턴 일반 새로고침으로 즉시 반영).
3. '번호 3자리 정리' 버튼 실행 (관리자, 확인창 개수 확인).
4. Firebase 승인 도메인 추가(neconsys.web.app) — 관리자 요청 필요, 이메일 링크 로그인 폰 테스트 선행조건.
5. ⚠ 엑셀 원본에 하위 줄(실행번호 s) 직접 넣기 금지 — 병합 매칭 규칙 아직 없음(하위는 웹에서만).
6. 오후분 커밋/푸시 (`커밋메시지_0720b.txt` 참조).
