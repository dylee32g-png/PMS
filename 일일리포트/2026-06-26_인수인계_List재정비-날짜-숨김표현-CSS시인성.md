# 2026-06-26 인수인계 (새 챗 이어가기용)
- 작업자: 염경록 팀장 / 프로젝트: pms-app
- 작성 사유: 챗이 길어져 새 챗에서 이어감. 아래만 보면 그대로 이어집니다.

---

## ★ 새 챗에서 "가장 먼저" 알아야 할 3가지

1. **오늘 변경은 전부 미커밋** — 마지막 커밋은 `3046aa0`(어제 분). 오늘 고친 `projectColumns.js · ProjectListScreen.jsx · index.css`는 **아직 커밋·push 안 됨.** 화면 확인 OK 되면 VS Code에서 커밋/push 필요. (커밋 메시지 초안은 맨 아래)

2. **표 스타일은 무조건 `src/index.css`에서 고친다.** index.css에 `!important` 규칙이 많아서 **JSX 인라인 style을 전부 이긴다.** (오늘 한참 헤맨 이유 = 인라인만 고쳐서 안 먹었음.) 특히:
   - `thead tr th/td`, `tbody tr td` 에 `border-right`, `background-color`, `color`가 `!important`로 박혀 있음.
   - 그래서 칸 색/세로선/배경은 **CSS에서, 클래스로(specificity 높게)** 바꿔야 함.

3. **엑셀은 "미저장 미리보기(pending)" 상태** — 아직 Firebase 확정저장 안 함. **새로고침(F5)하면 날아가고** 옛 데이터로 돌아감. 보던 상태 유지하려면 새로고침 말고 VS Code 저장(자동 리컴파일)만. 다시 보려면 수정본 엑셀 재업로드.

---

## 오늘 한 일 (순서대로)

1. **시작 브리핑** + 커밋상태 확인(git `3046aa0`, 깨끗). 어제까지는 다 커밋돼 있었음(팀장님 확인).
2. **엑셀 업로드 + 병합 미리보기(드라이런)**: 갱신 1566(값바뀜 46)·신규 317·엑셀에없음 275. → 클라우드가 이미 엑셀과 거의 같음. **'그냥 교체'는 pid 1566개 끊겨 손해**라 안 함.
3. **방향 확정(팀장님)**: 기존 데이터=throwaway 테스트용. pid는 나중에 규칙 부여. 이번 업로드를 리허설 삼아 **문제점 찾기**.
4. **업로드 audit (2026 탭 152행 직접 분석)** → 문제 발견 & 수정:
   - **① 공사계약·공사완료 날짜가 6자리(YYMMDD, 예 251125)** → 달력 파싱 100% 실패. **수정 완료**(`toDateInputVal`에 6자리 해석 추가, 251125→2025-11-25. "2022년" 같은 비날짜는 글자 그대로).
   - **헤더 기준 시트 버그**: 맨 앞 'SM대응'(10열)이 전체 헤더 기준이 돼 2026 열들이 가려짐. **수정 완료**(열 가장 많은 시트=2026을 기준으로).
5. **공사진행 12칸 정렬 (엑셀 수정본 제작)**: 월간 팝업의 '진행현황(%) 9항목'을 List 공사진행에 맞춤. **Claude가 수정본 엑셀 제작** → `outputs/01 2026년_파주 공사 List_기술3팀_260626_수정.xlsx`.
   - 공사진행 최종 12칸: **날짜·내용·도면입수·I/O Map·화면작성·기준정보·PLC·ETOS·HMI·자체시운전·통합시운전·포인트** (시운전(일반) 삭제). 데이터 152행 보존 검증.
   - 팀장님 재업로드 → 화면 반영 확인 (전체 35칸).
6. **표 기본 숨김/표현 (팀장님 지정)**:
   - **보임 13**: 번호·발주처·Project·진행현황·담당자 + 날짜·내용·PLC·ETOS·HMI·자체시운전·통합시운전·포인트
   - **숨김 22**: 공사계약·공사완료·관리자 / 도면입수·I/O Map·화면작성·기준정보 / 서류 7개 / 안전 8개
   - 실기기 확인(주요열 14 = 13+실행번호) OK.
7. **표 정리**: No.칸 제거(엑셀 '번호'와 중복), 실행번호 표에서 숨김, 시운전 % 칸 너비 210→60, 그 칸 중앙정렬, 너비 드래그 손잡이 14px·z-50으로 잡기 쉽게.
8. **★ 공사진행 묶음 시인성 (마지막, 실기기 확인 대기)**: 색·세로선이 계속 안 보였는데 **진짜 원인 = index.css `!important`**(세로선 전부 제거 + 배경 덮어씀). → **CSS 직접 수정**: 칸 세로 격자선 복원 + `prog-head/prog-cell/prog-l/prog-r` 클래스로 공사진행 강조(파란 막대+옅은배경+남색 굵은선).

---

## 변경한 파일 (전부 미커밋)

- **src/components/projectColumns.js**
  - `toDateInputVal`: 6자리 YYMMDD 파싱 추가.
  - `isDefaultHiddenCol`: 키워드식 → **정확목록 `DEFAULT_HIDDEN_COLS`**(공사계약·공사완료·관리자·도면입수·I/OMap·화면작성·기준정보·참조·업체담당자·안전 8개).
- **src/components/ProjectListScreen.jsx**
  - `handleFileUpload`: 헤더 기준 = 열 최다 시트(2026) (`canonColCount`).
  - 기본숨김 useEffect: **헤더 바뀔 때마다 재적용**(1회제한 버그 제거).
  - `mainVisibleHeaders/Groups`: 실행번호(EXEC_NO_COL) 표 제외.
  - `progFirst/progLast/isProgCol/centerCol` 신설(공사진행 범위·중앙정렬).
  - `getW`: 공사진행 날짜38·내용210·그외(시운전%) 60.
  - No. 헤더/본문 칸 제거(+frozen offset 22→0).
  - 표시 td: 날짜=`toDateInputVal||원본`, 공사진행% 중앙정렬, **prog 클래스 부여**.
  - 너비 손잡이: `w-[14px] z-50 hover:bg-blue-500/50`.
  - ※ 인라인 style도 일부 넣었으나 index.css에 져서 **클래스 방식이 실제 적용**. 인라인 잔재 있음(무해, 나중에 정리 가능).
- **src/index.css**
  - `thead tr th/td`(104행)·`tbody tr td`(124행): `border-right: none` → **`1px solid var(--line)`**(세로 격자선 복원).
  - 추가 블록(146행 뒤): `.prog-head`(파란막대+흰글씨+남색테), `.prog-cell`(옅은파랑), `.prog-l/.prog-r`(남색 3px).

## 백업 파일 (src/components/)
- projectColumns.js.bak-2026-06-26, -b
- ProjectListScreen.jsx.bak-2026-06-26, -b, -c, -d
- (※ bash로 뜬 거라 마운트 지연으로 1~2줄 옛버전일 수 있음. 진짜 복원점은 깃 `3046aa0`.)

---

## 다음 할 일
1. **마지막 CSS 수정(세로선+공사진행 강조) 화면 확인** — VS Code 저장→리컴파일. 칸마다 세로선, '공사 진행' 파란 막대 보이는지.
2. **코드 커밋/push** (VS Code). 오늘 분량 큼.
3. **공사진행 묶음** 더 강조 원하면: 묶음 안쪽 칸 선 제거(한 덩어리), 톤 조절 등.
4. **pid 표현 규칙 설계** (랜덤→의미 있는 표시, 예 T2-2026-0001. 단 내부 ID는 불변 유지 권고).
5. (장기) **Firebase 확정저장 = 실데이터 올릴 때.** 저장 전 백업 필수(월간보고 설정 '데이터 백업(JSON 저장)'=진짜 복구용. '엑셀 생성'은 보이는 컬럼만이라 보조).

## 막힌 점 / 꼭 기억할 주의
- **표 스타일 = index.css에서.** 인라인 style은 `!important`에 진다. 클래스+높은 specificity로.
- **bash 마운트가 실제 파일보다 지연.** 편집·검증은 파일도구(Read/Edit), 빌드는 VS Code, git은 신뢰 가능.
- **ProjectListScreen.jsx는 큰 파일** — Edit 후 끝부분(`export default ProjectListScreen;`) 절단 안 됐는지 확인 습관.
- 세로 격자선 CSS는 전역이라 **다른 표(월간보고 등)에도 적용**됨(보통 더 깔끔. 이상하면 .progress-modal-table처럼 클래스로 스코프).

## 커밋 메시지 초안 (VS Code에서)
```
fix(list): 엑셀 업로드 audit 반영 — 날짜6자리·헤더기준시트·표 숨김/표현·공사진행 시인성

- toDateInputVal 6자리(YYMMDD) 날짜 파싱
- 헤더 기준 = 열 최다 시트(2026)로 (SM대응 가림 해결)
- 기본숨김 헤더변경시 재적용 + 보임13/숨김22(팀장 지정)
- No.칸 제거·실행번호 숨김·시운전% 너비60·중앙정렬
- index.css: 세로 격자선 복원 + 공사진행 강조 클래스(prog-*)
- 공사진행 12칸 엑셀 수정본 제작(시운전 삭제, 9항목 정렬)
```
