# 2026-07-29 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app
- 주제: **오피스365 Business Standard 30일 평가판 — 가입 + 테스트 ①④ 완료**

## 오늘 한 일

### 1) 평가판 가입 (안전장치 우선)
- Business Standard 1개월 평가판 가입. **도메인은 임시 `neconsyspms.onmicrosoft.com`**
  → 회사 도메인 `neconsys.co.kr`은 건드리지 않음(MX 변경 위험 회피)
- 가입 화면에서 **구독 기간을 1년 → 1개월로 변경** (깜빡 시 손실 연 45만 → 월 4.5만으로 축소)
- 수량 2 (동시편집 테스트용). 평가판 중에는 25개까지 무료
- 가입 직후 **[되풀이 청구 편집] → "만료 시 취소"** 설정 → **2026-08-29 자동 만료, 청구 0원**
  - ⚠ Entra ID Free는 무료 기본 제품이라 손대지 않음
  - ⚠ 갱신을 다시 켜면 수량이 **25개**로 잡힘(월 56만원) — 실도입 시 수량부터 조정
- MFA(Microsoft Authenticator) 등록 필수 — 건너뛸 수 없음
- 테스트 계정 `test@` 생성 + Business Standard 라이선스 할당 (위치=한국 지정 필요)

### 2) 테스트① — 엑셀 업로드 / 수식·계산값 보존 **통과**
- 파일1 사본을 OneDrive 업로드 → 웹 엑셀로 열기 → 셀 편집 → 자동저장 → 다시 내려받아 기계검증
- **결과: 수식 322 / 계산값 6,709 / 시트 28 — 전 셀 대조 불일치 0**
- 7/27 실측(브라우저 라이브러리 재저장 시 계산값 15,558 소실)과 정반대.
  **웹 엑셀(Office for the web) = 정품 엔진이라 NAS+진짜엑셀 저장과 동일하게 안전**
- 재계산도 정상 (D9 입력 → 종속 셀 즉시 반영, 실행취소 정상)

### 3) OneDrive 데스크톱 앱 설치·설정
- 이 PC에 OneDrive 앱이 아예 없어서 microsoft.com에서 설치 (일부 윈도우 이미지에 미포함)
- 최초 실행 시 **개인 MS 계정으로 자동 로그인됨** → 설정 → 계정 → [업무/학교 계정 추가]로 회사 계정 별도 연결
- 동기화 폴더 위치를 **`D:\OneDrive-네콘시스`** 로 지정 (기본 C: 대신)
- "폴더 백업" 화면은 건너뜀 (바탕화면·문서가 통째로 이동하는 것 방지)

### 4) ★ 테스트④ — PMS 자동반영 **통과** (오늘의 핵심)
- OneDrive 동기화 폴더 안에 `PMS진척자료` 하위폴더 생성 → NAS의 파일1·파일2 복사
  (하위폴더로 분리한 이유: 루트의 4월 견본과 이름이 겹쳐 PMS가 엉뚱한 파일을 집을 수 있음)
- PMS → NAS 칩 → **[폴더 지정/변경]** 으로 OneDrive 폴더 지정 → 상태 초록 "폴더 지정됨: PMS진척자료"
- **표시용 NAS 주소·자동반영 규칙 3종은 클라우드 공유, 폴더 허가증만 PC별** → 이 PC만 바뀌고 공용 PC 무영향
- PMS 산출값이 직접 계산값과 일치: **ETOS 75 · PLC 89.6**
- 웹 엑셀에서 `#1 L1 진행현황` L25를 바꾼 뒤 → **PMS 변경감지 팝업 `ETOS 75 → 100`** 확인
  → **[나중에]** 선택하여 실제 장부 오염 없이 종료. 이후 L25 원복 + 전 셀 대조 불일치 0

### 5) NAS vs 오피스365 비교 설명자료 제작
- 초등학생 눈높이 비유(창고 vs 작업대) + 흐름도 + 항목별 정면비교 + 인원별 비용표 + 확인항목 5종
- 아티팩트 `nas-vs-office365-compare` 로 저장 (사이드바에서 재열람 가능)

## ★ 발견한 함정 4가지

1. **웹 엑셀 탭을 열어두면 PC 로컬 파일이 갱신되지 않음** (원인 규명 완료)
   - 편집 세션이 파일을 잠금 → 데스크톱 엑셀 "읽기 전용" 경고, OneDrive 로컬 갱신 보류
   - **탭을 닫으면 1분 이내 반영.** 실험: BZ200=`SYNCTEST_A` 마커로 확정
   - 위험한 이유 = **조용한 실패**. 입력자 화면엔 "자동 저장됨"이라 본인은 완료로 인식
   - → 운영 규칙 필요: **"입력 끝나면 웹 탭 닫기"**
2. **웹에서 `100%` 입력이 텍스트(`'100%`)로 저장됨** — PMS가 평균에서 제외 → 진척률 왜곡 가능.
   자동입력 탓일 수 있어 **손 타이핑 재현 확인이 내일 최우선**
3. PMS **[엑셀로 열기]** 는 NAS 주소 기준이라 OneDrive 폴더에선 실패
4. PMS **"찾은 원본 파일" 목록이 누적**됨 (지정 직후 2개 → 확인 후 4개)

## 판단 근거 / 결정
- 도메인은 임시 onmicrosoft.com 유지 — 30일 테스트에 회사 메일 위험을 걸 이유 없음
- 구독기간 1개월 선택 — 망각 리스크를 12분의 1로 축소
- 폴더는 하위폴더 지정 — 이름 겹침으로 인한 오선택 방지
- PMS 반영은 [나중에] — 테스트 값이 팀 전체 장부·주간실적에 남는 것 방지
- 테스트 순서 변경(팀장님 지시): **폰 테스트를 맨 마지막으로**

## 검증
- 파일1 전 셀 대조 2회(웹저장 후 / 원복 후) — 각각 6,709칸 **불일치 0**
- 파일2 총계 8칸이 CLAUDE.md 기록치와 일치 (94.5 / 93.8 / 21.9 / 17,230 / 3,766)
- PMS 규칙 손검산 = PMS 표시값 일치 (ETOS 75, PLC 89.6)
- 동기화 사슬 실증: 웹 편집 → 클라우드 → PC → PMS 감지

## 다음 할 일
1. **확인A** — 웹에서 `%` 입력이 숫자로 들어가는지 (손 타이핑)
2. 테스트③ 2명 동시편집 (`test@` 계정)
3. 테스트⑤ 버전 복원
4. 정리 — OneDrive 루트 테스트 사본 삭제 (BZ200 `SYNCTEST_A` 흔적)
5. 테스트② 폰·외부 열기 (**맨 마지막**)
6. 마무리 — CLAUDE.md 갱신 + 도입 판단(하이브리드 여부)

※ 이월: 7/28 오후분 커밋·배포, pms 계정 실검증, 직원 안내문, 인증서 26-10-01, 볼륨2 93%
※ 이어서 할 때는 `인수인계_오피스365_진행중.md` 부터 읽을 것
