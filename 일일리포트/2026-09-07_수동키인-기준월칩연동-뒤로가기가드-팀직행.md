# 2026-09-07 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. 기술2·3팀 발주처·업체담당자 = 수동 키인 (9882263)
- **문제**: 발주처·업체담당자 칸은 클릭하면 선택 목록(최대 260px, 넘치면 스크롤)만 떴고, 공사업체만 일반 입력창이 열렸음.
- **수리**: 팀 카드 `일반입력열: ['발주처','업체담당자']` 신설(tech2) — 클릭=바로 입력창, 목록·스크롤 제거. Del 지우기도 허용(canClearCell 게이트).
- 기술3팀 = tech2 스프레드 상속으로 자동 적용 / 기술1팀 = 카드에 없어 불변.
- 검증: Playwright 11/11 (편집창·키인 초안·공사업체 회귀·Del·취소 원복·상속·에러 0).

### 2. 기준월 드롭다운 건수 = 진행현황 칩 연동 (07d080e)
- **문제**: 진행중 칩을 골라도 기준월의 월별 (N)은 전체 기준(예: 6월 (4))이라 안 맞음.
- **수리**: monthCountMap을 statusFilterCol 뒤로 이동 + 칩 필터(chipPass — HOLD·(빈칸) 규칙 동일) 통과 메인 행만 집계. **전체 (N)도 같은 기준**으로 통일(종전 216=하위 포함) → 1~12월+기타=전체 항상 일치.
- 검증: Playwright 10/10 (칩 없음 202 자체일치 · 진행중 29=칩 · 5월(3) 표기=표 행=칩 상호 연동 · 해제 원복).

### 3. 브라우저 뒤로가기(←)도 미저장 초안 확인창 (40ccc11)
- **문제**: 팀 이동·홈 버튼은 guardNav 확인창이 뜨는데, 브라우저 ← 는 App.js popstate가 가드 없이 바로 이전 화면으로.
- **수리**: List가 초안 있을 때 전역 창구 `window.__pmsNavGuard` 등록 → App.js popstate가 먼저 확인, 막히면 같은 안내창 + 현재 화면 기록 재삽입(이동 취소, 연타해도 계속 차단). 초안 없으면 종전대로.
- 검증: Playwright 7/7 (← 확인창+화면 유지·연타 재차단·취소 후 정상 이동).

### 4. ★로그인 = 자기 팀 프로젝트 List 직행 (64c711c)
- **요청**: 기술1/2/3팀 직원 로그인 시 홈(팀 선택)을 건너뛰고 자기 팀 List로.
- **팀 결정 3단계**: ① **사용자 관리 [소속 팀] 신설**(select + 목록 팀 배지, `registeredUsers.team`) ② 미지정이면 이름(extractName)이 **정확히 한 팀** 카드 담당자목록에만 있으면 그 팀 ③ 못 정하면(공용 계정·미배정·**김윤재/김수민=기술1·2 두 팀 명단**) 종전대로 홈.
- 안전장치: 딱 1회(didTeamRouteRef — 홈 버튼으로 나가면 재진입 안 함) · 휴대폰=모바일 입력 우선 · 메인 PC=팀 순환 우선(양보).
- 검증: 로직 시뮬 11/11(App.js 실코드 추출+실카드 명단) + 실기 2/2(공용 계정 홈 유지·에러 0).
- **잔여**: 개인 계정 실제 직행은 팀장님 실기 확인 필요(관리자 → 사용자 관리 → 소속 팀 지정 → 해당 직원 로그인). 김윤재·김수민 책임은 지정 필수.

## 변경한 파일
- `src/components/ProjectListScreen.jsx` — isPlainKeyinCol, monthCountMap 칩 연동, __pmsNavGuard 등록
- `src/teamProfiles/tech2.js` — 일반입력열 신설
- `src/App.js` — popstate 가드(curScrnRef), 직원 팀 직행 effect, extractName import
- `src/components/UserManagementScreen.jsx` — 소속 팀 select·저장·목록 배지
- `src/notices.js` — 09-07 공지 4줄

## 결정·근거
- 일반입력열은 **팀 카드 방식**(고객담당자열 선례) — 기술1팀 무접촉·기술3팀 자동 상속, 화면 코드에 팀 이름 하드코딩 금지 원칙 준수.
- 기준월·칩 상호 연동은 9/4 하단 카운터 연동과 같은 기준(메인 행만) — 숫자 불일치 혼선 제거.
- 뒤로가기 가드는 **차단이 아니라 기록 재삽입** — 브라우저가 이미 되돌린 상태를 다시 얹어 화면·기록 둘 다 유지.
- 팀 직행은 지정(확실) + 이름 자동(편의) 2단 — 두 팀 명단 인원은 오배정 위험이 있어 자동 제외.

## 다음 할 일 / 막힌 점
- **★배포**: 오늘 4커밋 전부 미배포 — `npm run build` → `firebase deploy --only hosting`, 시크릿 탭 확인.
- 배포 후: 사용자 관리에서 직원별 소속 팀 지정(특히 김윤재·김수민) → 개인 계정 직행 실기 확인.
- 이월: 수행번호 일괄 기입(apply_exec_0902 --run 대기)·△4건·#058/#129 중복 정리 / 공용 PC NAS 재지정(010·011) 확인 / 파수꾼 재설치 확인 / 협의 3건(최영환·심광호·김승환) / Firebase Blaze 검토 / 사용설명서 v1.1(발주처 키인·팀 직행 반영 후보).
