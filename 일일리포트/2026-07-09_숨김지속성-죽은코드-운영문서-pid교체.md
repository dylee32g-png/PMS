# 2026-07-09 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. 프로젝트 List 전체 이상 유무 점검
- 핵심 4파일(`ProjectListScreen.jsx`·`projectColumns.js`·`DetailModal.jsx`·`projectListData.js`) 모두 제자리·최신·구문 파싱 OK.
- 코드 결함 없음. 남은 건 **미커밋 상태**뿐(저장 절차, 코드 이상 아님).

### 2. 숨김처리(열 표시/숨기기) 기능 점검 + 지속성 구현
- **기능 존재 확인**: ① 설정 메뉴 '열 표시/숨기기'(체크박스+‘N개 숨김’ 배지+‘모두 표시’) ② 상세팝업 각 항목 파랑/회색 토글. 둘이 같은 `hiddenCols` 상태 공유 → 정상 동작.
- **발견한 약점**: `hiddenCols`가 세션 한정(저장 없음) → 새로고침(F5) 시 초기화. 6/26에 정한 ‘보임13/숨김22’도 자동 적용 안 됨.
- **구현(팀장님 선택=옵션 A: 브라우저 localStorage, 팀별)**:
  - 모듈 헬퍼 `hiddenColsKey/loadHiddenCols/saveHiddenCols`(팀별 키 `pms_list_hiddenCols_{팀}`).
  - 초기값을 저장값에서 로드 + 팀 전환 시 재로드 useEffect + 3개 변경지점(상세팝업 토글·‘모두 표시’·설정 체크박스)에서 저장.
- **검증**: 구문 파싱 OK · 저장/로드 로직 시뮬 5/5(새로고침 유지·팀 독립·빈값 기억) · 삽입 6곳 육안 확인.

### 3. 죽은 코드 정리
- `projectColumns.js`의 미사용 `DEFAULT_HIDDEN_COLS`·`isDefaultHiddenCol`·`isProgHiddenCol`(6/26 ‘전체표시’ 전환 때 버려짐) + 주석 = 총 10줄 제거.
- **검증**: 전체 src 사용처 0건 재확인 후 삭제 · 파싱 OK · 삭제 경계 확인.

### 4. 항목 추가 대응 방안 논의 + 운영 문서 2종(docx) 작성
- 구조 확인: 표의 열 = **엑셀 헤더를 그대로 읽어 자동 생성**(`parseExcelHeaders`, 1~3층 헤더·중복헤더 자동구분). 특수 역할은 **열 이름**으로 자동 판정(`isDateCol/isStatusCol/isAssigneeCol/isClientCol/isCheckCol/isRefCol`…). 헤더는 팀별 독립 저장.
- 결론: 일반 항목 추가는 코드 0(엑셀만), 특수기능은 이름 규칙에 맞으면 자동, 아니면 규칙 1줄.
- 산출물(`D:\pms-app\운영가이드\`):
  - `치트시트_항목이름규칙_v1.0.docx` (1장) — 이름→기능 매핑, 예약어 주의, 3초 요약.
  - `표준절차서_항목추가변경_v1.0.docx` (2장) — 7단계 SOP·역할(RACI)·주의사항·롤백·변경로그 양식.
- LibreOffice 렌더 확인(한글·표·색상 정상).

### 5. pid 번호 규칙 논의 + 충돌 점검 + 발급기 교체
- **현재 방식**: `P-{시각 36진수}-{난수 3자리}`. 무의미 내부 연결 열쇠(실행번호는 빈값·중복 많아 A-3에서 연결키에서 제외됨).
- **방향 확정(옵션 1)**: 무의미 열쇠 유지 + 충돌만 보강. 의미형(팀-연도-순번)은 팀이동·중복 위험으로 A-3 함정 재현이라 배제.
- **스트레스 테스트(현행)**: N=2,198 ×30회 → 30회 전부 중복(총 458건, 최대 26). N=10,000 ×20회 → 20회 전부(총 1,372). 최악(같은 1ms) → ~52건. 즉 대량 발급 시 실제로 중복 발생.
- **실데이터 점검**(`PMS백업_기술2팀_20260709_1132.json`): 월간 367건 전부 고유·빈칸0 / List 1,883건 중 **_pid 중복 2건 발견**
  - `P-mquj359p-vki` = ‘P9 M2 SDMS Partcle 추가 공사’ ↔ ‘P8 AP2 ELA Training 교육용 장비 모니터링’
  - `P-mquj356x-kto` = ‘P10 STEP2 VFU 4대 추가 공사’ ↔ ‘A1 A2 온습도 추가 공사’
  - List↔월간 정상연결(공유 pid) 0건(이 테스트 자료 특성 — 문제 아님).
- **교체**: `utils.js`의 `generatePid` → crypto 기반 랜덤 32자리(hex), 구형/비보안 폴백 포함. 형식 `P-` 접두어 유지(뱃지 표시 불변). pid 형식 의존 코드 없음(전수 grep 확인).
- **검증**: 파싱 OK · 스트레스 재검증 0건(2,198×30 / 10,000×20) · 예시 `P-0a1f415d3ad9457a8c6f88e3b15b7c6f`.

## 변경한 파일
- `src/components/ProjectListScreen.jsx` — 숨김 localStorage 지속성 (백업 `.bak-2026-07-09`)
- `src/components/projectColumns.js` — 죽은 코드 10줄 제거 (백업 `.bak-2026-07-09`)
- `src/utils.js` — `generatePid` crypto 랜덤32 교체 (백업 `utils.js.bak-2026-07-09-pid`)
- (신규) `운영가이드/치트시트_항목이름규칙_v1.0.docx`
- (신규) `운영가이드/표준절차서_항목추가변경_v1.0.docx`

## 결정·근거
- **숨김 지속성 = localStorage(팀별)**: 이 PC 한정·위험 낮음·즉효. 팀 공유가 필요하면 추후 Firebase로 승격 가능.
- **pid = 무의미 열쇠 유지 + crypto 충돌 보강**: pid는 사람이 볼 일 없는 내부 키라 가독성 실익이 적고, 의미형은 팀이동·연도·중복 관리 부담(A-3 함정). 충돌은 실데이터 2건으로 확인 → 교체가 실질 해법.
- **죽은 코드 제거**: 사용처 0 확인 후 안전 제거(기능 불변).

## 다음 할 일 / 막힌 점
- ⚠ **최종 파일 업로드 순서(중요)**: ① VS Code `npm run build` → `firebase deploy`(또는 `npm start`)로 **새 발급기 먼저 반영** → ② 상세팝업 신규 저장 시 ID가 `P-`+32자리인지 1회 확인 → ③ 테스트 데이터 삭제 → ④ 최종 파일 업로드(충돌 0). 순서 틀리면 옛 발급기로 또 중복.
- 실기기 확인 대기: (a) 숨김 F5 유지·팀 독립, (b) 신규 ID = P-32자리.
- **미커밋 다수 → VS Code push 필요**(ProjectListScreen·projectColumns·utils + 운영가이드 docx 2종).
- 기존 잔여(후순위): 나머지 팀 UNC 경로, 증감 표시(월간 영역), 이력기록 4종.
- 기존 2,198건 pid는 불변 유지(어차피 테스트라 삭제 예정).
