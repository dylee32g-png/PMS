# 2026-08-21 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. 1층 백업 체계 검수 완료 + 관리 칸 정리 (전날 이월분)
- [전체 백업 (JSON)] → 셀 수정·행 추가 → [백업 복원] → 원복 확인 = 통과 (기술3팀으로 테스트).
- 관리 칸: 주간보고 연결·저장 버튼 숨김(`{false &&}`), NAS 아이콘 1개만. 미첨부 행 = 회색 아이콘+× (전 사용자 표시, 관리자 클릭=규칙 등록).
- 기술2팀 관리자 칩·기술3팀 담당자 칩 = 직책순(담당 → 팀장). 기술3팀은 카드 `담당자목록` 순서.

### 2. 지난 연도 날짜 "전부 26" 문제 — 원인·전수 조사·수리
- 원인: 엑셀 옛 시트 날짜가 `2/13`처럼 연도 없음 + 웹 표시기가 연도 없는 날짜에 올해(2026)를 붙임.
- 전수 조사(openpyxl): 기술1팀 2016~2025 약 350건 해당. 기술2팀 2026은 무관(YYMMDD 6자리).
- 수리: `displayDate/fmtDate(val, rowYear)` — 그 행의 `_year` 우선(저장값은 원문 유지). 셀 편집 시작값·미변경 판정도 동일.
- 기술2팀 과거 적재(실행 전)에 **진짜 날짜 셀 연도 복원** 추가(970건 대상, 그중 시트 연도와 다른 해 90건 정확히 살림).

### 3. 기술1팀 '관리자' 열 — 원인·제거
- 2026-07-22 기술2팀 기능(담당자 앞 '관리자' 자동 삽입)이 기술1팀 업로드에도 작동 → 발주처 그룹에 생김.
- 수리: `parseExcelHeaders` 게이트(`opts.관리자열`), tech1 카드 `관리자열:false`, 표시 필터(useMemo — 처음에 매 렌더 새 배열로 무한루프 발생 → 수리).

### 4. ★기술1팀 연도별 1:1 재정비 (팀장님 확정: 엑셀 시트 1장 = 웹 연도 1개, 열·값 100% 동일)
- 구조: `meta.byYear[연도] = {headers, colGroups}` — 기준연도 바꾸면 표 열이 그 해 시트 그대로. 번역·비고 덧붙임·관리자 열 없음. 기술2·3팀은 종전(팀 공통 헤더) 그대로.
- 도구: 설정/빈 화면 [연도별 1:1 적재·검증] — 연도마다 [적재](그 해만 교체)·[대조 검증](웹 저장본↔엑셀 전수: 열·행·값)·[지우기]. 같은 해 탭 2개는 합침(열 합집합), 2026은 카드 '한시트만'(2026 (2) 42열).
- 별칭 매핑 `aliasCol`: 옛 열 이름(견적→계약, 상태/현황/진행/작업 현황→작업, 수행 담당→수행…)을 열번역 역추적 — 칩·카드·드롭다운이 전 연도 동작(열 추가 없는 읽기 전용).
- 실엑셀 시뮬: 434건·13개 연도·구조 이상 0. 실전: 전체삭제 → 2026·2025 ✅ 100% 일치(2025: 열 27·행 24·값 648칸) → 나머지 연도 팀장님 진행.
- 백업/복원에 byYear 포함.

### 5. 기술1팀 화면 규칙 (팀장님 지시 다수)
- 드롭다운: 단어 드롭다운은 계약·작업·단계구분·부문만 / 담당·수행 = 담당자식 다중 선택(명단 5명 + 직접 입력, 이름만·`/` 연결) / 나머지 수동 키인. `splitAssigneeCell`에 `/`·`·` 구분자 추가. 담당자 드롭다운에 [직접 입력]·[빈칸으로] 푸터(전 팀).
- 드롭다운 단어 수집 = 기준연도 행만(옛 연도 'LGD'·'생산' 딸려오던 문제).
- 상단 카드: 전체 프로젝트(순번 기준)·준비·진행중·완료(+올해만 N월 완료), 지난 연도도 동일. 값 해석은 글자 그대로(대기·보류 등 미포함 — 팀장님 확정). 홈 카드도 같은 6항목 한 줄(공정률·달성률은 값 없으면 '—').
- 상태 칩: 카드 상태열의 연도별 별칭 칸도 칩(2015 '상태' 글자로만 보이던 문제), 기술1팀은 2단계 보조줄 숨김. 색 = 완료 빨강·진행 초록·그 외 회색(`상태색` 카드).
- 빈칸 = 진한 회색(`#a7aeb9`), 자동 칸은 하늘색 유지. 헤더 '자동' 꼬리표 → 열 이름에 파란 테두리.
- 기본 화면 맞춤(2026): 발주처 담당자까지 화면 100% 폭(말줄임), 손댄 열은 고정. 수동으로 줄인 열은 `…`(col-clip — `max-width:none !important` 예외).
- 수행번호 `YY-NNN`(편집·적재·[수행번호 3자리 정리] 버튼) · 빈칸 [+] = 그 해 최대+1 · 회수 ✕(행 hover, 중간 번호면 구멍 경고, 백로그 기록).
- 홈 팀 설명 문구 교체(1·2팀).

## 변경한 파일
- src/components/ProjectListScreen.jsx (대부분) · projectListData.js(관리자 게이트) · projectColumns.js(splitAssigneeCell) · teamStats.js(당해카드) · App.js(홈 카드·문구) · index.css(col-clip·cell-empty·exec-del) · teamProfiles/tech1.js(카드 다수) · tech3.js(담당자 순서) · notices.js(08-21)

## 결정·근거
- 기술1팀 List = 엑셀 시트별 1:1(열·값 100%), 연도 단위로 적재→대조 검증. 웹 전용 열 추가 금지.
- 2026은 테스트 데이터라 전체삭제 후 재적재 OK(팀장님). 날짜는 원문 저장 + 표시만 연도 보정.
- 카드 값 해석은 글자 그대로(추측 계산 금지).

## 다음 할 일 / 막힌 점
1. **차주 월요일(8/24) = 기술2팀 작업 시작** (변수 많음 — 시간 여유 두고). 과거 연도 적재 실전(1,712건, 날짜 복원 포함)부터 검토.
2. 기술1팀: 남은 연도 1:1 적재 결과(전부 100%?) 확인 · 설정 [수행번호 3자리 정리] 실행 · [월간 마감] 롤오버 실기기.
3. 배포: 8/18~21 전부 미배포 — `npm run build` → `firebase deploy --only hosting` → 시크릿 탭 + 공용/메인 PC Ctrl+F5.
4. 이월: 기술2팀 월간보고 협의(최영환)·기술1팀 협의(심광호: 엑셀 이상값·오타 '당당자'·'왼료처리'·부문 '진행')·VPN 집 접속·직원1 365·UNC 2건.
