# 2026-08-27 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. ★초안(모아서 저장) 시스템 — 메인표 키인 방식 전환 (팀장님 지시)
- 메인표 키인·드롭다운은 이제 **바로 저장되지 않고 노란 칸(임시 편집)** 으로 쌓임 → 헤더 **[저장 N칸]**(초록) 또는 **Ctrl+S**로 행별 1회 저장, **[취소]** = 전부 서버 값으로 복구.
- 안전장치: F5 해도 노란 칸 유지(이 PC localStorage `pms_list_draft_팀`) · 창 닫기 경고 · 팀/홈/월간/백로그 이동 차단(guardNav) · 저장 직전 동시수정 확인창(orig↔서버 대조).
- 즉시 저장 유지 대상: 상세팝업 [저장] · 진행실적 [적용하기] · NAS 자동 반영.
- **키인 중 Firebase 통신 0건 실측 확인**(네트워크 감시). 백로그는 행당 1건으로 묶임.
- 자동 검증 6단계(키인→노란칸→F5 유지→취소 원복→Ctrl+S→서버 값) + 드롭다운(진행현황) 경로 전부 통과.

### 2. ★키인·이동 속도 근본 개선 — 창 렌더링(화면 근처 행만 그리기)
- 원인 심층 규명(Playwright + CPU 프로파일 + 크롬 트레이스): 앱(React) 코드는 몇 ms뿐, **표가 202행×35칸(≈7,000칸)을 항상 전부 그려두는 구조**라 칸 하나만 바뀌어도 브라우저 네이티브(스타일 재계산·배치·Layerize)가 0.5~1.3초.
- 보조 수리 3건: ① [저장 N칸] 깜빡임을 box-shadow(매 프레임 페인트·Layerize 1.9초 실측)→**opacity**(합성기 전용)로 ② 값만 바뀐 커밋의 불필요한 틀고정 재실측 렌더 1회 제거(sortedIds 서명 비교) ③ 노란 칸 = 실제 바뀌는 칸만(변경 없는 자동 동반 칸 제외).
- **창 렌더링 본체**: 처음 1회는 전체를 그려 **열 폭 실측·고정(winPinW — 화면 모습·열 폭 종전과 동일)** → 이후 화면 근처 **±15행만** 그리고 위·아래는 같은 높이 빈 줄(스페이서, 스크롤바 동일). 스크롤은 10행 단위 갱신. 80행 이하 팀은 종전대로 전체.
- 검증: 스크롤 상단·중간·끝·복귀·**배율 80%** 전부 빈 행 없음, 틀고정 겹침 없음, 실행번호 하이라이트 점프는 위치 계산 보강.
- **실측 개선: 칸 열기 462~656→54~120ms, 엔터→닫힘 600~1,273→84~245ms (5~10배).**

### 3. ★일괄 저장 때 장부(팝업) 기록 소실 수리 — "PLC·ETOS·HMI 동기화 안 됨"의 원인
- 재현으로 확정: [저장]이 한 행의 여러 칸을 밀리초 간격으로 연속 장부 쓰기 → 각 쓰기가 **낡은 실시간 구독본을 base**로 삼아 **뒤 쓰기가 앞 쓰기를 통째로 지움**(마지막 Point만 생존 = 통합시운전만 팝업에 남음). 8/19 ETOS 소실 버그의 변종.
- 수리: `ledgerFreshRef` — **직전 15초 내 내가 쓴 장부 최신본을 base로 우선 사용**(공정률 역방향·Point 증분 두 함수 공통) + 공정률 쓰기도 onProgressSaved로 그래프·팝업 즉시 반영.
- 검증: ETOS+HMI 동시 저장 생존 → PLC 50·ETOS 60·HMI 70 3칸 일괄 저장 → 팝업 완전 일치, 진척률 57.5% 손검산 일치.

### 4. 드래그 범위 선택 → Del 일괄 지우기 (엑셀식)
- 마우스로 칸 범위 드래그(파란 하이라이트 — **DOM에만 칠해 재렌더 0**) → Del = 초안으로 일괄 적재(렌더 1회) → [저장]으로 확정.
- 자동 제외: NAS/수식/미적용 잠금 칸 · 드롭다운 칸(진행현황·담당자 등) · 실행번호(하위 s 마커) · 하위 합계 자동 포인트. Esc = 선택 해제.

### 5. 지우기 동기화 + "0 = 지우기" 통일 (팀장님 확정)
- 종전(7/10 설계)은 "빈칸 저장 = 장부 안 건드림"이라 지워도 팝업이 그대로였음(원설계, 드래그와 무관).
- 수리: **메인표에서 지우면(한 칸·드래그 모두) 장부 '이번 주' 기록 삭제** — 지난 주·지난 달 이력은 보존("이번에 넣은 걸 되돌린다").
- **0 입력 = 지우기(빈칸) 통일**: 팝업 규칙(7/10)을 메인표에도 — 대상 = 공정률 7개·Point·**포인트(총점)**. Point 0 키인 시 감소 차단 경고 혼란도 해소.
- 왕복 검증: 키인→저장→팝업 일치 → 드래그 Del→저장→팝업 빈칸·진척률 '—'. 001은 포인트 100, 나머지 빈칸으로 원상 정리.

### 6. 실적 그래프 라벨 겹침 v2
- 공정률 % 라벨과 막대 값 라벨 겹침(예: 50pt 막대+45% 점 — 8/20 고정 오프셋 방식의 빈틈) → **막대 라벨 실좌표를 계산해 그 위로 비켜 세움**, 꼭대기 밖이면 점 아래. DOM 좌표+캡처로 확인.

### 7. (마감 후) 담당자 직책 표기 한글 통일 (팀장님)
- 상단 담당자 칩이 팀 명단의 코드 표기('최영환DD')를 그대로 보여주던 것 → 셀·드롭다운·상세팝업과 같은 변환(`toExcelAssignee`: DD·TL·C → 담당·팀장·책임) 적용. 담당자 관리 화면의 기존 이름 표시도 동일.
- 팀 카드 명단(tech1·tech2 `담당자목록`)과 공용 기본 명단(`ASSIGNEE_LIST`)도 한글로 교체 — 저장 데이터·필터(이름 핵심 비교)는 불변.
- 확인: 기술2팀 칩 '최영환 담당(7) · 김준혁 팀장(7) · 조장현 팀장(39) …', 기술1팀은 이름만 모드 그대로('심광호·염경록…').

### 8. (마감 후) 기술1팀 — 이름만 넣어도 직책 자동 표시 (팀장님)
- 저장값은 이름만(엑셀 원본 그대로) 유지하고 **표시할 때 팀 명단에서 직책을 찾아 붙임**(`asgTitleOf`/`decorateAsg`): 심광호 담당·염경록 팀장·김윤재/양연수/김수민 책임. 여러 명은 '심광호 담당 / 염경록 팀장'. 명단 밖 이름(고객측 등)은 그대로.
- 적용 칸 = 카드 담당자식열 `담당·수행` + **`접수자`**(견적 접수 직원 — 이번에 추가, 드롭다운도 동일). 상단 칩·드롭다운 라벨도 직책 표기(저장 형식은 이름만 그대로).
- 확인: 칩 '심광호 담당(0)·염경록 팀장(2)·…', 셀 '염경록 팀장'·'심광호 담당 / 염경록 팀장', 접수자 칸 포함 이름만 남은 셀 0.

### 9. (마감 후) 기술1팀 순번 3자리 통일 — 설정 [번호 3자리 정리]가 순번을 못 찾던 문제
- 원인: 정리 도구·셀 편집 패딩·자동 +1이 전부 `'번호'` 열만 보고, 기술1팀 번호 열은 `'순번'`(카드 `번호패딩:false`) → "정리할 번호가 없습니다".
- 수리: 팀 번호 열(번호/순번) 공용 판정(`isProjNoCol`·`projNoColOf`)으로 통일 — 정리 도구(대상·저장 칸)·편집 패딩·자동 +1 모두 순번 3자리. 기술1팀 카드 `번호패딩:true`(업로드 때 01→001; 수행번호 26-001은 대시가 있어 무관).
- 확인: 순번 편집 '7'→'007'(초안, 취소 원복). **[번호 3자리 정리]는 관리자 전용이라 팀장님이 실행**(12건 01→001 예상).
- 관찰: 병합 매칭 `a4cNumCol`은 '번호' 우선·없으면 '번호' 포함 열(기술1팀은 '수행번호'가 잡힘) — 기존 동작 그대로 두었음(1:1 적재 도구는 별도 키).

### 10. (8/28 아침) 기술1팀 — 트리거 칸(ETOS 등) 클릭만 하고 나와도 누적·전체·금월이 '0' 노란 칸으로 잡히던 버그 (팀장님 발견)
- 원인: ETOS·PLC·HMI·총물량·금월은 '수식 트리거' 칸이라 편집창을 닫을 때마다 값이 안 바뀌어도 자동 칸을 재계산 → 빈칸 행에서 `'0'` 문자열이 나와 ''→'0' 변경으로 초안(노란 칸)에 올라감. 버그 맞음(의도 아님).
- 수리 2겹: ① 편집 칸 값이 실제로 바뀐 경우에만 수식·진행율% 재계산 ② 수식 결과 0 = 빈칸(8/27 '0=지우기' 통일과 일치 — 빈 행 자동 칸이 0으로 채워지지 않음).
- 확인(자동): 클릭→Enter·클릭→다른 곳 클릭 = 노란 칸 0 / ETOS 40 키인 → 전체·금월 10(노랑), 누적 '—' 유지 / 지우면 0 없이 '—'.
- ⚠ 이미 생긴 0 노란 칸은 저장하지 말고 **[취소]** 로 지울 것(취소 = 서버 값 그대로).

### 11. (8/28 아침) 수행번호 [+] 같은 번호 반복 부여 + 중복 저장 무방비 (팀장님 발견, 기술2팀)
- 원인: 행 렌더가 캐시(MemoRow)라 [+] 버튼의 클릭 처리기가 **초안(노란 칸) 반영 전 옛 행 목록**을 붙잡고 있음 → 저장 전까지 몇 번을 눌러도 최대번호+1이 같은 값(26-003). 저장 단계엔 수행번호 중복 검사가 없어(8/25 중복 차단은 '번호'만) 그대로 저장됨.
- 수리 3겹: ① [+]·최대번호 계산을 최신 목록 참조(`activeRowsRef`, 초안 포함)로 — 연속 클릭 = 004·005… / 혹시 겹치면 다음 번호로 건너뜀 ② 수행번호 **손 키인** 중복 → ⛔ 즉시 거부(초안 안 만듦) ③ **[저장] 직전 최종 검사** — 초안 수행번호가 다른 행(기존·초안)과 겹치면 저장 보류(노란 칸 유지, 어느 행과 겹치는지 표시). 같은 연도 메인 행 기준·하위 제외.
- 확인(자동): [+] 두 번 → 26-004 / 26-005 ✓ · 3행에 26-004 손 키인 → ⛔ '수행번호 중복' ✓ · 취소 원복 ✓.
- ⚠ 이미 저장된 중복(오늘 26-003 여러 행)은 데이터라 코드가 못 고침 — 겹친 행에서 ✕ 회수 후 [+]로 다시 받기.
- ⚠ 사고: 첫 패치 스크립트가 줄바꿈 이스케이프를 잘못 넣어 컴파일 에러 1회 → 즉시 git 원복 후 재패치(팀장님 '정상복귀' 확인).

### 12. (8/28 아침) "수행번호 넣으면 행이 아래로 바뀜" = 헤더 정렬(의도) + 정렬 해제 3단계 추가
- 진단: 수행번호 헤더가 눌려 **내림차순 정렬**(헤더 파랑+⌄)이 걸린 상태에서 [+]로 번호를 받으니 그 행이 위로 이동 — 정렬은 의도된 기능. 다만 종전엔 오름↔내림만 오가고 **끄는 방법이 없었음**(연도 바꿔야 초기화).
- 수리: 헤더 클릭 3단계 = 오름차순 → 내림차순 → **해제(기본 순서)**. 데이터·저장 무관(화면 정렬만).

### 13. (8/28 아침) 수행번호 손 키인 금지 — [+] 옆 빈 곳 클릭 시 편집창 열려 '011' 사고 (팀장님)
- 원인: 수행번호 칸이 일반 칸처럼 클릭=편집창이라 [+] 버튼을 비껴 누르면 아무 값이나 키인 가능(YY-NNN 형식 아니면 정형화도 안 됨).
- 수리: 당해 연도 메인 행의 수행번호 칸 = **클릭해도 편집창 안 열림 + 안내 토스트**("[+] = 자동 부여, ✕ = 회수"). 상세팝업·추가팝업도 같은 칸 🔒 잠금 표시(`DetailModal execLockedCols`). 지난 연도 행·하위 행은 종전대로(수동).
- 확인(자동): [+] 옆 빈 곳 클릭 → 편집창 안 열림·안내 ✓ / [+] 클릭 → 26-001 정상 ✓ / 취소 원복 ✓.
- ⚠ 이미 초안에 든 '011'은 [취소]로 되돌리기.

### 14. (8/28 아침) 정렬 상태 표시 + [해제] — "번호 순서가 바뀐다" 재질문
- 같은 원인(수행번호 헤더 정렬 ↓ 켜짐)이 다시 혼란을 줌 → 하단 상태줄에 **`· 정렬: 수행번호 ↓ 내림차순 [해제]`** 표시, [해제] 1클릭 = 기본 순서(번호 순). 정렬이 꺼져 있으면 아무것도 안 보임.

### 15. (8/28) 행 더블클릭 → 상세 보기/수정 팝업 제거 (팀장님: 셀 편집하다 더블클릭에 팝업이 튀어 혼란)
- 메인표 행 onDoubleClick 삭제. 상세 팝업은 **우클릭 메뉴 [상세 보기/수정]** 으로만. (헤더 더블클릭=틀고정 토글, 손잡이 더블클릭=열 폭 자동은 그대로)

### 16. (8/28) '행 선택됨 — 프로젝트 추가 시 초기값으로 복사' 문구 확인 (팀장님 질문)
- 뜻: 행을 클릭해 선택(보라 문구)한 뒤 [프로젝트 추가]를 누르면 그 행의 값(공장·발주처·날짜·내용 등)을 새 프로젝트의 초기값으로 복사(번호는 새 번호). 같은 현장 비슷한 프로젝트를 빨리 등록하려는 기능.
- 동작 확인(자동): 선택 → 추가 팝업 '선택한 행의 데이터를 초기값으로 복사했습니다' + 값 복사 ✓. **수리: 수행번호는 복사 제외**(그대로 따라오면 중복).
- 발견: 셀 클릭은 전파가 막혀 행이 선택되지 않고, **편집창이 열린 칸을 다시 클릭할 때만** 선택됨(우연 경로) → 우클릭 메뉴 '이 행 복사해서 추가'로 옮기는 게 명확(팀장님 결정 대기).

### 17. (8/28) 프로젝트 추가 팝업 버튼 [저장] → [추가] (팀장님: 수정 팝업의 '저장'과 혼동)
- DetailModal 푸터: 추가 모드=[취소]·[추가], 수정 모드=[닫기]·[저장]. 동작은 동일(문구만).

## 변경한 파일
- `src/components/ProjectListScreen.jsx` — 초안 시스템·창 렌더링·장부 캐시·드래그 삭제·지우기/0 통일 (CRLF, python 부분치환)
- `src/App.js` — 실적 그래프 공정률 라벨 겹침 회피 v2
- `src/index.css` — cell-draft(노란 칸)·pmsDraftPulse(opacity)·cell-selr(드래그 선택)·sel-noselect
- `src/teamProfiles/tech1.js`·`tech2.js`·`src/components/projectColumns.js` — 담당자 명단 한글 직책
- `src/components/DetailModal.jsx` — 수행번호 잠금 표시(execLockedCols, 8/28)
- `src/notices.js` — 08-27 공지(모아서 저장·창 렌더링·드래그 Del·지우기 동기화·0=지우기)

## 결정·근거
- 초안 방식 = 팝업 [저장]과 동일 철학(팀장님 지시) — 키인 중 서버 통신 0.
- 창 렌더링 채택 근거 = 40행 실험에서 4~8배 개선 실측. 열 폭은 전체 실측 고정이라 한 줄 펼침 불변 기준 유지.
- 지우기 = '이번 주만 되돌림'(이력 보존) — 과거 주간 기록 손상 방지.
- 0 = 지우기 — 팝업 기존 규칙(7/10) 확장 + 계산상 빈칸≈0이라 정보 손실 없음.

## 다음 할 일 / 막힌 점
- **배포**(오늘분 — 아래 명령) 후 실기기·공지 확인. 공용 PC 파수꾼 `설치.bat` 관리자 재실행(8/25 이월).
- Firebase 사용량 확인·Blaze 검토(전무님 보고) — 오후 저장 지연의 뿌리.
- 이월: 첫 로그인 ~18초 조사 / 기술3팀 실전 적재 / 협의 3건(최영환·심광호·김승환) / NAS 하위 장부 주차 전환 관찰(월요일).
- 관찰: 창 렌더링은 배포 후 직원 실사용에서 스크롤·필터 위화감 여부 확인(문제 시 WIN_MIN·OVERSCAN 조정).
