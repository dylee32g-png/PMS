# 2026-09-02 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. 정식 운영 개시 전수 점검 (읽기 전용, 오전)
- 3팀 894행 + 장부 332건 전수 검산: 기술1팀 수식 4종 12행 100% 일치 · 기술3팀 진행율% 50행 일치 · 번호중복/고아하위/유령sub 0건.
- E2E 3부(테스트 행 생성→검증→완전 삭제): 셀 키인 수식, 팝업 이월(빈 주 직전값 placeholder), 적용 수식, Point 왕복(50→진행율 25%→지우기), 화면 이동(홈↔List·팀 전환·뒤로가기), 페이지 에러 0.
- 발견: F1(기술2팀 010·011 시운전 장부 미심김) / F2(하위 진행율% 빈칸 — 무해) / F3(고아 장부 — 무해) / F4(기술1팀 옛 연도 번호 중복 — 원본 사양).

### 2. F1 수리 — [진행실적 심기] 하위(공종) 체제 지원 (커밋 `ed7f8be`, 배포됨)
- **원인 확정**(실데이터 프로브): 하위 있는 행(010·011)은 팝업·그래프가 부모 장부의 `sub_○_intCommissioning`만 읽는데, 심기는 ①부모 최상위 키 ②하위 자체 pid 문서에 써서 어디서도 안 보였음. 팀장님이 9/1 저녁·9/2 아침 두 번 실행한 흔적 확인(일반 행 ~17건은 정상 심김).
- 수리: 하위 있는 행 = 부모 `sub_○` 키로 심기(NAS '하위별 통합'과 동일 규칙) + 최상위 잔재 자동 삭제 + 유령 칸 정리 + 하위 행 건너뜀 + 수식 팀(기술1팀) 시운전 심기 제외 + 진행율% 식 paRecalc 통일(소수 1자리). 목 시뮬 15/15.
- **일회성 복구**(팀장님 터미널 실행): 010 sub_0=350 · 011 sub_0~7 합 4,534(=Point 일치) 기입, 최상위 잔재 2건 삭제, 하위 pid 무의미 장부 14건 삭제 — 재프로브 검산 통과. 월간마감 8월본 3팀 전부 존재 확인(추가 조치 불요).

### 3. 공용 PC 'NAS 자동 확인 경고' 원인 규명 (파수꾼 무죄)
- 빨간 토스트 "실패 2건" = 기술2팀 NAS 규칙 행이 010·011 둘뿐이라 정확히 그 둘. 파수꾼(트레이)은 창 숨김·재시작만 담당 — 무관.
- 진짜 원인: 폴더 허가증이 PC에 **행 _id별로** 저장되는데(ProjectListScreen 2380행) 8/24 재적재로 _id가 전부 바뀌어 무효(7/28 확인 사양 "행ID 바뀌면 허가증 무용"). 8/24 숙제 "공용PC 허가증 재설정"이 이제 나타난 것.
- 조치 안내: 공용 PC에서 010·011 NAS 칩 → [폴더 지정/변경](방문할 때마다 허용) + 010은 [공용 폴더]=011 폴더 → [지금 확인]. **결과 미보고 — 확인 대기.**

### 4. 날짜 칸 엑셀식 개편 — 전 팀 공통 (커밋 `784898c`, ★미배포)
- 팀장님 요청: 날짜 칸을 Del로 지우고, 키보드로 치고, 달력으로도 넣게 — 엑셀처럼.
- 구현: `parseDateFlex` 신설(projectColumns — 260126·26/01/26·26-1-5·2026-1-5·20260126 인식, 13월/32일 거부) / 메인표 날짜 편집기 = 텍스트 입력(클릭 시 전체선택→Del 한 번에 지움)+달력 버튼(숨은 date input showPicker) / 커밋 시 정규화·검증(못 읽는 값 거부 안내 — displayDate가 해석 불가 시 원본을 돌려주는 함정 수리, 실기 테스트가 잡은 진짜 버그) / 상세·추가 팝업 날짜 칸 동일 / 드래그+Del 일괄 지우기는 기존 지원 확인. 저장값은 표준 YYYY-MM-DD 그대로.
- 검증: 해석기 단위 21/21 + Playwright 실기 13/13(타이핑 2형식·Del·거부·드래그 2칸·취소 원복·팝업·페이지 에러 0 — 전부 초안 단계라 실데이터 무변경).
- ⚠사고·복구: 패치 스크립트의 이모지 서러게이트 이스케이프(`📅`)가 UTF-8 인코딩 불가 → 쓰기 도중 실패로 PLS 0바이트 절단 → git checkout 즉시 복구(커밋 직후라 무손실). **교훈: python 패치는 인코딩을 먼저 끝내고 바이트로 쓰기(v2 방식) + 이모지는 `\U0001F4C5`.**

### 5. 수행번호 매칭 — 월간보고 엑셀 ↔ 웹 기술2팀 List
- 원본: `월간보고_기술2팀_2026년9월_260831_파주.xlsx` '기술2팀 진행현황' 탭 B열 26-* = 58건. **구조 발견: 상단 322~387행=실행 구간(26-1~44) / 하단 409~422행=예상 구간(번호 재사용 14건)** → 예상 구간은 기입 보류 권고(웹은 연도당 수행번호 중복 금지).
- 매칭(정규화+바이그램 유사도+포함 가점): ◉동일 54 · △확인 필요 4 · ✕0. 0.9대 판정 근거 = 웹 이름 괄호에 엑셀 이름 병기 실측(#110 등).
- △ 4건: 26-028(자동제어 vs #132 건설 L1) · 26-031(후보 #119, 재이용=RUW) · 26-037(후보 #058 조주현 DMS — ★웹 #058·#129 동명 중복 의심) · 26-038(웹에 없어 보임).
- 산출물: `outputs/수행번호매칭_기술2팀_260902.xlsx`(58행 전체 — 3자리 변환·판정·유사도·웹 번호·비고) + 일괄 기입 스크립트(scratchpad `apply_exec_0902.js`, ◉+실행만·드라이런 40건 확인·중복/기존값 가드·변경이력 기록). **수행번호 손 키인은 8/28 잠금이라 직접 타이핑 불가 → 스크립트 기입 권장. 팀장님 실행 대기.**

## 변경한 파일
- `src/components/ProjectListScreen.jsx` — 심기 하위 체제(ed7f8be) + 날짜 편집기·가드(784898c)
- `src/components/projectColumns.js` — parseDateFlex 신설
- `src/components/DetailModal.jsx` — 팝업 날짜 칸 타이핑+달력
- `src/notices.js` — 09-02 공지
- `outputs/수행번호매칭_기술2팀_260902.xlsx` — 신규(비추적)
- scratchpad — probe/repair/match/apply 스크립트 다수(자격증명은 CLI 인자로만)

## 결정·근거
- 심기의 하위 행 자체 장부 문서는 코드에서 자동 삭제하지 않음(실행번호 s 전환 행의 과거 이력 오삭제 위험) — 이번 14건만 프로브로 안전 확인 후 스크립트 삭제.
- 날짜 저장 형식은 기존 표준(YYYY-MM-DD) 유지, 비표준 기존 값은 안 건드림(엑셀 원본 우선 원칙) — 편집·달력 입력 시에만 정규화.
- 수행번호는 실행 구간만 기입(예상 구간은 수행 전 + 번호 중복으로 웹 규칙과 충돌).

## 다음 할 일 / 막힌 점
1. **날짜 칸 개편 배포** (784898c — 미배포. `npm run build` → `firebase deploy --only hosting`)
2. **수행번호 일괄 기입**: 팀장님 파일 검토 → apply_exec_0902.js 드라이런→`--run` → 검산. △4건 결정(26-038은 신규 등록 검토) + 웹 #058·#129 동명 중복 정리
3. **공용 PC NAS 폴더 재지정 결과 확인** (010·011 — 빨간 경고 소멸 여부, 다음 30분 주기)
4. 공용 PC 트레이 아이콘(9/1 건) 재부팅 후 확인 결과 미보고
5. 이월: 고아 장부 청소(선택) · Firebase Blaze 검토 · 협의 3건(최영환·심광호·김승환) · NAS 9월 주차 이중 합산 실전 관찰
