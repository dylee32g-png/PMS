# 2026-06-18 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. #8 이전값 표시 (완료)
- 진행실적 팝업의 공정 항목 빈 칸에 **직전 값(이월)을 흐린 회색 placeholder**로 표시. 시운전은 제외(useMax 게이트), 참고용이라 저장 안 되고 입력 시작하면 사라짐.
- 구현: `ProgressModal.jsx` renderRow에 `prevVal/showPrev`(cumByKey 재사용) + `::placeholder` CSS.
- 검증: 자동 6/6 + 실기기 26-1(화면작성·기준정보·PLC 회색 힌트, 시운전 없음) + 손검산 진척률 21.2% 일치.

### 2. B-2 프로젝트 수정 팝업 상세 설명 (완료, 코드 변경 없음)
- 여는 법 4가지, 좌(정보 입력)·우(TOTAL POINTS·월별 실적) 구조, **적용 항목(#7)·TOTAL POINTS(시운전 만점=분모)·월별 실적(진행실적 '적용하기'와 같은 장부 monthlyData)** 연계, % 값은 기준월별 저장 구조임을 그림으로 설명.

### 3. B-3 시운전 정산 엑셀 검증 + 만점(point) 정합성 (완료)
- 정산 엑셀(누적/전월/금월)은 통일 계산기 `getPointsTriple` 경유 → 정상.
- 발견: '만점' 칸은 `point`를 읽는데 수정 팝업은 `totalCommissioningPoints`만 저장 → 어긋남(옛값/빈값).
- 수정(읽기쪽): 정산엑셀 point 열·`ProgressModal.totalPt`를 `totalCommissioningPoints || point`로 통일. 자동 6/6.

### 4. TOTAL POINTS 저장 반영 버그 (완료)
- 증상: 수정 팝업에서 TOTAL POINTS(11→100) 저장해도 표 POINT 열이 안 바뀜(옛 `point` 읽음).
- 원인: `handleSubmit`이 `totalCommissioningPoints`만 저장, `point` 미동기화. (인라인 POINT 편집은 둘 다 갱신했음)
- 수정(쓰기쪽): `handleSubmit`에서 `point = totalCommissioningPoints` 동기화. 자동 5/5 + 실기기(50 반영 확인).

### 5. sub → '하위' 라벨 전면 변경 (완료, 표시만)
- 'sub'는 ① 하위 행에 저장된 상태값 ② 실행번호 's' 마커 ③ 특별 접기/펼치기 칩. **저장값('sub'·'s')은 시스템 판별 기준이라 그대로 두고 표시만 '하위'로.**
- 월간 표(App.js): 필터 칩·진행현황 뱃지·실행번호 마커·프로젝트명 앞 마커. 진행현황 드롭다운(인라인·수정팝업) 옵션. 엑셀 내보내기 마커 `└ 하위`. List 화면(ProjectListScreen) 진행현황 칩.

### 6. 부수 버그 2건 (완료)
- **프로젝트명 인라인 수정이 화면에 반영 안 됨**: 프로젝트명 셀이 원본(`p.project`)을 그려서 임시 수정분이 안 보임 → `dp.project`로 변경(메인·하위 공통 버그였음).
- **하위 행 실행번호 칸**: 표시는 '하위'인데 편집창엔 's' 노출 + 편집하면 하위가 '메인'으로 풀릴 위험 → **하위 행 실행번호 읽기전용** 처리(`isEditableField`에 `!(p.isSub && col.key==='execNo')` 추가).

### 7. D 주간보고 라인 (보류)
- 사용 여부 미정 → 내부회의 후 결정. **건들지 말 것.** 개요·흐름(입력→뷰어→요약, 주간→월간 연계)만 그림으로 설명. 메모: `memory/weekly-report-line-on-hold.md`.

### 8. A-4c 자동연결 1단계 = 드라이런 (구현 완료, 실기기 대기)
- 설계(기준문서 A4): 엑셀 업로드 '전체 삭제→새로쓰기'를 **매칭 병합**으로 — 연도+번호 → Project명 매칭, `_pid`·실행번호·이력 보존, 엑셀에 없는 행은 삭제 대신 표시, 확정 전 "갱신 N·신규 M·없음 K" 미리보기.
- 1단계 구현: `computeMergePreview`(읽기전용 순수함수) + List 설정메뉴 **'병합 미리보기(드라이런)'** 버튼(업로드 pending 상태에서만). 자동 12/12.
- **엑셀 파일이 없어 실기기 테스트는 나중에** → 결과 보고 **2단계(실제 보존 병합 저장)** 진행(저장 직전 JSON 백업).

## 변경한 파일
- `src/App.js`: 정산엑셀 point fallback / `handleSubmit` point=tCP 동기화 / sub→하위(필터칩·뱃지·실행번호·프로젝트명 마커·드롭다운 2곳·엑셀 마커 2곳) / 프로젝트명 `dp.project` / 하위 실행번호 읽기전용.
- `src/components/ProgressModal.jsx`: #8 이전값 placeholder + CSS / `totalPt` fallback.
- `src/components/ProjectListScreen.jsx`: A-4c `computeMergePreview` + 병합 미리보기 버튼·핸들러 / List 칩 sub→하위.
- `CLAUDE.md`: 진행상황 갱신.
- 백업: `.bak-2026-06-18`(3파일), `App.js.bak-2026-06-18b`. 새 메모: `weekly-report-line-on-hold.md`.

## 결정·근거
- **이전값 = placeholder(항상 흐리게)**: 계산의 '이월'을 시각화, 입력 시작 시 사라짐, 위험 0.
- **만점 정합성 = 읽기 fallback + 쓰기 동기화 둘 다**: `point`/`tCP` 항상 일치 + 기존 어긋난 데이터도 커버.
- **sub→하위 = 표시만**: 저장값은 판별 기준이라 유지(데이터·필터·접기 안전).
- **하위 실행번호 읽기전용**: 's'는 마커 — 편집 시 하위가 메인으로 풀려 사고.
- **A-4c 드라이런 먼저**: 실데이터로 매칭률 확인 후 실제 병합(가장 안전).

## 검증 (규칙8: 자동 / 실기기 / 손검산)
- #8: 자동 6/6 · 실기기 26-1 · 손검산 21.2% ✓
- B-3 만점 읽기: 자동 6/6 ✓ (실기기 대기)
- TOTAL POINTS 쓰기 동기화: 자동 5/5 · 실기기 50 반영 ✓
- A-4c 드라이런: 자동 12/12 ✓ (엑셀 실기기 대기)
- sub→하위·표시 버그: 표시 변경이라 실기기 눈확인

## 다음 할 일 / 막힌 점
- **A-4c**: 엑셀 확보 후 '병합 미리보기' 실기기 → 결과 보고 **2단계(실제 보존 병합 저장, 저장 직전 JSON 백업)** → A-4d 어긋남 표시.
- **미커밋 전부 — VS Code에서 커밋·push 필요.**
- 실기기 미확인: B-3 정산엑셀 만점 / 프로젝트명 dp 수정 / 하위 실행번호 읽기전용 / sub→하위 전 화면 / 엑셀 마커.
- **D(주간보고)는 내부회의 전까지 보류.**
- 정리 후보: 엑셀 `selfEnabled` 유령코드 / 엑셀 정산 '상태' 텍스트 'sub'(마커는 하위로 됨) / List 필터의 'sub' 값.
