# 2026-07-06 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app
- (오늘 분량 매우 큼 — 코드 수정 patch 20+회, 전부 Python 부분치환·babel parse 점검·백업 다수)

## 오늘 한 일

### 1. 시운전 검증 + '만점'→'총점' 용어 통일
- 진행실적 [자체]/[통합] 택1 → 적용 → 메인표: 자체 200÷200=100%, 통합 150÷200=75% 정상(손검산 일치).
- 화면의 '만점'·'총 Point' 표기를 전부 **'총점'**으로 통일(주석은 유지). 그래프 지표·기준선·초과경고 전부.

### 2. 총점 초과 빨강 경고 4중
- ① 메인표 포인트 칸(실적>총점) ② 진행실적 팝업 시운전 합계 ③ 팝업 하단 경고 배너 ④ 적용 확인 패널. 글자 빨강+옅은배경+⚠. 판정=실적>총점.
- 포인트 칸 열 넘침 → 너비 60→88, 배지 패딩 콤팩트.

### 3. 진행실적 초기화(백지) 기능
- List 우클릭 '진행실적 초기화(백지)': progressRecords weekly + 메인표 반영필드 + 연결 월간보고 monthlyData·monthlyPoints 비움. window.confirm 확인창.
- 관리자 게이팅 예정(코드 TODO + 메모리).

### 4. 메뉴 정리
- 프로젝트 연결(실행번호+ID)·주간보고 적용(버튼+기간+파일선택 패널) → `{false && ...}`로 숨김(기능 코드 보관).
- 디버그 모드·진행실적 초기화 → 로그인 등급 구현 후 '관리자만' 표시(지금은 유지, 코드 TODO 주석 + 메모리 pms-admin-gating).

### 5. 실적 그래프 — List에서 보기 + 인포그래픽 재디자인
- 우클릭 그래프: 실행번호 매칭 → **pid 기반 graphObj**로. List 진행실적만으로 그래프.
- ★**progressRecordsMap 로드 조건 정정**: `currentMode==='pms'`만 → `pms || projectList`(App.js 1272). List 화면에서 시운전이 F5하면 사라지던 **근본 원인**.
- **공정률 추이**: 월간보고 monthlyData 없어도 진행실적(progressRecords)에서 월별 공정률 계산 `getRecordMonthlyProgress`(App.js). 공정률=PLC·ETOS·HMI(누적%)+자체·통합시운전%(누적÷총점) 5개 평균. **유닛테스트 4/4, 7월 62% 손검산 일치.**
- 재디자인: 달성률 도넛·총점 대비 진행 바·지표카드(누적/잔여/공정률금월)·Y축 총점 포함(빨간 기준선 보임)·공정률 점+값(2개월+면 선)·색/범례 정리.

### 6. List 뼈대 재정비
- 기준월(월선택기)+그달만/이전전체 = **"모양만"**(monthMode 필터 미적용, 기준월은 연도만 사용) → **연도 선택 드롭다운**으로 단순화.
- **진행현황·담당자 마스터 관리 1·2단계**(핵심):
  - 1단계: 하드코딩(projectColumns의 DEFAULT_STATUS_OPTIONS·ASSIGNEE_LIST·STATUS_CHIP_COLORS) → teamSettings 폴백 지역변수(STATUS_OPTIONS·ASSIGNEES·STATUS_COLORS)로. 사용처 전부 교체.
  - ★**구조 정정**: teamSettings는 **팀별** `{status(객체배열 [{label,color,...}])·manager·factory·progressItems·...}`. **List 상태는 월간보고 status와 별개** → List 전용 `teamSettings[팀].listStatus`(문자열 배열)로 폴백. (팀장님 확정: List 전용 별도)
  - 2단계 관리 UI: 설정 메뉴 '진행현황 관리' 모달(이름 추가/수정/삭제/↑↓순서). 저장=`teamSettings[팀].listStatus` setDoc(merge) → onSnapshot이 App.js 자동 갱신.
  - **일괄 반영(이름 변경 시 기존 데이터 상태값도 함께 변경) 시도 → ★confirm 건수 불안정 버그(눌러도 152→88 변동) → 롤백**(patch21 제거, 백업 .bak-2026-07-06v). **현재는 목록 이름표만 저장(데이터 안 건드림).**

## 변경한 파일 (전부 미커밋)
- **src/App.js** — 총점 통일, 그래프 재디자인(도넛·진행바·지표·Y축·공정률선), progressRecordsMap 로드조건, getRecordMonthlyProgress, teamSettings props 전달.
- **src/components/ProjectListScreen.jsx** — 총점 경고·포인트칸너비, 진행실적 초기화, 메뉴 숨김·관리자게이팅 주석, 그래프 pid, 연도선택기, 마스터관리 1·2단계, 일괄반영 롤백.
- **src/components/ProgressModal.jsx** — 총점 경고(합계배지·하단배너·적용패널), 총점 통일, 주간보고 적용 숨김.
- 백업: 세 파일 각각 `.bak-2026-07-06*`(a~v 다수). 메모리 신규 3: pms-edit-tool-truncation-trap, no-wrap-up-suggestions, pms-admin-gating.

## 결정·근거
- 공정률 5개 평균(SIMPLE 4개 제외, 6/17·6/29 기준) — 유닛+손검산 62% 검증.
- List 상태 ≠ 월간보고 상태 → List 전용 listStatus 별도(팀장님 확정).
- 일괄 반영은 위험(건수 불안정) → 롤백, 데이터 안전 우선.
- 디버그·초기화 = 삭제 말고 관리자 게이팅(재사용+안전).
- 대형 파일은 Edit 절단 반복 → 전부 Python 부분치환+parse+wc 점검으로 회피.

## 다음 할 일 / 막힌 점
1. **★막힌 점: 일괄 반영 confirm 건수 불안정(152→88) 원인 미규명.** 단 **데이터는 안전**(팀장님 확인: 칩 기존과 동일 152, 확인 3번 눌러도 실제 데이터 안 바뀜 — 실제 setDoc 미반영 추정).
2. **2단계 관리 UI 재개** — 이름 변경 데이터 반영을 안전하게 재설계(activeRows 스냅샷 고정·저장 1회·건수 확정 후 반영) **또는** 이름 변경은 막고 추가/삭제/순서만 허용. → **팀장님 결정 필요.**
3. 상태 색 선택 UI, 삭제 시 '사용 중 N건' 경고(안전장치).
4. 3단계 담당자 관리(`teamSettings[팀].listManager`, ASSIGNEE 정규화 통합).
5. 미사용 코드 정리(viewMonth·monthMode), 백업 파일(.bak a~v) 정리.
6. **VS Code에서 커밋·push**(오늘 분량 큼).

## 현재 상태 (내일 바로 이어가기)
- **데이터·칩 정상(안전).** 관리 UI(설정 '진행현황 관리')는 살아있음 — 목록 이름표만 저장(일괄반영 롤백됨).
- 총점 경고·그래프 재디자인·초기화·연도선택기·메뉴 숨김 다 반영됨(팀장님 화면 확인 완료).
- 시작하면: ① 이 리포트 읽고 ② "일괄 반영 재설계 vs 이름변경 막기" 팀장님 결정 → 2단계 마무리 ③ 3단계 담당자.
