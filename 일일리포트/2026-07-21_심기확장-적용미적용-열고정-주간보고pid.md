# 2026-07-21 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일
1. **전체 삭제 → 최종 엑셀 확정 저장** (기술2팀 163행, 포인트·누적 체계로 재정비)
2. **진행실적 심기 신설·확장** (설정 메뉴) — 공정률 7개('90%' 표기도 인식) → 현재 주차 장부 심기 / **누적(진행 pt) → 통합시운전 장부 + 통합시운전% 자동(누적÷포인트)**, 값 다를 때만 갱신. 그래프 즉시 반영. 재실행 안전(같은 주차 덮어쓰기)
3. **포인트=총점·누적=진행분 확정 반영** — 포인트 칸 '0/126'→'126'(엑셀 값만, 클릭=값 편집, 하위 Σ합·잠금 유지), 실적그래프 pt=자체+통합 합산(통합만 쓰는 팀도 막대·달성률 표시)
4. **프로젝트별 적용/미적용** — 상세팝업 공정/시운전 9칸 오른쪽 토글이 '적용/미적용'(별도 섹션 안 만듦=이중관리 방지). 미적용 → 메인표 회색 ×+편집 잠금, 진행실적 팝업 행 숨김+진척률 재계산, 실적그래프 공정률 제외. 저장 = _naItems(끔)·_naOn(기본off 예외 켬)
5. **비엑셀 5항목 전 프로젝트 기본 미적용** — 도면입수·I/O Map·화면작성·기준정보·자체시운전. 기본값 규칙이라 기존·신규·엑셀 재업로드 전부 자동. 상세팝업에 토글 전용 줄(주간 키인 전용 표시). 모바일도 동일 적용(projectListData.naProgressItemsOf 공용 헬퍼 신설)
6. **총점 읽기 폴백** — '포인트' 없으면 '총' (effTotalPt·subPtByParent·ProgressModal totalPt 3곳)
7. **열 고정** — 기본=Project까지(번호·발주처·Project), 헤더 더블클릭 토글+이 PC 팀별 기억, **헤더 우클릭 메뉴 '이 열까지 고정/해제'**, 오프셋 실측(첫 데이터행 offsetLeft·첫칸 기준 정규화·±2px 허용·의존성 기반 재측정), 경계 그림자(.frz-edge). 열 스냅(scroll-snap)은 도입 후 팀장님 불편 의견으로 제거(자유 스크롤 복원)
8. **주간보고 연결 키 실행번호→pid** — '실행번호 미등록' 문구·검사 제거, 모든 행 바로 연결 가능
9. **NAS 진척 엑셀 2종 구조 분석** — 첨부 업로드 시 자동 공정률 반영 준비

## 변경한 파일
- src/components/ProjectListScreen.jsx — 심기 확장·포인트 칸·적용/미적용·열 고정·우클릭 메뉴·wKey pid
- src/components/DetailModal.jsx — 적용/미적용 토글·비엑셀 5항목 토글 줄·기본 미적용
- src/components/ProgressModal.jsx — totalPt '총' 폴백
- src/components/projectListData.js — naProgressItemsOf 공용 헬퍼
- src/components/MobileInputScreen.jsx — progressItems 연결
- src/App.js — 그래프 pt 자체+통합 합산
- src/index.css — .frz-edge 경계 그림자
- 백업: 각 파일 .bak-2026-07-21* 다수

## 결정·근거
- 포인트=총점, 누적=현재 진행 갯수, 통합시운전%=누적÷포인트 자동 (팀장님 확정, 데이터 정합 검증 일치)
- 적용/미적용은 상세팝업 기존 토글 자리 하나로 (별도 섹션=이중관리 반려)
- 비엑셀 5항목 기본 미적용 — 안 쓰는 항목의 진척률 희석 방지 (098 검산 6.3%→16.7%)
- 열 스냅 제거 — 스크롤은 멈춘 자리 유지가 우선
- 검증: 자동 시뮬 다수(심기 14/14·기본미적용 11/11·계산제외 12/12·왕복 13/13 등) + babel 전건 + 실기기
- ⚠사고 2건 복구: 샌드박스 미러 낡음→심기 코드 소실→백업 재통합(이후 디바이스 직접 패치로 전환) / 실측 무한루프→의존성+허용오차 2중 장치

## 다음 할 일 / 막힌 점
- ★ (내일) NAS 엑셀 매핑 4건 결정: PLC 값(블록 공정률 85.4/76.6 vs PLC Program 97/78) · ETOS 출처(전용 열 없음) · HMI 값(화면개발 94.5% vs 시운전 93.8%) · 통합시운전 21.9% 확인 → 확정 후 '첨부 업로드→미리보기→자동 반영' 구현
- L1 파일 = P9(010)·P10(011) 두 프로젝트 포함 → 한 파일-두 행 연결 규칙 설계 필요
- 커밋·push 필요(분량 큼) + build/deploy (오후분 localhost만, 마지막 배포 13:50)
- 시운전 값 엑셀 원본 오류(구 090 등) 정리 여부 확인
