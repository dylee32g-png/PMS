# 2026-09-09 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일
1. **웹 백업 체계 설명 → 2겹 체계 확정·구현**
   - 현행 실태: [전체 백업]/[백업 복원](8/20)·위험 작업 직전 자동 JSON(행만)·다운로드 폴더 한정·정기 백업 없음·Firebase 자체 백업 없음(Spark).
   - ① 주간 수동 루틴 문서화 `운영가이드/운영루틴_데이터백업_v1.0.md` (월요일 1클릭 → NAS `PMS백업\수동`).
   - ② **메인 PC 자동 전체 백업** 신설(ProjectListScreen): 관리자 메뉴 [자동 백업 폴더 지정 (이 PC · 매일)]/[지금 백업 → 폴더]/[자동 백업 끄기]. 매일 설정 시각(기본 06시, 파수꾼 05:30 재시작 뒤) 이후 **하루 1회**, 3팀(LIST_TEAMS) 전체 백업(PMS-FULL-1, 복원 호환)을 IndexedDB 폴더 허가증으로 NAS 폴더에 직접 저장. 1분 심장박동+벽시계·부팅 늦어도 그날 따라잡기·실패 시 1시간 간격 재시도·90일 지난 자기 파일만 정리·실행에 isAdmin 가드 없음(7/27 원칙)·상태를 클라우드 `pmsBackupStatus/팀`에 기록 → 하단 상태줄 `● 자동 백업 · 9/9 06:00 ✓`(초록/주황 26h/빨강). 시뮬 39/39(실코드 추출).
   - **[전체 백업 (JSON)] = 3팀 한 번에**(종전 화면 팀 1개 → 파일 3개, buildFullBackupFor 재사용). 복원은 팀 단위 유지.
2. **파수꾼 v2** (`운영가이드/메인PC파수꾼`): 원인=9/1 트레이가 창을 SW_HIDE로 숨긴 채 두고 설치.bat는 '프로세스 살아있으면 무반응'·로그온 트레이 유예 0분 → "설치해도 아무 반응 없음". 상태보기 [5] 트레이 판정이 자기 자신을 세는 버그. 수정: 설치=옛 트레이 종료→창 없으면 띄우고 숨겨졌으면 즉시 표시→먹통이면 전용 창만 재시작→트레이 상주 / **숨김 기준=마지막 키보드·마우스 조작 후 3분(GetLastInputInfo)** / 상태보기 [0]에서 창 즉시 표시+뮤텍스 판정 / guard.bat 트레이 자가복구(뮤텍스) / 바로가기=`pms_show_hidden.vbs`(창 보이기). 배치 괄호 함정 4곳 정리(원본에도 1곳 있었음). 인코딩 원본 동일(bat/vbs CP949·ps1 UTF-8 BOM). 검증 13/13(구문·ShowOnly·IdleMs·상태보기 드라이런·guard 자가복구·중복방지·종료).
3. **★보안 사고 — 팀장 노트북 채굴 악성코드 발견·조치** (별도 보고서 `일일리포트/2026-09-09_보안사고_채굴악성코드_노트북.md`). 파수꾼 테스트 중 프로세스 목록에서 발견(cmd.exe 위장 XMRig, 부모 svchost 위장, 65.21.237.96 연결, 2024-04 감염 추정). 부활 경로=가짜 예약작업 `\Microsoft\Windows\Google\GoogleUpdateTask`(NirCmd → powershell 복사·Defender 예외·구글드라이브/Gist 원격 스크립트 실행). 승인 후 작업 삭제·프로세스 종료·파일 격리(`C:\Quarantine_20260909`)·hosts 차단, 5분 관찰 재발 없음. Defender 예외 레지스트리는 보호 키라 제거 실패(IT). ⚠Bash 경유 조치 스크립트는 자동모드 분류기가 차단 → PowerShell 도구로 실행.
4. **인증 메일 한국어화**: 원인=`auth.languageCode` 미지정 → 영어 기본 템플릿. App.js `auth.languageCode='ko'` 1줄(콘솔 불필요). REST로 ko 템플릿 테스트 메일 1통 팀장님 메일함에 발송. 보낸 사람 이름·회사 도메인 발신은 콘솔(관리자)+DNS 필요. 개인 비밀번호 로그인 전환은 제안만(팀장님: 메일 방식 유지).
5. **로그인 항상 유지**: 원인=체크박스 한 번 끄면 브라우저가 '0' 기억→이후 세션 저장→재부팅 시 풀림 + 링크가 다른 브라우저로 열리면 원래 창 미로그인. 수정: `ALWAYS_STAY_LOGGED_IN=true`(App.js·LoginScreen, 3경로+링크 탭 항상 local), 체크박스→안내문, 발송 후 화면에 **링크 붙여넣기 → [이 창에서 로그인]**(sessionStorage 표식으로 '창 닫기' 화면 생략), 링크 탭에 **[이 창에서 계속]**. PC 환경 원인 체크리스트(종료 시 데이터 삭제·정리 프로그램·다른 브라우저/프로필·공용 PC 전용 프로필) 안내.
6. 공지 09-09(백업 체계·한국어 메일·로그인 유지·붙여넣기 로그인). 배포 1회(팀장님, 자동백업·파수꾼·한국어·로그인유지 포함 / [전체 백업 3팀]은 배포 후 수정이라 **미배포**).

## 변경한 파일
- `src/components/ProjectListScreen.jsx` (+자동 백업 전체, 전체 백업 3팀), `src/components/projectListData.js` (backupStatusRef), `src/App.js` (languageCode ko·항상 유지·붙여넣기·이 창에서 계속), `src/components/LoginScreen.jsx`, `src/notices.js`
- `운영가이드/메인PC파수꾼/` PMS_메인PC_설치.bat·상태보기.bat·pms_guard.bat·pms_tray.ps1·mk_shortcut.vbs·**pms_show_hidden.vbs(신규)**
- `운영가이드/운영루틴_데이터백업_v1.0.md`(신규), `일일리포트/2026-09-09_보안사고_채굴악성코드_노트북.md`(신규)
- 백업 `.bak-2026-09-09`(App.js·LoginScreen·ProjectListScreen·projectListData, gitignore 대상)

## 결정·근거
- 백업 2겹(수동 주간 + 자동 매일 NAS): 코드 0으로 오늘부터 가능한 수동 + 사람이 안 눌러도 되는 자동. 자동은 파수꾼 인프라(메인 PC·1분 심장박동·IndexedDB 허가증) 재사용.
- 자동 백업 실행에 관리자 가드 없음(플래그가 PC 단위, 공용 계정 상주 원칙). 상태를 클라우드에 기록해 9/2 NAS 멈춤을 6일 몰랐던 일 재발 방지.
- 파수꾼 숨김 = 고정 시간이 아니라 유휴 시간: 작업 중 숨김 방지 + 직원이 작업표시줄 창을 만지는 문제 해결(팀장님 요구).
- 인증 메일 방식 유지(팀장님). 한국어화는 코드 1줄로 즉시, 발신 도메인은 관리자·DNS.
- 악성코드: 삭제 대신 격리(IT 분석), 사람 승인 후 조치, 증거(XML·해시·IOC) 보고서화.

## 다음 할 일 / 막힌 점
1. **배포**: [전체 백업 3팀 한 번에] 미배포 → build+deploy. 커밋·push는 오늘 마감에 완료.
2. **공용 PC 방문 1회**: ①악성코드 점검 한 줄 명령 ②파수꾼 v2 설치(`설치.bat` 관리자) ③010·011 NAS 폴더 재지정(9/2부터 멈춤) ④[자동 백업 폴더 지정] → NAS `PMS백업\자동` ⑤공용 계정 재로그인·3분 유휴 숨김 확인.
3. **보안**: IT 보고(보고서+격리 폴더)·V3 정밀검사·비밀번호 변경(메일·구글/Firebase·GitHub·NAS necon-kr26·VPN)·다른 PC 점검·Defender 예외 정리(IT)·OS 재설치 검토.
4. 인증 메일 보낸 사람 이름·회사 도메인 발신 → 관리자(전무님) 요청.
5. 로그인 유지: 배포 후 "풀리는 PC"에서 체크리스트(브라우저 종료 시 삭제·정리 프로그램·다른 브라우저/프로필).
6. 이월: 사용자 관리 소속 팀 지정(김윤재·김수민)·수행번호 △4건·협의 3건·Firebase Blaze.
7. 메모: 작업 폴더에 `AGENTS.md`(CLAUDE.md 사본, 13:58 생성, 출처 불명)가 미추적으로 생김 — 커밋 제외, 팀장님 확인 필요.
