# 2026-08-26 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. Point 양방향 동기화 실기기 테스트 3단계 통과
- ① 001 팝업 [적용하기] → Point 185·진행율 92.5% ② Point 200 키인 → 팝업 이번 주 +15 ③ Point 100 키인 → ⛔ 감소 차단 경고 (캡처 확인)
- 적용 범위 확인: NAS 파일이 없는(건설공사 아닌) 메인 행만 — NAS 잠금·하위(sub) 장부는 제외 (팀장님 "맞지?" → 맞음)

### 2. ★ 메인표 키인 느림 — 구조 분석·원인 규명 (하루 종일)
- 초반: 팀장님 콘솔 캡처 8장 판독 → 클릭 자체(4ms)·브라우저 배치(10ms)는 무죄, "JSX 구성 1,128ms"처럼 **같은 코드가 30배 느려지는 현상** 발견 → 환경(개발 서버 2중 렌더 + 크롬 'Claude 디버깅' 세션) 의심
- **완성본(`npm run build` → localhost:5000)에서 재측정**: 클릭 렌더 24ms → 코드 병목 대부분은 오전 수리(비제어 입력창·행 메모·스냅샷 재사용·낙관적 저장)로 해소된 상태
- ★ 캡처 요청 중단(팀장님 지적) → **Playwright(자동화 크롬)로 직접 재현** 체계 구축: 공용 계정 로그인 → 기술2팀 List → hover 프레임 측정 → PLC 클릭·키인·Enter → 로그 수집 → 값 복구 (`scratchpad/pw/run_speed.js`, `set_cell.js`)
- **원인 ① Firebase 무료 할당량(하루 한도) 소진 — 확정**: 서버에 직접 질의(REST) 결과 15:59까지 `429 Quota exceeded` → **16:00:37부터 정상(403)**. 한국시간 16시 = 미국 태평양 자정(초기화). 저장 확정 3.8초·가끔 멈춤·hover 잔상(다시 칠하기 지연)의 뿌리. 서비스 장애 아님(상태 페이지 확인)
- **원인 ②** 저장 직후 열 폭 4px 흔들림 → 틀고정 재실측 → **188행 전체 재렌더 433ms + 강제 배치 478ms** (완성본 재현 로그로 입증)
- **원인 ③** 편집 입력창(고유폭 ~150px·테두리 4면·패딩 축소)이 열 폭을 바꿔 **표 전체 재배치 ~300ms**(클릭 때·닫을 때)
- **원인 ④** 행 저장 = 행 전체+무한히 쌓이는 `_changeHistory` 업로드 / 장부 서버 재읽기(왕복 +1) / App.js 장부 구독 전체 재풀기
- **원인 ⑤** hover 색 애니메이션(30칸×0.15초) / 회색 빈칸 배경 = 영향 없음(팀장님 질문 답)
- 헤드리스 벤치(188×30 표): 초기 배치 280ms, 칸 1개 변경 재배치 11ms → "칸 하나 고치면 전체 재배치"가 아니라 **열 폭 재분배**가 있을 때만 전체 재배치

### 3. 수리 (전부 완성본 빌드 + 자동 재현으로 검증)
- 편집 칸 = 보통 칸과 같은 상자(폭·패딩·테두리) + 안쪽 그림자 강조, 입력창 폭 = 클릭 시 칸 내용 폭 고정(`editWRef`) → 열 재배치 0
- 틀고정 오프셋 = React 상태(`frzMeasured`) → **표 CSS 변수(`--frz-i`)** 로 직접 기록(`frzVarsRef`) → 재실측이 행 재렌더를 못 일으킴 / 값 변경 시 실측은 0.4초 뒤(강제 배치 0) / 구조 변경만 즉시
- 기본맞춤: 자연 폭 캐시(`natCacheRef`, 창 폭 바뀔 때 1패스 해제 재렌더 생략) + 창 폭 캐시(`fitWRef`, 저장 때 DOM 측정 0회) + 결과 동일 시 재렌더 생략
- 행 저장 = **바뀐 칸만 전송**(`setDoc merge`) + 변경이력 300건 상한(`HIST_MAX`) / 장부 읽기 = App.js `progressRecordsMap` 실시간 구독본 우선(prop 추가, 없을 때만 getDoc)
- App.js 장부 구독 증분 갱신(docChanges, 첫 스냅샷은 전체) / 표 행·칸 `transition-colors` 제거
- 영구 캐시(IndexedDB) 스위치 `USE_PERSISTENT_CACHE` 신설 — 메모리 캐시 실험 결과 저장 후 처리량 비슷 + 새로고침마다 전량 재다운로드(읽기 할당량 소모)라 **영구 유지(true)**
- 계측 코드(LoAF·3단 분리·gsig 원인·세분 마커·App/구독 로그) 전부 제거, 8/24 `addLog` 도착 시간만 유지
- 결과(완성본): 클릭 렌더 20~50ms · Enter→닫기 3~12ms · 저장 후 전체 재렌더 0회 · 고정 열 오프셋 검증(0/44/96/130/182/250px)
- 테스트로 바뀐 001 PLC 값(71~73%) → **70%로 복구 확인**

## 변경한 파일
- `src/components/ProjectListScreen.jsx` (편집 칸·틀고정 CSS 변수·기본맞춤 캐시·merge 저장·이력 상한·장부 구독본·transition 제거·계측 제거)
- `src/App.js` (progressRecordsMap prop 전달·장부 구독 증분) · `src/firebase.js` (영구 캐시 스위치) · `src/notices.js` (08-26)
- scratchpad: `pw/run_speed.js`(재현)·`pw/set_cell.js`(값 복구)·`bench_table.html`(표 배치 벤치)·`patch_pls_0826g/k.py`·`cleanup_speed_logs.py`

## 결정·근거
- 캡처 요청 방식 폐기 → 자동화 브라우저 재현(팀장님 지적). 공용 계정(일반 권한)으로만 사용, 파일에 미기록
- 영구 캐시 유지(할당량·재진입 속도), 저장 시점 규칙(Enter/칸 밖 클릭=저장, Esc=취소) 유지 — "창 전환 시 취소"로 바꾸려면 1줄

## 다음 할 일 / 막힌 점
1. **Firebase 할당량**(팀장님): 콘솔 → Firestore → 사용량으로 오늘 읽기/쓰기 확인 → 관리자(전무님)께 **Blaze(종량제) 전환** 요청 검토(이 규모 월 수천 원). 매일 오후 느려지면 이게 원인
2. **배포**(8/25 오후분 + 8/26 전부 미배포) — 제 PC firebase 인증 있음, 팀장님 "배포해" 지시 시 실행 → 공용 PC 파수꾼 `설치.bat` 재실행(새벽 재시작)
3. 첫 로그인 후 홈 표시 ~18초("접근 권한 확인 중") — 새 브라우저 기준, 원인 조사
4. 전체 재렌더 1회 ≈ 1초(188행×30칸, 완성본) — 칩/정렬/열 너비 변경 때 체감 → 셀 계산 캐시로 줄이기(후순위) / List 진입 시 `weeklyLinks` 도착으로 전체 재렌더 1회
5. 기술3팀 실전 적재 / 협의 3건 / NAS 하위 장부 주차 넘어갈 때 합계 관찰(월요일)
