# 2026-07-22 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일
1. **NAS 진척자료 자동 연결 완성 (011 실전 적용)** — 첨부(복사본) 방식 폐기, NAS 원본 직접 읽기.
   - 규칙·경로 = 프로젝트 행 `_extSync` (클라우드 공유, 모든 PC 동일) / 폴더 읽기 허가증 = PC별 브라우저(IndexedDB)
   - List 열 때 자동 확인(팀별 1회) → 변경 감지 미리보기 → [모두 반영] = 메인표(도장·이력·백로그) + 주간 장부(현재 주차) + 그래프 즉시
   - 최신 파일 규칙: 이름 조각 포함(공백·대소문자 무시) → 파일명 마지막 6자리 날짜(YYMMDD) → 수정시각. 하위폴더 3층 탐색, Backup·백업·~$ 제외
2. **파일1** (`01 진행현황`, 시트 `#1 L1 진행현황`): PLC = (L5+M5+L14+M14)÷4 = 89.6 / ETOS = L24:M25 평균 = 75 (ETOS 판넬 2행 — PLC와 겹침 유지: 전체 vs 부분 지표). 규칙 셀 범위 입력(F24:M25) 지원
3. **파일2** (`진척자료`, 시트 `진척률요약(Main)`) — 하위 공종표 규칙 (프리셋 버튼 1클릭):
   - 공종 8행(공조~SCADA) 하위 자동 생성/갱신 — 이름열(C)로 행을 찾아 행 밀림에 안전. 6항목 %(도면H·IO M·화면R·기준Z·자체AH·통합AP) + 포인트(AG) + 누적(AN), 기본 미적용 5항목 자동 켬
   - 부모(011): 도면입수·I/O Map·화면작성·기준정보 = 총계행 % + **HMI = 기준정보생성 총계(94.5)** + 누적(3,766) → 통합% = 누적÷Σ총점 = 21.9 자동 + 포인트 = Σ하위(17,230, 기존 규칙)
   - 부모 팝업 '하위별 통합' 자동 기록 (자체는 그래프 자체+통합 합산 규칙과 충돌해 보류 — 반영 시 115% 왜곡)
4. **잠금 체계**: NAS 칸 전 경로(메인표 셀·상세팝업·진행실적팝업·모바일) + [적용하기]·심기가 NAS 칸 안 덮게 보호. 하위 행 진행실적 = 부모 팝업으로 통일
5. **원본 열기**: ms-excel 프로토콜 = 오피스 보안 원천 차단 확인 → 폐기. 공식 방법 = 파일별 [경로 복사](따옴표 자동) → Win+R. '이 PC용 주소(Z:)' 우선(밑줄 호스트명 이름해석 실패 PC 대응)
6. **관리자**: 업로드 시 담당자 앞에 열 자동 보장, 담당자식 드롭다운(셀·팝업·하위 상속), 상단 칩 = 실제 값만 건수순 자동 나열(표기 변형은 이름 핵심으로 묶음). 기존 92건 = 실데이터 확인(김준혁 팀장 45·최영환 담당 18·조장현 팀장 16, 7/9 백업 검증)
7. **실적 그래프 색 통일**: 포인트 계열=파랑(도넛·계산식·누적 카드), 공정률=초록(카드·선). 반영 완료창 요약형(8줄+외 N건)
8. **최종 검증**: 전체 삭제 → 최초 엑셀 재업로드 → NAS 재설정 → 반영, 백지 상태에서 2회 전건 통과

## 변경한 파일
- src/components/ProjectListScreen.jsx — NAS 연결 전체(버튼·모달·검사/반영·잠금·자동확인)·관리자 필터·요약창
- src/components/projectListData.js — 계산 엔진(단일값·범위·공종표)·최신파일 선택·잠금 키·관리자 열 자동 보장
- src/components/ProgressModal.jsx — lockedItems 키인 잠금(🔒 NAS)
- src/components/DetailModal.jsx — NAS 칸 보기전용·관리자 select
- src/components/MobileInputScreen.jsx — 잠금 연결
- src/components/projectColumns.js — isManagerCol
- src/App.js — 그래프 지표 카드 색 통일
- 백업: 각 파일 .bak-2026-07-22

## 결정·근거 (전부 팀장님 확정)
- NAS 원본 직접 + 폴더 지정(파일 아님) — 새 날짜 파일 자동 대응, 이중관리 제거
- ETOS/PLC 겹침 유지 — 전체 vs 부분 지표, 엑셀 드래그 검산 가능성 유지
- HMI = 파일2 기준정보생성 총계 / 저장 소수 1자리 / 모든 입력 경로 잠금
- 부모 누적 = 파일2 총계(3,766) → 통합% 21.9 자동 (어제 '21.9% 확인' 잔여 해결)
- 수치 검산: 달성률 22%=3766÷17230 / 그래프 공정률 70%=(89.6+75+94.5+21.9)÷4 / 팝업 진척률 83.1%=적용 8항목 평균
- 검증: 시뮬(엔진 20/20·범위 6/6·공종표 12/12·디바이스 8/8) + babel 전건 + 실기기

## 다음 할 일 / 막힌 점
- **커밋·push (7/21분+오늘분 누적, 분량 매우 큼) + build/deploy** — 배포해야 타 PC·폰에서 사용 가능
- 파일1 규칙 = 셀 주소 고정(L5·M5·L14·M14·L24:M25) — 엑셀에 판넬 행 추가/삭제 시 규칙 셀 수정 필요 (잦아지면 자동 추적 업그레이드)
- 월간보고 쪽 진행실적 팝업 잠금 미적용(List만) — 추후 연결
- 팝업 진척률(적용 항목 평균) ↔ 그래프 공정률(핵심 공정 평균) 통일 여부 — 결정 대기 (7/16 잔여)
- 하위 자체시운전 부모 팝업 반영 — 그래프 합산 규칙 논의 후
- 운영 루틴: 월요일 아침 담당 PC에서 List 열기 → 변경 감지 시 [모두 반영]
