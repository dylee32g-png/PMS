# 2026-06-09 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app
- 비고: pms-app 인수인계 첫날 (이날부터 담당)

## 오늘 한 일
- Cowork 초기 설정: 플러그인 3종(operations·productivity·product-management) 설치, `D:\pms-app` 폴더 연결.
- pms-app 전체 구조·동작 흐름 파악:
  - React + Firebase 웹앱(TechTeam PMS v6.9.x), 팀별 주간·월간 공정현황 보고 시스템.
  - 핵심은 전부 `src` 안. 대장 파일 `App.js`(~7100줄)가 화면 전환(`currentMode`) 담당.
  - 동작 흐름: 로그인 → 팀 선택 → 메뉴 고르기 → 공정현황 입력·수정 → Firebase 저장.
- 작업 루틴 7개 규칙 확정 및 `CLAUDE.md`에 저장(기억 파일 생성).
- 일일리포트 폴더 및 양식(`일일리포트/_양식.md`) 생성.
- 미리보기(`npm start`) 실행: 포트 3000을 다른 앱(run-pick) 서버가 점유 → 기존 프로세스 종료 후 재실행, `localhost:3000` 정상 동작 확인.
- 깃허브 연동: 비공개 저장소 `TipPick07/pms-app` 생성, 최초 커밋(`78784f1`, 72개 파일) push 완료.

## 변경한 파일
- `CLAUDE.md` — 신규 생성(역할·프로젝트 구조·용어·작업 루틴 규칙·진행 상황).
- `일일리포트/_양식.md` — 신규 생성(일일 리포트 양식).
- `일일리포트/2026-06-09_Cowork초기설정-깃허브연동.md` — 본 리포트.
- (앱 코드 변경 없음 — 오늘은 분석·환경 설정 위주.)

## 결정·근거
- 플러그인은 **operations 우선** — `status-report`·`process-doc`가 공정현황 보고 업무와 직결. productivity·product-management는 보조.
- 깃허브 **비공개(Private)** 선택 — 회사 내부 업무·결재 자료 보호. 필요 시 추후 공개 전환 가능.
- 일일리포트 폴더는 **한글명** 사용, 단계별 답변은 **작업·학습에만** 적용(단순 질문은 즉답)으로 효율 확보.
- 실제 push는 **인증된 VS Code 쪽**에서 수행 — 권한 없는 Cowork에서 시도하면 실패만 반복하므로 역할 분담.

## 다음 할 일 / 막힌 점
- (선택) 불필요해 보이는 파일 `src/import tkinter as tk.ini` 정리.
- 특정 화면(예: `WeeklyInputScreen` 주간 입력)의 동작을 자세히 분석.
- 막힌 점: 없음. 깃허브 연동까지 정상 완료.
