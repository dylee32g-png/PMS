# 2026-08-11 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일

### 1. 작업 규칙 (영구)
- **규칙 5-1 신설**: "오늘 작업 종료" 전까지 무조건 다음 작업 계속, 종료 제안·추측성 질문 금지 (CLAUDE.md + 메모리 파일).

### 2. 기술1팀 팀 프로파일 완성 (3단계)
- 팀장님 확정: 공정률 계산식 없음(직접 키인) / 담당자 칩='수행' 칸 / 상태 칩='계약' 칸 / UNC 미정 유지.
- 담당자 5명: 심광호DD·염경록TL·김윤재C·양연수C·김수민C (김윤재C·김수민C는 기술2팀 지원중 → 양쪽 명단).
- 파서 카드: 한시트만·헤더 3행째(0-based 2)·헤더 2층·필수열 ['순번','공사명'] AND·번호패딩 없음 → 집계 4행 자동 제외, 12행 정확.
- 기술1팀 엑셀 확정 저장 완료(12행). `기준문서/항목정의서_기술1팀_v1.0.md` 실측 보정(43열·자기검산).
- teamProfiles 체계 가동: LIST_TEAMS·generic 폴백·UNC 카드화(projectColumns가 카드 참조).

### 3. 전면 디자인 개편 (벤치마크 기반)
- getdesign.md 분석 + monday.com/리니어 실사용 구경(팀장님) + 갤러리 3곳(SaaS Interface·SaaSFrame·Nicelydone) 조사.
- 비교 시안 HTML 2종 제작(`outputs/`): 스타일 A(현재)/B(monday풍)/C(리니어풍) + 기능 D(KPI탭)/E(그룹소계)/F(타임라인). **D안 승인·구현, F안·보고모드 후속 후보, E 보류.**
- 홈 랜딩 개편: 파스텔 팀 카드·[열기 ›] 행·클릭 영역 구분·주간보고 3종 숨김(`{false&&}` — 삭제 아님)·Software팀 '공사 중' 안내.
- List 헤더: [🏠]+팀 탭(switchTeam — 엑셀 미저장 상태 이동 차단)·버튼 위계 정리·[전체 저장] 숨김(원개발자 의도 불명이라 보존).
- 표 시안: 글자 12.5px·시운전/공정률 칸 막대+굵은 숫자(pctCell, 100%=초록)·웜 잉크 #37352f·행선 --line-soft·tabular-nums.
- 백로그 화면 전면 통일(BacklogScreen — 스타일 값만 50줄 교체, 로직 불변 검증).

### 4. KPI 대시보드 2단 (표시 전용 — 저장·엑셀 무접촉)
- **List 상단 카드 4장**(kpiData): 모수=칩 필터와 동일(monthFilteredRows·하위/삭제 제외). 공정률=PLC·ETOS·HMI·통합 평균(그래프와 동일 4항목)·포인트=Σ누적÷Σ총점(effTotalPt 하위합 규칙)·N월 완료=**공사완료 날짜 기준 명시**.
- **4장 항상 표시**: 없는 데이터=회색 '—' + 사유 캡션("항목 없음" vs "아직 입력 없음") — 0%는 오해 소지라 배제(팀장님 위임 후 추천 채택).
- **홈 팀 카드 미니 대시보드**(`src/components/teamStats.js` 신규): 같은 규칙·올해(_year) 기준·세션 캐시. 확대판=진행중 큰 숫자(30px)+도넛 68px 2개(공정률·포인트)+근거 캡션(전체 N행 중 하위·삭제 제외 / 값 있는 N건 평균 / 누적÷총점).
- 검증: 실소스 추출 시뮬 11/11(kpiData)·9/9(teamStats)·6/6(기술1팀 시나리오)·근거식 PASS + 실기기(130건=197−14−53 칩과 일치).

### 5. List 진입 속도 3단 개선
- ① `firebase.js` 영구 로컬 캐시(initializeFirestore + persistentLocalCache/multipleTabManager, 실패 시 기존 폴백).
- ② 전체 화면 '처리 중' 가림막 제거 → 표 안 스피너(fbLoaded), isLoading 초기 false.
- ③ 세션 메모리 캐시(_memRowsCache/_memMetaCache) — 재진입·팀 탭 전환 0초, 서버 최신값 자동 교체.
- ④ 빈 상태 게이트 — 데이터 도착 전 '엑셀 업로드 안내' 번쩍임 제거.
- 규모 전망: 재진입은 데이터량 무관 즉시, 최초 로드만 비례(수천 행 되면 연도별 분리 — 지금은 불필요).

### 6. 틀고정 잘림 근본 수리 (크롬 헤드리스 실증)
- 원인: 고정 열 오프셋의 예비 계산(getW 기본폭 누적: 번호22px…)이 실제 내용맞춤 폭보다 좁아 스크롤 시 이웃 고정 열이 덮음.
- 실험 입증: 재현 HTML — 실측=잘림 0 / 폴백=정확히 22px 조각(캡처와 일치).
- 수리: 실측을 colSpan 행 스킵하고 데이터 행에서만 + 폴백 폐기(실측 전 찰나는 고정 미적용).
- 재발 이슈: VS Code 파일 워처가 수정 놓침(8/10 동일) → touch 강제 재컴파일 + '새 코드 표식 문구'로 확인 체계.

### 7. 공지사항 시스템
- `src/notices.js`(원본 — 배포하면 자동 반영, DB·관리화면 없음)+홈 공지 줄(최신 1건)+전체 목록 팝업+안 본 공지 빨간 점(localStorage). 과거 공지 누적 열람 가능.
- 오늘 자 공지 작성 완료. **마무리 루틴에 '공지 추가' 포함.**

### 8. 기타
- 모바일: 헤더 flex-wrap(글자 넘침 수리)·제목 말줄임. 속도는 DevTools 영향 설명, 실폰=MobileInputScreen 자동 진입 안내.
- 홈 세로 스크롤 제거(여백 축소, FHD 한 화면).
- 배포 완료(빌드+hosting, 팀장님 실행). 공용 PC 팀순환 점검=오늘 변경과 충돌 없음, **기술1팀은 NAS 규칙 0이라 순환 목록 제외 권고**(기술1팀 List 설정→[이 PC를 메인 PC로 지정] 토글=해제, 관리자 계정 필요).
- `기준문서/메인대시보드_구상메모_v0.1.md` — 월말 보고 대시보드 아이디어(지표 후보·배치 A/B/C·논의 안건 4개, **나중에 팀장님과 결정**).

## 변경한 파일
- 신규: `src/teamProfiles/`(index·tech1·tech2·tech3), `src/components/teamStats.js`, `src/notices.js`, `기준문서/항목정의서_기술1팀_v1.0.md`, `기준문서/메인대시보드_구상메모_v0.1.md`, `outputs/디자인시안_*_20260811.html` 2종
- 수정: `src/App.js`, `src/components/ProjectListScreen.jsx`, `src/components/BacklogScreen.jsx`, `src/components/projectColumns.js`, `src/components/projectListData.js`, `src/firebase.js`, `src/index.css`, `CLAUDE.md`
- 백업: `.bak-2026-08-11` a~s 시리즈 (정리는 추후)

## 결정·근거
- 없는 데이터 '—' 표기(0% 배제): 0%는 "측정했더니 0" 오해 → 간부 보고 정확성.
- 공지=코드 파일 방식: 배포와 함께 반영·위험 0·Claude가 마무리 루틴에서 작성.
- 홈 미니 대시보드=List KPI와 동일 규칙 공유: 화면 간 숫자 불일치 원천 차단.
- E안(그룹 소계) 보류: '한 줄 펼침' 불변 기준·정렬·필터와 충돌 위험 > 효과(D안과 중복).

## 다음 할 일 / 막힌 점
1. 직원 화면 반응 수렴 (배포 완료 상태) · 공용 PC 기술1팀 순환 제외 실행(관리자 계정)
2. 디자인 후속 후보: F안(공사기간 타임라인) · 보고 모드(B요소 스위치) · 월말 대시보드(구상메모 안건 4개 — 팀장님과 결정)
3. 기술1팀 UNC prefix(협의) · [엑셀 반영] 실전 왕복 테스트 · 기술3팀 회신 대기
4. 8/10 잔여: 직원1 PC 365 재테스트 · VPN 문서 Word 확인 · 노트북 재부팅 검증
5. 백업 파일(.bak-2026-08-11 a~s) 정리
