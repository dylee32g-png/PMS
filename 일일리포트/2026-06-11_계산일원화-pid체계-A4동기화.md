# 2026-06-11 일일 작업 리포트
- 작업자: 염경록 팀장 / 프로젝트: pms-app

## 오늘 한 일
1. **시운전 포인트 계산 일원화 (어제 미해결 버그 해결)**
   - 전수검사로 원인 확정: 표의 누적 칸만 장부B(progressRecords)를 보고, 금월 키인은 장부A(monthlyData)에 저장되는 엇갈림
   - 통일 계산기 신설(`getPointsTriple` — 달마다 A→레거시→B 한 규칙), 표·내보내기·그래프·셀편집 전부 일원화
   - 자체/통합 병기 표시(`monthlyData.intPoints` 신설), 수정팝업 월별 리스트→장부A 직결, 이월 씨앗에서 실적 제외, sub행 주간키 누락 해결
   - 검증: 자동 8케이스 + 실기기(금월 30·50 키인→누적 합산·저장 유지) + 공정률 5행 손검산 일치
2. **작업 로드맵 확정** (0 보안 → A 어미줄기 → B 팝업 → C 기준문서 잔여 → D 주간 → E 운영) + 루틴 규칙 8 신설(단계별 수치 시뮬레이션 필수 — 시스템 수치 전체가 미검증 초안이므로)
3. **0단계 보류**: Firebase는 팀장님이 소유자 아님(편집 권한만) 확인 → 관리자 협의 후 진행으로 변경, 요청 메시지 초안 작성
4. **A-1 완료**: 프로젝트 List 화면 상세 분석·설명 (저장 3층 구조, 이력 장치 발견)
5. **A-2 완료**: List↔월간보고 데이터 흐름 실태 — 두 장부 완전 분리(동기화 0건), 함정 5개 확정. 문서: `기준문서/A2_List-월간보고_데이터흐름_실태_v1.0.md`
6. **A-3 확정**: 최신 엑셀 2종 실측 분석(실행번호 62% 빈값·1/3 중복·List 원본엔 컬럼 없음·이름매칭 6%) → **연결 키 = 내부 고유 ID** 팀장님 승인. 변경·삭제 정책(ID 불변, soft delete) 포함. 문서: `기준문서/A3_연결키_결정안_v1.0.md`
7. **A-4 설계 승인 + A-4a 구현·검증 완료**: 데이터 백업 버튼(1·2호 백업 확보), generatePid+신규 4경로 자동발급, 일괄 발급(월간 362+List 1,836건, 멱등성 검증), pid 뱃지, 프로젝트 연결 모달(연결 후 양쪽 ID 동일 실기기 확인), 삭제 도장(soft delete), 우클릭 메뉴 잘림 수정
8. **A-4b 구현 (진행 중)**: 주간장부 docKey pid 우선(+옛 장부 읽기 폴백), 통일 계산기·주간패널 적용 경로 pid화+메모리 즉시 갱신, 주간장부 통일 병합 도구(스캔→확인→실행, 충돌 보류). 팀장님이 병합 실행함.
9. **A-4b 결함 발견·수정**: 중복 실행번호(26-1 등) 장부가 여러 프로젝트에 복제될 수 있던 구멍 → '주인 불명 보류' 분리로 차단

## 변경한 파일
- `src/App.js` — 통일 계산기, 표 병기, 백업/일괄발급/병합 도구+모달 3종, 삭제 도장, pid 뱃지, 메뉴 잘림 수정, 엑셀 임포트 pid 등
- `src/components/ProgressModal.jsx` — 통합시운전 월별 합산(monthlyIntCommSums), docKey pid 우선+폴백
- `src/components/ProjectListScreen.jsx` — _pid 발급(업로드·새행), 프로젝트 연결 모달(pid 통일·연결 뱃지), 상세 pid 뱃지, 메뉴 잘림 수정
- `src/utils.js` — generatePid 신설
- `CLAUDE.md`, `기준문서/` 4종(전수검사·A2·A3·A4), 본 리포트

## 결정·근거
- **연결 키 = 내부 고유 ID(pid)**: 키 3조건(항상 있음·유일·불변)을 실행번호는 전부 미달(실측), pid는 전부 충족. 실행번호·견적번호는 표시용 속성으로 강등
- **삭제 = soft delete('삭제 도장')**: 현장 특성(삭제·보류 잦음, 번복 있음) + 이력기록 요구 + 원본 엑셀의 기존 관행(상태=삭제 14행)과 일치
- **빌드 점검 = 팀장님 VS Code**: Claude 샌드박스 파일 미러가 낡을 수 있음을 확인(ProjectListScreen 6/9 상태로 박제) → 샌드박스 빌드 신뢰 금지
- **보존 병합(A-4c) 전 원본 엑셀로 웹 List 덮어쓰기 금지**: 확정 저장이 기존 행 전부 삭제 후 재작성(코드 실증) → 웹 등록 실행번호·이력 소실 위험

## 다음 할 일 / 막힌 점
- **[막힌 점·내일 1순위] A-4b 원복 버그**: 진행실적 팝업에서 주차 키인→적용 저장(초록 ✓ 확인)→새로고침하면 옛값으로 원복. 저장 장부와 읽기 장부의 이름 불일치로 의심. **확인 방법 준비됨**: F12 콘솔에서 팝업 열 때 `[ProgressModal] loadData docKey=` 줄 캡처(키인 전/저장 후 새로고침 각 1회) → docKey가 P-시작인지 26-1인지 대조
- 병합을 이미 실행했으므로 26-1(중복 실행번호) 장부의 이중 복제 여부 점검 필요 (2호 백업으로 복원 가능)
- A-4b 마무리 → 검증(병합 전후 합계 대조) → A-4c 보존 병합(6/11 엑셀 실전 시험) → A-4d 어긋남 표시
- 0단계: 관리자 회신 대기 (이번 주 내 리마인드)
