# 🔧 인수인계 — NAS WebDAV로 "웹에서 엑셀 열고 저장하기" (진행 중)

> **작성 2026-07-27 / 다음 작업자: 새 챗의 Claude**
> 이 파일만 읽으면 바로 이어서 진행할 수 있습니다.

---

## 0. 한 줄 요약

**관문 4개 중 3개 통과. 마지막 "탐색기에서 WebDAV 폴더 열기"에서 막혀 있음.**
다음 할 일 = **오류 번호 확인** (아래 4번의 `net use` 명령 2줄).

---

## 1. 왜 이걸 하고 있나

팀장님 문제 제기:

> "엑셀에서 수정하면 웹이 따라오는 방식이면 엑셀이 주인이고 **웹은 뷰어**가 된다.
> 웹에서 열고 고치고 저장하면, 다른 PC에서도 웹으로 이어받는 방식으로 바꾸고 싶다."

### 실험으로 확정된 사실 (★ 다시 실험하지 말 것)

진척자료 엑셀은 **수식 73,005개짜리 계산 파일**입니다. 값 하나 안 고치고 파이썬/브라우저
라이브러리로 **다시 저장만** 해봤더니:

| 항목 | 원본 | 재저장 후 | |
|---|---|---|---|
| 시트 / 수식 / 병합 / 조건부서식 | 13 / 73,005 / 233 / 1 | 동일 | ✅ 보존 |
| **계산된 값(캐시)** | **168,311** | **152,753** | ❌ **15,558개 소실** |
| PMS가 읽는 총계 8칸 (H·M·R·Z·AH·AP·AG·AN) | 100%·94.5%·17230·3766 … | **전부 None** | ❌ |

→ **웹이 엑셀을 "고쳐서 저장"하면 PMS가 못 읽는 파일이 됩니다.**
→ 하지만 **진짜 엑셀 프로그램이 저장하면 아무 문제 없습니다.**
→ 그래서 **WebDAV로 진짜 엑셀을 띄워서 저장**하는 길(길 A)을 선택.

### 7/22 기록 정정
CLAUDE.md에 "원본열기=ms-excel 프로토콜 오피스 원천차단 확인→폐기"라고 되어 있는데,
**그때는 네트워크 경로(`\\neconsys_pj\...`)를 썼기 때문**입니다.
Office는 UNC 경로를 막지만 **http(s) 주소는 엽니다** (SharePoint가 이 방식).

---

## 2. 목표 그림

```
웹(PMS) 화면에서 파일 클릭
      ↓  ms-excel:ofe|u|https://...
진짜 엑셀 프로그램이 열림          ← 흉내가 아님. 수식 100% 살아있음
      ↓  수정 후 Ctrl+S
NAS 원본에 그대로 저장             ← WebDAV가 통로
      ↓
PMS 자동 반영(30분)이 다음 검사 때 값을 읽어감
```

부수 효과: **Office가 편집 중 파일을 잠가서** 동시 편집 충돌도 자동 방지.

---

## 3. 확보된 정보 (그대로 쓰면 됨)

| 항목 | 값 |
|---|---|
| NAS 모델 / DSM | **Synology DS412+ / DSM 6.2.4-25556 Update 8** (DS412+의 마지막 버전) |
| NAS 내부 IP | **192.168.0.110** |
| NAS 이름 | NECONSYS_PJ |
| DDNS 이름 | **necon-pj.synology.me** (공인 DNS로는 222.101.203.90 = 사무실 공인 IP) |
| WebDAV 포트 | HTTP **5005** / HTTPS **5006** (둘 다 켬, 로그 켬, 익명·DavDepthInfinity 끔) |
| 인증서 | **necon-pj.synology.me** (Synology DDNS Certificate = 공인, **2026-10-01 만료**) |
| 인증서 할당 | FTPS · 시스템 기본 · **WebDAV Server** 모두 이미 `necon-pj.synology.me` ✅ |
| 공유 폴더 | **`001 파주`** (한글 + 띄어쓰기 ⚠) |
| 엑셀 경로 | `001 파주 / 107 기술2팀 2026년 Project / 04 건설공사 / 011 P10 AP4 Infra 보완 투자 대응 자동제어 공사_건설 / 05 수행_작업 지도서 / 01 진척자료 / 01 진행현황_L1L2` |
| 볼륨 | 볼륨1 841GB 여유(77%) / 볼륨2 **93% 경고 중** → 설치는 볼륨1에 |

---

## 4. 관문 진행 상황

| # | 관문 | 상태 | 증거 |
|---|---|---|---|
| 1 | NAS WebDAV Server 설치·켜기 | ✅ | 패키지 설치(볼륨1), HTTP/HTTPS 활성화, 로그 켬 |
| 2 | 윈도우 WebClient 서비스 | ✅ | 실행 중 (시작 유형 수동 → **자동**으로 변경함) |
| 3 | **인증서 신뢰** | ✅ | 크롬 `https://necon-pj.synology.me:5006` → **경고 없이 접속** |
| 4 | **탐색기에서 WebDAV 폴더 열기** | ❌ | `액세스할 수 없습니다` (뭉뚱그린 메시지, 원인 불명) |

### 관문 3을 어떻게 통과했나 (중요)
- IP(`192.168.0.110`)로 접속하면 `ERR_CERT_COMMON_NAME_INVALID` — 인증서 이름이 IP가 아니라서
- 해결: **팀장님 PC hosts 파일**에 아래 한 줄 추가 (Docker 구역 `# End of section` **아래**)
  ```
  # PMS - NAS WebDAV (2026-07-27)
  192.168.0.110  necon-pj.synology.me
  ```
- `ipconfig /flushdns` → `ping necon-pj.synology.me` → **192.168.0.110, 1ms** 확인
- ⚠ 저장 시 **안랩 Safe Transaction**이 "hosts 변경 탐지 → 복원할까요?" 팝업 → **[아니오]**
- ※ HTTP(5005)는 포기함 — 윈도우가 **암호화 없는 연결의 Basic 인증을 기본 거부**(BasicAuthLevel=1)

### 참고: 404는 정상
브라우저로 WebDAV 폴더를 열면 `Not Found`가 뜹니다. WebDAV는 **폴더를 웹페이지로 보여주는
기능이 없습니다.** 서버가 살아있다는 증거이지 실패가 아닙니다.
(서비스가 죽었으면 `ERR_CONNECTION_REFUSED`가 뜹니다)

---

## 5. ★ 다음에 바로 할 것

### STEP 1 — 오류 번호 확보 (제일 먼저)

**관리자 권한 명령 프롬프트**에서 두 줄을 각각 실행하고 **"시스템 오류 ○○"** 번호를 확인:

```cmd
net use * "\\necon-pj.synology.me@SSL@5006\001 파주"
net use * "\\necon-pj.synology.me@SSL@5006\DavWWWRoot\001 파주"
```

번호별 해석:

| 오류 | 뜻 | 대응 |
|---|---|---|
| **67** 네트워크 이름을 찾을 수 없음 | 경로/공유명 문제 (한글 가능성 큼) | STEP 2 (영문 공유 폴더) |
| **53** 네트워크 경로를 찾을 수 없음 | 연결 자체 실패 | WebClient 재시작, 방화벽 확인 |
| **1244** 인증되지 않음 | 자격 증명 | DSM 계정 권한 확인 |
| **5** 액세스 거부 | 공유 폴더 권한 | DSM 사용자에게 `001 파주` 권한 부여 |
| **224** | Office 보호 정책 | 신뢰할 수 있는 사이트 등록 |

### STEP 2 — 가장 유력한 가설: **한글 공유 폴더명**

공유 폴더 이름이 **`001 파주`** — 한글 + 띄어쓰기입니다.
윈도우 WebDAV 클라이언트는 오래된 부품이라 **한글 경로에서 자주 실패**합니다.
(브라우저는 잘 처리하지만 탐색기는 못 하는 경우가 흔함)

**검증 방법 (5분)**: DSM에서 **영문 이름 테스트 공유 폴더**를 하나 만들고 붙여본다.

1. DSM → 제어판 → 공유 폴더 → **생성** → 이름 `davtest` (영문 소문자, 공백 없음)
2. 권한: 사용할 DSM 계정에 **읽기/쓰기**
3. 탐색기에서 `\\necon-pj.synology.me@SSL@5006\davtest`
   - **열리면 → 한글이 원인 확정.** → 진척자료 폴더를 가리키는 **영문 이름 공유 폴더**를 만들어
     (예: `PJ`) 경로를 짧고 영문으로 만든다
   - **안 열리면 → 한글 문제 아님.** STEP 1의 오류 번호로 다시 판단

### STEP 3 — 열리면 바로 실전 테스트 (핵심 검증)

1. 탐색기에서 폴더를 타고 들어가 **진척자료 엑셀을 더블클릭**
2. 진짜 엑셀이 열리는지 (제목 표시줄 확인 — 읽기 전용이면 실패)
3. **값 하나 바꾸고 Ctrl+S**
4. ★ **저장 후 PMS의 NAS 자동 반영이 그 값을 제대로 읽는지** 확인
   → `진척률요약(Main)` 총계행의 H·M·R·Z·AH·AP·AG·AN 이 숫자로 읽히면 성공

### STEP 4 — 성공하면 PMS에 붙이기

- ProjectListScreen NAS 모달에 **[엑셀로 열기]** 버튼 추가
- 클릭 시 `ms-excel:ofe|u|https://necon-pj.synology.me:5006/<경로>` 실행
- 지금 있는 `[경로 복사] + Win+R` 방식을 대체
- 참고: 기존 코드의 `extBase()` / `extJoin()` / `extLocalKey`(이 PC용 주소) 구조 재사용

### STEP 5 — 전 직원 확산 (테스트 성공 후)

hosts 파일 방식은 **PC마다 편집 + 안랩 팝업**이 뜹니다. 대신:

> **NAS에 `DNS Server` 패키지 설치 → `necon-pj.synology.me` A 레코드를 192.168.0.110으로 등록
> → DHCP Server(NAS에서 이미 운영 중)가 그 DNS를 나눠주게 설정**

이렇게 하면 **NAS에서 1회 등록으로 전 직원 PC가 자동 해결**됩니다. hosts 편집·안랩 팝업 모두 사라짐.
⚠ 단, DHCP/DNS는 사내 전체 네트워크에 영향 → **반드시 테스트 성공 후에, 전무님 확인 받고** 진행.

---

## 6. 실패 시 대안 (길 A가 끝내 안 될 때)

**길 B — 웹이 파일을 "나르기만" 하는 체크아웃/체크인 방식**

- 웹 [수정하러 가져오기] → 파일 다운로드 + Firebase에 "○○님 편집 중" 잠금 기록(전 PC에 표시)
- 진짜 엑셀로 수정
- 웹 [수정본 올리기] → 파일을 **바이트 통째로** NAS에 덮어쓰기 (`showDirectoryPicker({mode:'readwrite'})`)
- 웹이 파일 내용을 안 건드리므로 **수식·계산값 100% 보존**
- 잠금·이력이 PMS 안으로 들어와 **웹이 관제탑**이 됨
- 단점: PC마다 쓰기 허가증 1회 필요(사무실 PC만)

---

## 7. 같이 처리해야 할 미결 사항

- [ ] **오늘 코드 3개 파일 미배포·미커밋** → `npm run build` → `dir build\index.html` → `firebase deploy --only hosting`
      / `git add -A` → `git commit -F 커밋메시지_0727.txt` → `git push`
- [ ] 메인 PC 자동 반영 **실기기 확인** (설정 메뉴 표시 → 켜기 → 하단 `● 메인 PC 자동 반영 중`)
- [ ] 공용 PC 세팅: 관리자 계정 로그인 → 메인 PC 켜기 → **일반 계정으로 전환** → 윈도우 절전 끄기
- [ ] 인증서 **2026-10-01 만료** — 시놀로지 자동 갱신되는지 9월에 확인
- [ ] 볼륨2 디스크 93% 경고 (별건)
