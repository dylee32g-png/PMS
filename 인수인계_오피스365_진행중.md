# 인수인계 — 오피스365 클라우드 연동 (2026-07-30 종료 시점)

> **새 채팅에서 이어서 할 때 이 파일부터 읽으면 바로 이어집니다.**
> 이전 문서(2026-07-29 버전)는 OneDrive 동기화 폴더 방식 기록이며 **그 방식은 폐기되었습니다.**

---

## 0. ⚠ 가장 먼저 — 두 방식을 섞으면 안 됩니다

진척자료를 읽는 방식이 **두 가지** 존재하고, **하나만 켜져 있어야 합니다.**

| | NAS 방식 (구) | **오피스365 방식 (신)** |
|---|---|---|
| 읽는 주체 | PMS 브라우저 (공용 메인 PC) | **NAS Docker 컨테이너** |
| 코드 위치 | `src/components/ProjectListScreen.jsx`, `projectListData.js` | `NAS:/volume1/docker/pms-reader/main.py` |
| 스위치 | `projectListData.js` 의 `NAS_SYNC_ENABLED` | Docker 컨테이너 실행/정지 |
| **현재 상태** | **꺼짐 (false)** | **켜짐 (15분 주기)** |

**겹치는 것 2개 — 이래서 동시에 켜면 안 됩니다**

```
① 규칙 : 둘 다 행의 _extSync.rules 를 읽는다
② 쓰기 : 둘 다 같은 메인표 칸 + 같은 주간장부에 쓴다
```

→ **`NAS_SYNC_ENABLED` 를 true 로 되돌릴 때는 반드시 Docker 컨테이너를 먼저 정지할 것.**

누가 썼는지는 `_updatedBy` 로 구분됩니다.

```
_updatedBy = "jeep@neconsys.co.kr"   → PMS(NAS 방식)
_updatedBy = "NAS-Graph-Reader"      → 오피스365 방식
```

---

## 0.5 새 채팅에서 처음 5분에 할 일

1. **이 문서를 끝까지 읽는다** (특히 0장·5장)
2. **현재 상태 확인** — 아래 3개만 보면 지금 어디까지 왔는지 알 수 있습니다

   | 확인 | 어디서 | 정상 값 |
   |---|---|---|
   | NAS 프로그램이 도는가 | DSM → Docker → 컨테이너 `pms-reader` | 실행 중 |
   | 마지막으로 언제 돌았나 | 그 컨테이너 → 로그 (`↻` 눌러야 갱신됨) | 15분 내 |
   | PMS에 값이 들어왔나 | `localhost:3000` → 011행 | PLC 91 · ETOS 75 |

3. **팀장님께 무엇부터 할지 묻는다.** 임의로 진도 나가지 말 것 (2026-07-30 지시)
4. 코드 수정 전 반드시 **`.bak-YYYY-MM-DD`** 백업을 만들 것 (이 프로젝트 관례)

---

## 1. 오늘 무엇이 완성되었나

```
오피스365 클라우드 엑셀
        │  Microsoft Graph API (앱 권한 · 사람 로그인 없음)
        ▼
   NAS Docker 컨테이너 pms-reader  (15분 주기)
        │  PMS와 동일한 규칙으로 계산 (PLC · ETOS · 하위 공종표 총계)
        ▼
   Firestore  ─ 메인표 칸 + 주간 진행실적 장부 둘 다 기록
        ▼
   직원 PMS 화면 (변화 없음 · MS 로그인 불필요)
```

**없어진 것**: 공용 PC 상주 · 브라우저 탭 열어두기 · PC별 폴더 허가증 · OneDrive 동기화 대기 · 30분 주기

**실측**: 전 구간 12~13초. 검증값 `PLC 91.0` · `ETOS 75.0` — PMS·테스트페이지·NAS 세 곳 모두 일치.

---

## 2. 왜 방식을 바꿨나 (판단 근거)

**OneDrive 로컬 동기화가 신뢰할 수 없음이 실측으로 확정됨.**

| 시각 | 사건 |
|---|---|
| 09:58 | 웹 엑셀에서 편집 → 로컬 파일 미반영 |
| 13:14 | 다시 편집 → 로컬 파일 미반영 |
| 13:48 | 34분 경과, OneDrive는 "백업 및 동기화됨" 표시, 내용은 어제 것 |
| 15:01 | **로컬 파일의 수정시각은 클라우드와 동일(13:14:23)한데 내용만 옛것** |

→ OneDrive가 **시각만 맞추고 내용을 안 가져옴**. OneDrive·웹 엑셀·PMS 세 화면이 전부 "정상"인데 값은 틀림. 경고 없음.

**결론**: 동기화 폴더 경유는 버리고, Graph API로 클라우드를 직접 읽는다.

---

## 3. 계정·설정 정보

| 항목 | 값 |
|---|---|
| 테넌트 | `neconsyspms.onmicrosoft.com` |
| 관리자 | `jeep@neconsyspms.onmicrosoft.com` |
| 테스트 계정 | `test@neconsyspms.onmicrosoft.com` (라이선스 할당됨, **첫 로그인 미실시**) |
| 평가판 만료 | **2026-08-29** (되풀이 청구 "만료 시 취소" 설정 → 청구 0원) |
| 관리센터 | admin.cloud.microsoft |
| Entra 앱 | `PMS-GraphReader` |
| 클라이언트 ID | `583ff4a8-dc8e-4114-9a05-9d57b5d2098f` |
| 권한 | `Files.Read.All` (위임 + **애플리케이션**) · `User.Read` — 관리자 동의 완료 |
| 리디렉션 URI (SPA) | `http://localhost:3000` · `http://localhost:3000/graphtest.html` |
| **클라이언트 비밀키** | NAS의 `config.json` 안에만 존재. **만료 2028-07-30 (24개월)** |
| Firebase 프로젝트 | `techteampms` (**Spark 무료 요금제** → Cloud Functions 사용 불가) |
| Firebase 역할 | 소유자 `dylee32g@gmail.com` / 편집자 `jeepno1ykr1@gmail.com` |
| appId | `tech-team-pms-app` |
| 진척자료 위치 | `jeep@` OneDrive → `PMS진척자료` 폴더 |
| NAS | `192.168.0.110` (NECONSYS_PJ) · DSM Docker(구버전, `프로젝트` 메뉴 없음) |
| GitHub | `https://github.com/dylee32g-png/PMS.git` |

---

## 4. NAS 프로그램

### 4.1 구성

```
NAS  /volume1/docker/pms-reader/
   ├─ main.py            프로그램 본체
   ├─ requirements.txt   requests==2.32.3 · firebase-admin==6.5.0
   ├─ start.sh           컨테이너 시작 스크립트 (라이브러리 설치 후 main.py 실행)
   ├─ config.json        ★ 비밀키 포함. 외부 반출 금지
   ├─ firebase-key.json  ★ Firestore 마스터 키. 외부 반출 금지
   └─ .pip/              설치된 라이브러리 (자동 생성)

Docker 컨테이너 : pms-reader   (이미지 python:3.12.0-slim)
   볼륨   : /docker/pms-reader → /app
   명령   : sh /app/start.sh
   환경   : TZ=Asia/Seoul · PYTHONUNBUFFERED=1
   자동 재시작 : 켬
```

**PC 사본**: `D:\pms-app\nas-reader\` — 같은 파일들 (비밀키는 빈 상태). 여기서 고쳐서 NAS에 덮어쓰는 흐름.

### 4.2 운영

| 하고 싶은 일 | 방법 |
|---|---|
| 로그 보기 | Docker → 컨테이너 → pms-reader → 세부 정보 → 로그 (**자동갱신 안 됨, `↻` 눌러야 함**) |
| 주기 바꾸기 | `config.json` 의 `interval_min` |
| 쓰기 없이 시험 | `config.json` 의 `dry_run: true` |
| 정지 | Docker → 컨테이너 → 스위치 끄기 |
| 코드 수정 후 | 파일 덮어쓰기 → 컨테이너 **다시 시작** |

### 4.3 하는 일 / 안 하는 일

| | 상태 |
|---|---|
| 메인표 PLC·ETOS 반영 | ✅ |
| 하위 공종표 **부모 총계** 반영 (도면입수·I/O Map·화면작성·기준정보·HMI·누적) | ✅ |
| **주간 진행실적 장부** 기록 (팝업·그래프·주간보고 일치) | ✅ |
| `_naOn` 자동 켜기 (기본 미적용 항목) | ✅ |
| 메인표에 **문자열**로 저장 (PMS와 동일: `91.0` → `"91"`) | ✅ |
| **하위 8행 각각의 값** 갱신 | ❌ 미구현 → 5장 2순위 |
| PMS `[지금 확인]` 즉시 실행 | ❌ 미구현 → 5장 3순위 |

**자기교정형**: 메인표가 이미 맞아도 주간장부가 어긋나 있으면 장부만 고침. 이미 같은 값이면 아무것도 안 씀.

### 4.4 main.py 함수 지도 (다음 수정 시 어디를 볼지)

```
load_config()                설정 읽기·필수값 검사
ms_token(cfg)                앱 권한 토큰 (client_credentials · 1시간 캐시)
g_get(cfg, path)             Graph GET 공통

col_to_idx / idx_to_col / split_addr / expand_cells      엑셀 주소 도구
pct(v)                       0~1.5는 ×100 (PMS와 동일 규칙)
num_or_none(v)               문자열 "100%" → 100
name_date(name)              파일명 마지막 6자리 YYMMDD

list_folder(cfg)             /users/{drive_user}/drive/root:/{folder}:/children
pick_latest(files, pattern)  파일명 조각 → 날짜 → 수정시각 (PMS pickLatestExtFile 동일)
sheet_id(cfg, item, name)    시트명으로 찾아 id 반환 (# 특수문자 회피 · 캐시)
read_range(cfg, item, ws, addr)   range(address='L5:M14')

compute_cells_rule()         PLC·ETOS 같은 단순 규칙. 흩어진 셀을 최소 사각형 1회 읽기로 처리
compute_subtable()           하위 공종표. A1:AZ200 한 번 읽고 '총계' 행까지 훑음
                             → (sub_rows, total) 반환.  ★ sub_rows 는 지금 로그만 찍고 안 씀
                                → 2순위 작업은 여기 결과를 쓰는 코드만 추가하면 됨

PROG_COL_TO_KEY              헤더 → 장부 항목키 (PMS와 동일)
prog_item_key(header)
week_key()                   ceil(일/7) 최대 5 · '2026-7-5' 형식
doc_key_of(row_id, row)      _pid → pid → 실행번호 → execNo → 행ID
_write_week()                장부 한 항목·현재 주차 기록 (같은 값이면 안 씀)
sync_ledger()                메인표 공정률 칸 → 장부
sync_int_commissioning()     누적 → intCommissioning 장부
js_num_str(v)                91.0 → "91"  (자바스크립트 String()과 동일)

firestore_client(cfg) / rows_collection(db,cfg) / ledger_ref(db,cfg,key)
run_once(cfg, db)            한 바퀴 — 여기서 위 함수들을 조립
main()                       설정 로드 → 무한 루프 (interval_min 간격)
```

### 4.5 Firestore 데이터 구조 (정확한 경로)

```
artifacts / tech-team-pms-app / public / data /
   │
   ├─ projectListRows_기술2팀 / {rowId}          ← List 화면 행 (현재 181행)
   │      _id, _pid, _year, Project, PLC, ETOS, HMI, 포인트, 누적, ...
   │      _extSync : { uncPath, rules:[...], lastApplied:{...} }   ← 규칙이 여기
   │      _naOn    : ['도면입수','I/O Map',...]   ← 기본 미적용 항목 중 켠 것
   │      _updatedAt, _updatedBy
   │      ※ 하위 행 _id 형식 = "{부모_id}_sub{n}"
   │
   ├─ progressRecords_기술2팀 / {pid}            ← 주간 진행실적 장부
   │      docKey, execNo
   │      weekly : {
   │         plc      : { "2026-7-5": 91, "2026-7-4": 89.6, ... }
   │         etos · hmi · drawing · iomap · screen · baseinfo
   │         intCommissioning : { ... }              ← 누적(진행 pt)
   │         sub_0_intCommissioning ... sub_7_...    ← 하위별 통합시운전
   │      }
   │
   ├─ projectListMeta / 기술2팀                  ← 헤더·열그룹
   └─ projects / {id}                            ← 구 진행현황 화면 (별개 장부)
```

**주차 규칙**: 1주=1~7일 · 2주=8~14 · 3주=15~21 · 4주=22~28 · 5주=29~. 키는 `2026-7-5` (월 0채움 없음)
**규칙**: 같은 달에서 현재 주차보다 **미래** 주차 값은 지움 → 현재 주차가 "누적 최신값"

### 4.6 PMS 원본 로직 대응표 (참고할 곳)

다음 기능을 만들 때 PMS의 이 함수를 그대로 옮기면 됩니다. (줄번호는 바뀔 수 있으니 **함수명으로 검색**)

| 만들 것 | PMS 원본 위치 |
|---|---|
| 하위 8행 생성·갱신 | `ProjectListScreen.jsx` → `extApplyProposals` 안의 `kind === 'subCreate'` / `'subSet'` 처리 |
| 하위별 통합시운전 장부 | 같은 함수의 `kind === 'subLedger'` · `sub_${i}_intCommissioning` |
| 주간장부 기록 | `syncProgressCellToLedger` (이미 main.py 로 옮김) |
| 헤더 ↔ 장부키 | `PROG_COL_TO_KEY` (이미 옮김) |
| 하위 공종표 프리셋 | `ProjectListScreen.jsx` 상단 `EXT_SUBTABLE_PRESET` |
| 규칙 계산 | `projectListData.js` → `computeExtRuleValue` · `computeExtSubTable` (이미 옮김) |
| 잠금 열 판정 | `projectListData.js` → `extLockedColsOf` · `ProjectListScreen.jsx` → `extLockedColsRow` |

---

## 5. 남은 작업 (우선순위 순)

### ★ 1순위 — 규칙 등록 화면 (가장 급함)

**지금 새 프로젝트에 규칙을 붙일 방법이 없습니다.**

- 프로그램 코드는 프로젝트가 1개든 100개든 **안 고쳐도 됩니다.** 행의 `_extSync.rules` 를 전부 훑어 처리
- 파일이 10개여도 각 규칙이 `filePattern` 으로 자기 파일만 골라냄
- 하지만 **프로젝트마다 규칙 등록은 한 번씩 필요** (엑셀 셀 주소가 다르므로)
- 오늘 NAS 동기화를 끄면서 **규칙 등록 모달도 같이 숨김.** 011행 규칙은 그 전에 등록된 것이 Firestore에 남아 돌고 있는 것

**할 일**: 기존 NAS 모달을 오피스365용으로 되살리기. 규칙 구조가 동일하므로 화면 재사용 가능.

```
빼야 할 것 : [폴더 지정/변경] · [이 PC 지정 해제] · 표시용 NAS 주소
             [엑셀로 열기] (NAS WebDAV 전용이라 OneDrive에선 원리상 불가)
남길 것    : 규칙 추가/삭제/저장 · 하위 공종표 프리셋 버튼 · [NAS 잔재 정리]
더할 것    : "마지막 반영 시각" 표시 (NAS 프로그램이 언제 돌았는지)
             → 구현안: NAS 프로그램이 매 회차 Firestore에 상태 문서를 하나 쓰고 PMS가 그걸 표시
```

**011행 현재 규칙 (복원·복제용)**

| 대상 | 파일 이름 조각 | 시트 | 셀 | 계산 |
|---|---|---|---|---|
| `PLC` | `01 진행현황` | `#1 L1 진행현황` | `L5,M5,L14,M14` | 평균 |
| `ETOS` | `01 진행현황` | `#1 L1 진행현황` | `L24,L25,M24,M25` | 평균 |
| `하위 공종표` | `진척자료` | `진척률요약(Main)` | 프리셋 `EXT_SUBTABLE_PRESET` | — |

프리셋 내용:
```js
{ type:'subTable', target:'하위 공종표', filePattern:'진척자료',
  sheet:'진척률요약(Main)', nameCol:'C',
  subCols:{ '도면입수':'H','I/O Map':'M','화면작성':'R','기준정보':'Z',
            '자체시운전':'AH','통합시운전':'AP' },
  subPtCol:'AG',
  parentCols:{ '도면입수':'H','I/O Map':'M','화면작성':'R','기준정보':'Z','HMI':'Z' },
  parentAccCol:'AN', decimals:1 }
```

### 2순위 — 하위 8행 값 갱신

파일2 `진척률요약(Main)` 의 공종 8줄 → PMS 하위 8행에 각각 반영.

```
하위 행에 들어가는 칸 8개
   도면입수(H) · I/O Map(M) · 화면작성(R) · 기준정보(Z) · 자체시운전(AH)
   통합시운전(AP) · 포인트(AG) · 누적(AN)

※ 하위 행에서 PLC·ETOS·HMI 는 프로젝트 단위 지표라 입력 금지(잠금) — 비어 있는 게 정상
※ 하위 행은 별도 진행실적 장부가 없음.
   부모 팝업의 '시운전' 섹션에 sub_{i}_intCommissioning 형태로 기록됨
※ 하위 행 매칭은 이름 기준 (엑셀 공종명 ↔ PMS 하위 행 Project명, 앞의 '- ' 제거 후 비교)
```

**읽기는 이미 됨** — `compute_subtable()` 이 8개를 정확히 찾아 `sub_rows` 로 반환 중. 로그에도 찍힘.
**쓰는 쪽만 추가하면 됨.**

### 3순위 — PMS `[지금 확인]` → 즉시 실행

지금은 최대 15분 대기. 구현안 2가지:
- (a) Firestore에 "요청 플래그" 문서를 쓰고, NAS 프로그램이 1분마다 그것만 확인
- (b) NAS 프로그램에 간단한 HTTP 수신 포트를 열고 PMS가 호출 (사내망 한정)

(a)가 방화벽·포트 문제가 없어 간단함.

### 4순위 — 배포

**오늘 소스 변경은 `localhost:3000` 에만 적용됨. 배포 안 함.**

```
변경된 파일
   src/components/projectListData.js      NAS_SYNC_ENABLED = false 추가 + extRulesOf 가드
   src/components/ProjectListScreen.jsx   9곳 가드 + [NAS 잔재 정리] 버튼 추가
   public/graphtest.html                  Graph API 테스트 페이지 (신규)

백업 (원본)
   projectListData.js.bak-2026-07-30-nasON
   ProjectListScreen.jsx.bak-2026-07-30-nasON     ← NAS 켜져 있던 원본
   ProjectListScreen.jsx.bak-2026-07-30           ← 잔재정리 버튼 추가 직전

배포 : npm run build → firebase deploy --only hosting
       프로젝트 techteampms / target neconsys
```

⚠ 배포하면 **공용 메인 PC에서도 NAS 칩·메인 PC 메뉴가 사라집니다.** 의도한 동작이지만 사전 공지 필요.
⚠ 배포 사이트에서도 쓰려면 Entra 앱의 리디렉션 URI에 **배포 주소를 추가**해야 합니다 (지금은 localhost만).

### 5순위 — 오피스365 나머지 테스트

| # | 항목 | 상태 |
|---|---|---|
| ① | 수식·서식 보존 | 통과 (7/29) — 6,709칸 대조 불일치 0 |
| ④ | PMS 자동반영 | **통과 (7/30, Graph API 방식으로)** |
| 확인A | 웹 `%` 입력이 숫자인가 | 통과 (7/30) — 손 입력은 정상 |
| ③ | 2명 동시편집 | 미실시 — `test@` 첫 로그인(비번변경·MFA등록) 필요 |
| ⑤ | 버전 복원 | 미실시 |
| ② | 폰·외부 열기 | 미실시 (팀장님 지시로 맨 마지막) |

### 6순위 — 비용 판단

- 라이선스는 **사용자(계정)당** 과금. **동시접속 개념 아님**
- 웹 엑셀로 **편집하는 사람 수만큼** 필요 (프로젝트 담당자 전원 — 팀장님 확인)
- **보기만 하는 사람은 불필요** (NAS가 대신 읽으므로)
- Business Standard 연 약정 ₩18,900/인·월 → 8명이면 **연 약 181만원** (부가세 별도)
- 계정 공유는 권하지 않음 — 동시편집 구분 불가·이력 추적 불가·MFA 공유 문제
- **미확인**: Business Basic(웹 전용·더 저렴)으로 낮출 수 있는지

### 7순위 — 보안·정리

- [ ] NAS `pms-reader` 폴더 권한 제한 (일반 직원 접근 차단)
- [ ] 클라이언트 비밀키 **2028-07-30 만료** — 달력 등록
- [ ] `public/graphtest.html` 은 테스트용 → 배포 전 삭제 또는 접근 제한
- [ ] DSM `admin` 계정 보안 경고 (별건)
- [ ] NAS 볼륨2 93% (별건)
- [ ] NAS 인증서 2026-10-01 만료 (별건)
- [ ] 개인 PC의 OneDrive 회사 계정 연결 해제 (테스트 종료 후)
- [ ] 회사 OneDrive에 `Documents` 폴더가 백업되고 있음 — 의도 확인 필요
- [ ] 공용 메인 PC 화면에 "NAS 자동 확인 실패" 토스트가 뜰 수 있음 — 정상. 폴더 재지정하지 말 것

---

## 6. 오늘 확정된 함정·사실

| # | 내용 |
|---|---|
| 1 | **OneDrive 동기화는 시각만 맞추고 내용을 안 가져올 수 있다.** 세 화면이 모두 초록불인데 값이 틀림 |
| 2 | 웹 엑셀에 손으로 `100%` 입력 → **정상 숫자.** 7/29의 텍스트 현상은 브라우저 자동화 탓 |
| 3 | 백분율 서식 칸에 `1`만 치면 **1%**가 됨 (엑셀 기본 옵션 "자동 백분율 입력"). 직원 안내문에 명기 필요 |
| 4 | PMS `[엑셀로 열기]` 는 **NAS WebDAV 전용** (`davUrlFor`가 NAS 주소를 만듦). OneDrive에선 원리상 불가 |
| 5 | "찾은 원본 파일" 중복 표시 = 과거 반영기록(`lastApplied`)이 같이 표시된 것. 기능 영향 없음 |
| 6 | `[엑셀 생성]` 은 **화면에 보이는 행만** 내려줌 (필터 적용). 재업로드 전 필터 해제 필수 |
| 7 | **전체 삭제 + 재업로드 시 pid가 새로 발급**되어 주간실적 연결이 끊김 (7/30 백업 기준 105건 중 95건이 이미 고아 상태였음) |
| 8 | List 화면 `[전체 데이터 삭제]` 는 `projectListRows_` 를 지움. App.js의 `[데이터 초기화]` 는 `projects` 컬렉션을 지움 — **서로 다른 장부** |
| 9 | Firebase Spark(무료) 요금제에서는 Cloud Functions 배포 불가 → 그래서 NAS를 택함 |

---

## 7. 되돌리는 법

**오피스365 방식 정지**: Docker → 컨테이너 → pms-reader 스위치 끄기

**NAS 방식 복구**:
1. Docker 컨테이너 **정지 확인** (동시 실행 금지)
2. `projectListData.js` 의 `NAS_SYNC_ENABLED = false` → `true`
3. `npm run build` → `firebase deploy --only hosting` (배포본에 반영하려면)
4. 공용 메인 PC에서 NAS 칩 → `[폴더 지정/변경]` → NAS 폴더 재지정
   (7/30 전체 삭제로 행 ID가 바뀌어 **기존 폴더 허가증이 무효화됨**)
5. 설정 메뉴 → `[이 PC를 메인 PC로 지정]` 다시 켜기

---

## 8. 2026-07-30 팀장님 지시·결정 기록

| 결정 | 내용 |
|---|---|
| NAS 기능 전면 비활성화 | 오피스365와 값이 섞이면 검증 불가 → 스위치 하나로 끄고 나중에 하나만 살림 |
| 전체 데이터 삭제 + 재업로드 | NAS 링크(`_extSync`)를 걷어내기 위해. 백업은 테스트 데이터라 불필요하다고 판단 |
| 재업로드 원본 | 앱에서 뽑은 엑셀이 아니라 **원래 쓰던 List 원본 엑셀** 사용 |
| 방식 선택 | **방식 B**(서버가 대신 읽기). 직원 로그인 불필요·라이선스 최소화 |
| 실행 위치 | **NAS**. 사내에서 완전히 통제됨 (코드·비밀키·실행이 전부 회사 장비 안) |
| 진행 방식 | **단계별로 하나씩.** 임의로 진도 나가지 말 것. 마무리 여부는 팀장님이 판단 |
| 문서 갱신 | 인수인계 문서는 **요청 시에만** 작성 |

---

## 9. 잔여 (7/28~29에서 이월)

- 7/28 오후분 커밋·push (`커밋메시지_0728.txt`)
- pms 전용계정 실사용 검증 (직원 첫 로그인)
- 직원 안내문 + `.bat` 공지 (전무님 확인 후)
- 비교 설명자료 아티팩트 `nas-vs-office365-compare` (사이드바에서 재열람 가능)
