# 인수인계 — 오피스365 클라우드 연동 (2026-07-31 종료 시점)

> **새 채팅에서 이어서 할 때 이 파일부터 읽으면 바로 이어집니다.**
> 이전 문서(2026-07-30 버전)는 `인수인계_오피스365_진행중.md.bak-2026-07-31` 로 남겨두었습니다.
> 그 문서에 적힌 "1·2·3순위 미구현", "[엑셀로 열기] 원리상 불가" 는 **오늘 전부 해결되었습니다.**

---

## 0. ⚠ 가장 먼저 — 두 방식을 섞으면 안 됩니다

진척자료를 읽는 방식이 **두 가지** 존재하고, **하나만 켜져 있어야 합니다.**

| | NAS 방식 (구) | **오피스365 방식 (신)** |
|---|---|---|
| 읽는 주체 | PMS 브라우저 (공용 메인 PC) | **NAS Docker 컨테이너** |
| 코드 위치 | `src/components/ProjectListScreen.jsx` | `NAS:/volume1/docker/pms-reader/main.py` |
| 스위치 | `projectListData.js` 의 `NAS_SYNC_ENABLED` | Docker 컨테이너 실행/정지 |
| **현재 상태** | **꺼짐 (false)** | **켜짐 (15분 주기 + 요청 즉시)** |

**겹치는 것 2개 — 이래서 동시에 켜면 안 됩니다**

```
① 규칙 : 둘 다 행의 _extSync.rules 를 읽는다
② 쓰기 : 둘 다 같은 메인표 칸 + 같은 주간장부에 쓴다
```

→ **`NAS_SYNC_ENABLED` 를 true 로 되돌릴 때는 반드시 Docker 컨테이너를 먼저 정지할 것.**

누가 썼는지는 `_updatedBy` 로 구분됩니다.

```
_updatedBy = "jeep@neconsys.co.kr"   → 사람이 PMS에서 입력
_updatedBy = "NAS-Graph-Reader"      → 오피스365 자동 반영기
```

### 0.1 스위치가 두 개입니다 (2026-07-31 신설)

```
NAS_SYNC_ENABLED = false   ← 브라우저가 직접 파일 읽기 · 30분 자동반영 · 메인PC 메뉴 · 칸 잠금
RULE_UI_ENABLED  = true    ← 규칙 등록 화면(칩 버튼 · 모달)만 켜는 스위치  ★ 새로 만듦
```

**왜 나눴나**: 오피스365 방식에서도 *규칙 등록은 사람이* 해야 합니다. 그런데 규칙을 읽고 쓰는 화면은 NAS 스위치에 묶여 있어 함께 꺼져 있었습니다. 그래서 **화면만 켜는 스위치**를 따로 만들었습니다.

---

## 0.5 새 채팅에서 처음 5분에 할 일

1. **이 문서를 끝까지 읽는다** (특히 0장 · 5장 · 6장)
2. **현재 상태 확인** — 아래 3개만 보면 지금 어디까지 왔는지 알 수 있습니다

   | 확인 | 어디서 | 정상 값 |
   |---|---|---|
   | NAS 프로그램이 도는가 | DSM → Docker → 컨테이너 `pms-reader` | 실행 중 |
   | 마지막으로 언제 돌았나 | **PMS List 화면 맨 아래 `● 자동 반영기 … 확인` 배지** | 초록 · 15분 내 |
   | PMS에 값이 들어왔나 | `localhost:3000` → 011행 | PLC 91 · ETOS 75 |

   → 로그를 직접 볼 때: Docker → 컨테이너 → 세부 정보 → 로그 (**자동갱신 안 됨, `↻` 눌러야 함**)

3. **팀장님께 무엇부터 할지 묻는다.** 임의로 진도 나가지 말 것 (2026-07-30 지시, 계속 유효)
4. 코드 수정 전 반드시 **`.bak-YYYY-MM-DD`** 백업을 만들 것 (이 프로젝트 관례)
5. **커밋 상태는 문서가 아니라 `git log` 로 확인할 것.** (2026-07-31: 문서의 "미커밋" 메모를 믿고 잘못 보고한 사례 있음)

---

## 1. 2026-07-31 에 완성된 것

```
오피스365 클라우드 엑셀
        │  Microsoft Graph API (앱 권한 · 사람 로그인 없음)
        ▼
   NAS Docker 컨테이너 pms-reader
        │  15분 주기  +  PMS [지금 확인] 누르면 20초 내 즉시
        ▼
   Firestore  ─ 메인표 · 하위 8행 · 주간 진행실적 장부 · 자동반영기 상태
        ▼
   직원 PMS 화면 (MS 로그인 불필요)
```

| 순위 | 항목 | 결과 |
|---|---|---|
| 1순위 | **규칙 등록 화면** | ✅ 완료 · 실증 |
| 2순위 | **하위 8행 값 갱신** | ✅ 완료 · 실증 |
| 3순위 | **`[지금 확인]` 즉시 실행** | ✅ 완료 · 최대 20초 |
| 추가 | **오피스365 `[엑셀 열기]`** | ✅ 완료 (문서엔 "원리상 불가"로 적혀 있던 것) |
| 추가 | **프로젝트별 폴더 구조** | ✅ 완료 · 011행 적용 |
| 추가 | **Graph 일시 오류 재시도** | ✅ 완료 |
| 수정 | 부모 행 열 이름 매칭 버그 | ✅ 수정 |

**검증 기록 (실측)**

```
· 규칙 4개 등록 상태에서 기존 3개 보존 확인 (헛규칙 왕복 시험)
· 하위 8행 짝 8/8 매칭 · 엑셀 원본과 24칸 전부 일치
· 하위 재이용 포인트 48→40 강제 변경 → 리더가 48로 자동 교정
· 새 프로젝트에 규칙 등록 → [지금 확인] → PLC 70 반영
· 웹 엑셀에서 70→85 수정 → [지금 확인] → 20초 내 PMS 85 반영   ★ 전 구간 왕복
· 폴더 구조 도입 후에도 011행 값 흔들림 0칸
```

---

## 2. 왜 방식을 바꿨나 (판단 근거 · 2026-07-30)

**OneDrive 로컬 동기화가 신뢰할 수 없음이 실측으로 확정됨.**

| 시각 | 사건 |
|---|---|
| 09:58 | 웹 엑셀에서 편집 → 로컬 파일 미반영 |
| 13:14 | 다시 편집 → 로컬 파일 미반영 |
| 13:48 | 34분 경과, OneDrive는 "백업 및 동기화됨" 표시, 내용은 어제 것 |
| 15:01 | **로컬 파일의 수정시각은 클라우드와 동일(13:14:23)한데 내용만 옛것** |

→ OneDrive가 **시각만 맞추고 내용을 안 가져옴**. 경고도 없음.

**결론**: 동기화 폴더 경유는 버리고, Graph API로 클라우드를 직접 읽는다.

> 참고: **올리는 방향(로컬 → 클라우드)은 정상**이었습니다. 2026-07-31 폴더 정리 때 탐색기의 OneDrive 동기화 폴더로 파일을 옮겼고 클라우드에 즉시 반영됐습니다. 문제는 **내려받는 방향**입니다.

---

## 3. 계정·설정 정보

| 항목 | 값 |
|---|---|
| 테넌트 | `neconsyspms.onmicrosoft.com` |
| 관리자 | `jeep@neconsyspms.onmicrosoft.com` |
| 테스트 계정 | `test@neconsyspms.onmicrosoft.com` (라이선스 할당됨, **첫 로그인 미실시**) |
| 평가판 만료 | **2026-08-29** (되풀이 청구 "만료 시 취소" 설정 → 청구 0원) |
| 관리센터 | admin.cloud.microsoft |
| Entra 앱 | `PMS-GraphReader` |
| 클라이언트 ID | `583ff4a8-dc8e-4114-9a05-9d57b5d2098f` |
| 권한 | `Files.Read.All` (위임 + **애플리케이션**) · `User.Read` — 관리자 동의 완료 |
| **클라이언트 비밀키** | NAS의 `config.json` 안에만 존재. **만료 2028-07-30 (24개월)** |
| Firebase 프로젝트 | `techteampms` (**Spark 무료 요금제** → Cloud Functions 사용 불가) |
| appId | `tech-team-pms-app` |
| 진척자료 위치 | `jeep@` OneDrive → `PMS진척자료` 폴더 |
| NAS | `192.168.0.110` (NECONSYS_PJ) · DSM Docker(구버전, `프로젝트` 메뉴 없음) |
| GitHub | `https://github.com/dylee32g-png/PMS.git` |

---

## 4. 클라우드 폴더 구조 (2026-07-31 팀장님 확정)

```
OneDrive / PMS진척자료 /
   ├─ 01 진행현황_P9_10 AP4 …_CNS_260420.xlsx    ← 맨 위 = 공통 자리
   │                                                (P9·P10 두 프로젝트가 같이 쓰는 파일)
   ├─ P10 AP4 Infra 보완투자 /                    ← 프로젝트 폴더
   │      P10 AP4 Infra … 진척자료_260706.xlsx
   ├─ P9 AP4 Infra 보완투자 /
   │      …
   └─ Backup / 백업 / Archive / 보관 /            ← 리더가 자동으로 건너뜀
```

**운영 규칙 — 문장 하나입니다**

```
이 프로젝트 전용 파일         →  프로젝트 폴더 안에
여러 프로젝트가 같이 쓰는 파일  →  맨 위(공통 자리)에
```

리더는 각 행에 대해 **`_extSync.folder` 폴더(와 그 하위) + 맨 위** 를 함께 봅니다.
폴더를 지정하지 않은 행은 **폴더 전체**에서 찾습니다(예전 동작 · 하위 호환).

**왜 이렇게 정했나**
- 규칙마다 폴더를 붙이는 방식은 정확하지만, 프로젝트 20개면 폴더를 60번 골라야 함
- 이 방식은 **프로젝트당 한 번**만 고르면 되고, 파일 놓는 판단도 한 가지뿐

**⚠ 엑셀 파일 이름은 바꾸면 안 됩니다.** 고객사에 제출되는 파일이라 우리가 정할 수 없습니다.
(폴더로 구분되므로 이름을 줄일 필요도 없습니다. 규칙의 `파일 이름 조각`은 `01 진행현황`·`진척자료` 그대로 모든 프로젝트에서 재사용됩니다.)

---

## 5. NAS 프로그램 (pms-reader)

### 5.1 구성

```
NAS  /volume1/docker/pms-reader/
   ├─ main.py            프로그램 본체
   ├─ requirements.txt   requests==2.32.3 · firebase-admin==6.5.0
   ├─ start.sh           컨테이너 시작 스크립트
   ├─ config.json        ★ 비밀키 포함. 외부 반출 금지
   ├─ firebase-key.json  ★ Firestore 마스터 키. 외부 반출 금지
   └─ .pip/              설치된 라이브러리 (자동 생성)

Docker 컨테이너 : pms-reader   (이미지 python:3.12.0-slim)
   볼륨   : /docker/pms-reader → /app
   명령   : sh /app/start.sh
   환경   : TZ=Asia/Seoul · PYTHONUNBUFFERED=1
   자동 재시작 : 켬
```

**PC 사본**: `D:\pms-app\nas-reader\` — 여기서 고쳐서 NAS에 덮어쓰고 컨테이너를 다시 시작하는 흐름.

### 5.2 운영

| 하고 싶은 일 | 방법 |
|---|---|
| 지금 즉시 한 바퀴 | **PMS 규칙 모달 → `[지금 확인]`** (최대 20초) |
| 로그 보기 | Docker → 컨테이너 → pms-reader → 세부 정보 → 로그 (`↻` 눌러야 갱신) |
| 주기 바꾸기 | `config.json` 의 `interval_min` |
| 쓰기 없이 시험 | `config.json` 의 `dry_run: true` |
| 정지 | Docker → 컨테이너 → 스위치 끄기 |
| 코드 수정 후 | 파일 덮어쓰기 → 컨테이너 **다시 시작** |

### 5.3 하는 일 / 안 하는 일

| | 상태 |
|---|---|
| 메인표 PLC·ETOS 등 단순 규칙 반영 | ✅ |
| 하위 공종표 **부모 총계** 반영 | ✅ |
| **하위 8행 각각의 값** 갱신 (8칸) | ✅ **2026-07-31 완성** |
| 하위 행 주간 장부 + 부모 `sub_i_intCommissioning` | ✅ **2026-07-31 완성** |
| 주간 진행실적 장부 기록 | ✅ |
| `_naOn` 자동 켜기 | ✅ |
| 메인표에 **문자열**로 저장 (PMS와 동일: `91.0` → `"91"`) | ✅ |
| 프로젝트별 폴더 + 공통 자리 | ✅ **2026-07-31 완성** |
| PMS `[지금 확인]` 즉시 실행 | ✅ **2026-07-31 완성** |
| Graph 일시 오류 자동 재시도 | ✅ **2026-07-31 완성** |
| 하위 행 **신규 생성**(subCreate) | ❌ 미구현 → 6장 참조 |

**자기교정형**: 메인표가 이미 맞아도 장부가 어긋나 있으면 장부만 고칩니다. 이미 같은 값이면 아무것도 안 씁니다.

### 5.4 main.py 함수 지도 (함수명으로 검색할 것)

```
load_config()                설정 읽기·필수값 검사
ms_token(cfg)                앱 권한 토큰 (client_credentials · 1시간 캐시)
g_get(cfg, path)             Graph GET 공통  ★재시도 내장 (504·429·500·502·503·연결오류)
                               G_RETRY_MAX=3 · 대기 3·8·15초 · 429는 Retry-After 준수
                               권한(403)·주소(404) 오류는 즉시 포기

col_to_idx / idx_to_col / split_addr / expand_cells      엑셀 주소 도구
pct(v) / num_or_none(v) / name_date(name) / js_num_str(v)

list_folder(cfg)             ★하위 폴더까지 재귀 (SKIP_DIRS 제외 · MAX_DEPTH=5)
                               → (파일목록, 폴더목록) · 파일마다 rel·dir 부여
in_dir(f, base_dir)          ★그 폴더(와 하위) + 맨 위(공통) 통과 판정
pick_latest(files, pat, dir) 파일 고르기 (조각 → 파일명 날짜 → 수정시각)
sheet_id / read_range        시트 찾기 · 범위 읽기

compute_cells_rule()         PLC·ETOS 같은 단순 규칙
compute_subtable()           하위 공종표 → (sub_rows, total)

── 하위 8행 (2026-07-31) ──
norm_key / hdr_of            ★열 이름 찾기 (공백·대소문자 무시) — PMS hOfA 와 동일
sub_name_key                 하위 행 이름 비교 (앞의 '- ' 제거)
project_name_col
plan_subtable_rows()         엑셀 공종 ↔ 하위 행 짝짓기 + 바꿀 칸 계산 (읽기만)
plan_sub_ledger()            부모 sub_i 장부 차이 확인 (읽기만)
log_sub_plan()               계획을 사람이 읽게 로그로
apply_sub_rows()             하위 행 메인표 + _naOn + 변경이력 + 하위 장부 쓰기
write_sub_ledger()           부모 장부 sub_0~7_intCommissioning
write_week_multi()           한 행 장부에 여러 항목 한 번에 (읽기1·쓰기1)

── 주간 장부 ──
PROG_COL_TO_KEY / prog_item_key / week_key / doc_key_of / _write_week
sync_ledger / sync_int_commissioning

── 상태·요청 (2026-07-31) ──
status_ref / write_status    매 회차 상태 1건 기록 (PMS가 읽음)
request_ref / read_request   PMS [지금 확인] 요청 읽기
RUN_TRIGGER                  이번 회차가 '정기'인지 '요청'인지

firestore_client / rows_collection / ledger_ref
run_once(cfg, db)            한 바퀴
main()                       20초 틱 루프 (REQ_TICK_SEC=20)
```

### 5.5 Firestore 데이터 구조 (정확한 경로)

```
artifacts / tech-team-pms-app / public / data /
   │
   ├─ projectListRows_기술2팀 / {rowId}          ← List 화면 행
   │      _id, _pid, _year, Project, PLC, ETOS, HMI, 포인트, 누적, ...
   │      _extSync : {
   │           folder      : 'P10 AP4 Infra 보완투자'   ★2026-07-31 신설
   │           rules       : [ ... ]
   │           lastApplied : { 'PLC': {value, fileName, at}, ... }
   │      }
   │      _naOn, _changeHistory, _updatedAt, _updatedBy
   │      ※ 하위 행 _id 형식 = "{부모_id}_sub{nn}"
   │
   ├─ progressRecords_기술2팀 / {pid}            ← 주간 진행실적 장부
   │      weekly : { plc, etos, hmi, drawing, iomap, screen, baseinfo,
   │                 intCommissioning, sub_0_intCommissioning … sub_7_… }
   │
   ├─ pmsReaderStatus / 기술2팀                  ★2026-07-31 신설 (리더가 씀 · PMS는 읽기만)
   │      at, ok, trigger('정기'|'요청'), lastRequestAt, dryRun, intervalMin,
   │      targets, rules, wrote, ledger, errors[],
   │      folder, dirs[], files[{name, rel, dir, modified, webUrl}]
   │
   ├─ pmsReaderRequest / 기술2팀                 ★2026-07-31 신설 (PMS가 씀 · 리더가 읽음)
   │      { at: ISO시각, by: 이메일, team }
   │
   ├─ projectListMeta / 기술2팀                  ← 헤더·열그룹
   └─ projects / {id}                            ← 구 진행현황 화면 (별개 장부)
```

**주차 규칙**: 1주=1~7일 · 2주=8~14 · 3주=15~21 · 4주=22~28 · 5주=29~. 키는 `2026-7-5` (월 0채움 없음)
**규칙**: 같은 달에서 현재 주차보다 **미래** 주차 값은 지움 → 현재 주차가 "누적 최신값"

### 5.6 하위 8행이 쓰는 것 (PMS subSet 과 동일)

```
짝짓기   엑셀 공종명  ↔  하위 행 Project명 (앞의 '- ' 떼고 공백 제거·대문자 비교)
쓰는 칸  도면입수(H) · I/O Map(M) · 화면작성(R) · 기준정보(Z)
         자체시운전(AH) · 통합시운전(AP) · 포인트(AG) · 누적(AN)
같이     _naOn 5항목 켜기 · _changeHistory 1줄 · _updatedBy
안 씀    |현재값 - 새값| < 0.05 면 건너뜀

주간장부 (가) 하위 행 자기 장부 — 공정률 4개 + 누적(intCommissioning)
              (포인트·자체시운전·통합시운전은 장부 항목이 아님)
         (나) 부모 장부 sub_0~7_intCommissioning = 공종별 누적(AN)
              ★ i 는 엑셀 공종표의 줄 순서. 엑셀에서 순서를 바꾸면 짝이 어긋남
```

---

## 6. 남은 작업 (우선순위 순 · 2026-07-31 기준)

### ★ 1순위 — 배포

**오늘 소스 변경은 `localhost:3000` 에만 적용됨. 배포 안 함.**

```
변경된 파일 (2026-07-31 · 전부 미커밋)
   src/components/projectListData.js      RULE_UI_ENABLED · extRulesRawOf
                                          readerStatusRef · readerRequestRef
   src/components/ProjectListScreen.jsx   규칙 모달 오피스365용 개편 · 자동반영기 상태칸
                                          [지금 확인] · 폴더 고르기 · 파일목록 + [엑셀 열기]
   nas-reader/main.py                     ※ NAS에는 이미 반영 완료

백업 (같은 폴더)
   projectListData.js.bak-2026-07-31 / -b
   ProjectListScreen.jsx.bak-2026-07-31 ~ -i
   nas-reader/main.py.bak-2026-07-31 ~ -i

배포 : npm run build → firebase deploy --only hosting
       프로젝트 techteampms / target neconsys
```

⚠ **값 반영은 배포와 무관합니다.** NAS 리더가 Firestore에 직접 쓰므로, 배포 안 해도 직원 화면에는 이미 최신 값이 나옵니다. 배포는 **"규칙 등록 화면을 다른 PC에서도 쓸 것인가"** 의 문제입니다.
⚠ 배포하면 공용 메인 PC에서 NAS 칩·메인 PC 메뉴가 사라집니다 (의도한 동작, 사전 공지 필요).

### 2순위 — 010행(P9) 규칙 등록

지금 010행은 PLC 90%·HMI 32% 가 손으로 들어가 있습니다.

```
필요한 것 : 파일1 '01 진행현황' 의 시트 '#1 L1 진행현황' 에서 P9 블록의 셀 주소
            (011=P10 은 PLC L5,M5,L14,M14 / ETOS L24,L25,M24,M25)
확보한 것 : 그 파일의 실제 시트 목록
            '#0 진행현황' · '2. L1 물량 ' · '#1 L1 진행현황' · '#1'~'#7'
주의      : 규칙을 걸면 손으로 넣은 90%·32% 가 파일 값으로 덮어써집니다
```

### 3순위 — 하위 행 신규 생성 (subCreate)

```
지금    엑셀에 새 공종이 생기면 → 로그에 "짝 없음" 만 뜨고 하위 행은 안 만들어짐
        새 프로젝트에 하위 공종표 규칙을 걸어도 하위 행이 자동 생성되지 않음
옮길 곳 PMS ProjectListScreen.jsx 의 extApplyProposals → kind === 'subCreate'
        (_id = "{부모}_sub{nn}" · _pid 발급 · 실행번호 's' · 진행현황 'sub'
         Project명 '- 공종명' · _naOn 5항목 · 부모에서 공장/발주처/담당자 복사)
```

### 4순위 — 화면 문구 손질 (5분)

```
규칙 저장 후 안내가 "NAS 자동 프로그램이 15분 안에 …" 라고 뜸
→ [지금 확인]을 누르면 20초 라는 안내로 바꿔야 함
   (ProjectListScreen.jsx 에서 '15분 안에' 검색)
```

### 5순위 — 오피스365 나머지 테스트

| # | 항목 | 상태 |
|---|---|---|
| ① | 수식·서식 보존 | 통과 (7/29) — 6,709칸 대조 불일치 0 |
| ④ | PMS 자동반영 | **통과 (7/30~31)** |
| 확인A | 웹 `%` 입력이 숫자인가 | 통과 (7/30) |
| 확인B | 웹 엑셀 수정 → PMS 반영 | **통과 (7/31, 20초)** |
| ③ | 2명 동시편집 | 미실시 — `test@` 첫 로그인(비번변경·MFA등록) 필요 |
| ⑤ | 버전 복원 | 미실시 |
| ② | 폰·외부 열기 | 미실시 (팀장님 지시로 맨 마지막) |

### 6순위 — 비용 판단

- 라이선스는 **사용자(계정)당** 과금. **동시접속 개념 아님**
- 웹 엑셀로 **편집하는 사람 수만큼** 필요 (프로젝트 담당자 전원)
- **보기만 하는 사람은 불필요** (NAS가 대신 읽으므로)
- Business Standard 연 약정 ₩18,900/인·월 → 8명이면 **연 약 181만원** (부가세 별도)
- 계정 공유는 권하지 않음 — 동시편집 구분 불가·이력 추적 불가·MFA 공유 문제
- **미확인**: Business Basic(웹 전용·더 저렴)으로 낮출 수 있는지
- 참고: PMS 규칙 화면의 `[엑셀 열기]` 도 **라이선스 있는 계정에서만** 열립니다

### 7순위 — 보안·정리

- [ ] NAS `pms-reader` 폴더 권한 제한 (일반 직원 접근 차단)
- [ ] 클라이언트 비밀키 **2028-07-30 만료** — 달력 등록
- [ ] `public/graphtest.html` 은 테스트용 → 배포 전 삭제 또는 접근 제한
- [ ] 시험 잔재 정리 — 178행 `★자동반영 시험` · OneDrive `PMS자동반영시험_260731.xlsx`
- [ ] DSM `admin` 계정 보안 경고 (별건)
- [ ] NAS 볼륨2 93% (별건)
- [ ] NAS 인증서 2026-10-01 만료 (별건)
- [ ] 개인 PC의 OneDrive 회사 계정 연결 해제 (테스트 종료 후)

### ※ 팀장님 예정 — 전체 재테스트

전체 데이터 삭제 → 원본 엑셀 재업로드 → 클라우드 폴더 정리 → 규칙 재등록까지
처음부터 끝까지 직접 진행 예정. **7항 "전체 삭제 시 pid 새로 발급" 을 반드시 먼저 읽을 것.**

---

## 7. 확정된 함정·사실

| # | 내용 |
|---|---|
| 1 | **OneDrive 동기화는 시각만 맞추고 내용을 안 가져올 수 있다** (내려받는 방향). 올리는 방향은 정상 |
| 2 | 웹 엑셀에 손으로 `100%` 입력 → **정상 숫자.** 7/29의 텍스트 현상은 브라우저 자동화 탓 |
| 3 | 백분율 서식 칸에 `1`만 치면 **1%**가 됨 (엑셀 "자동 백분율 입력"). 직원 안내문에 명기 필요 |
| 4 | ~~PMS `[엑셀로 열기]`는 OneDrive에선 원리상 불가~~ → **틀린 판단이었음.** 오피스365는 파일마다 `webUrl` 을 주므로 그대로 열 수 있다 (2026-07-31 구현) |
| 5 | `[엑셀 생성]` 은 **화면에 보이는 행만** 내려줌 (필터 적용). 재업로드 전 필터 해제 필수 |
| 6 | **전체 삭제 + 재업로드 시 pid가 새로 발급**되어 주간실적 연결이 끊김 |
| 7 | List 화면 `[전체 데이터 삭제]` 는 `projectListRows_` 를, App.js `[데이터 초기화]` 는 `projects` 를 지움 — **서로 다른 장부** |
| 8 | Firebase Spark(무료)에서는 Cloud Functions 배포 불가 → 그래서 NAS를 택함 |
| 9 | **`extRulesOf` 는 `NAS_SYNC_ENABLED=false` 면 무조건 빈 배열을 준다.** 규칙 화면이 이걸로 목록을 그리면 "등록된 규칙 없음"으로 보이고, 거기에 규칙을 추가·저장하면 **기존 규칙이 통째로 지워진다.** → 화면은 반드시 **`extRulesRawOf`** 를 쓸 것 |
| 10 | **실제 열 이름은 `통합 시운전`(공백 있음)** 인데 규칙에는 `통합시운전` 이라 적는다. 열 이름은 반드시 **`hdr_of` (공백·대소문자 무시)** 로 찾을 것. 안 그러면 없는 열이 새로 생기고, 현재값을 못 읽어 매 회차 계속 쓴다 |
| 11 | **Graph 504 "taking too long"** — 워크북을 처음 열 때 오피스365가 로딩하다 ~19초에 포기. 다음 호출은 3초면 됨. → `g_get` 재시도로 해결 |
| 12 | **엑셀 파일 이름은 고객사 제출물** — 우리 맘대로 못 바꿈. 폴더로 구분할 것 |
| 13 | **파일 하나를 여러 프로젝트가 공유한다** (`01 진행현황_P9_10 …` 에 P9·P10 둘 다) → 그래서 "맨 위 = 공통 자리" 규칙이 필요 |
| 14 | 상태 문서의 파일 목록은 **폴더 전체**다. 프로젝트 모달에서 남의 파일이 보이지 않도록 화면에서 걸러야 한다 (현재: 행 폴더 + 규칙이 읽는 파일만, `↓ 폴더 전체 보기` 로 펼침) |
| 15 | 하위 행의 `sub_i` 는 **엑셀 공종표의 줄 순서**다. 엑셀에서 공종 순서를 바꾸거나 중간에 끼워 넣으면 부모 팝업의 하위별 실적이 어긋난다 |

---

## 8. 되돌리는 법

**오피스365 방식 정지**: Docker → 컨테이너 → pms-reader 스위치 끄기

**규칙 등록 화면만 숨기기**: `projectListData.js` 의 `RULE_UI_ENABLED = false`

**NAS 방식 복구**:
1. Docker 컨테이너 **정지 확인** (동시 실행 금지)
2. `projectListData.js` 의 `NAS_SYNC_ENABLED = false` → `true`
   → 모달의 NAS 전용 부분(폴더 주소·폴더 지정·지금 확인·엑셀로 열기)이 **자동으로 되살아납니다.**
     지운 게 아니라 `NAS_SYNC_ENABLED &&` 로 감싸 두었습니다.
3. `npm run build` → `firebase deploy --only hosting`
4. 공용 메인 PC에서 NAS 칩 → `[폴더 지정/변경]` → NAS 폴더 재지정
5. 설정 메뉴 → `[이 PC를 메인 PC로 지정]` 다시 켜기

---

## 9. 팀장님 지시·결정 기록

### 2026-07-30

| 결정 | 내용 |
|---|---|
| NAS 기능 전면 비활성화 | 값이 섞이면 검증 불가 → 스위치 하나로 끔 |
| 방식 선택 | **방식 B**(서버가 대신 읽기) · 실행 위치 **NAS** |
| 진행 방식 | **단계별로 하나씩.** 임의로 진도 나가지 말 것 |
| 문서 갱신 | 인수인계 문서는 **요청 시에만** 작성 |

### 2026-07-31

| 결정 | 내용 |
|---|---|
| 규칙 화면 방식 | 기존 NAS 모달 **재사용 + 스위치 분기** (새 모달 안 만듦) |
| `[지금 확인]` | NAS 방식에선 숨김 → 오피스365용으로 새로 구현 |
| 칸 잠금 | **이번엔 손대지 않음** — 영향 범위가 넓어 별도 단계로 미룸 |
| 폴더 구조 | **프로젝트별 하위 폴더로 간다.** 전체 재테스트 전에 정리 |
| 프로젝트 구분 | **행마다 폴더 지정** (`_extSync.folder`) — 규칙마다 지정은 손이 너무 감 |
| 공통 파일 | **맨 위(루트)에 둔다** — 운영 관리가 수월한 쪽 |
| 파일 이름 | **줄이지 않는다.** 고객사 제출물이라 우리가 못 정함 |
| 기존 파일 이동 | **지금 같이 옮김** (011행 완료) |
| 010행 규칙 | 나중에 — 전체 재테스트 때 |
| 검증 방식 | 원본 엑셀은 수식이 걸려 있어 못 건드림 → **시험 전용 파일·행**으로 왕복 검증 |

---

## 10. 잔여 (7/28~29에서 이월)

- pms 전용계정 실사용 검증 (직원 첫 로그인)
- 직원 안내문 + `.bat` 공지 (전무님 확인 후)
  → 안내문에 추가할 것: **파일 두는 자리 규칙**(전용=프로젝트 폴더 / 공통=맨 위) · 백분율 서식 주의
- 비교 설명자료 아티팩트 `nas-vs-office365-compare` (사이드바에서 재열람 가능)
