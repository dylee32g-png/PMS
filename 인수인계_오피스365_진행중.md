# 인수인계 — 오피스365 평가판 테스트 (2026-07-29 진행중)

> 새 채팅에서 이어서 할 때 **이 파일부터 읽으면** 바로 이어집니다.
> 상세 경위는 `일일리포트/2026-07-29_오피스365평가판-가입-테스트④.md` 참고.

---

## 1. 계정·환경 정보 (필수)

| 항목 | 값 |
|---|---|
| 테넌트 도메인 | `neconsyspms.onmicrosoft.com` (임시 도메인 — 회사 도메인 미연결) |
| 관리자 계정 | `jeep@neconsyspms.onmicrosoft.com` |
| 테스트 계정 | `test@neconsyspms.onmicrosoft.com` (동시편집용, 라이선스 할당 완료) |
| 라이선스 | Business Standard 평가판 · 구매수량 2 · **평가판 중 25개 사용 가능** |
| 관리센터 | admin.cloud.microsoft |
| OneDrive | neconsyspms-my.sharepoint.com (1TB) |
| MFA | 폰 Microsoft Authenticator 등록 완료 (로그인 시마다 승인 필요) |
| 주문번호 | `b97a04a1c45e` |

### 결제 안전장치 — 조치 완료
- **되풀이 청구 = "만료 시 취소"로 설정 완료**
- 평가판 종료 **2026-08-29** → 그날 자동 만료, **청구 0원**
- 아무것도 안 해도 안전. 단, 실제 도입 시에는 **수량부터 줄이고** 갱신을 켤 것
  (지금 갱신을 켜면 2개가 아니라 **25개(월 56만원)** 결제됨)
- 요금(참고): 월 약정 ₩22,680/인·월 / 연 약정 ₩18,900/인·월 (부가세 별도)

---

## 2. PC 환경 (팀장님 개인 PC — desktop-imf4i47)

| 항목 | 값 |
|---|---|
| OneDrive 앱 | 설치 완료 (윈도우에 없어서 microsoft.com에서 별도 설치함) |
| 동기화 폴더 | `D:\OneDrive-네콘시스\OneDrive - 네콘시스` |
| 테스트 자료 폴더 | `D:\OneDrive-네콘시스\OneDrive - 네콘시스\PMS진척자료` |
| 폴더 안 파일 | 파일1 `01 진행현황_..._CNS_260420.xlsx` / 파일2 `P10 ... 진척자료_260706.xlsx` |
| 개인 OneDrive | 별도로 함께 돌고 있음(naver 계정) — 회사 것과 분리되어 문제 없음 |

### ★ 상태 변경 사항 (되돌릴 것 기억)
1. **PMS의 "이 PC 폴더 지정"이 NAS → OneDrive 폴더로 바뀌어 있음**
   - 이 PC 한정(폴더 허가증은 PC별 IndexedDB) → **공용 메인 PC는 그대로 NAS를 봄. 영향 없음**
   - 되돌리려면: PMS → NAS 칩 → [폴더 지정/변경] → 원래 NAS 폴더 선택
   - NAS 표시용 주소는 모달에 그대로 남아 있음
     `\\192.168.0.110\neconsys_pj\001 Project\001 파주\107 기술2팀 2026년 Project\04 건설...`
2. **OneDrive 루트에 테스트 사본이 남아 있음** — `01 진행현황..._260420 - 복사본.xlsx`
   - 시트 `#1 L1 진행현황` **BZ200 = `SYNCTEST_A`** (동기화 실험 흔적)
   - **삭제 예정** (원본은 `D:\pms-app`에 그대로 있음). 지우면 "찾은 파일 중복" 혼란도 해소
3. **테스트 종료 후** 개인 PC에서 OneDrive 회사 계정 연결 해제 필요
   (OneDrive 설정 → 계정 → [이 PC 연결 해제])

---

## 3. 테스트 결과 (5종 중 2종 완료)

| # | 항목 | 결과 |
|---|---|---|
| ① | 수식·서식 보존 | **통과** — 웹 저장 후 전 셀 대조 **불일치 0** |
| ④ | **PMS 자동반영** | **통과** — 웹 편집 → 클라우드 → PC → PMS 감지(ETOS 75→100) |
| ② | 폰·외부 열기 | 미실시 (**맨 마지막에 하기로 확정**) |
| ③ | 2명 동시편집 | 미실시 |
| ⑤ | 버전 복원 | 미실시 |

### ① 기계검증 수치 (재현용 기준값)
- 파일1 `01 진행현황..._260420`: **수식 322 / 계산값 6,709 / 시트 28**
  - 웹 엑셀 저장 후 전 셀 대조 → **계산값 불일치 0, 수식원문 불일치 0**
  - 7/27 브라우저 라이브러리 재저장(계산값 15,558 소실)과 정반대 → **웹 엑셀은 안전**
- 파일2 `P10 ... 진척자료_260706`: **수식 73,009 / 계산값 168,311 / 시트 13**
  - `진척률요약(Main)` 30행 총계 8칸:
    H=100% · M/R/Z=94.48% · AH=93.84% · AP=21.86% · **AG=17,230** · **AN=3,766**
  - CLAUDE.md 기록치와 일치 확인

### ④ PMS 규칙 검산 (직접 계산 = PMS 표시 일치)
- ETOS = `#1 L1 진행현황` L24,L25,M24,M25 = 1,0,1,1 → 평균 **0.75 = 75** ✓
- PLC = L5,M5,L14,M14 = 0.971,1,0.778,0.833 → 평균 **0.8956 = 89.6** ✓
- PMS 변경감지 팝업에서 `ETOS 75 → 100` 확인 후 **[나중에]** 선택 → **PMS 데이터 무변경**
- 테스트로 바꾼 L25는 **0으로 원복 완료** (원본과 6,709칸 전수 대조 불일치 0)

---

## 4. ★ 오늘 발견한 함정 4가지

### (1) 웹 엑셀 탭을 열어두면 PC 파일이 갱신되지 않는다 ← 가장 중요
- 웹 편집 세션이 열려 있는 동안 파일이 잠김
  → 데스크톱 엑셀은 "읽기 전용", OneDrive는 로컬 갱신 보류
  → **PMS가 옛날 값을 계속 읽음. 경고 없음**
- **탭을 닫으면 1분 이내 해제·반영** (실험으로 확정)
- 입력한 사람 화면엔 "자동 저장됨"이라 본인은 다 된 줄 앎 → **조용한 실패**
- 대응안: ⓐ 직원 안내문에 "입력 끝나면 탭 닫기" ⓑ PMS 하단에 마지막 반영 경과시간 경고
  ⓒ 웹 세션은 방치 시 자동 만료되긴 하나 수십 분 걸릴 수 있음

### (2) 웹에서 `100%` 입력이 텍스트로 저장됨 (내일 최우선 확인)
- 수식 입력줄에 `'100%` (작은따옴표) → 숫자가 아닌 글자
- PMS는 숫자 아닌 칸을 평균에서 제외 → **진척률이 조용히 틀어짐**
- 실제로 ETOS가 100이 된 것도 "L25=1이 되어서"가 아니라 **"L25가 계산에서 빠져서"**일 수 있음
- ⚠ 자동입력(브라우저 자동화) 때문일 가능성 있음 → **팀장님이 직접 손으로 타이핑해서 재현 여부 확인 필요**

### (3) PMS [엑셀로 열기] 버튼이 NAS 주소 기준
- 폴더를 OneDrive로 바꾸면 이 버튼은 안 맞음 ("요청한 정보를 다운로드할 수 없습니다")
- 오피스365 도입 시엔 웹에서 바로 열면 되므로 이 버튼 자체가 불필요해질 수 있음

### (4) PMS "찾은 원본 파일" 목록에 이전 스캔 결과가 남음
- 폴더 지정 직후 2개 → [지금 확인] 후 4개로 증가 (같은 파일이 두 벌)
- 기능엔 영향 없으나 혼동 유발. 목록 갱신 시 교체(replace)가 아니라 누적(append)되는 것으로 보임

---

## 5. 내일 할 일 (확정된 순서)

| 순서 | 할 일 | 비고 |
|---|---|---|
| **1** | **확인A — 웹에서 `%` 입력이 숫자로 들어가는지** | 팀장님이 직접 타이핑. 수식줄에 `'` 붙는지 확인 |
| **2** | **테스트③ — 2명 동시편집** | `test@` 계정 사용. NAS로는 불가능한 유일 기능 |
| **3** | **테스트⑤ — 버전 복원** | 파일 우클릭 → 버전 기록 |
| **4** | **정리 — OneDrive 루트 테스트 사본 삭제** | `- 복사본.xlsx` (SYNCTEST_A 흔적 포함) |
| **5** | **테스트② — 폰·외부에서 열기** | **맨 마지막** (팀장님 지시) |
| **6** | **마무리 — 일일리포트·CLAUDE.md 정리 + 도입 판단** | |

---

## 6. 현재 시점 잠정 판단

- **기술적으로는 도입 가능**이 확인됨 (①④ 통과 — 파일도 안 깨지고 PMS 자동반영도 됨)
- 남은 쟁점은 **데이터 정확성(함정2)** 과 **운영 규칙(함정1)**
- 비용: 편집하는 인원수만큼만. 도면·사진 등은 NAS 유지하는 **하이브리드** 방향이 유력
- 비교 설명자료 아티팩트 있음: **`nas-vs-office365-compare`** (사이드바에서 다시 열람 가능)

## 7. 별건 잔여 (7/28에서 이월)

- 7/28 오후분 커밋·push + build·deploy (`커밋메시지_0728.txt`)
- pms 전용계정 실사용 검증 (직원 첫 로그인)
- 직원 안내문 + `.bat` 공지 (전무님 확인 후)
- NAS 인증서 2026-10-01 만료 → 9월에 자동갱신 확인
- NAS 볼륨2 93% 경고
